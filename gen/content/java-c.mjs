const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body) => `public class Main {\n    public static void main(String[] args) {\n${body}\n    }\n}\n`;
const A = (body) => `import java.util.Arrays;\n\npublic class Main {\n    public static void main(String[] args) {\n${body}\n    }\n}\n`;

export const javaMethods = [
  {
    slug: 'java-method-basics',
    title: 'Functions inside a class',
    subtitle: 'Static methods',
    concept:
      'A function inside a class is called a **method**. To call one from `main` without creating an object, mark it `static` \u2014 static members belong to the class itself.\n\nThe signature is `modifiers returnType name(parameters)`. Use `void` when nothing is returned.\n\nUnlike C, Java does not care about declaration order: methods can be defined in any order and call each other freely, because the compiler reads the whole class before resolving anything.',
    example: 'public class Main {\n    static int square(int n) {\n        return n * n;\n    }\n\n    static void banner() {\n        System.out.println("=====");\n    }\n\n    public static void main(String[] args) {\n        banner();\n        System.out.println(square(7));\n        System.out.println(square(3) + square(4));\n        banner();\n    }\n}',
    note: 'square is defined above main here, but it could equally be below \u2014 order does not matter.',
    task: 'Write a static `int maxOf(int, int)` returning the larger value, and a static `void line()` printing five dashes. Print maxOf(17, 42) and maxOf(7, 3) between two lines.',
    solution: 'public class Main {\n    static int maxOf(int a, int b) {\n        return a > b ? a : b;\n    }\n\n    static void line() {\n        System.out.println("-----");\n    }\n\n    public static void main(String[] args) {\n        line();\n        System.out.println(maxOf(17, 42));\n        System.out.println(maxOf(7, 3));\n        line();\n    }\n}\n',
    hint: 'Both methods go inside the class, alongside main.',
  },
  {
    slug: 'java-overloading',
    title: 'One name, several versions',
    subtitle: 'Method overloading',
    concept:
      'Several methods may share a name provided their **parameter lists differ** in number or type. The compiler selects the match from the argument types \u2014 this is resolved entirely at compile time.\n\nThe return type alone cannot distinguish overloads, since the compiler has no way to know which you wanted.\n\nThis is why `System.out.println` accepts every type: it is dozens of overloads. Use overloading when the operations are conceptually the same; different names are clearer when the behaviour differs.',
    example: 'public class Main {\n    static int area(int side) {\n        return side * side;\n    }\n\n    static int area(int width, int height) {\n        return width * height;\n    }\n\n    static double area(double radius) {\n        return 3.14159 * radius * radius;\n    }\n\n    static String describe(int n) { return "int " + n; }\n    static String describe(String s) { return "string " + s; }\n\n    public static void main(String[] args) {\n        System.out.println(area(4));\n        System.out.println(area(3, 5));\n        System.out.println(area(2.0));\n        System.out.println(describe(42));\n        System.out.println(describe("text"));\n    }\n}',
    note: 'area(4) and area(2.0) call different methods, chosen purely by the argument type.',
    task: 'Overload a static `describe` method for an int, a double and a String, each returning a distinguishing string, then print all three results.',
    solution: 'public class Main {\n    static String describe(int n) {\n        return "integer: " + n;\n    }\n\n    static String describe(double d) {\n        return "double: " + d;\n    }\n\n    static String describe(String s) {\n        return "string: " + s;\n    }\n\n    public static void main(String[] args) {\n        System.out.println(describe(42));\n        System.out.println(describe(2.5));\n        System.out.println(describe("hello"));\n    }\n}\n',
    hint: 'The parameter type distinguishes each overload.',
  },
  {
    slug: 'java-varargs',
    title: 'Any number of arguments',
    subtitle: 'Varargs',
    concept:
      'A parameter declared `int... numbers` accepts any number of arguments, arriving inside the method as an **array**.\n\nThere can be at most one varargs parameter and it must come **last**. You may also pass an array directly where varargs is expected.\n\nThis is how `String.format` and `System.out.printf` accept a variable argument list. Calling with no arguments gives an empty array, so `numbers.length` is 0 rather than null.',
    example: 'public class Main {\n    static int total(int... numbers) {\n        int sum = 0;\n        for (int n : numbers) sum += n;\n        return sum;\n    }\n\n    static String join(String separator, String... parts) {\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < parts.length; i++) {\n            if (i > 0) sb.append(separator);\n            sb.append(parts[i]);\n        }\n        return sb.toString();\n    }\n\n    public static void main(String[] args) {\n        System.out.println(total());\n        System.out.println(total(1, 2, 3));\n        System.out.println(total(1, 2, 3, 4, 5));\n\n        int[] existing = {10, 20};\n        System.out.println(total(existing));\n\n        System.out.println(join("-", "a", "b", "c"));\n    }\n}',
    note: 'An existing array can be passed straight to a varargs parameter, as the fourth call shows.',
    task: 'Write a static `int largest(int... numbers)` returning the biggest value, or 0 for no arguments. Call it with three values, five values and an existing array.',
    solution: 'public class Main {\n    static int largest(int... numbers) {\n        if (numbers.length == 0) return 0;\n        int best = numbers[0];\n        for (int n : numbers) {\n            if (n > best) best = n;\n        }\n        return best;\n    }\n\n    public static void main(String[] args) {\n        System.out.println(largest(3, 9, 1));\n        System.out.println(largest(5, 2, 8, 12, 7));\n        int[] existing = {40, 15};\n        System.out.println(largest(existing));\n        System.out.println(largest());\n    }\n}\n',
    hint: 'Check numbers.length before reading the first element.',
  },
  {
    slug: 'java-parameter-passing',
    title: 'What a method receives',
    subtitle: 'Pass by value, always',
    concept:
      'Java passes **everything by value**. For a primitive that means a copy of the number, so reassigning the parameter cannot affect the caller.\n\nFor an object it means a copy of the **reference**. That has two consequences worth separating carefully:\n\n1. The method **can** modify the object the reference points to \u2014 adding to a passed list is visible to the caller.\n2. The method **cannot** make the caller\u2019s variable point somewhere else \u2014 reassigning the parameter changes only the local copy.\n\nUnderstanding that distinction removes most confusion about Java\u2019s argument semantics.',
    example: 'import java.util.ArrayList;\n\npublic class Main {\n    static void changeNumber(int n) {\n        n = 999;\n    }\n\n    static void addItem(ArrayList<String> list) {\n        list.add("added inside");\n    }\n\n    static void reassign(ArrayList<String> list) {\n        list = new ArrayList<>();\n        list.add("lost");\n    }\n\n    public static void main(String[] args) {\n        int value = 5;\n        changeNumber(value);\n        System.out.println(value);\n\n        var items = new ArrayList<String>();\n        addItem(items);\n        System.out.println(items.size());\n\n        reassign(items);\n        System.out.println(items.size());\n    }\n}',
    note: 'addItem modified the object; reassign only repointed its local copy, so the caller saw no change.',
    task: 'Write a static method that tries to change an int parameter, one that adds an element to a passed ArrayList, and one that reassigns its list parameter. Demonstrate all three, printing the caller\u2019s state each time.',
    solution: 'import java.util.ArrayList;\n\npublic class Main {\n    static void tryChange(int n) {\n        n = 100;\n    }\n\n    static void append(ArrayList<Integer> list) {\n        list.add(42);\n    }\n\n    static void replace(ArrayList<Integer> list) {\n        list = new ArrayList<>();\n        list.add(999);\n    }\n\n    public static void main(String[] args) {\n        int value = 5;\n        tryChange(value);\n        System.out.println(value);\n\n        var numbers = new ArrayList<Integer>();\n        append(numbers);\n        System.out.println(numbers.size());\n\n        replace(numbers);\n        System.out.println(numbers.size());\n    }\n}\n',
    hint: 'The list keeps the element added by append but is unaffected by replace.',
  },
  {
    slug: 'java-recursion',
    title: 'Methods that call themselves',
    subtitle: 'Recursion',
    concept:
      'A recursive method calls itself on a smaller problem, with a **base case** that returns without recursing. Without one it exhausts the stack and throws `StackOverflowError`.\n\nEach call gets its own parameters and locals, stacked until the base case unwinds them.\n\nRecursion suits naturally recursive structures. For plain counting a loop is faster. Naive recursive Fibonacci is the classic warning: it recomputes the same values exponentially often, which is why memoisation exists.',
    example: 'public class Main {\n    static long factorial(int n) {\n        if (n <= 1) return 1;\n        return n * factorial(n - 1);\n    }\n\n    static int sumDigits(int n) {\n        if (n < 10) return n;\n        return n % 10 + sumDigits(n / 10);\n    }\n\n    static int gcd(int a, int b) {\n        return b == 0 ? a : gcd(b, a % b);\n    }\n\n    public static void main(String[] args) {\n        System.out.println(factorial(10));\n        System.out.println(sumDigits(9875));\n        System.out.println(gcd(48, 36));\n    }\n}',
    note: 'gcd fits on one line because the conditional operator supplies both the base case and the recursive step.',
    task: 'Write recursive static methods `power(int base, int exp)` and `int fib(int n)`. Print power(2, 10), power(5, 3) and the first ten Fibonacci numbers on one line.',
    solution: 'public class Main {\n    static int power(int base, int exp) {\n        if (exp == 0) return 1;\n        return base * power(base, exp - 1);\n    }\n\n    static int fib(int n) {\n        if (n < 2) return n;\n        return fib(n - 1) + fib(n - 2);\n    }\n\n    public static void main(String[] args) {\n        System.out.println(power(2, 10));\n        System.out.println(power(5, 3));\n        for (int i = 0; i < 10; i++) System.out.print(fib(i) + " ");\n        System.out.println();\n    }\n}\n',
    hint: 'Anything to the power zero is 1, and n < 2 covers both Fibonacci base cases.',
  },
  d('java-m-square', 'A squaring method', 'Write static square(int) and print the squares of 3, 7 and 12.',
    'public class Main {\n    static int square(int n) { return n * n; }\n\n    public static void main(String[] args) {\n        System.out.println(square(3));\n        System.out.println(square(7));\n        System.out.println(square(12));\n    }\n}\n', 'Return the product.'),
  d('java-m-predicate', 'A boolean method', 'Write static boolean isEven(int) and print it for 4, 7 and 0.',
    'public class Main {\n    static boolean isEven(int n) { return n % 2 == 0; }\n\n    public static void main(String[] args) {\n        System.out.println(isEven(4));\n        System.out.println(isEven(7));\n        System.out.println(isEven(0));\n    }\n}\n', 'The comparison already yields a boolean.'),
  d('java-m-void', 'A void method', 'Write static void banner() printing ten dashes, and call it around a message.',
    'public class Main {\n    static void banner() { System.out.println("----------"); }\n\n    public static void main(String[] args) {\n        banner();\n        System.out.println("MENU");\n        banner();\n    }\n}\n', 'A void method returns nothing.'),
  d('java-m-min3', 'Smallest of three', 'Write static min3(int, int, int) using Math.min and print two results.',
    'public class Main {\n    static int min3(int a, int b, int c) {\n        return Math.min(a, Math.min(b, c));\n    }\n\n    public static void main(String[] args) {\n        System.out.println(min3(5, 2, 9));\n        System.out.println(min3(7, 7, 3));\n    }\n}\n', 'Math.min can be nested.'),
  d('java-m-grade', 'A char-returning method', 'Write static char grade(int) returning A/B/C/D/F, and print it for 95, 82 and 50.',
    'public class Main {\n    static char grade(int score) {\n        if (score >= 90) return \'A\';\n        if (score >= 80) return \'B\';\n        if (score >= 70) return \'C\';\n        if (score >= 60) return \'D\';\n        return \'F\';\n    }\n\n    public static void main(String[] args) {\n        System.out.println(grade(95));\n        System.out.println(grade(82));\n        System.out.println(grade(50));\n    }\n}\n', 'Early returns avoid nesting.'),
  d('java-m-celsius', 'A double-returning method', 'Write static double toFahrenheit(int) and print it for 0, 37 and 100 to one decimal.',
    'public class Main {\n    static double toFahrenheit(int celsius) {\n        return celsius * 9.0 / 5 + 32;\n    }\n\n    public static void main(String[] args) {\n        System.out.printf("%.1f%n", toFahrenheit(0));\n        System.out.printf("%.1f%n", toFahrenheit(37));\n        System.out.printf("%.1f%n", toFahrenheit(100));\n    }\n}\n', 'Use 9.0 to force floating-point division.'),
  d('java-m-overload-area', 'Overloaded area', 'Overload area for one int (square) and two ints (rectangle), calling both.',
    'public class Main {\n    static int area(int side) { return side * side; }\n    static int area(int w, int h) { return w * h; }\n\n    public static void main(String[] args) {\n        System.out.println(area(5));\n        System.out.println(area(3, 7));\n    }\n}\n', 'The parameter count distinguishes them.'),
  d('java-m-overload-types', 'Overload by type', 'Overload show for int and String, calling each.',
    'public class Main {\n    static void show(int n) { System.out.println("int " + n); }\n    static void show(String s) { System.out.println("string " + s); }\n\n    public static void main(String[] args) {\n        show(42);\n        show("text");\n    }\n}\n', 'The argument type selects the overload.'),
  d('java-m-varargs-sum', 'Varargs sum', 'Write static int sum(int...) and call it with zero, two and five arguments.',
    'public class Main {\n    static int sum(int... nums) {\n        int total = 0;\n        for (int n : nums) total += n;\n        return total;\n    }\n\n    public static void main(String[] args) {\n        System.out.println(sum());\n        System.out.println(sum(1, 2));\n        System.out.println(sum(1, 2, 3, 4, 5));\n    }\n}\n', 'The varargs parameter arrives as an array.'),
  d('java-m-varargs-array', 'Passing an array to varargs', 'Write static int count(int...) returning the argument count, and call it with an existing array and with loose values.',
    'public class Main {\n    static int count(int... nums) { return nums.length; }\n\n    public static void main(String[] args) {\n        int[] existing = {1, 2, 3};\n        System.out.println(count(existing));\n        System.out.println(count(4, 5));\n    }\n}\n', 'An array can be passed directly.'),
  d('java-m-varargs-strings', 'Joining with varargs', 'Write static String join(String sep, String... parts) and call it with three parts.',
    'public class Main {\n    static String join(String sep, String... parts) {\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < parts.length; i++) {\n            if (i > 0) sb.append(sep);\n            sb.append(parts[i]);\n        }\n        return sb.toString();\n    }\n\n    public static void main(String[] args) {\n        System.out.println(join("-", "a", "b", "c"));\n    }\n}\n', 'The varargs parameter must come last.'),
  d('java-m-pass-primitive', 'Primitives are copied', 'Write a method reassigning its int parameter, and show the caller\'s variable is unchanged.',
    'public class Main {\n    static void change(int n) { n = 100; }\n\n    public static void main(String[] args) {\n        int value = 5;\n        change(value);\n        System.out.println(value);\n    }\n}\n', 'The method only modified its copy.'),
  d('java-m-pass-object', 'Objects can be modified', 'Write a method adding an element to a passed ArrayList, and print the size before and after.',
    'import java.util.ArrayList;\n\npublic class Main {\n    static void append(ArrayList<Integer> list) { list.add(1); }\n\n    public static void main(String[] args) {\n        var list = new ArrayList<Integer>();\n        System.out.println(list.size());\n        append(list);\n        System.out.println(list.size());\n    }\n}\n', 'The reference copy still points at the same object.'),
  d('java-m-return-array', 'Returning an array', 'Write static int[] range(int n) returning 1 to n, and print the array with Arrays.toString.',
    'import java.util.Arrays;\n\npublic class Main {\n    static int[] range(int n) {\n        int[] out = new int[n];\n        for (int i = 0; i < n; i++) out[i] = i + 1;\n        return out;\n    }\n\n    public static void main(String[] args) {\n        System.out.println(Arrays.toString(range(5)));\n    }\n}\n', 'Arrays.toString prints a readable form.'),
  d('java-m-recursive-factorial', 'Recursive factorial', 'Write a recursive factorial returning long and print it for 0, 1, 5 and 15.',
    'public class Main {\n    static long factorial(int n) {\n        if (n <= 1) return 1;\n        return n * factorial(n - 1);\n    }\n\n    public static void main(String[] args) {\n        System.out.println(factorial(0));\n        System.out.println(factorial(1));\n        System.out.println(factorial(5));\n        System.out.println(factorial(15));\n    }\n}\n', 'n <= 1 covers both base cases.'),
  d('java-m-recursive-gcd', 'Recursive GCD', 'Write a recursive gcd and print gcd(48, 36) and gcd(17, 5).',
    'public class Main {\n    static int gcd(int a, int b) {\n        return b == 0 ? a : gcd(b, a % b);\n    }\n\n    public static void main(String[] args) {\n        System.out.println(gcd(48, 36));\n        System.out.println(gcd(17, 5));\n    }\n}\n', 'The base case is b reaching zero.'),
  d('java-m-recursive-digits', 'Recursive digit sum', 'Write a recursive digit sum and print it for 9875 and 7.',
    'public class Main {\n    static int digitSum(int n) {\n        if (n < 10) return n;\n        return n % 10 + digitSum(n / 10);\n    }\n\n    public static void main(String[] args) {\n        System.out.println(digitSum(9875));\n        System.out.println(digitSum(7));\n    }\n}\n', 'Peel the last digit and recurse.'),
  d('java-m-recursive-reverse', 'Recursive string reversal', 'Write a recursive method reversing a String and print the reverse of "recursion".',
    'public class Main {\n    static String reverse(String s) {\n        if (s.isEmpty()) return s;\n        return reverse(s.substring(1)) + s.charAt(0);\n    }\n\n    public static void main(String[] args) {\n        System.out.println(reverse("recursion"));\n    }\n}\n', 'Recurse on the tail, then append the head.'),
  d('java-m-compose', 'Composing methods', 'Write doubled(int) and increment(int), then print doubled(increment(5)) and increment(doubled(5)).',
    'public class Main {\n    static int doubled(int n) { return n * 2; }\n    static int increment(int n) { return n + 1; }\n\n    public static void main(String[] args) {\n        System.out.println(doubled(increment(5)));\n        System.out.println(increment(doubled(5)));\n    }\n}\n', 'The inner call runs first.'),
  d('java-m-helper-chain', 'Several helpers', 'Write area(int, int), perimeter(int, int) and report(int, int) calling both, then run report on 7 and 3.',
    'public class Main {\n    static int area(int w, int h) { return w * h; }\n    static int perimeter(int w, int h) { return 2 * (w + h); }\n\n    static void report(int w, int h) {\n        System.out.println("area: " + area(w, h));\n        System.out.println("perimeter: " + perimeter(w, h));\n    }\n\n    public static void main(String[] args) {\n        report(7, 3);\n    }\n}\n', 'One method can call several others.'),
  d('java-m-capstone', 'Method summary', 'Write grade(int), average(int...) and report(int...) printing each score with its grade plus the average to one decimal. Run it on 95, 82, 71 and 50.',
    'public class Main {\n    static char grade(int score) {\n        if (score >= 90) return \'A\';\n        if (score >= 80) return \'B\';\n        if (score >= 70) return \'C\';\n        if (score >= 60) return \'D\';\n        return \'F\';\n    }\n\n    static double average(int... scores) {\n        int total = 0;\n        for (int s : scores) total += s;\n        return (double) total / scores.length;\n    }\n\n    static void report(int... scores) {\n        for (int s : scores) {\n            System.out.println(s + ": " + grade(s));\n        }\n        System.out.printf("average: %.1f%n", average(scores));\n    }\n\n    public static void main(String[] args) {\n        report(95, 82, 71, 50);\n    }\n}\n', 'The varargs array can be forwarded to another varargs method.'),
  {
    kind: 'quiz',
    slug: 'java-methods-quiz',
    title: 'Checkpoint: methods',
    questions: [
      { q: 'What distinguishes two overloads of a method?', options: ['The return type', 'The parameter list', 'The order of definition', 'The access modifier'], answer: 1, explain: 'Overload resolution uses the argument types, so the return type alone cannot distinguish two methods.' },
      { q: 'Where must a varargs parameter appear?', options: ['First', 'Last, and there can only be one', 'Anywhere', 'It must be the only parameter'], answer: 1, explain: 'Since it absorbs all remaining arguments, it must come last and cannot be repeated.' },
      { q: 'Can a method modify the object a reference parameter points to?', options: ['No, everything is copied', 'Yes \u2014 the reference is copied, but it points at the same object', 'Only for arrays', 'Only with final'], answer: 1, explain: 'Java copies the reference, not the object, so mutations to the object are visible to the caller.' },
      { q: 'Can a method make the caller\u2019s variable point at a different object?', options: ['Yes', 'No \u2014 reassigning the parameter changes only the local copy', 'Only for primitives', 'Only with static'], answer: 1, explain: 'This is the other half of pass-by-value: the reference itself is a copy, so repointing it is invisible outside.' },
      { q: 'What happens when a recursion has no base case?', options: ['It returns null', 'It throws StackOverflowError', 'It loops forever silently', 'A compile error'], answer: 1, explain: 'Each call consumes stack space, so unbounded recursion eventually exhausts it and throws.' },
    ],
  },
];

export const javaStrings = [
  {
    slug: 'java-string-methods',
    title: 'What a String can do',
    subtitle: 'The core methods',
    concept:
      '`String` carries a large interface, and because Strings are **immutable** every method returns a new String rather than modifying the original.\n\nThe essentials:\n- `.length()`, `.charAt(i)`, `.substring(from, to)`\n- `.indexOf(text)` returning `-1` when absent, `.contains(text)`\n- `.toUpperCase()`, `.toLowerCase()`, `.trim()` / `.strip()`\n- `.replace(old, new)`, `.startsWith()`, `.endsWith()`\n- `.isEmpty()`, `.isBlank()`, `.repeat(n)`\n\nNote `substring(from, to)` excludes the end index, matching the half-open convention used everywhere.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        String text = "  Programming in Java  ";\n        String clean = text.strip();\n\n        System.out.println(clean);\n        System.out.println(clean.length());\n        System.out.println(clean.substring(0, 11));\n        System.out.println(clean.indexOf("Java"));\n        System.out.println(clean.indexOf("Python"));\n        System.out.println(clean.toUpperCase());\n        System.out.println(clean.replace("Java", "Kotlin"));\n        System.out.println(clean);\n        System.out.println("ab".repeat(3));\n    }\n}',
    note: 'The second print of clean shows it unchanged \u2014 replace returned a new String.',
    task: 'Given `String raw = "  Data Science Rocks  "`, print the stripped text, its length, its uppercase form, the index of `"Science"`, whether it contains `"Rocks"`, and the original stripped value again to show immutability.',
    given: 'String raw = "  Data Science Rocks  ";',
    solution: M('        String raw = "  Data Science Rocks  ";\n        String clean = raw.strip();\n\n        System.out.println(clean);\n        System.out.println(clean.length());\n        System.out.println(clean.toUpperCase());\n        System.out.println(clean.indexOf("Science"));\n        System.out.println(clean.contains("Rocks"));\n        System.out.println(clean);'),
    hint: 'Store the stripped version once and reuse it.',
  },
  {
    slug: 'java-string-immutability',
    title: 'Why Strings never change',
    subtitle: 'Immutability and the string pool',
    concept:
      'A String\u2019s characters can never be modified. Every apparent change produces a new object, which is why `text.toUpperCase();` on its own accomplishes nothing \u2014 you must assign the result.\n\nImmutability brings real benefits: Strings are safe to share between threads, safe to use as map keys, and can be **interned**. Identical literals share one object in the **string pool**, which is why `"abc" == "abc"` happens to be true while `new String("abc") == "abc"` is false.\n\nThat inconsistency is precisely why `.equals()` is the only correct comparison.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        String a = "hello";\n        a.toUpperCase();\n        System.out.println(a);\n\n        String b = a.toUpperCase();\n        System.out.println(b);\n\n        String literal1 = "abc";\n        String literal2 = "abc";\n        String made = new String("abc");\n\n        System.out.println(literal1 == literal2);\n        System.out.println(literal1 == made);\n        System.out.println(literal1.equals(made));\n    }\n}',
    note: 'The two literals shared one pooled object; the explicitly constructed String did not. Only .equals gave the right answer both times.',
    task: 'Show that calling toUpperCase without assigning changes nothing, then assign it and print the result. Then compare two identical literals with ==, a literal against a new String with ==, and the same pair with .equals().',
    solution: M('        String a = "hello";\n        a.toUpperCase();\n        System.out.println(a);\n\n        String b = a.toUpperCase();\n        System.out.println(b);\n\n        String x = "abc";\n        String y = "abc";\n        String z = new String("abc");\n\n        System.out.println(x == y);\n        System.out.println(x == z);\n        System.out.println(x.equals(z));'),
    hint: 'The result of a String method must be assigned to be kept.',
  },
  {
    slug: 'java-stringbuilder',
    title: 'Building strings efficiently',
    subtitle: 'StringBuilder',
    concept:
      'Because Strings are immutable, `result += piece` inside a loop creates a **new String every iteration**, copying everything each time. For a thousand appends that is a thousand allocations and quadratic total work.\n\n`StringBuilder` is the mutable alternative. `.append()` modifies the buffer in place, and `.toString()` produces the final String once at the end. It also offers `.insert()`, `.reverse()`, `.deleteCharAt()` and `.setCharAt()`.\n\nThe rule: for a handful of concatenations use `+`, since the compiler optimises simple cases. Inside a loop, always use StringBuilder.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        StringBuilder sb = new StringBuilder();\n        for (int i = 1; i <= 5; i++) {\n            sb.append(i);\n            if (i < 5) sb.append(",");\n        }\n        System.out.println(sb.toString());\n        System.out.println(sb.length());\n\n        StringBuilder other = new StringBuilder("hello");\n        other.append(" world");\n        other.insert(0, ">> ");\n        other.reverse();\n        System.out.println(other);\n        other.reverse();\n        System.out.println(other);\n    }\n}',
    note: 'Every operation modified the same buffer \u2014 no intermediate Strings were created at all.',
    task: 'Use a StringBuilder to build `1-2-3-4-5-6` in a loop, printing the result and its length. Then build `abc`, append `def`, insert `>>` at the front, reverse it and print.',
    solution: M('        StringBuilder sb = new StringBuilder();\n        for (int i = 1; i <= 6; i++) {\n            if (i > 1) sb.append("-");\n            sb.append(i);\n        }\n        System.out.println(sb);\n        System.out.println(sb.length());\n\n        StringBuilder other = new StringBuilder("abc");\n        other.append("def");\n        other.insert(0, ">>");\n        other.reverse();\n        System.out.println(other);'),
    hint: 'Append the separator before the value when the index is above the first.',
  },
  {
    slug: 'java-string-split-join',
    title: 'Between text and arrays',
    subtitle: 'split, join and formatting',
    concept:
      '`.split(regex)` breaks a String into an array. The argument is a **regular expression**, so characters like `.` and `|` must be escaped: splitting on a literal dot needs `"\\\\."`.\n\n`String.join(separator, parts)` is the inverse, accepting an array, a list or varargs.\n\n`String.format(pattern, args...)` builds a formatted String using the same specifiers as printf, returning it rather than printing. That is the right tool whenever you need formatted text in a variable.',
    example: 'import java.util.Arrays;\n\npublic class Main {\n    public static void main(String[] args) {\n        String csv = "red,green,blue";\n        String[] parts = csv.split(",");\n\n        System.out.println(parts.length);\n        System.out.println(Arrays.toString(parts));\n        System.out.println(String.join(" | ", parts));\n\n        String sentence = "the quick brown fox";\n        System.out.println(sentence.split("\\\\s+").length);\n\n        String version = "1.2.3";\n        System.out.println(Arrays.toString(version.split("\\\\.")));\n\n        System.out.println(String.format("%s scored %d (%.1f%%)", "ada", 91, 91.0));\n    }\n}',
    note: 'Splitting on a dot required escaping it, because an unescaped dot matches any character in a regex.',
    task: 'Split `"red,green,blue"` on commas, printing the count and the array. Rejoin with `" | "`. Split `"1.2.3"` on literal dots and print the array. Then build `"ada: 91"` with String.format.',
    solution: A('        String csv = "red,green,blue";\n        String[] parts = csv.split(",");\n\n        System.out.println(parts.length);\n        System.out.println(Arrays.toString(parts));\n        System.out.println(String.join(" | ", parts));\n\n        System.out.println(Arrays.toString("1.2.3".split("\\\\.")));\n        System.out.println(String.format("%s: %d", "ada", 91));'),
    hint: 'A literal dot in a regex is written as two backslashes then a dot in Java source.',
  },
  {
    slug: 'java-char-processing',
    title: 'Working character by character',
    subtitle: 'char loops and Character methods',
    concept:
      'To examine every character, either index with `charAt(i)` or walk `toCharArray()` with an enhanced for.\n\nThe `Character` class supplies the tests: `Character.isLetter`, `isDigit`, `isWhitespace`, `isUpperCase`, `toUpperCase`, `toLowerCase`.\n\nA `char` is also a 16-bit number, so arithmetic works: `c - \'0\'` converts a digit character to its value, and `(char)(c + 1)` gives the next letter. Casting back to `char` is required, since the arithmetic promotes to int.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        String text = "Hello World 42";\n        int letters = 0, digits = 0, spaces = 0;\n\n        for (char c : text.toCharArray()) {\n            if (Character.isLetter(c)) letters++;\n            else if (Character.isDigit(c)) digits++;\n            else if (Character.isWhitespace(c)) spaces++;\n        }\n\n        System.out.println(letters + " " + digits + " " + spaces);\n\n        System.out.println((int) \'A\');\n        System.out.println((char) (\'A\' + 3));\n        System.out.println(\'7\' - \'0\');\n        System.out.println(Character.toUpperCase(\'q\'));\n    }\n}',
    note: "'7' - '0' gives 7 because the digit character codes are consecutive.",
    task: 'For `"Java 21 Rocks"`, count the letters, digits and spaces. Then print the numeric value of the character `\'8\'`, and the character three places after `\'A\'`.',
    given: 'String text = "Java 21 Rocks";',
    solution: M('        String text = "Java 21 Rocks";\n        int letters = 0, digits = 0, spaces = 0;\n\n        for (char c : text.toCharArray()) {\n            if (Character.isLetter(c)) letters++;\n            else if (Character.isDigit(c)) digits++;\n            else if (Character.isWhitespace(c)) spaces++;\n        }\n\n        System.out.println(letters);\n        System.out.println(digits);\n        System.out.println(spaces);\n        System.out.println(\'8\' - \'0\');\n        System.out.println((char) (\'A\' + 3));'),
    hint: 'The Character class provides all the classification tests.',
  },
  d('java-s-basics', 'String basics', 'For "keyboard", print the string, its length, its first character and its last character.',
    M('        String w = "keyboard";\n        System.out.println(w);\n        System.out.println(w.length());\n        System.out.println(w.charAt(0));\n        System.out.println(w.charAt(w.length() - 1));'), 'The last index is length() - 1.'),
  d('java-s-substring', 'Substrings', 'For "abcdefgh", print the first three characters, the last three, and characters 2 to 4.',
    M('        String s = "abcdefgh";\n        System.out.println(s.substring(0, 3));\n        System.out.println(s.substring(s.length() - 3));\n        System.out.println(s.substring(2, 5));'), 'substring excludes the end index.'),
  d('java-s-indexof', 'Finding substrings', 'In "the quick brown fox", print the index of "brown", the index of "cat", and whether it contains "quick".',
    M('        String s = "the quick brown fox";\n        System.out.println(s.indexOf("brown"));\n        System.out.println(s.indexOf("cat"));\n        System.out.println(s.contains("quick"));'), 'indexOf returns -1 when absent.'),
  d('java-s-case', 'Changing case', 'For "Hello World", print the uppercase, lowercase and original forms.',
    M('        String s = "Hello World";\n        System.out.println(s.toUpperCase());\n        System.out.println(s.toLowerCase());\n        System.out.println(s);'), 'The original is never modified.'),
  d('java-s-strip', 'Trimming whitespace', 'For "   spaced   ", print the stripped version wrapped in pipes and its length before and after.',
    M('        String s = "   spaced   ";\n        System.out.println(s.length());\n        System.out.println("|" + s.strip() + "|");\n        System.out.println(s.strip().length());'), 'strip is the modern replacement for trim.'),
  d('java-s-replace', 'Replacing text', 'For "the cat sat on the mat", print it with every "at" replaced by "og", then the original.',
    M('        String s = "the cat sat on the mat";\n        System.out.println(s.replace("at", "og"));\n        System.out.println(s);'), 'replace returns a new String.'),
  d('java-s-repeat', 'Repeating text', 'Print "ab" repeated four times and a line of twenty dashes using repeat.',
    M('        System.out.println("ab".repeat(4));\n        System.out.println("-".repeat(20));'), 'String.repeat was added in Java 11.'),
  d('java-s-startswith', 'Prefix and suffix', 'For "report_2026.csv", print whether it starts with "report", ends with ".csv", and ends with ".txt".',
    M('        String f = "report_2026.csv";\n        System.out.println(f.startsWith("report"));\n        System.out.println(f.endsWith(".csv"));\n        System.out.println(f.endsWith(".txt"));'), 'Both methods return booleans.'),
  d('java-s-equals-ignore', 'Case-insensitive comparison', 'Print whether "Java" equals "java" with equals and with equalsIgnoreCase.',
    M('        System.out.println("Java".equals("java"));\n        System.out.println("Java".equalsIgnoreCase("java"));'), 'equalsIgnoreCase exists for exactly this.'),
  d('java-s-pool', 'The string pool', 'Compare two identical literals with ==, a literal with a new String using ==, and the same pair with equals.',
    M('        String a = "abc";\n        String b = "abc";\n        String c = new String("abc");\n        System.out.println(a == b);\n        System.out.println(a == c);\n        System.out.println(a.equals(c));'), 'Only equals gives the right answer in both cases.'),
  d('java-s-builder-loop', 'Building in a loop', 'Use a StringBuilder to build "1,2,3,4,5" and print it with its length.',
    M('        StringBuilder sb = new StringBuilder();\n        for (int i = 1; i <= 5; i++) {\n            if (i > 1) sb.append(",");\n            sb.append(i);\n        }\n        System.out.println(sb);\n        System.out.println(sb.length());'), 'Append the separator before all but the first value.'),
  d('java-s-builder-reverse', 'Reversing with StringBuilder', 'Reverse "stressed" using StringBuilder and print both the original and the reverse.',
    M('        String s = "stressed";\n        String r = new StringBuilder(s).reverse().toString();\n        System.out.println(s);\n        System.out.println(r);'), 'The calls chain in one expression.'),
  d('java-s-palindrome', 'Palindrome check', 'Print whether "racecar" and "python" are palindromes using StringBuilder.',
    'public class Main {\n    static boolean isPalindrome(String s) {\n        return s.equals(new StringBuilder(s).reverse().toString());\n    }\n\n    public static void main(String[] args) {\n        System.out.println(isPalindrome("racecar"));\n        System.out.println(isPalindrome("python"));\n    }\n}\n', 'Compare the string with its reverse.'),
  d('java-s-builder-insert', 'Inserting and deleting', 'Start with "abcdef" in a StringBuilder, insert "XY" at index 3, print it, delete the character at index 0 and print again.',
    M('        StringBuilder sb = new StringBuilder("abcdef");\n        sb.insert(3, "XY");\n        System.out.println(sb);\n        sb.deleteCharAt(0);\n        System.out.println(sb);'), 'Both operations modify the buffer in place.'),
  d('java-s-split-comma', 'Splitting on commas', 'Split "a,b,c,d" on commas and print the count and each part.',
    A('        String[] parts = "a,b,c,d".split(",");\n        System.out.println(parts.length);\n        for (String p : parts) System.out.println(p);'), 'split returns an array.'),
  d('java-s-split-whitespace', 'Splitting into words', 'Split "the quick brown fox" on whitespace and print each word numbered from 1.',
    M('        String[] words = "the quick brown fox".split("\\\\s+");\n        for (int i = 0; i < words.length; i++) {\n            System.out.println((i + 1) + ". " + words[i]);\n        }'), 'The regex \\\\s+ matches runs of whitespace.'),
  d('java-s-split-dot', 'Splitting on a literal dot', 'Split "1.2.3" on dots and print the three parts.',
    A('        System.out.println(Arrays.toString("1.2.3".split("\\\\.")));'), 'A dot must be escaped in a regex.'),
  d('java-s-join', 'Joining parts', 'Join the array {"usr", "local", "bin"} with slashes and print the result with a leading slash.',
    M('        String[] parts = {"usr", "local", "bin"};\n        System.out.println("/" + String.join("/", parts));'), 'String.join accepts an array or varargs.'),
  d('java-s-format', 'String.format', 'Build the text "ada scored 91 (95.5%)" with String.format and print it.',
    M('        String line = String.format("%s scored %d (%.1f%%)", "ada", 91, 95.5);\n        System.out.println(line);'), 'Double the percent sign to print it literally.'),
  d('java-s-char-loop', 'Character classification', 'For "Java 21!", count the letters, digits and other characters.',
    M('        String s = "Java 21!";\n        int letters = 0, digits = 0, other = 0;\n        for (char c : s.toCharArray()) {\n            if (Character.isLetter(c)) letters++;\n            else if (Character.isDigit(c)) digits++;\n            else other++;\n        }\n        System.out.println(letters);\n        System.out.println(digits);\n        System.out.println(other);'), 'The Character class supplies the tests.'),
  d('java-s-vowels', 'Counting vowels', 'Count the vowels in "encyclopedia" and print the count.',
    M('        String s = "encyclopedia";\n        int vowels = 0;\n        for (char c : s.toCharArray()) {\n            if ("aeiou".indexOf(Character.toLowerCase(c)) >= 0) vowels++;\n        }\n        System.out.println(vowels);'), 'indexOf on a vowel string is a neat membership test.'),
  d('java-s-char-arithmetic', 'Character arithmetic', 'Print the value of the digit character \'7\', the character after \'A\', and the uppercase of \'q\'.',
    M('        System.out.println(\'7\' - \'0\');\n        System.out.println((char) (\'A\' + 1));\n        System.out.println(Character.toUpperCase(\'q\'));'), 'Cast back to char to display a letter.'),
  d('java-s-capstone', 'String summary', 'For "  Data Science Rocks  ", print the stripped length, the word count, the vowel count, the reversed string, and the words joined by underscores.',
    M('        String raw = "  Data Science Rocks  ";\n        String clean = raw.strip();\n\n        System.out.println(clean.length());\n\n        String[] words = clean.split("\\\\s+");\n        System.out.println(words.length);\n\n        int vowels = 0;\n        for (char c : clean.toCharArray()) {\n            if ("aeiou".indexOf(Character.toLowerCase(c)) >= 0) vowels++;\n        }\n        System.out.println(vowels);\n\n        System.out.println(new StringBuilder(clean).reverse());\n        System.out.println(String.join("_", words));'), 'Strip once, then split and reuse the array.'),
  {
    kind: 'quiz',
    slug: 'java-strings-quiz',
    title: 'Checkpoint: strings',
    questions: [
      { q: 'What does text.toUpperCase(); on its own accomplish?', options: ['Uppercases text', 'Nothing \u2014 the result must be assigned', 'Throws', 'Returns null'], answer: 1, explain: 'Strings are immutable, so every method returns a new String and the original is untouched.' },
      { q: 'Why is "abc" == "abc" true but new String("abc") == "abc" false?', options: ['A JVM bug', 'Identical literals share one pooled object, while new always creates a distinct one', '== always compares contents', 'Both are false'], answer: 1, explain: 'The string pool interns literals, which is exactly why relying on == for text comparison is unreliable.' },
      { q: 'Why use StringBuilder inside a loop?', options: ['It is shorter', 'String += creates a new object each iteration, giving quadratic work', 'It sorts the text', 'It is required'], answer: 1, explain: 'A mutable buffer avoids copying the entire accumulated string on every append.' },
      { q: 'What does split take as its argument?', options: ['A plain string', 'A regular expression, so characters like . must be escaped', 'A char', 'An array'], answer: 1, explain: 'An unescaped dot matches any character, which is why splitting on a literal dot needs "\\\\.".' },
      { q: "What does '7' - '0' evaluate to?", options: ['7', '55', "'7'", 'A compile error'], answer: 0, explain: 'Digit character codes are consecutive, so subtracting the code of zero converts a digit character to its value.' },
    ],
  },
];
