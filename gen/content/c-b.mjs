const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body) => `#include <stdio.h>\n\nint main(void) {\n${body}\n    return 0;\n}\n`;

export const cOperators = [
  {
    slug: 'c-arithmetic',
    title: 'The arithmetic operators',
    subtitle: 'Five operators and their quirks',
    concept:
      'C has five arithmetic operators: `+ - * / %`.\n\nThere is no exponentiation operator \u2014 `pow()` from `<math.h>` covers that, or you multiply by hand.\n\n`/` behaves differently depending on the operand types: integer division truncates, floating-point division does not. `%` requires integers and gives the remainder.\n\nUnary minus negates. Note there is no separate integer-division operator as in Python; the types decide.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int a = 17, b = 5;\n\n    printf("%d %d %d\\n", a + b, a - b, a * b);\n    printf("%d %d\\n", a / b, a % b);\n    printf("%.3f\\n", (double)a / b);\n    printf("%d\\n", -a);\n    return 0;\n}',
    note: 'The same / symbol means two different operations depending on whether the operands are integers.',
    task: 'With `int a = 23` and `int b = 4`, print the sum, difference, product, quotient, remainder and exact division to two decimals \u2014 six lines.',
    given: 'int a = 23, b = 4;',
    solution: M('    int a = 23, b = 4;\n\n    printf("%d\\n", a + b);\n    printf("%d\\n", a - b);\n    printf("%d\\n", a * b);\n    printf("%d\\n", a / b);\n    printf("%d\\n", a % b);\n    printf("%.2f\\n", (double)a / b);'),
    hint: 'Only the last line needs a cast.',
  },
  {
    slug: 'c-increment',
    title: 'Adding one, two ways',
    subtitle: 'Prefix and postfix',
    concept:
      '`i++` and `++i` both add one to i, but they **evaluate** to different things. `i++` yields the old value then increments; `++i` increments then yields the new value.\n\nAs a standalone statement they are identical. Inside a larger expression they differ, which is why mixing them into complex expressions is discouraged.\n\n`--` works the same way. The augmented operators `+= -= *= /= %=` cover the general case.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int i = 5;\n    printf("%d\\n", i++);\n    printf("%d\\n", i);\n\n    int j = 5;\n    printf("%d\\n", ++j);\n    printf("%d\\n", j);\n\n    int k = 10;\n    k += 5;\n    k *= 2;\n    printf("%d\\n", k);\n    return 0;\n}',
    note: 'i++ printed 5 then left i at 6; ++j printed 6 immediately. Both variables end at 6.',
    task: 'Start `int n = 8;`. Print n, then n++ and n again, then ++n and n again \u2014 five lines total.',
    given: 'int n = 8;',
    solution: M('    int n = 8;\n\n    printf("%d\\n", n);\n    printf("%d\\n", n++);\n    printf("%d\\n", n);\n    printf("%d\\n", ++n);\n    printf("%d\\n", n);'),
    hint: 'Postfix reports the old value; prefix reports the new one.',
  },
  {
    slug: 'c-comparison',
    title: 'Comparing values',
    subtitle: 'Relational operators and truth',
    concept:
      'The six comparisons are `== != < > <= >=`. Each produces `1` for true and `0` for false \u2014 C had no boolean type until C99, and even now `bool` is a thin layer over an integer.\n\nThat means **any non-zero value is true** and zero is false. `if (5)` is always taken.\n\nThe notorious pitfall: `=` assigns and `==` compares. `if (x = 5)` assigns 5 to x and is always true, which compiles happily. Enabling warnings catches it.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int x = 7;\n\n    printf("%d\\n", x == 7);\n    printf("%d\\n", x != 7);\n    printf("%d\\n", x < 10);\n    printf("%d\\n", x > 10);\n    printf("%d\\n", x >= 7);\n    return 0;\n}',
    note: 'Comparisons print as 1 or 0 because that is literally what they evaluate to.',
    task: 'With `int n = 15;` print whether it equals 15, is under 10, is at least 15, and differs from 20 \u2014 four lines of 1 or 0.',
    given: 'int n = 15;',
    solution: M('    int n = 15;\n\n    printf("%d\\n", n == 15);\n    printf("%d\\n", n < 10);\n    printf("%d\\n", n >= 15);\n    printf("%d\\n", n != 20);'),
    hint: 'Print each comparison directly with %d.',
  },
  {
    slug: 'c-logical',
    title: 'Combining conditions',
    subtitle: '&&, || and !',
    concept:
      '`&&` is logical and, `||` is logical or, `!` is negation. All treat any non-zero value as true and produce 1 or 0.\n\nBoth `&&` and `||` **short-circuit**: `&&` stops at the first false operand, `||` at the first true one. The remaining operands are not evaluated at all.\n\nThis is not merely an optimisation \u2014 it is a guarantee you can rely on. `if (n != 0 && 100 / n > 2)` is safe precisely because the division is skipped when n is zero.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int age = 25, member = 0;\n\n    printf("%d\\n", age > 18 && age < 65);\n    printf("%d\\n", member || age > 21);\n    printf("%d\\n", !member);\n\n    int n = 0;\n    printf("%d\\n", n != 0 && 100 / n > 2);\n    return 0;\n}',
    note: 'The last line does not crash: && never evaluates the division because n != 0 is already false.',
    task: 'With `int temp = 22` and `int raining = 1`, print whether it is warm and dry, warm or dry, and not raining. Treat warm as above 20.',
    given: 'int temp = 22;\nint raining = 1;',
    solution: M('    int temp = 22;\n    int raining = 1;\n\n    printf("%d\\n", temp > 20 && !raining);\n    printf("%d\\n", temp > 20 || !raining);\n    printf("%d\\n", !raining);'),
    hint: 'Dry is simply !raining.',
  },
  {
    slug: 'c-bitwise',
    title: 'Working with individual bits',
    subtitle: 'Bitwise operators',
    concept:
      'C gives direct access to the bits of an integer:\n\n- `&` and \u2014 1 only where both bits are 1\n- `|` or \u2014 1 where either is\n- `^` xor \u2014 1 where exactly one is\n- `~` not \u2014 flips every bit\n- `<<` and `>>` shift left and right\n\nShifting left by n multiplies by 2\u207f; shifting right divides. These are the tools for flags, masks and low-level protocol work.\n\nDo not confuse `&` with `&&`: the first works bit by bit, the second on whole truth values.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int a = 12, b = 10;\n\n    printf("%d\\n", a & b);\n    printf("%d\\n", a | b);\n    printf("%d\\n", a ^ b);\n    printf("%d\\n", a << 1);\n    printf("%d\\n", a >> 2);\n    return 0;\n}',
    note: '12 is 1100 and 10 is 1010 in binary, so & gives 1000 (8) and | gives 1110 (14).',
    task: 'With `int flags = 0b1010;` (or 10), print the result of anding with 12, oring with 5, xoring with 15, shifting left once and shifting right once.',
    given: 'int flags = 10;',
    solution: M('    int flags = 10;\n\n    printf("%d\\n", flags & 12);\n    printf("%d\\n", flags | 5);\n    printf("%d\\n", flags ^ 15);\n    printf("%d\\n", flags << 1);\n    printf("%d\\n", flags >> 1);'),
    hint: 'Each operator applies to the binary representation.',
  },
  {
    slug: 'c-precedence',
    title: 'Which operation binds tighter',
    subtitle: 'Precedence and parentheses',
    concept:
      'C has fifteen precedence levels. The ones that matter daily, tightest first:\n\n1. `++ -- !` and casts\n2. `* / %`\n3. `+ -`\n4. `<< >>`\n5. `< > <= >=`\n6. `== !=`\n7. `&`, then `^`, then `|`\n8. `&&`, then `||`\n9. `?:` and assignment\n\nThe surprise is that the bitwise operators bind **looser** than comparisons, so `a & 1 == 0` means `a & (1 == 0)` \u2014 almost never what you wanted. Parenthesise bitwise expressions.',
    example: '#include <stdio.h>\n\nint main(void) {\n    printf("%d\\n", 2 + 3 * 4);\n    printf("%d\\n", (2 + 3) * 4);\n    printf("%d\\n", 10 - 4 - 3);\n    printf("%d\\n", 8 & 1 == 0);\n    printf("%d\\n", (8 & 1) == 0);\n    return 0;\n}',
    note: 'The last two differ: without parentheses the comparison happens first, giving 8 & 0 which is 0.',
    task: 'Print `5 + 2 * 3`, `(5 + 2) * 3`, `20 / 4 / 5` and `(12 & 10) == 8` \u2014 four lines.',
    solution: M('    printf("%d\\n", 5 + 2 * 3);\n    printf("%d\\n", (5 + 2) * 3);\n    printf("%d\\n", 20 / 4 / 5);\n    printf("%d\\n", (12 & 10) == 8);'),
    hint: 'Division is left-associative, so 20 / 4 / 5 is (20 / 4) / 5.',
  },
  {
    slug: 'c-ternary',
    title: 'A choice inside an expression',
    subtitle: 'The conditional operator',
    concept:
      'The conditional operator `?:` is C\u2019s only three-operand operator:\n\n`condition ? value_if_true : value_if_false`\n\nUnlike `if`, it is an **expression**, so it produces a value usable in an assignment or directly inside printf. It is the concise way to choose between two values.\n\nKeep it simple. Nesting several is legal but rapidly unreadable \u2014 use `if` for anything with real branching.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int n = 7;\n\n    int larger = n > 5 ? n : 5;\n    printf("%d\\n", larger);\n    printf("%s\\n", n % 2 == 0 ? "even" : "odd");\n\n    int a = 17, b = 42;\n    printf("%d\\n", a > b ? a : b);\n    return 0;\n}',
    note: 'Both branches must have compatible types, since the whole thing must have one type.',
    task: 'With `int n = -8;` use the conditional operator to print its magnitude and the word `neg` or `pos`.',
    given: 'int n = -8;',
    solution: M('    int n = -8;\n\n    printf("%d\\n", n < 0 ? -n : n);\n    printf("%s\\n", n < 0 ? "neg" : "pos");'),
    hint: 'Negating a negative gives its magnitude.',
  },
  d('c-op-mixed', 'Four expressions', 'Print 7 + 3 * 2, (7 + 3) * 2, 19 % 4 and 19 / 4.',
    M('    printf("%d\\n", 7 + 3 * 2);\n    printf("%d\\n", (7 + 3) * 2);\n    printf("%d\\n", 19 % 4);\n    printf("%d\\n", 19 / 4);'), 'Multiplication binds tighter than addition.'),
  d('c-op-even', 'Even and divisible', 'With int n = 34, print whether it is even and whether it divides by 3, as 1 or 0.',
    M('    int n = 34;\n    printf("%d\\n", n % 2 == 0);\n    printf("%d\\n", n % 3 == 0);'), 'A zero remainder means it divides exactly.'),
  d('c-op-range', 'Inside a range', 'With int score = 72, print whether it lies between 70 and 80 inclusive using &&.',
    M('    int score = 72;\n    printf("%d\\n", score >= 70 && score <= 80);'), 'C has no chained comparisons, so use two tests joined by &&.'),
  d('c-op-increment-loop', 'Increment sequence', 'Start at 0. Print the value after ++, after +=3, after *=4 and after --.',
    M('    int v = 0;\n    ++v;\n    printf("%d\\n", v);\n    v += 3;\n    printf("%d\\n", v);\n    v *= 4;\n    printf("%d\\n", v);\n    --v;\n    printf("%d\\n", v);'), 'Each operator updates in place.'),
  d('c-op-leap', 'Leap year expression', 'With int year = 2024, print 1 or 0 for whether it is a leap year, as a single expression.',
    M('    int year = 2024;\n    printf("%d\\n", (year % 4 == 0 && year % 100 != 0) || year % 400 == 0);'), 'Parenthesise the && clause before the ||.'),
  d('c-op-bit-flags', 'Flag testing', 'With int flags = 0b0110 (6), test bit 1 and bit 3 with & and print each result as 1 or 0.',
    M('    int flags = 6;\n    printf("%d\\n", (flags & 2) != 0);\n    printf("%d\\n", (flags & 8) != 0);'), 'Bit n has the value 2 to the power n.'),
  d('c-op-set-bit', 'Setting and clearing bits', 'Start with int x = 8. Set bit 0 with |, then clear bit 3 with & and ~, printing after each.',
    M('    int x = 8;\n    x |= 1;\n    printf("%d\\n", x);\n    x &= ~8;\n    printf("%d\\n", x);'), '&= ~mask clears the bits in the mask.'),
  d('c-op-shift-power', 'Shifts as powers of two', 'Print 1 shifted left by 0, 4, 8 and 10.',
    M('    printf("%d\\n", 1 << 0);\n    printf("%d\\n", 1 << 4);\n    printf("%d\\n", 1 << 8);\n    printf("%d\\n", 1 << 10);'), 'Shifting left by n multiplies by 2 to the power n.'),
  d('c-op-xor-swap', 'Swapping with xor', 'Swap int a = 5 and b = 9 using only ^= operations, printing before and after.',
    M('    int a = 5, b = 9;\n    printf("%d %d\\n", a, b);\n    a ^= b;\n    b ^= a;\n    a ^= b;\n    printf("%d %d\\n", a, b);'), 'Three xor steps exchange the values without a temporary.'),
  d('c-op-ternary-max', 'Largest of three', 'With ints 14, 27 and 9, print the largest using nested conditional operators.',
    M('    int a = 14, b = 27, c = 9;\n    int m = a > b ? a : b;\n    printf("%d\\n", m > c ? m : c);'), 'Find the larger pair first, then compare with the third.'),
  d('c-op-ternary-grade', 'Grade with ternaries', 'With int score = 84, print A, B, C or F using chained conditional operators.',
    M('    int score = 84;\n    printf("%s\\n", score >= 90 ? "A" : score >= 80 ? "B" : score >= 70 ? "C" : "F");'), 'Conditional operators chain to the right.'),
  d('c-op-short-circuit', 'Safe division', 'With int n = 0, print the result of n != 0 && 100 / n > 2 without crashing.',
    M('    int n = 0;\n    printf("%d\\n", n != 0 && 100 / n > 2);'), '&& guarantees the right side is skipped when the left is false.'),
  d('c-op-precedence-trap', 'The bitwise trap', 'Print 8 & 1 == 0 and (8 & 1) == 0 to see why parentheses matter.',
    M('    printf("%d\\n", 8 & 1 == 0);\n    printf("%d\\n", (8 & 1) == 0);'), 'Comparisons bind tighter than bitwise and.'),
  d('c-op-negatives', 'Negative division', 'Print -7 / 2, 7 / 2, -7 % 2 and 7 % 2.',
    M('    printf("%d\\n", -7 / 2);\n    printf("%d\\n", 7 / 2);\n    printf("%d\\n", -7 % 2);\n    printf("%d\\n", 7 % 2);'), 'C truncates toward zero, so -7 / 2 is -3.'),
  d('c-op-divisible-report', 'Divisibility report', 'With int n = 60, print 1 or 0 for divisibility by 2, 3, 4 and 7.',
    M('    int n = 60;\n    printf("%d\\n", n % 2 == 0);\n    printf("%d\\n", n % 3 == 0);\n    printf("%d\\n", n % 4 == 0);\n    printf("%d\\n", n % 7 == 0);'), 'A zero remainder means it divides exactly.'),
  d('c-op-compound-all', 'Every compound operator', 'Start at 100. Apply -=20, /=2, %=7 and <<=3, printing after each.',
    M('    int v = 100;\n    v -= 20;\n    printf("%d\\n", v);\n    v /= 2;\n    printf("%d\\n", v);\n    v %= 7;\n    printf("%d\\n", v);\n    v <<= 3;\n    printf("%d\\n", v);'), 'Bitwise operators have compound forms too.'),
  d('c-op-abs-ternary', 'Absolute value', 'With ints -17, 0 and 42, print the absolute value of each using the conditional operator.',
    M('    int values[3] = {-17, 0, 42};\n    for (int i = 0; i < 3; i++) {\n        int n = values[i];\n        printf("%d\\n", n < 0 ? -n : n);\n    }'), 'A small array plus a loop avoids repeating the expression.'),
  d('c-op-capstone', 'Operator summary', 'With int n = 145, print the number of full dozens, the leftover, whether it is odd, n squared, and n shifted left by two.',
    M('    int n = 145;\n    printf("%d\\n", n / 12);\n    printf("%d\\n", n % 12);\n    printf("%d\\n", n % 2 == 1);\n    printf("%d\\n", n * n);\n    printf("%d\\n", n << 2);'), 'Five separate expressions.'),
  {
    kind: 'quiz',
    slug: 'c-operators-quiz',
    title: 'Checkpoint: operators',
    questions: [
      { q: 'What does i++ evaluate to?', options: ['The new value', 'The old value, before incrementing', 'Always 1', 'Nothing'], answer: 1, explain: 'Postfix yields the value before the increment; ++i yields the value after it. Both leave i one larger.' },
      { q: 'What does a comparison like x < 10 produce in C?', options: ['true or false keywords', '1 or 0', 'A string', 'Nothing'], answer: 1, explain: 'C comparisons are integer expressions producing 1 or 0, which is why they can be printed with %d.' },
      { q: 'Why is 8 & 1 == 0 surprising?', options: ['It is a syntax error', 'The comparison binds tighter, so it means 8 & (1 == 0)', 'It always gives 1', '& cannot be used with integers'], answer: 1, explain: 'Bitwise operators sit below comparisons in precedence, so bitwise expressions almost always need parentheses.' },
      { q: 'What is the difference between & and &&?', options: ['None', '& works bit by bit; && works on whole truth values and short-circuits', '&& is faster', '& only works on floats'], answer: 1, explain: '& combines corresponding bits of both operands; && evaluates truth and may skip the right operand entirely.' },
      { q: 'What does 1 << 10 equal?', options: ['10', '1024', '11', '100'], answer: 1, explain: 'Shifting left by n multiplies by 2 to the power n, and 2 to the power 10 is 1024.' },
    ],
  },
];

export const cConditionals = [
  {
    slug: 'c-if',
    title: 'Branching',
    subtitle: 'if and else',
    concept:
      '`if` takes a condition in **parentheses** and a block in **braces**. Indentation is purely cosmetic here \u2014 the braces define the block.\n\nOmitting braces for a single statement is legal, but it is a well-known source of bugs: adding a second line later silently leaves it outside the if. Always use braces.\n\nSince the condition is just an integer test, `if (count)` means "if count is non-zero".',
    example: '#include <stdio.h>\n\nint main(void) {\n    int temp = 31;\n\n    if (temp > 30) {\n        printf("hot\\n");\n    } else {\n        printf("not hot\\n");\n    }\n\n    int count = 0;\n    if (count) {\n        printf("has items\\n");\n    } else {\n        printf("empty\\n");\n    }\n    return 0;\n}',
    note: 'if (count) is idiomatic C for "count is non-zero" \u2014 no comparison needed.',
    task: 'With `int n = 12;` print `even` or `odd`, then `big` or `small` treating over 10 as big.',
    given: 'int n = 12;',
    solution: M('    int n = 12;\n\n    if (n % 2 == 0) {\n        printf("even\\n");\n    } else {\n        printf("odd\\n");\n    }\n\n    if (n > 10) {\n        printf("big\\n");\n    } else {\n        printf("small\\n");\n    }'),
    hint: 'Use == not = in the comparison.',
  },
  {
    slug: 'c-else-if',
    title: 'Many branches',
    subtitle: 'else if chains',
    concept:
      'C has no `elif` keyword \u2014 it simply allows another `if` directly after `else`, giving `else if`.\n\nConditions are tested **in order** and the first match wins. Order therefore matters: with descending thresholds, put the highest first, or a broader condition will capture everything below it.\n\nThe final `else` is optional but usually wise, since it documents the fallback case.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int score = 74;\n\n    if (score >= 90) {\n        printf("A\\n");\n    } else if (score >= 80) {\n        printf("B\\n");\n    } else if (score >= 70) {\n        printf("C\\n");\n    } else {\n        printf("F\\n");\n    }\n    return 0;\n}',
    note: '74 fails the first two tests and passes the third. Reversing the order would print C for 95 as well.',
    task: 'With `int temp = 5;` print `freezing` below 0, `cold` below 10, `mild` below 25, otherwise `hot`.',
    given: 'int temp = 5;',
    solution: M('    int temp = 5;\n\n    if (temp < 0) {\n        printf("freezing\\n");\n    } else if (temp < 10) {\n        printf("cold\\n");\n    } else if (temp < 25) {\n        printf("mild\\n");\n    } else {\n        printf("hot\\n");\n    }'),
    hint: 'Ascending thresholds mean each else if only sees what fell through.',
  },
  {
    slug: 'c-switch',
    title: 'Matching one value against many',
    subtitle: 'switch, case and break',
    concept:
      '`switch` compares one integer or character against a list of `case` labels. It is often clearer than a long `else if` chain when every test compares the same variable.\n\nThe crucial detail is **fall-through**: without a `break`, execution continues into the next case. That is occasionally useful for grouping labels, and is otherwise a classic bug.\n\n`default` handles anything unmatched. `switch` works only on integer types \u2014 not on strings or floats.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int day = 6;\n\n    switch (day) {\n        case 6:\n        case 7:\n            printf("weekend\\n");\n            break;\n        case 1:\n            printf("monday\\n");\n            break;\n        default:\n            printf("weekday\\n");\n            break;\n    }\n    return 0;\n}',
    note: 'Case 6 has no body, so it falls through into case 7 \u2014 the intentional use of fall-through for grouping.',
    task: 'With `char grade = \'B\';` use switch to print `excellent` for A, `good` for B, `pass` for C, otherwise `fail`.',
    given: "char grade = 'B';",
    solution: M('    char grade = \'B\';\n\n    switch (grade) {\n        case \'A\':\n            printf("excellent\\n");\n            break;\n        case \'B\':\n            printf("good\\n");\n            break;\n        case \'C\':\n            printf("pass\\n");\n            break;\n        default:\n            printf("fail\\n");\n            break;\n    }'),
    hint: 'Character literals in single quotes work as case labels.',
  },
  {
    slug: 'c-nested-if',
    title: 'Conditions within conditions',
    subtitle: 'Nesting and flattening',
    concept:
      'An `if` may contain another. Each level adds indentation and one more thing for the reader to hold in mind.\n\nDeep nesting is a smell. Two techniques flatten it: combine conditions with `&&`, or use an **early return** to handle the failure case first and let the main path continue unindented.\n\nEarly returns are particularly effective in functions with several validation checks.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int age = 25, ticket = 1;\n\n    if (age >= 18) {\n        if (ticket) {\n            printf("enter\\n");\n        } else {\n            printf("buy a ticket\\n");\n        }\n    } else {\n        printf("too young\\n");\n    }\n\n    if (age >= 18 && ticket) {\n        printf("enter (flat)\\n");\n    }\n    return 0;\n}',
    note: 'The flattened version handles only the success case, which is often all you need.',
    task: 'With `int logged_in = 1` and `int is_admin = 0`, print `admin panel` when both hold, `user area` when logged in only, and `please log in` otherwise.',
    given: 'int logged_in = 1;\nint is_admin = 0;',
    solution: M('    int logged_in = 1;\n    int is_admin = 0;\n\n    if (logged_in) {\n        if (is_admin) {\n            printf("admin panel\\n");\n        } else {\n            printf("user area\\n");\n        }\n    } else {\n        printf("please log in\\n");\n    }'),
    hint: 'Test logged_in on the outside.',
  },
  {
    slug: 'c-bool-header',
    title: 'A real boolean type',
    subtitle: 'stdbool.h',
    concept:
      'C99 added `<stdbool.h>`, which defines `bool`, `true` and `false`. Underneath it is still an integer, but the names document intent far better than `int flag = 1;`.\n\nUse `bool` for anything genuinely two-valued. Print it with `%d` (giving 1 or 0) or convert it yourself with the conditional operator.\n\nModern C code uses `bool` throughout; older code you will read uses plain `int`.',
    example: '#include <stdio.h>\n#include <stdbool.h>\n\nint main(void) {\n    bool raining = true;\n    bool warm = false;\n\n    printf("%d %d\\n", raining, warm);\n    printf("%s\\n", raining ? "wet" : "dry");\n\n    if (raining && !warm) {\n        printf("miserable\\n");\n    }\n    return 0;\n}',
    note: 'A bool assigned any non-zero value becomes exactly 1, which makes comparisons predictable.',
    task: 'Using stdbool.h, declare `bool is_open = true` and `bool has_key = false`, print both as numbers, then print `enter` or `wait` based on whether either is true.',
    solution: '#include <stdio.h>\n#include <stdbool.h>\n\nint main(void) {\n    bool is_open = true;\n    bool has_key = false;\n\n    printf("%d\\n", is_open);\n    printf("%d\\n", has_key);\n    printf("%s\\n", (is_open || has_key) ? "enter" : "wait");\n    return 0;\n}\n',
    hint: 'Remember to include <stdbool.h>.',
  },
  d('c-cond-sign', 'Sign of a number', 'With int n = 0, print positive, negative or zero.',
    M('    int n = 0;\n\n    if (n > 0) {\n        printf("positive\\n");\n    } else if (n < 0) {\n        printf("negative\\n");\n    } else {\n        printf("zero\\n");\n    }'), 'Zero belongs in the final else.'),
  d('c-cond-grade', 'Letter grade', 'With int score = 88, print A/B/C/D/F using the usual ten-point bands.',
    M('    int score = 88;\n\n    if (score >= 90) {\n        printf("A\\n");\n    } else if (score >= 80) {\n        printf("B\\n");\n    } else if (score >= 70) {\n        printf("C\\n");\n    } else if (score >= 60) {\n        printf("D\\n");\n    } else {\n        printf("F\\n");\n    }'), 'Highest threshold first.'),
  d('c-cond-fizz-one', 'FizzBuzz for one value', 'With int n = 15, print FizzBuzz, Fizz, Buzz or the number.',
    M('    int n = 15;\n\n    if (n % 15 == 0) {\n        printf("FizzBuzz\\n");\n    } else if (n % 3 == 0) {\n        printf("Fizz\\n");\n    } else if (n % 5 == 0) {\n        printf("Buzz\\n");\n    } else {\n        printf("%d\\n", n);\n    }'), 'Check the combined case first.'),
  d('c-cond-max3', 'Largest of three', 'With ints 14, 27 and 9, print the largest using if/else if only.',
    M('    int a = 14, b = 27, c = 9;\n\n    if (a >= b && a >= c) {\n        printf("%d\\n", a);\n    } else if (b >= c) {\n        printf("%d\\n", b);\n    } else {\n        printf("%d\\n", c);\n    }'), 'Once a is ruled out only b and c remain.'),
  d('c-cond-leap', 'Leap year branches', 'With int year = 1900, print leap or not leap using nested conditions.',
    M('    int year = 1900;\n\n    if (year % 400 == 0) {\n        printf("leap\\n");\n    } else if (year % 100 == 0) {\n        printf("not leap\\n");\n    } else if (year % 4 == 0) {\n        printf("leap\\n");\n    } else {\n        printf("not leap\\n");\n    }'), '1900 divides by 100 but not 400.'),
  d('c-cond-ticket', 'Ticket price bands', 'With int age = 67, print 0 under 5, 8 under 18, 15 under 65, otherwise 10.',
    M('    int age = 67;\n\n    if (age < 5) {\n        printf("0\\n");\n    } else if (age < 18) {\n        printf("8\\n");\n    } else if (age < 65) {\n        printf("15\\n");\n    } else {\n        printf("10\\n");\n    }'), 'Ascending bands.'),
  d('c-cond-switch-month', 'Days in a month', 'With int month = 4, use switch to print 30, 31 or 28.',
    M('    int month = 4;\n\n    switch (month) {\n        case 2:\n            printf("28\\n");\n            break;\n        case 4:\n        case 6:\n        case 9:\n        case 11:\n            printf("30\\n");\n            break;\n        default:\n            printf("31\\n");\n            break;\n    }'), 'Group the 30-day months by stacking case labels.'),
  d('c-cond-switch-vowel', 'Vowel check with switch', 'With char ch = \'e\', use switch to print vowel or consonant.',
    M('    char ch = \'e\';\n\n    switch (ch) {\n        case \'a\':\n        case \'e\':\n        case \'i\':\n        case \'o\':\n        case \'u\':\n            printf("vowel\\n");\n            break;\n        default:\n            printf("consonant\\n");\n            break;\n    }'), 'Stack the five vowel labels together.'),
  d('c-cond-switch-op', 'A tiny calculator', 'With char op = \'*\', int a = 6 and b = 7, use switch to print the result of the operation.',
    M('    char op = \'*\';\n    int a = 6, b = 7;\n\n    switch (op) {\n        case \'+\':\n            printf("%d\\n", a + b);\n            break;\n        case \'-\':\n            printf("%d\\n", a - b);\n            break;\n        case \'*\':\n            printf("%d\\n", a * b);\n            break;\n        default:\n            printf("unknown\\n");\n            break;\n    }'), 'Each case handles one operator.'),
  d('c-cond-bmi', 'BMI bands', 'With double bmi = 22.5, print under, normal, over or obese.',
    M('    double bmi = 22.5;\n\n    if (bmi < 18.5) {\n        printf("under\\n");\n    } else if (bmi < 25) {\n        printf("normal\\n");\n    } else if (bmi < 30) {\n        printf("over\\n");\n    } else {\n        printf("obese\\n");\n    }'), 'Float comparisons work like integer ones.'),
  d('c-cond-triangle', 'Triangle type', 'With sides 5, 5 and 8, print equilateral, isosceles or scalene.',
    M('    int a = 5, b = 5, c = 8;\n\n    if (a == b && b == c) {\n        printf("equilateral\\n");\n    } else if (a == b || b == c || a == c) {\n        printf("isosceles\\n");\n    } else {\n        printf("scalene\\n");\n    }'), 'C needs && to chain equality tests.'),
  d('c-cond-password', 'Password strength', 'With int len = 9, print weak under 8, medium under 12, otherwise strong.',
    M('    int len = 9;\n\n    if (len < 8) {\n        printf("weak\\n");\n    } else if (len < 12) {\n        printf("medium\\n");\n    } else {\n        printf("strong\\n");\n    }'), 'Ascending thresholds again.'),
  d('c-cond-quadrant', 'Coordinate quadrant', 'With int x = -3 and y = 4, print Q1 to Q4 or axis.',
    M('    int x = -3, y = 4;\n\n    if (x == 0 || y == 0) {\n        printf("axis\\n");\n    } else if (x > 0 && y > 0) {\n        printf("Q1\\n");\n    } else if (x < 0 && y > 0) {\n        printf("Q2\\n");\n    } else if (x < 0 && y < 0) {\n        printf("Q3\\n");\n    } else {\n        printf("Q4\\n");\n    }'), 'Handle the axis case first.'),
  d('c-cond-bool-flags', 'Boolean flags', 'Using stdbool.h with bool a = true and bool b = false, print a && b, a || b and !b.',
    '#include <stdio.h>\n#include <stdbool.h>\n\nint main(void) {\n    bool a = true, b = false;\n    printf("%d\\n", a && b);\n    printf("%d\\n", a || b);\n    printf("%d\\n", !b);\n    return 0;\n}\n', 'Print booleans with %d.'),
  d('c-cond-speed', 'Speeding penalty', 'With int speed = 92 and limit 70, print ok, fine 100, fine 250 or court.',
    M('    int speed = 92, limit = 70;\n    int over = speed - limit;\n\n    if (over <= 0) {\n        printf("ok\\n");\n    } else if (over <= 10) {\n        printf("fine 100\\n");\n    } else if (over <= 30) {\n        printf("fine 250\\n");\n    } else {\n        printf("court\\n");\n    }'), 'Compute the excess once, then band it.'),
  d('c-cond-shipping', 'Free shipping', 'With int total = 45 and int member = 0, print 0 when total is at least 50 or the customer is a member, otherwise 6.',
    M('    int total = 45, member = 0;\n\n    if (total >= 50 || member) {\n        printf("0\\n");\n    } else {\n        printf("6\\n");\n    }'), 'A single || covers both routes.'),
  d('c-cond-fallthrough', 'Deliberate fall-through', 'With int level = 2, use a switch without breaks so levels 3, 2 and 1 all print their own line, demonstrating fall-through.',
    M('    int level = 2;\n\n    switch (level) {\n        case 3:\n            printf("three\\n");\n        case 2:\n            printf("two\\n");\n        case 1:\n            printf("one\\n");\n    }'), 'Without break, execution continues into the next case.'),
  d('c-cond-capstone', 'Conditional summary', 'With int hour = 14, print the part of day (morning/afternoon/evening/night), then working or off for 9 to 17 inclusive using the conditional operator.',
    M('    int hour = 14;\n\n    if (hour < 12) {\n        printf("morning\\n");\n    } else if (hour < 18) {\n        printf("afternoon\\n");\n    } else if (hour < 22) {\n        printf("evening\\n");\n    } else {\n        printf("night\\n");\n    }\n\n    printf("%s\\n", (hour >= 9 && hour <= 17) ? "working" : "off");'), 'The second line suits the ternary operator.'),
  {
    kind: 'quiz',
    slug: 'c-conditionals-quiz',
    title: 'Checkpoint: conditionals',
    questions: [
      { q: 'What is wrong with if (x = 5)?', options: ['Nothing', 'It assigns 5 to x and is always true', 'It is a syntax error', 'It compares incorrectly'], answer: 1, explain: 'A single = assigns and the expression evaluates to 5, which is non-zero and therefore always true.' },
      { q: 'What happens in a switch case without break?', options: ['The switch ends', 'Execution falls through into the next case', 'A compile error', 'The default runs'], answer: 1, explain: 'Fall-through is the default behaviour, useful for grouping labels but a common source of bugs otherwise.' },
      { q: 'What can a switch statement test?', options: ['Any type', 'Only integer types including char', 'Only strings', 'Only booleans'], answer: 1, explain: 'switch requires an integral expression, so strings and floating-point values need if chains instead.' },
      { q: 'What does if (count) mean when count is an int?', options: ['If count exists', 'If count is non-zero', 'If count is exactly 1', 'A syntax error'], answer: 1, explain: 'Any non-zero value is true in C, so this is the idiomatic test for a non-zero counter or a non-null pointer.' },
      { q: 'Why should single-statement if bodies still use braces?', options: ['It is required', 'Adding a second line later would silently fall outside the if', 'It runs faster', 'To satisfy the preprocessor'], answer: 1, explain: 'Without braces only the first statement is conditional, which has caused real security bugs in shipped software.' },
    ],
  },
];
