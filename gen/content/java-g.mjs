const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body) => `public class Main {\n    public static void main(String[] args) {\n${body}\n    }\n}\n`;

export const javaGenerics = [
  {
    slug: 'java-exceptions-basics',
    title: 'When things go wrong',
    subtitle: 'try, catch and finally',
    concept:
      'An **exception** signals that an operation cannot proceed. Left unhandled it terminates the program with a stack trace.\n\n`try` wraps the risky code, `catch` handles a specific type, and `finally` **always** runs \u2014 on success, on failure, even on an early return \u2014 which makes it the right place for cleanup.\n\nCatch specific types before general ones, since the first matching block wins. `catch (Exception e)` at the top would swallow everything below it.\n\nEvery exception carries `.getMessage()`, and `e.getClass().getSimpleName()` names the type.',
    example: 'public class Main {\n    static int divide(int a, int b) {\n        try {\n            return a / b;\n        } catch (ArithmeticException e) {\n            System.out.println("caught: " + e.getMessage());\n            return 0;\n        } finally {\n            System.out.println("finally runs");\n        }\n    }\n\n    public static void main(String[] args) {\n        System.out.println(divide(10, 2));\n        System.out.println(divide(10, 0));\n\n        try {\n            Integer.parseInt("abc");\n        } catch (NumberFormatException e) {\n            System.out.println(e.getClass().getSimpleName());\n        }\n    }\n}',
    note: 'The finally block ran on both the successful and the failing call, even though the first returned normally.',
    task: 'Write a static `safeParse(String)` returning the parsed int or -1 on failure, printing `done` in a finally block. Call it with `"42"`, `"3.5"` and `"abc"`.',
    solution: 'public class Main {\n    static int safeParse(String text) {\n        try {\n            return Integer.parseInt(text);\n        } catch (NumberFormatException e) {\n            return -1;\n        } finally {\n            System.out.println("done");\n        }\n    }\n\n    public static void main(String[] args) {\n        System.out.println(safeParse("42"));\n        System.out.println(safeParse("3.5"));\n        System.out.println(safeParse("abc"));\n    }\n}\n',
    hint: 'Integer.parseInt throws NumberFormatException for "3.5" as well as "abc".',
  },
  {
    slug: 'java-checked-unchecked',
    title: 'Two kinds of exception',
    subtitle: 'Checked, unchecked and throws',
    concept:
      'Java uniquely divides exceptions into two categories.\n\n**Unchecked** exceptions extend `RuntimeException` \u2014 `NullPointerException`, `IllegalArgumentException`, `ArrayIndexOutOfBoundsException`. They represent programming errors and need no declaration.\n\n**Checked** exceptions extend `Exception` directly \u2014 `IOException`, `ParseException`. The compiler **forces** you either to catch them or to declare `throws` on the method. They represent conditions a correct program must anticipate.\n\nThe guidance: throw unchecked for programmer mistakes such as invalid arguments, and checked for genuinely external failures. `throw` raises one; `throws` declares that a method may propagate one.',
    example: 'public class Main {\n    static void validate(int age) {\n        if (age < 0) throw new IllegalArgumentException("age cannot be negative");\n        System.out.println("age " + age + " accepted");\n    }\n\n    static void mightFail(boolean fail) throws Exception {\n        if (fail) throw new Exception("checked failure");\n        System.out.println("no failure");\n    }\n\n    public static void main(String[] args) {\n        validate(30);\n\n        try {\n            validate(-5);\n        } catch (IllegalArgumentException e) {\n            System.out.println("rejected: " + e.getMessage());\n        }\n\n        try {\n            mightFail(false);\n            mightFail(true);\n        } catch (Exception e) {\n            System.out.println("caught: " + e.getMessage());\n        }\n    }\n}',
    note: 'validate needed no throws clause because IllegalArgumentException is unchecked; mightFail required one.',
    task: 'Write `withdraw(int balance, int amount)` throwing IllegalArgumentException for a non-positive amount and IllegalStateException for insufficient funds. Call it three times in try/catch blocks printing the result or the message.',
    solution: 'public class Main {\n    static int withdraw(int balance, int amount) {\n        if (amount <= 0) throw new IllegalArgumentException("amount must be positive");\n        if (amount > balance) throw new IllegalStateException("insufficient funds");\n        return balance - amount;\n    }\n\n    public static void main(String[] args) {\n        int[] attempts = {30, -5, 500};\n        for (int amount : attempts) {\n            try {\n                System.out.println(withdraw(100, amount));\n            } catch (IllegalArgumentException e) {\n                System.out.println("invalid: " + e.getMessage());\n            } catch (IllegalStateException e) {\n                System.out.println("failed: " + e.getMessage());\n            }\n        }\n    }\n}\n',
    hint: 'Both exception types here are unchecked, so no throws clause is needed.',
  },
  {
    slug: 'java-custom-exceptions',
    title: 'Your own exception types',
    subtitle: 'Extending Exception',
    concept:
      'A custom exception is a class extending `RuntimeException` (unchecked) or `Exception` (checked). The body can be nothing more than a constructor forwarding the message with `super(message)`.\n\nThe value is precision: callers can catch exactly your error rather than a generic type, and the class name documents the failure at the throw site.\n\nCustom exceptions can carry extra data as fields \u2014 a failing account number, a rejected value \u2014 which a plain message string cannot express in a machine-readable way.\n\nName them ending in `Exception`, and prefer unchecked unless callers can realistically recover.',
    example: 'class InsufficientFundsException extends RuntimeException {\n    private final int shortfall;\n\n    InsufficientFundsException(String message, int shortfall) {\n        super(message);\n        this.shortfall = shortfall;\n    }\n\n    int getShortfall() {\n        return shortfall;\n    }\n}\n\npublic class Main {\n    static int withdraw(int balance, int amount) {\n        if (amount > balance) {\n            throw new InsufficientFundsException("cannot withdraw " + amount, amount - balance);\n        }\n        return balance - amount;\n    }\n\n    public static void main(String[] args) {\n        System.out.println(withdraw(100, 40));\n\n        try {\n            withdraw(100, 250);\n        } catch (InsufficientFundsException e) {\n            System.out.println(e.getMessage());\n            System.out.println("short by " + e.getShortfall());\n        }\n    }\n}',
    note: 'The exception carried the shortfall as a field, which the caller could use programmatically.',
    task: 'Define `InvalidAgeException extends RuntimeException` carrying the rejected value as a field. Write a validate method throwing it for negatives, then call it with a valid and an invalid value, printing the message and the stored value.',
    solution: 'class InvalidAgeException extends RuntimeException {\n    private final int value;\n\n    InvalidAgeException(String message, int value) {\n        super(message);\n        this.value = value;\n    }\n\n    int getValue() {\n        return value;\n    }\n}\n\npublic class Main {\n    static int validate(int age) {\n        if (age < 0) throw new InvalidAgeException("age must not be negative", age);\n        return age;\n    }\n\n    public static void main(String[] args) {\n        System.out.println(validate(30));\n\n        try {\n            validate(-7);\n        } catch (InvalidAgeException e) {\n            System.out.println(e.getMessage());\n            System.out.println(e.getValue());\n        }\n    }\n}\n',
    hint: 'Forward the message to super and store the extra data in a field.',
  },
  {
    slug: 'java-generic-methods',
    title: 'One method, any type',
    subtitle: 'Generic methods',
    concept:
      'A **generic method** declares a type parameter before its return type:\n\n`static <T> T first(List<T> items)`\n\nThe `<T>` introduces T as a placeholder, and the compiler infers it from the arguments at each call. The result is type-safe: `first(listOfStrings)` returns a String, with no cast and no possibility of a ClassCastException.\n\nBefore generics, collections held `Object` and every retrieval needed a cast that could fail at run time. Generics moved that check to compile time.\n\nA **bounded** parameter constrains what T can be: `<T extends Comparable<T>>` guarantees T can be compared, which is what a generic max function needs.',
    example: 'import java.util.List;\n\npublic class Main {\n    static <T> void printAll(List<T> items) {\n        for (T item : items) System.out.println(item);\n    }\n\n    static <T> T firstOf(List<T> items) {\n        return items.get(0);\n    }\n\n    static <T extends Comparable<T>> T largest(List<T> items) {\n        T best = items.get(0);\n        for (T item : items) {\n            if (item.compareTo(best) > 0) best = item;\n        }\n        return best;\n    }\n\n    public static void main(String[] args) {\n        printAll(List.of(1, 2, 3));\n        printAll(List.of("a", "b"));\n\n        String s = firstOf(List.of("first", "second"));\n        System.out.println(s);\n\n        System.out.println(largest(List.of(3, 9, 2)));\n        System.out.println(largest(List.of("apple", "cherry", "banana")));\n    }\n}',
    note: 'largest required the Comparable bound; without it the compiler would reject the compareTo call.',
    task: 'Write a generic `<T> int count(List<T>)`, a generic `<T> T lastOf(List<T>)`, and a bounded `<T extends Comparable<T>> T smallest(List<T>)`. Use all three with a List<Integer> and a List<String>.',
    solution: 'import java.util.List;\n\npublic class Main {\n    static <T> int count(List<T> items) {\n        return items.size();\n    }\n\n    static <T> T lastOf(List<T> items) {\n        return items.get(items.size() - 1);\n    }\n\n    static <T extends Comparable<T>> T smallest(List<T> items) {\n        T best = items.get(0);\n        for (T item : items) {\n            if (item.compareTo(best) < 0) best = item;\n        }\n        return best;\n    }\n\n    public static void main(String[] args) {\n        List<Integer> nums = List.of(5, 2, 9);\n        List<String> words = List.of("pear", "apple", "fig");\n\n        System.out.println(count(nums));\n        System.out.println(lastOf(nums));\n        System.out.println(smallest(nums));\n\n        System.out.println(count(words));\n        System.out.println(lastOf(words));\n        System.out.println(smallest(words));\n    }\n}\n',
    hint: 'The type parameter goes before the return type, after any modifiers.',
  },
  {
    slug: 'java-generic-classes',
    title: 'Generic types of your own',
    subtitle: 'Generic classes',
    concept:
      'A class can take type parameters: `class Box<T>`. Inside, T is used like any other type, and `Box<String>` and `Box<Integer>` become distinct, fully checked types.\n\nA class may take several parameters, as `Map<K, V>` does.\n\nOne limitation worth knowing: Java implements generics by **erasure**, meaning the type argument is discarded after compilation. So `new T()` is impossible, primitive arguments are not allowed, and `List<String>` and `List<Integer>` are the same class at run time. Erasure kept generics backward compatible with older code, at the cost of these restrictions.',
    example: 'public class Main {\n    static class Box<T> {\n        private T value;\n\n        Box(T value) { this.value = value; }\n\n        T get() { return value; }\n        void set(T value) { this.value = value; }\n\n        @Override\n        public String toString() { return "Box(" + value + ")"; }\n    }\n\n    static class Pair<A, B> {\n        final A first;\n        final B second;\n\n        Pair(A first, B second) {\n            this.first = first;\n            this.second = second;\n        }\n    }\n\n    public static void main(String[] args) {\n        Box<Integer> a = new Box<>(42);\n        Box<String> b = new Box<>("text");\n\n        System.out.println(a);\n        System.out.println(b.get().toUpperCase());\n\n        a.set(99);\n        System.out.println(a.get());\n\n        Pair<String, Integer> p = new Pair<>("ada", 91);\n        System.out.println(p.first + " " + p.second);\n    }\n}',
    note: 'b.get() returned a String, so toUpperCase compiled with no cast \u2014 that is the safety generics provide.',
    task: 'Define a generic `Stack<T>` backed by an ArrayList with push, pop, size and isEmpty. Use it with Integers and Strings, printing sizes and popped values.',
    solution: 'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n    static class Stack<T> {\n        private final List<T> items = new ArrayList<>();\n\n        void push(T item) { items.add(item); }\n\n        T pop() { return items.remove(items.size() - 1); }\n\n        int size() { return items.size(); }\n\n        boolean isEmpty() { return items.isEmpty(); }\n    }\n\n    public static void main(String[] args) {\n        Stack<Integer> numbers = new Stack<>();\n        numbers.push(1);\n        numbers.push(2);\n        numbers.push(3);\n        System.out.println(numbers.size());\n        System.out.println(numbers.pop());\n        System.out.println(numbers.size());\n\n        Stack<String> words = new Stack<>();\n        words.push("first");\n        words.push("second");\n        System.out.println(words.pop());\n        System.out.println(words.isEmpty());\n    }\n}\n',
    hint: 'Store the elements in a List<T> field.',
  },
  {
    slug: 'java-wildcards',
    title: 'Accepting related types',
    subtitle: 'Wildcards',
    concept:
      'Generics are **invariant**: a `List<Integer>` is *not* a `List<Number>`, even though Integer is a Number. That surprises everyone at first, but it is necessary \u2014 if it were allowed you could add a Double to a list of Integers.\n\nWildcards restore the flexibility safely.\n\n`List<?>` is a list of unknown type. You can read from it as Object and check its size, but not add anything.\n\n`List<? extends Number>` accepts a List of Number or any subtype, and is **read-only** for the same reason. `List<? super Integer>` accepts Integer or any supertype and is safe to **write** to.\n\nThe mnemonic is PECS: Producer Extends, Consumer Super.',
    example: 'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n    static void printSize(List<?> items) {\n        System.out.println("size " + items.size());\n    }\n\n    static double total(List<? extends Number> numbers) {\n        double sum = 0;\n        for (Number n : numbers) sum += n.doubleValue();\n        return sum;\n    }\n\n    static void addNumbers(List<? super Integer> target) {\n        target.add(1);\n        target.add(2);\n    }\n\n    public static void main(String[] args) {\n        printSize(List.of("a", "b"));\n        printSize(List.of(1, 2, 3));\n\n        System.out.println(total(List.of(1, 2, 3)));\n        System.out.println(total(List.of(1.5, 2.5)));\n\n        List<Number> target = new ArrayList<>();\n        addNumbers(target);\n        System.out.println(target);\n    }\n}',
    note: 'total accepted both a List<Integer> and a List<Double> because of the extends wildcard.',
    task: 'Write `printSize(List<?>)`, `double sum(List<? extends Number>)` and `void fill(List<? super Integer>)`. Demonstrate all three, calling sum with both a List<Integer> and a List<Double>.',
    solution: 'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n    static void printSize(List<?> items) {\n        System.out.println(items.size());\n    }\n\n    static double sum(List<? extends Number> numbers) {\n        double total = 0;\n        for (Number n : numbers) total += n.doubleValue();\n        return total;\n    }\n\n    static void fill(List<? super Integer> target) {\n        target.add(10);\n        target.add(20);\n    }\n\n    public static void main(String[] args) {\n        printSize(List.of("a", "b", "c"));\n        System.out.println(sum(List.of(1, 2, 3)));\n        System.out.println(sum(List.of(1.5, 2.5)));\n\n        List<Number> target = new ArrayList<>();\n        fill(target);\n        System.out.println(target);\n    }\n}\n',
    hint: 'Use extends when reading and super when writing.',
  },
  d('java-g-try-catch', 'Catching an exception', 'Divide by zero inside try/catch and print the caught message.',
    M('        try {\n            System.out.println(10 / 0);\n        } catch (ArithmeticException e) {\n            System.out.println(e.getMessage());\n        }'), 'ArithmeticException is thrown for integer division by zero.'),
  d('java-g-parse-catch', 'Guarding a parse', 'Parse "42" and "abc" inside try/catch, printing the number or `invalid`.',
    M('        for (String s : new String[]{"42", "abc"}) {\n            try {\n                System.out.println(Integer.parseInt(s));\n            } catch (NumberFormatException e) {\n                System.out.println("invalid");\n            }\n        }'), 'parseInt throws NumberFormatException.'),
  d('java-g-finally', 'A finally block', 'Write a method dividing two ints inside try/catch/finally, printing `cleanup` every time, and call it twice.',
    'public class Main {\n    static void go(int a, int b) {\n        try {\n            System.out.println(a / b);\n        } catch (ArithmeticException e) {\n            System.out.println("cannot divide");\n        } finally {\n            System.out.println("cleanup");\n        }\n    }\n\n    public static void main(String[] args) {\n        go(10, 2);\n        go(10, 0);\n    }\n}\n', 'finally runs whether or not an exception occurred.'),
  d('java-g-multi-catch', 'Several catch blocks', 'Trigger both a NumberFormatException and an ArithmeticException, each with its own catch block.',
    M('        try {\n            Integer.parseInt("x");\n        } catch (NumberFormatException e) {\n            System.out.println("format");\n        }\n\n        try {\n            System.out.println(1 / 0);\n        } catch (ArithmeticException e) {\n            System.out.println("arithmetic");\n        }'), 'Each catch handles one type.'),
  d('java-g-catch-order', 'Specific before general', 'Catch a NumberFormatException specifically before a general Exception handler.',
    M('        try {\n            Integer.parseInt("nope");\n        } catch (NumberFormatException e) {\n            System.out.println("specific");\n        } catch (Exception e) {\n            System.out.println("general");\n        }'), 'The first matching block wins, so order matters.'),
  d('java-g-array-bounds', 'An out-of-bounds access', 'Access an invalid array index inside try/catch and print the exception class name.',
    M('        int[] nums = {1, 2, 3};\n        try {\n            System.out.println(nums[10]);\n        } catch (ArrayIndexOutOfBoundsException e) {\n            System.out.println(e.getClass().getSimpleName());\n        }'), 'getSimpleName gives the class name without the package.'),
  d('java-g-null-pointer', 'A null dereference', 'Trigger a NullPointerException by calling a method on a null String and catch it.',
    M('        String s = null;\n        try {\n            System.out.println(s.length());\n        } catch (NullPointerException e) {\n            System.out.println("null pointer");\n        }'), 'NullPointerException is unchecked.'),
  d('java-g-throw-unchecked', 'Throwing an exception', 'Write a validate method throwing IllegalArgumentException for negatives, and call it with a valid and an invalid value.',
    'public class Main {\n    static int validate(int n) {\n        if (n < 0) throw new IllegalArgumentException("negative");\n        return n;\n    }\n\n    public static void main(String[] args) {\n        System.out.println(validate(5));\n        try {\n            validate(-1);\n        } catch (IllegalArgumentException e) {\n            System.out.println(e.getMessage());\n        }\n    }\n}\n', 'Unchecked exceptions need no throws clause.'),
  d('java-g-throws-checked', 'A checked exception', 'Write a method declaring throws Exception and throwing one, then catch it in main.',
    'public class Main {\n    static void risky(boolean fail) throws Exception {\n        if (fail) throw new Exception("checked");\n        System.out.println("ok");\n    }\n\n    public static void main(String[] args) {\n        try {\n            risky(false);\n            risky(true);\n        } catch (Exception e) {\n            System.out.println(e.getMessage());\n        }\n    }\n}\n', 'A checked exception must be declared or caught.'),
  d('java-g-custom-exception', 'A custom exception', 'Define EmptyInputException extends RuntimeException, throw it from a method given an empty string, and catch it.',
    'class EmptyInputException extends RuntimeException {\n    EmptyInputException(String message) {\n        super(message);\n    }\n}\n\npublic class Main {\n    static String process(String text) {\n        if (text.isEmpty()) throw new EmptyInputException("nothing to process");\n        return text.toUpperCase();\n    }\n\n    public static void main(String[] args) {\n        System.out.println(process("hello"));\n        try {\n            process("");\n        } catch (EmptyInputException e) {\n            System.out.println(e.getMessage());\n        }\n    }\n}\n', 'Forward the message with super(message).'),
  d('java-g-custom-with-field', 'An exception carrying data', 'Define a custom exception with an extra int field, throw it, and print both the message and the field.',
    'class LimitException extends RuntimeException {\n    private final int limit;\n\n    LimitException(String message, int limit) {\n        super(message);\n        this.limit = limit;\n    }\n\n    int getLimit() { return limit; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        try {\n            throw new LimitException("over limit", 100);\n        } catch (LimitException e) {\n            System.out.println(e.getMessage());\n            System.out.println(e.getLimit());\n        }\n    }\n}\n', 'The field carries machine-readable detail.'),
  d('java-g-generic-print', 'A generic method', 'Write a generic printAll(List<T>) and call it with a List<Integer> and a List<String>.',
    'import java.util.List;\n\npublic class Main {\n    static <T> void printAll(List<T> items) {\n        for (T item : items) System.out.println(item);\n    }\n\n    public static void main(String[] args) {\n        printAll(List.of(1, 2));\n        printAll(List.of("a", "b"));\n    }\n}\n', 'The type parameter goes before the return type.'),
  d('java-g-generic-first', 'A generic return type', 'Write a generic firstOf(List<T>) returning the first element, and use it with two different types.',
    'import java.util.List;\n\npublic class Main {\n    static <T> T firstOf(List<T> items) {\n        return items.get(0);\n    }\n\n    public static void main(String[] args) {\n        String s = firstOf(List.of("first", "second"));\n        Integer n = firstOf(List.of(10, 20));\n        System.out.println(s);\n        System.out.println(n);\n    }\n}\n', 'No cast is needed at the call site.'),
  d('java-g-generic-bounded', 'A bounded type parameter', 'Write a generic largest(List<T>) with a Comparable bound and use it with Integers and Strings.',
    'import java.util.List;\n\npublic class Main {\n    static <T extends Comparable<T>> T largest(List<T> items) {\n        T best = items.get(0);\n        for (T item : items) {\n            if (item.compareTo(best) > 0) best = item;\n        }\n        return best;\n    }\n\n    public static void main(String[] args) {\n        System.out.println(largest(List.of(3, 9, 2)));\n        System.out.println(largest(List.of("apple", "cherry")));\n    }\n}\n', 'The bound guarantees compareTo exists.'),
  d('java-g-generic-swap', 'A generic swap', 'Write a generic method swapping the first two elements of a List<T> and demonstrate it on a mutable list.',
    'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n    static <T> void swapFirstTwo(List<T> items) {\n        T tmp = items.get(0);\n        items.set(0, items.get(1));\n        items.set(1, tmp);\n    }\n\n    public static void main(String[] args) {\n        List<String> items = new ArrayList<>(List.of("a", "b", "c"));\n        swapFirstTwo(items);\n        System.out.println(items);\n    }\n}\n', 'List.of is immutable, so wrap it in an ArrayList.'),
  d('java-g-generic-box', 'A generic class', 'Define a generic Box<T> with get and set, and use it with an Integer and a String.',
    'public class Main {\n    static class Box<T> {\n        private T value;\n        Box(T value) { this.value = value; }\n        T get() { return value; }\n        void set(T value) { this.value = value; }\n    }\n\n    public static void main(String[] args) {\n        Box<Integer> a = new Box<>(1);\n        a.set(42);\n        System.out.println(a.get());\n\n        Box<String> b = new Box<>("text");\n        System.out.println(b.get().toUpperCase());\n    }\n}\n', 'No cast is needed when reading from a typed Box.'),
  d('java-g-generic-pair', 'A two-parameter generic', 'Define a generic Pair<A, B> and print both members for a String/Integer pair.',
    'public class Main {\n    static class Pair<A, B> {\n        final A first;\n        final B second;\n        Pair(A first, B second) {\n            this.first = first;\n            this.second = second;\n        }\n    }\n\n    public static void main(String[] args) {\n        Pair<String, Integer> p = new Pair<>("ada", 91);\n        System.out.println(p.first);\n        System.out.println(p.second);\n    }\n}\n', 'A class can take several type parameters.'),
  d('java-g-generic-stack', 'A generic stack', 'Define a generic Stack<T> over an ArrayList with push, pop and size, and use it with Integers.',
    'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n    static class Stack<T> {\n        private final List<T> items = new ArrayList<>();\n        void push(T item) { items.add(item); }\n        T pop() { return items.remove(items.size() - 1); }\n        int size() { return items.size(); }\n    }\n\n    public static void main(String[] args) {\n        Stack<Integer> s = new Stack<>();\n        s.push(1);\n        s.push(2);\n        System.out.println(s.pop());\n        System.out.println(s.size());\n    }\n}\n', 'A List<T> field does the storage.'),
  d('java-g-wildcard-unknown', 'The unknown wildcard', 'Write a method taking List<?> and printing its size, then call it with two differently typed lists.',
    'import java.util.List;\n\npublic class Main {\n    static void printSize(List<?> items) {\n        System.out.println(items.size());\n    }\n\n    public static void main(String[] args) {\n        printSize(List.of("a", "b"));\n        printSize(List.of(1, 2, 3));\n    }\n}\n', 'List<?> accepts any element type but cannot be added to.'),
  d('java-g-wildcard-extends', 'A read-only wildcard', 'Write a sum method taking List<? extends Number> and call it with a List<Integer> and a List<Double>.',
    'import java.util.List;\n\npublic class Main {\n    static double sum(List<? extends Number> numbers) {\n        double total = 0;\n        for (Number n : numbers) total += n.doubleValue();\n        return total;\n    }\n\n    public static void main(String[] args) {\n        System.out.println(sum(List.of(1, 2, 3)));\n        System.out.println(sum(List.of(1.5, 2.5)));\n    }\n}\n', 'extends lets the method read any Number subtype.'),
  d('java-g-wildcard-super', 'A write-only wildcard', 'Write a method taking List<? super Integer> and adding two values, then call it with a List<Number>.',
    'import java.util.ArrayList;\nimport java.util.List;\n\npublic class Main {\n    static void fill(List<? super Integer> target) {\n        target.add(1);\n        target.add(2);\n    }\n\n    public static void main(String[] args) {\n        List<Number> target = new ArrayList<>();\n        fill(target);\n        System.out.println(target);\n    }\n}\n', 'super makes the list safe to write Integers into.'),
  d('java-g-invariance', 'Generic invariance', 'Show that a List<Integer> can be passed to List<? extends Number> but demonstrate the sum works for both Integer and Double lists.',
    'import java.util.List;\n\npublic class Main {\n    static double total(List<? extends Number> nums) {\n        double t = 0;\n        for (Number n : nums) t += n.doubleValue();\n        return t;\n    }\n\n    public static void main(String[] args) {\n        List<Integer> ints = List.of(1, 2, 3);\n        List<Double> doubles = List.of(0.5, 1.5);\n        System.out.println(total(ints));\n        System.out.println(total(doubles));\n    }\n}\n', 'A plain List<Number> parameter would reject both.'),
  d('java-g-capstone', 'Generics and exceptions summary', 'Define a generic Repository<T> with add, get(index) throwing a custom NotFoundException for a bad index, and size. Use it with Strings, printing a successful get and a caught failure.',
    'import java.util.ArrayList;\nimport java.util.List;\n\nclass NotFoundException extends RuntimeException {\n    private final int index;\n\n    NotFoundException(String message, int index) {\n        super(message);\n        this.index = index;\n    }\n\n    int getIndex() { return index; }\n}\n\npublic class Main {\n    static class Repository<T> {\n        private final List<T> items = new ArrayList<>();\n\n        void add(T item) { items.add(item); }\n\n        T get(int index) {\n            if (index < 0 || index >= items.size()) {\n                throw new NotFoundException("no item at " + index, index);\n            }\n            return items.get(index);\n        }\n\n        int size() { return items.size(); }\n    }\n\n    public static void main(String[] args) {\n        Repository<String> repo = new Repository<>();\n        repo.add("first");\n        repo.add("second");\n\n        System.out.println(repo.size());\n        System.out.println(repo.get(1));\n\n        try {\n            repo.get(9);\n        } catch (NotFoundException e) {\n            System.out.println(e.getMessage());\n            System.out.println(e.getIndex());\n        }\n    }\n}\n', 'The generic class and the custom exception work together.'),
  {
    kind: 'quiz',
    slug: 'java-generics-quiz',
    title: 'Checkpoint: generics and exceptions',
    questions: [
      { q: 'What is the difference between a checked and an unchecked exception?', options: ['None', 'Checked exceptions must be caught or declared with throws; unchecked need not be', 'Unchecked exceptions are more serious', 'Checked exceptions cannot be caught'], answer: 1, explain: 'The compiler enforces handling of checked exceptions, which represent anticipated external failures rather than programming bugs.' },
      { q: 'When does a finally block run?', options: ['Only on success', 'Only on failure', 'Always, including after a return', 'Never'], answer: 2, explain: 'finally executes on every exit path, which makes it the right place for cleanup that must not be skipped.' },
      { q: 'Why is List<Integer> not a List<Number>?', options: ['An oversight', 'Because you could then add a Double to a list of Integers', 'Integer is not a Number', 'It is'], answer: 1, explain: 'Generic invariance prevents that unsound write, and wildcards restore the flexibility safely.' },
      { q: 'What does <T extends Comparable<T>> guarantee?', options: ['T is a number', 'T has a compareTo method, so it can be ordered', 'T is immutable', 'Nothing'], answer: 1, explain: 'A bound tells the compiler which operations are available on T, which is what a generic max function needs.' },
      { q: 'What does type erasure mean for generics?', options: ['Nothing', 'Type arguments are discarded after compilation, so new T() is impossible', 'Generics are checked at run time', 'Primitives are allowed'], answer: 1, explain: 'Erasure kept generics backward compatible, at the cost of restrictions like no primitive arguments and no runtime type information.' },
    ],
  },
];
