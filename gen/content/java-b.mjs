const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body) => `public class Main {\n    public static void main(String[] args) {\n${body}\n    }\n}\n`;

export const javaOperators = [
  {
    slug: 'java-arithmetic',
    title: 'Arithmetic operators',
    subtitle: 'Five operators and integer division',
    concept:
      'Java has `+ - * / %`. There is no exponentiation operator \u2014 `Math.pow(base, exp)` covers that, returning a double.\n\nThe division rule depends on the operand types: two ints divide as integers, truncating, while any double operand gives floating-point division. `%` works on both, and on doubles gives a fractional remainder.\n\nInteger overflow **wraps silently** rather than throwing, so `Integer.MAX_VALUE + 1` becomes the minimum value. Use `long` when values may grow large, or `Math.addExact` which throws on overflow.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        int a = 17, b = 5;\n\n        System.out.println(a + b);\n        System.out.println(a / b);\n        System.out.println(a % b);\n        System.out.println((double) a / b);\n        System.out.println(Math.pow(2, 10));\n        System.out.println(7.5 % 2);\n\n        System.out.println(Integer.MAX_VALUE + 1);\n    }\n}',
    note: 'The overflow wrapped to the minimum int without any warning \u2014 a genuine hazard.',
    task: 'With `int a = 23` and `int b = 4`, print the sum, product, integer quotient, remainder, exact quotient to two decimals, and 2 to the power 8 as an int.',
    given: 'int a = 23, b = 4;',
    solution: M('        int a = 23, b = 4;\n\n        System.out.println(a + b);\n        System.out.println(a * b);\n        System.out.println(a / b);\n        System.out.println(a % b);\n        System.out.printf("%.2f%n", (double) a / b);\n        System.out.println((int) Math.pow(2, 8));'),
    hint: 'Math.pow returns a double, so cast it to print a whole number.',
  },
  {
    slug: 'java-increment-compound',
    title: 'Updating a value',
    subtitle: 'Increment and compound assignment',
    concept:
      '`i++` and `++i` both add one, but they **evaluate** differently. Postfix yields the old value, prefix the new one. As a standalone statement they are identical.\n\nThe compound operators `+= -= *= /= %=` update in place. They have a hidden feature: they perform an **implicit cast** back to the target type. So `int i = 5; i += 2.7;` compiles and gives 7, whereas `i = i + 2.7;` would refuse to compile because the result is a double.\n\nThat asymmetry is worth knowing, since it can silently truncate.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        int i = 5;\n        System.out.println(i++);\n        System.out.println(i);\n        System.out.println(++i);\n\n        int k = 10;\n        k += 5;\n        k *= 2;\n        k -= 4;\n        System.out.println(k);\n\n        int truncated = 5;\n        truncated += 2.7;\n        System.out.println(truncated);\n    }\n}',
    note: 'truncated became 7, not 7.7 \u2014 the compound operator cast the double result back to int.',
    task: 'Start `int n = 8`. Print n, then n++, then n, then ++n, then n. Then start at 100 and apply -=20, /=2 and %=7, printing after each.',
    solution: M('        int n = 8;\n        System.out.println(n);\n        System.out.println(n++);\n        System.out.println(n);\n        System.out.println(++n);\n        System.out.println(n);\n\n        int v = 100;\n        v -= 20;\n        System.out.println(v);\n        v /= 2;\n        System.out.println(v);\n        v %= 7;\n        System.out.println(v);'),
    hint: 'Postfix reports the old value, prefix the new one.',
  },
  {
    slug: 'java-comparison-logic',
    title: 'Comparing and combining',
    subtitle: 'Relational and logical operators',
    concept:
      'The six comparisons produce a genuine `boolean`, so `if (x = 5)` is a **compile error** rather than a silent bug as in C.\n\n`&&`, `||` and `!` combine booleans and **short-circuit**: `&&` stops at the first false, `||` at the first true. That guarantee is what makes `if (s != null && s.length() > 0)` safe.\n\nJava also has `&` and `|` for booleans, which do **not** short-circuit and always evaluate both sides. They are rarely what you want.\n\nJava has no chained comparisons, so `0 < x < 10` will not compile.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        int x = 7;\n\n        System.out.println(x == 7);\n        System.out.println(x != 7);\n        System.out.println(x < 10 && x > 5);\n        System.out.println(x > 100 || x == 7);\n        System.out.println(!(x == 7));\n\n        String s = null;\n        System.out.println(s != null && s.length() > 0);\n    }\n}',
    note: 'The last line is safe because && never evaluates length() when s is null.',
    task: 'With `int n = 15`, print whether it equals 15, is under 10, lies strictly between 10 and 20, and is not 20. Then with a null String, safely print whether it is non-null and non-empty.',
    given: 'int n = 15;',
    solution: M('        int n = 15;\n\n        System.out.println(n == 15);\n        System.out.println(n < 10);\n        System.out.println(10 < n && n < 20);\n        System.out.println(n != 20);\n\n        String s = null;\n        System.out.println(s != null && !s.isEmpty());'),
    hint: 'The range test needs two comparisons joined by &&.',
  },
  {
    slug: 'java-bitwise',
    title: 'Working with bits',
    subtitle: 'Bitwise and shift operators',
    concept:
      'Java has `& | ^ ~` and the shifts `<< >>`, plus one operator C lacks: `>>>`, the **unsigned right shift**.\n\nThe distinction matters because Java has no unsigned integer types. `>>` preserves the sign bit, so `-8 >> 1` is `-4`. `>>>` fills with zeros, so `-8 >>> 1` becomes a large positive number.\n\nThe standard idioms are the same as in C: set with `|=`, clear with `&= ~`, toggle with `^=`, test with `(x >> n) & 1`.\n\n`Integer.toBinaryString(n)` prints the bits, which makes experimenting far easier.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        int a = 12, b = 10;\n\n        System.out.println(a & b);\n        System.out.println(a | b);\n        System.out.println(a ^ b);\n        System.out.println(a << 1);\n        System.out.println(-8 >> 1);\n        System.out.println(-8 >>> 28);\n\n        System.out.println(Integer.toBinaryString(a));\n        System.out.println(Integer.bitCount(255));\n    }\n}',
    note: '-8 >> 1 gave -4 while -8 >>> 28 gave a positive value, because the sign bit was not preserved.',
    task: 'With `int flags = 10`, print the result of anding with 12, oring with 5, xoring with 15, shifting left once, the binary string, and Integer.bitCount of 255.',
    given: 'int flags = 10;',
    solution: M('        int flags = 10;\n\n        System.out.println(flags & 12);\n        System.out.println(flags | 5);\n        System.out.println(flags ^ 15);\n        System.out.println(flags << 1);\n        System.out.println(Integer.toBinaryString(flags));\n        System.out.println(Integer.bitCount(255));'),
    hint: 'Integer.bitCount counts the set bits directly.',
  },
  {
    slug: 'java-precedence-ternary',
    title: 'Order of evaluation and choices',
    subtitle: 'Precedence and the conditional operator',
    concept:
      'Precedence, tightest first: unary operators and casts, then `* / %`, then `+ -`, then shifts, then comparisons, then `& ^ |`, then `&&`, then `||`, then `?:`, then assignment.\n\nAs in C, the bitwise operators bind looser than comparisons, so `a & 1 == 0` means `a & (1 == 0)` \u2014 which in Java is a compile error rather than a silent bug, since you cannot bitwise-and an int with a boolean. Java\u2019s type system catches this one for you.\n\nThe conditional operator `?:` is the only ternary operator. It produces a value, so it fits inside expressions where an `if` statement cannot.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        System.out.println(2 + 3 * 4);\n        System.out.println((2 + 3) * 4);\n        System.out.println(20 / 4 / 5);\n\n        int n = 7;\n        System.out.println(n % 2 == 0 ? "even" : "odd");\n\n        int a = 17, b = 42;\n        System.out.println(a > b ? a : b);\n\n        int score = 84;\n        System.out.println(score >= 90 ? "A" : score >= 80 ? "B" : "C");\n    }\n}',
    note: 'Conditional operators chain to the right, which is what makes the grade ladder work.',
    task: 'Print `5 + 2 * 3`, `(5 + 2) * 3` and `20 / 4 / 5`. Then with `int n = -8`, use the conditional operator to print its magnitude and the word `neg` or `pos`.',
    solution: M('        System.out.println(5 + 2 * 3);\n        System.out.println((5 + 2) * 3);\n        System.out.println(20 / 4 / 5);\n\n        int n = -8;\n        System.out.println(n < 0 ? -n : n);\n        System.out.println(n < 0 ? "neg" : "pos");'),
    hint: 'Division is left-associative, so 20 / 4 / 5 is (20 / 4) / 5.',
  },
  d('java-o-mixed', 'Four expressions', 'Print 7 + 3 * 2, (7 + 3) * 2, 19 % 4 and 19 / 4.',
    M('        System.out.println(7 + 3 * 2);\n        System.out.println((7 + 3) * 2);\n        System.out.println(19 % 4);\n        System.out.println(19 / 4);'), 'Multiplication binds tighter than addition.'),
  d('java-o-even', 'Divisibility', 'With int n = 34, print whether it is even and whether it divides by 3.',
    M('        int n = 34;\n        System.out.println(n % 2 == 0);\n        System.out.println(n % 3 == 0);'), 'A zero remainder means it divides exactly.'),
  d('java-o-range', 'Range test', 'With int score = 72, print whether it lies between 70 and 80 inclusive.',
    M('        int score = 72;\n        System.out.println(score >= 70 && score <= 80);'), 'Java has no chained comparisons.'),
  d('java-o-increment', 'Prefix and postfix', 'Start at 5. Print i++, then i, then ++i, then i.',
    M('        int i = 5;\n        System.out.println(i++);\n        System.out.println(i);\n        System.out.println(++i);\n        System.out.println(i);'), 'Postfix reports the old value.'),
  d('java-o-compound', 'Compound assignment', 'Start at 100. Apply -=20, /=2, %=7 and *=3, printing after each.',
    M('        int v = 100;\n        v -= 20;\n        System.out.println(v);\n        v /= 2;\n        System.out.println(v);\n        v %= 7;\n        System.out.println(v);\n        v *= 3;\n        System.out.println(v);'), 'Each operator updates in place.'),
  d('java-o-compound-cast', 'The hidden cast', 'Start with int i = 5, apply += 2.7, and print the result to see the implicit truncation.',
    M('        int i = 5;\n        i += 2.7;\n        System.out.println(i);'), 'Compound operators cast the result back to the target type.'),
  d('java-o-logic-table', 'Truth table', 'Print the four && combinations of true and false.',
    M('        System.out.println(true && true);\n        System.out.println(true && false);\n        System.out.println(false && true);\n        System.out.println(false && false);'), 'Only the first is true.'),
  d('java-o-leap', 'Leap year', 'With int year = 2024, print whether it is a leap year as one expression.',
    M('        int year = 2024;\n        System.out.println((year % 4 == 0 && year % 100 != 0) || year % 400 == 0);'), 'Group the && clause before the ||.'),
  d('java-o-null-safe', 'Null-safe check', 'With a null String, print whether it is non-null and non-empty without throwing.',
    M('        String s = null;\n        System.out.println(s != null && !s.isEmpty());'), '&& skips the right side when the left is false.'),
  d('java-o-short-circuit', 'Short-circuit safety', 'With int n = 0, print n != 0 && 100 / n > 2 without dividing by zero.',
    M('        int n = 0;\n        System.out.println(n != 0 && 100 / n > 2);'), 'The division is never evaluated.'),
  d('java-o-math-pow', 'Powers and roots', 'Print Math.pow(2, 10), Math.sqrt(144) and Math.abs(-17).',
    M('        System.out.println(Math.pow(2, 10));\n        System.out.println(Math.sqrt(144));\n        System.out.println(Math.abs(-17));'), 'Math.pow and Math.sqrt return doubles.'),
  d('java-o-math-round', 'Rounding', 'Print Math.round(3.4), Math.round(3.6), Math.ceil(3.1) and Math.floor(3.9).',
    M('        System.out.println(Math.round(3.4));\n        System.out.println(Math.round(3.6));\n        System.out.println(Math.ceil(3.1));\n        System.out.println(Math.floor(3.9));'), 'ceil and floor return doubles.'),
  d('java-o-bits-set', 'Setting bits', 'Starting from 0, set bits 0, 2 and 5, printing the value after each.',
    M('        int f = 0;\n        f |= (1 << 0);\n        System.out.println(f);\n        f |= (1 << 2);\n        System.out.println(f);\n        f |= (1 << 5);\n        System.out.println(f);'), 'Bit n has the value 1 << n.'),
  d('java-o-bits-test', 'Testing bits', 'For the value 22, print whether bits 0 to 4 are set, one per line.',
    M('        int v = 22;\n        for (int i = 0; i < 5; i++) {\n            System.out.println((v >> i) & 1);\n        }'), 'Shift right then mask with 1.'),
  d('java-o-binary-string', 'Binary representation', 'Print the binary strings of 5, 42 and 255 using Integer.toBinaryString.',
    M('        System.out.println(Integer.toBinaryString(5));\n        System.out.println(Integer.toBinaryString(42));\n        System.out.println(Integer.toBinaryString(255));'), 'Leading zeros are omitted.'),
  d('java-o-bitcount', 'Counting set bits', 'Print Integer.bitCount of 255, 256 and 1023.',
    M('        System.out.println(Integer.bitCount(255));\n        System.out.println(Integer.bitCount(256));\n        System.out.println(Integer.bitCount(1023));'), 'bitCount does the work for you.'),
  d('java-o-shift-signed', 'Signed versus unsigned shift', 'Print -8 >> 1 and -8 >>> 28 to see the difference.',
    M('        System.out.println(-8 >> 1);\n        System.out.println(-8 >>> 28);'), '>> preserves the sign bit while >>> fills with zeros.'),
  d('java-o-ternary-max3', 'Largest of three', 'With ints 14, 27 and 9, print the largest using conditional operators.',
    M('        int a = 14, b = 27, c = 9;\n        int m = a > b ? a : b;\n        System.out.println(m > c ? m : c);'), 'Compare two, then the winner against the third.'),
  d('java-o-ternary-grade', 'Chained ternaries', 'With int score = 84, print A, B, C or F using chained conditional operators.',
    M('        int score = 84;\n        System.out.println(score >= 90 ? "A" : score >= 80 ? "B" : score >= 70 ? "C" : "F");'), 'They chain to the right.'),
  d('java-o-overflow', 'Integer overflow', 'Print Integer.MAX_VALUE, then MAX_VALUE + 1, then the same sum computed as a long.',
    M('        System.out.println(Integer.MAX_VALUE);\n        System.out.println(Integer.MAX_VALUE + 1);\n        System.out.println((long) Integer.MAX_VALUE + 1);'), 'Casting to long before adding avoids the wrap.'),
  d('java-o-negatives', 'Negative division', 'Print -7 / 2, 7 / 2, -7 % 2 and 7 % 2.',
    M('        System.out.println(-7 / 2);\n        System.out.println(7 / 2);\n        System.out.println(-7 % 2);\n        System.out.println(7 % 2);'), 'Java truncates toward zero.'),
  d('java-o-capstone', 'Operator summary', 'With int n = 145, print the full dozens, the leftover, whether it is odd, n squared, and its binary string.',
    M('        int n = 145;\n        System.out.println(n / 12);\n        System.out.println(n % 12);\n        System.out.println(n % 2 == 1);\n        System.out.println(n * n);\n        System.out.println(Integer.toBinaryString(n));'), 'Five separate expressions.'),
  {
    kind: 'quiz',
    slug: 'java-operators-quiz',
    title: 'Checkpoint: operators',
    questions: [
      { q: 'Why is if (x = 5) a compile error in Java but not in C?', options: ['Java has no assignment', 'Java requires a boolean condition, and an int assignment is not one', 'Java forbids = in conditions', 'It is an error in both'], answer: 1, explain: 'Because boolean is a distinct type, an int expression cannot be used as a condition \u2014 the type system catches the typo.' },
      { q: 'What does i += 2.7 do when i is an int holding 5?', options: ['A compile error', 'Sets i to 7, casting the double result back to int', 'Sets i to 7.7', 'Sets i to 8'], answer: 1, explain: 'Compound operators perform an implicit narrowing cast, which is why this compiles while i = i + 2.7 does not.' },
      { q: 'What is the difference between >> and >>> ?', options: ['None', '>> preserves the sign bit, >>> fills with zeros', '>>> is for doubles', '>> is faster'], answer: 1, explain: 'Java has no unsigned types, so >>> exists to provide a logical shift that ignores the sign.' },
      { q: 'What happens on integer overflow in Java?', options: ['An exception', 'It wraps silently to the opposite end of the range', 'It saturates at the maximum', 'The program exits'], answer: 1, explain: 'Overflow is silent, which is why long or Math.addExact is needed when values may grow large.' },
      { q: 'How do you raise 2 to the power 10?', options: ['2 ** 10', '2 ^ 10', 'Math.pow(2, 10)', '2 << 10'], answer: 2, explain: 'Java has no exponentiation operator, and ^ is bitwise xor. Math.pow returns a double.' },
    ],
  },
];

export const javaFlow = [
  {
    slug: 'java-if-else',
    title: 'Branching',
    subtitle: 'if, else if, else',
    concept:
      '`if` takes a **boolean** condition in parentheses and a block in braces. Unlike C, the condition must genuinely be a boolean \u2014 an int will not do.\n\n`else if` chains test in order, and the first match wins. Order therefore matters with overlapping thresholds.\n\nBraces are optional for a single statement but should always be used: adding a second line later would silently leave it outside the if, a mistake that has caused real security bugs.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        int score = 74;\n\n        if (score >= 90) {\n            System.out.println("A");\n        } else if (score >= 80) {\n            System.out.println("B");\n        } else if (score >= 70) {\n            System.out.println("C");\n        } else {\n            System.out.println("F");\n        }\n    }\n}',
    note: '74 fails the first two tests and passes the third. Reversing the order would print C for 95 as well.',
    task: 'With `int temp = 5`, print `freezing` below 0, `cold` below 10, `mild` below 25, otherwise `hot`. Then print `even` or `odd` for the same value.',
    given: 'int temp = 5;',
    solution: M('        int temp = 5;\n\n        if (temp < 0) {\n            System.out.println("freezing");\n        } else if (temp < 10) {\n            System.out.println("cold");\n        } else if (temp < 25) {\n            System.out.println("mild");\n        } else {\n            System.out.println("hot");\n        }\n\n        if (temp % 2 == 0) {\n            System.out.println("even");\n        } else {\n            System.out.println("odd");\n        }'),
    hint: 'Ascending thresholds mean each else if only sees what fell through.',
  },
  {
    slug: 'java-switch',
    title: 'Matching one value',
    subtitle: 'switch, fall-through and arrow labels',
    concept:
      'Java\u2019s classic `switch` compares one value against `case` labels, with **fall-through** if you omit `break`. Unlike C, it works on `String` as well as integral types and enums.\n\nJava 14 added the **arrow form**, which does not fall through and can group labels with commas:\n\n`case "sat", "sun" -> System.out.println("weekend");`\n\nThat is now the preferred style, since the commonest bug in the old form was a forgotten break. A `switch` can also be an **expression** that yields a value with `yield` or an arrow.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        String day = "sat";\n\n        switch (day) {\n            case "sat", "sun" -> System.out.println("weekend");\n            case "mon" -> System.out.println("monday");\n            default -> System.out.println("weekday");\n        }\n\n        int month = 4;\n        int days = switch (month) {\n            case 2 -> 28;\n            case 4, 6, 9, 11 -> 30;\n            default -> 31;\n        };\n        System.out.println(days);\n    }\n}',
    note: 'The second switch is an expression producing a value, which the old form could not do.',
    task: 'With `char op = \'*\'` and ints 6 and 7, use an arrow switch to print the result of +, -, * or / with a default of `unknown`. Then use a switch expression to store the number of days in month 4 and print it.',
    given: "char op = '*';\nint a = 6, b = 7;",
    solution: M('        char op = \'*\';\n        int a = 6, b = 7;\n\n        switch (op) {\n            case \'+\' -> System.out.println(a + b);\n            case \'-\' -> System.out.println(a - b);\n            case \'*\' -> System.out.println(a * b);\n            case \'/\' -> System.out.println(a / b);\n            default -> System.out.println("unknown");\n        }\n\n        int month = 4;\n        int days = switch (month) {\n            case 2 -> 28;\n            case 4, 6, 9, 11 -> 30;\n            default -> 31;\n        };\n        System.out.println(days);'),
    hint: 'The arrow form needs no break statements.',
  },
  {
    slug: 'java-loops',
    title: 'The three loop forms',
    subtitle: 'for, while and do-while',
    concept:
      'The counting `for` packs initialisation, condition and step into one line. `while` tests before each pass. `do...while` tests **after** the body, so it always runs at least once, and needs a trailing semicolon.\n\nDeclaring the counter inside the for scopes it to the loop, which is the standard style.\n\nAs always, a `while` loop needs something inside the body to move the condition toward false, or it never ends.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        for (int i = 1; i <= 3; i++) {\n            System.out.print(i + " ");\n        }\n        System.out.println();\n\n        int n = 100, steps = 0;\n        while (n > 1) {\n            n /= 2;\n            steps++;\n        }\n        System.out.println(steps);\n\n        int k = 0;\n        do {\n            System.out.println("ran once");\n            k++;\n        } while (k < 1);\n    }\n}',
    note: 'The do-while body executed before the condition was ever tested.',
    task: 'Print the first five multiples of 3 one per line, then their sum. Then use a while loop to count how many times 1000 can be halved before reaching 1.',
    solution: M('        int total = 0;\n        for (int i = 1; i <= 5; i++) {\n            int value = i * 3;\n            System.out.println(value);\n            total += value;\n        }\n        System.out.println("sum: " + total);\n\n        int n = 1000, halvings = 0;\n        while (n > 1) {\n            n /= 2;\n            halvings++;\n        }\n        System.out.println("halvings: " + halvings);'),
    hint: 'Declare the accumulator before the loop so it survives afterwards.',
  },
  {
    slug: 'java-enhanced-for',
    title: 'Looping over a collection',
    subtitle: 'The enhanced for loop',
    concept:
      'The **enhanced for** (or for-each) walks an array or any `Iterable` without an index:\n\n`for (int value : values) { ... }`\n\nThere is no index to get wrong and no off-by-one possible. Use it whenever you need the elements rather than their positions.\n\nOne important limitation: assigning to the loop variable changes only the local copy, so it **cannot** modify the array. To write back you need an indexed loop.\n\nFor strings, `text.toCharArray()` produces something the enhanced for can walk.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        int[] nums = {4, 8, 15, 16};\n\n        for (int n : nums) {\n            System.out.print(n + " ");\n        }\n        System.out.println();\n\n        int total = 0;\n        for (int n : nums) total += n;\n        System.out.println(total);\n\n        for (int n : nums) n = 0;\n        System.out.println(nums[0]);\n\n        for (char c : "abc".toCharArray()) {\n            System.out.print(c + ".");\n        }\n        System.out.println();\n    }\n}',
    note: 'Setting n to 0 changed nothing, because n is a copy \u2014 nums[0] is still 4.',
    task: 'Given an int array of 2, 4, 6, 8, 10, print each element with an enhanced for, then the total, then double each element using an indexed loop and print the array total again.',
    given: 'int[] nums = {2, 4, 6, 8, 10};',
    solution: M('        int[] nums = {2, 4, 6, 8, 10};\n\n        for (int n : nums) System.out.print(n + " ");\n        System.out.println();\n\n        int total = 0;\n        for (int n : nums) total += n;\n        System.out.println(total);\n\n        for (int i = 0; i < nums.length; i++) nums[i] *= 2;\n\n        total = 0;\n        for (int n : nums) total += n;\n        System.out.println(total);'),
    hint: 'Only an indexed loop can write back into the array.',
  },
  {
    slug: 'java-break-continue',
    title: 'Leaving early and skipping',
    subtitle: 'break, continue and labels',
    concept:
      '`break` exits the innermost loop; `continue` skips to the next iteration.\n\nJava adds something C lacks: **labelled** break and continue. Placing a label before a loop lets you break out of several levels at once:\n\n```\nouter:\nfor (...) {\n    for (...) {\n        if (found) break outer;\n    }\n}\n```\n\nThat is far clearer than the flag variable the same job requires in C. Use it sparingly \u2014 often extracting the loops into a method and returning is cleaner still.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        for (int n = 1; n <= 8; n++) {\n            if (n % 2 == 0) continue;\n            System.out.print(n + " ");\n        }\n        System.out.println();\n\n        outer:\n        for (int r = 1; r <= 3; r++) {\n            for (int c = 1; c <= 3; c++) {\n                if (r * c > 4) break outer;\n                System.out.print(r * c + " ");\n            }\n        }\n        System.out.println();\n    }\n}',
    note: 'break outer left both loops at once, which an unlabelled break could not do.',
    task: 'Loop from 1 to 30, skipping multiples of 3 with continue and breaking once the value exceeds 12. Then use a labelled break to stop a nested 3x3 loop as soon as the product exceeds 4.',
    solution: M('        for (int n = 1; n <= 30; n++) {\n            if (n > 12) break;\n            if (n % 3 == 0) continue;\n            System.out.println(n);\n        }\n\n        outer:\n        for (int r = 1; r <= 3; r++) {\n            for (int c = 1; c <= 3; c++) {\n                if (r * c > 4) break outer;\n                System.out.println(r + "x" + c);\n            }\n        }'),
    hint: 'The label goes on the line before the outer loop, followed by a colon.',
  },
  d('java-f-sign', 'Sign of a number', 'With int n = 0, print positive, negative or zero.',
    M('        int n = 0;\n        if (n > 0) {\n            System.out.println("positive");\n        } else if (n < 0) {\n            System.out.println("negative");\n        } else {\n            System.out.println("zero");\n        }'), 'Zero goes in the final else.'),
  d('java-f-grade', 'Letter grade', 'With int score = 88, print A/B/C/D/F using ten-point bands.',
    M('        int score = 88;\n        if (score >= 90) System.out.println("A");\n        else if (score >= 80) System.out.println("B");\n        else if (score >= 70) System.out.println("C");\n        else if (score >= 60) System.out.println("D");\n        else System.out.println("F");'), 'Highest threshold first.'),
  d('java-f-fizzbuzz', 'FizzBuzz to 20', 'Print 1 to 20 with Fizz, Buzz and FizzBuzz substitutions.',
    M('        for (int n = 1; n <= 20; n++) {\n            if (n % 15 == 0) System.out.println("FizzBuzz");\n            else if (n % 3 == 0) System.out.println("Fizz");\n            else if (n % 5 == 0) System.out.println("Buzz");\n            else System.out.println(n);\n        }'), 'Test divisibility by 15 first.'),
  d('java-f-switch-string', 'Switching on a String', 'With String day = "sat", use an arrow switch to print weekend or weekday.',
    M('        String day = "sat";\n        switch (day) {\n            case "sat", "sun" -> System.out.println("weekend");\n            default -> System.out.println("weekday");\n        }'), 'Java allows String in a switch.'),
  d('java-f-switch-expression', 'A switch expression', 'Use a switch expression to map month 2 to 28 days, 4/6/9/11 to 30 and everything else to 31, then print the result for month 6.',
    M('        int month = 6;\n        int days = switch (month) {\n            case 2 -> 28;\n            case 4, 6, 9, 11 -> 30;\n            default -> 31;\n        };\n        System.out.println(days);'), 'A switch expression yields a value.'),
  d('java-f-switch-vowel', 'Vowel check', 'With char ch = \'e\', use an arrow switch to print vowel or consonant.',
    M('        char ch = \'e\';\n        switch (ch) {\n            case \'a\', \'e\', \'i\', \'o\', \'u\' -> System.out.println("vowel");\n            default -> System.out.println("consonant");\n        }'), 'Group the vowels with commas.'),
  d('java-f-sum100', 'Sum to one hundred', 'Print the sum of every integer from 1 to 100.',
    M('        int total = 0;\n        for (int n = 1; n <= 100; n++) total += n;\n        System.out.println(total);'), 'Accumulate before the loop ends.'),
  d('java-f-countdown', 'Counting down', 'Print 10 down to 1 on one line separated by spaces, then `liftoff`.',
    M('        for (int n = 10; n >= 1; n--) System.out.print(n + " ");\n        System.out.println();\n        System.out.println("liftoff");'), 'Use a decrementing step.'),
  d('java-f-times-table', 'Times table', 'Print the seven times table from 1 to 10 as `7 x 3 = 21`.',
    M('        for (int n = 1; n <= 10; n++) {\n            System.out.println("7 x " + n + " = " + 7 * n);\n        }'), 'Concatenate the parts.'),
  d('java-f-factorial', 'Factorial', 'Compute 15 factorial with a loop using long and print it.',
    M('        long result = 1;\n        for (int n = 1; n <= 15; n++) result *= n;\n        System.out.println(result);'), 'Use long since the value grows quickly.'),
  d('java-f-while-double', 'Doubling loop', 'Starting at 1, print each value while it stays below 100, doubling each time, then print the count.',
    M('        int value = 1, count = 0;\n        while (value < 100) {\n            System.out.println(value);\n            value *= 2;\n            count++;\n        }\n        System.out.println("doublings: " + count);'), 'Print before doubling.'),
  d('java-f-do-while', 'A do-while loop', 'Using do-while, print the squares of 1 to 4 on separate lines.',
    M('        int n = 1;\n        do {\n            System.out.println(n * n);\n            n++;\n        } while (n <= 4);'), 'The body runs before the first test.'),
  d('java-f-enhanced-for', 'Enhanced for loop', 'Given an int array of 5, 10, 15, print each element and then the total.',
    M('        int[] nums = {5, 10, 15};\n        for (int n : nums) System.out.println(n);\n        int total = 0;\n        for (int n : nums) total += n;\n        System.out.println(total);'), 'The enhanced for needs no index.'),
  d('java-f-chars', 'Looping a string', 'Print each character of "loop" on its own line using toCharArray.',
    M('        for (char c : "loop".toCharArray()) {\n            System.out.println(c);\n        }'), 'toCharArray gives something the enhanced for can walk.'),
  d('java-f-nested-grid', 'A multiplication grid', 'Print a 4x4 multiplication grid with tab separators.',
    M('        for (int r = 1; r <= 4; r++) {\n            for (int c = 1; c <= 4; c++) {\n                System.out.print(r * c + "\\t");\n            }\n            System.out.println();\n        }'), 'The row newline belongs to the outer loop.'),
  d('java-f-pyramid', 'A star pyramid', 'Print a five-row centred pyramid where row n has 2n-1 stars.',
    M('        int rows = 5;\n        for (int n = 1; n <= rows; n++) {\n            System.out.println(" ".repeat(rows - n) + "*".repeat(2 * n - 1));\n        }'), 'String.repeat builds the padding and the stars.'),
  d('java-f-primes', 'Primes below thirty', 'Print every prime below 30, one per line.',
    M('        for (int n = 2; n < 30; n++) {\n            boolean prime = true;\n            for (int dv = 2; dv * dv <= n; dv++) {\n                if (n % dv == 0) { prime = false; break; }\n            }\n            if (prime) System.out.println(n);\n        }'), 'Testing divisors up to the square root suffices.'),
  d('java-f-fibonacci', 'Fibonacci numbers', 'Print the first twelve Fibonacci numbers on one line, starting 0 1.',
    M('        int a = 0, b = 1;\n        for (int i = 0; i < 12; i++) {\n            System.out.print(a + " ");\n            int next = a + b;\n            a = b;\n            b = next;\n        }\n        System.out.println();'), 'A temporary holds the next value.'),
  d('java-f-collatz', 'Collatz steps', 'Starting at 27, count the Collatz steps to reach 1 and print the count.',
    M('        int n = 27, steps = 0;\n        while (n != 1) {\n            n = (n % 2 == 0) ? n / 2 : 3 * n + 1;\n            steps++;\n        }\n        System.out.println(steps);'), 'The conditional operator fits the rule.'),
  d('java-f-labelled-break', 'Labelled break', 'Use a labelled break to find and print the first pair (r, c) in a 5x5 grid where r * c exceeds 6.',
    M('        outer:\n        for (int r = 1; r <= 5; r++) {\n            for (int c = 1; c <= 5; c++) {\n                if (r * c > 6) {\n                    System.out.println(r + " " + c);\n                    break outer;\n                }\n            }\n        }'), 'The label lets one break exit both loops.'),
  d('java-f-continue-filter', 'Filtering with continue', 'Print the numbers 1 to 25 that are divisible by neither 2 nor 3.',
    M('        for (int n = 1; n <= 25; n++) {\n            if (n % 2 == 0 || n % 3 == 0) continue;\n            System.out.println(n);\n        }'), 'continue skips the print.'),
  d('java-f-capstone', 'Control flow summary', 'In one pass over 1 to 40, count multiples of 6 and sum them, then print the count, sum and average to two decimals.',
    M('        int count = 0, sum = 0;\n        for (int n = 1; n <= 40; n++) {\n            if (n % 6 != 0) continue;\n            count++;\n            sum += n;\n        }\n        System.out.println("count: " + count);\n        System.out.println("sum: " + sum);\n        System.out.printf("average: %.2f%n", (double) sum / count);'), 'continue skips the non-multiples.'),
  {
    kind: 'quiz',
    slug: 'java-flow-quiz',
    title: 'Checkpoint: control flow',
    questions: [
      { q: 'What can a Java switch test that a C switch cannot?', options: ['Nothing', 'Strings and enums', 'Doubles', 'Arrays'], answer: 1, explain: 'Java supports String and enum switches in addition to the integral types C allows.' },
      { q: 'What is the advantage of the arrow form case x -> ... ?', options: ['It is shorter', 'It does not fall through, so no break is needed', 'It is faster', 'It allows doubles'], answer: 1, explain: 'The commonest classic switch bug was a forgotten break, which the arrow form eliminates entirely.' },
      { q: 'Can an enhanced for loop modify the array it walks?', options: ['Yes', 'No \u2014 the loop variable is a copy', 'Only for objects', 'Only with a label'], answer: 1, explain: 'Assigning to the loop variable changes only the copy, so writing back requires an indexed loop.' },
      { q: 'What does break outer; do?', options: ['A syntax error', 'Exits the loop labelled outer, escaping several levels at once', 'Breaks the innermost loop', 'Continues the outer loop'], answer: 1, explain: 'Labelled break is Java\u2019s alternative to the flag variable that C requires for the same job.' },
      { q: 'What is unique about do-while?', options: ['It cannot use break', 'The body always executes at least once', 'It has no condition', 'It only counts down'], answer: 1, explain: 'The condition is tested after the body, which suits prompt-then-validate patterns.' },
    ],
  },
];
