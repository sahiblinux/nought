const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body, headers = ['iostream']) =>
  headers.map((h) => `#include <${h}>`).join('\n') + `\n\nint main() {\n${body}\n}\n`;

export const cppBasics = [
  {
    slug: 'cpp-first',
    title: 'Output with streams',
    subtitle: 'iostream, cout and <<',
    concept:
      'C++ starts from C but replaces printf with a **stream**. `std::cout` is the standard output stream, and `<<` pushes values into it.\n\nThere are no format specifiers. The compiler already knows each value\u2019s type, so it selects the right formatting itself \u2014 which also means a type mismatch is impossible. That is a genuine safety improvement over printf.\n\n`std::` is a **namespace** qualifier: cout lives in the standard library\u2019s namespace, so its full name is `std::cout`. Writing it out is the unambiguous style.',
    example: '#include <iostream>\n\nint main() {\n    std::cout << "Hello, world!" << \'\\n\';\n    std::cout << "mixing " << 42 << " and " << 3.5 << \'\\n\';\n}',
    note: '42 needs no %d \u2014 the stream works out that it is an int. That type safety is the whole point.',
    task: 'Print exactly `Hello, world!` using std::cout.',
    solution: M('    std::cout << "Hello, world!" << \'\\n\';'),
    hint: 'The two less-than signs are the stream insertion operator, not a comparison.',
  },
  {
    slug: 'cpp-endl-newline',
    title: 'Ending a line',
    subtitle: "endl versus '\\n'",
    concept:
      'Two ways to end a line. `\'\\n\'` writes a newline character. `std::endl` writes a newline **and flushes the stream**, forcing the buffer out immediately.\n\nFlushing costs time. In a loop printing thousands of lines, `endl` can be dramatically slower for no benefit. Prefer `\'\\n\'` and reserve `endl` for the rare case where you need output to appear right now.\n\nNote the single quotes: `\'\\n\'` is a char, `"\\n"` is a string. Both work, but the char is marginally cheaper.',
    example: '#include <iostream>\n\nint main() {\n    std::cout << "with newline\\n";\n    std::cout << "with endl" << std::endl;\n    std::cout << "chained" << \'\\n\' << "two lines" << \'\\n\';\n}',
    note: 'The visible output is identical; the difference is only whether the buffer is flushed.',
    task: "Print three lines: `first` using '\\n', `second` using std::endl, and `third` using a newline inside the string literal.",
    solution: M('    std::cout << "first" << \'\\n\';\n    std::cout << "second" << std::endl;\n    std::cout << "third\\n";'),
    hint: 'All three approaches produce the same visible result.',
  },
  {
    slug: 'cpp-namespaces',
    title: 'Where names live',
    subtitle: 'Namespaces and using',
    concept:
      'A **namespace** groups names so that libraries cannot collide. Everything in the standard library lives in `std`.\n\n`using namespace std;` imports every name at once, letting you write `cout`. It is convenient in a tiny program and a genuine problem in a large one, because `std` contains hundreds of common words like `count`, `sort` and `data` that will collide with your own.\n\nThe measured position: qualify with `std::` in headers and real code; a targeted `using std::cout;` is an acceptable middle ground.',
    example: '#include <iostream>\n\nnamespace geometry {\n    int area(int w, int h) { return w * h; }\n}\n\nnamespace physics {\n    int area(int r) { return 3 * r * r; }\n}\n\nint main() {\n    std::cout << geometry::area(3, 4) << \'\\n\';\n    std::cout << physics::area(2) << \'\\n\';\n\n    using std::cout;\n    cout << "targeted using\\n";\n}',
    note: 'Both namespaces define area, and the qualification makes clear which one is meant.',
    task: 'Define a namespace `util` containing `int twice(int)`, call it as `util::twice(21)`, then use a targeted `using std::cout;` to print a second line.',
    solution: '#include <iostream>\n\nnamespace util {\n    int twice(int n) { return n * 2; }\n}\n\nint main() {\n    std::cout << util::twice(21) << \'\\n\';\n    using std::cout;\n    cout << "done\\n";\n}\n',
    hint: 'The namespace block goes above main.',
  },
  {
    slug: 'cpp-comments-structure',
    title: 'The shape of a C++ file',
    subtitle: 'Includes, comments and main',
    concept:
      'A C++ file has a predictable structure: includes first, then declarations, then definitions, with `main` as the entry point.\n\nIn C++, `int main()` needs no `void` in the parameter list \u2014 empty parentheses already mean "no parameters". The `return 0;` may be omitted entirely; the compiler supplies it.\n\nBoth C comment styles work. Header includes come in two forms: `<angle brackets>` for library headers, `"quotes"` for your own files.',
    example: '#include <iostream>\n#include <string>\n\n// A short helper, defined before use.\nint doubled(int n) {\n    return n * 2;\n}\n\n/* main is the entry point.\n   No explicit return is needed. */\nint main() {\n    std::cout << doubled(21) << \'\\n\';\n    std::cout << "structure matters\\n";\n}',
    note: 'Angle brackets search the system include paths; quotes search your project first.',
    task: 'Write a file with two includes, a commented helper `int halve(int)`, and a main that prints halve(84) and a message.',
    solution: '#include <iostream>\n#include <string>\n\n// Halve an integer, truncating any remainder.\nint halve(int n) {\n    return n / 2;\n}\n\nint main() {\n    std::cout << halve(84) << \'\\n\';\n    std::cout << "structured\\n";\n}\n',
    hint: 'Define the helper above main so it is known when called.',
  },
  {
    slug: 'cpp-stream-formatting',
    title: 'Controlling how numbers look',
    subtitle: 'iomanip and stream manipulators',
    concept:
      'Streams accept **manipulators** that change formatting from that point on.\n\nFrom `<iomanip>`:\n- `std::setprecision(n)` significant digits, or decimals when combined with `std::fixed`\n- `std::setw(n)` minimum field width for the **next** item only\n- `std::setfill(c)` the padding character\n- `std::left` / `std::right` alignment\n\nThe important asymmetry: `setw` applies once, while `fixed` and `setprecision` persist until changed. That catches everyone the first time.',
    example: '#include <iostream>\n#include <iomanip>\n\nint main() {\n    double value = 3.14159265;\n\n    std::cout << value << \'\\n\';\n    std::cout << std::fixed << std::setprecision(2) << value << \'\\n\';\n    std::cout << std::setprecision(4) << value << \'\\n\';\n\n    std::cout << std::setw(10) << "right" << \'|\' << \'\\n\';\n    std::cout << std::left << std::setw(10) << "left" << \'|\' << \'\\n\';\n}',
    note: 'The alignment and precision settings stick; setw affects only the item immediately after it.',
    task: 'Print 2.718281828 with two decimals then with five, and print the word `total` right-aligned in a field of 12 followed by a pipe.',
    solution: '#include <iostream>\n#include <iomanip>\n\nint main() {\n    double e = 2.718281828;\n\n    std::cout << std::fixed << std::setprecision(2) << e << \'\\n\';\n    std::cout << std::setprecision(5) << e << \'\\n\';\n    std::cout << std::setw(12) << "total" << \'|\' << \'\\n\';\n}\n',
    hint: 'std::fixed plus setprecision gives decimal places rather than significant digits.',
  },
  d('cpp-b-hello', 'Hello line', 'Print `C++ is compiled` on one line.',
    M('    std::cout << "C++ is compiled" << \'\\n\';'), 'One insertion plus a newline.'),
  d('cpp-b-three-lines', 'Three lines', 'Print `ready`, `steady` and `go` on three lines using a single statement.',
    M('    std::cout << "ready\\n" << "steady\\n" << "go\\n";'), 'Chain several insertions in one statement.'),
  d('cpp-b-mixed-types', 'Mixing types', 'In one statement print `count: 7 price: 2.5 grade: A` using an int, a double and a char.',
    M('    std::cout << "count: " << 7 << " price: " << 2.5 << " grade: " << \'A\' << \'\\n\';'), 'The stream picks the right formatting for each type.'),
  d('cpp-b-expressions', 'Computed values', 'Print the results of 17 + 25, 8 * 9 and 100 - 37 on three lines.',
    M('    std::cout << 17 + 25 << \'\\n\';\n    std::cout << 8 * 9 << \'\\n\';\n    std::cout << 100 - 37 << \'\\n\';'), 'Expressions are evaluated before being inserted.'),
  d('cpp-b-banner', 'A star banner', 'Print ten asterisks, then `   WELCOME   `, then ten asterisks.',
    M('    std::cout << "**********\\n";\n    std::cout << "   WELCOME   \\n";\n    std::cout << "**********\\n";'), 'Three separate lines.'),
  d('cpp-b-escapes', 'Escape sequences', 'Print a tab-separated header `lang\tyear` then `C++\t1985`.',
    M('    std::cout << "lang\\tyear\\n";\n    std::cout << "C++\\t1985\\n";'), 'Escapes work exactly as in C.'),
  d('cpp-b-quotes', 'Quoted text', 'Print exactly `"Keep it simple."` including the double quotes.',
    M('    std::cout << "\\"Keep it simple.\\"" << \'\\n\';'), 'Escape the inner quotes with backslashes.'),
  d('cpp-b-blank', 'A blank line', 'Print `above`, an empty line, then `below`.',
    M('    std::cout << "above\\n\\n" << "below\\n";'), 'Two consecutive newlines create the gap.'),
  d('cpp-b-namespace', 'A custom namespace', 'Define namespace math with square(int), and print math::square(9).',
    '#include <iostream>\n\nnamespace math {\n    int square(int n) { return n * n; }\n}\n\nint main() {\n    std::cout << math::square(9) << \'\\n\';\n}\n', 'Qualify the call with the namespace name.'),
  d('cpp-b-nested-namespace', 'Nested namespaces', 'Define outer::inner::value() returning 42 and print it.',
    '#include <iostream>\n\nnamespace outer {\n    namespace inner {\n        int value() { return 42; }\n    }\n}\n\nint main() {\n    std::cout << outer::inner::value() << \'\\n\';\n}\n', 'Chain the qualifications with double colons.'),
  d('cpp-b-using-decl', 'A targeted using', 'Use `using std::cout;` and print two lines without the std:: prefix.',
    '#include <iostream>\n\nint main() {\n    using std::cout;\n    cout << "first\\n";\n    cout << "second\\n";\n}\n', 'A using declaration imports one name only.'),
  d('cpp-b-precision', 'Decimal precision', 'Print 1.23456789 with zero, two, four and six decimal places.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    double v = 1.23456789;\n    std::cout << std::fixed;\n    std::cout << std::setprecision(0) << v << \'\\n\';\n    std::cout << std::setprecision(2) << v << \'\\n\';\n    std::cout << std::setprecision(4) << v << \'\\n\';\n    std::cout << std::setprecision(6) << v << \'\\n\';\n}\n', 'std::fixed makes setprecision mean decimal places.'),
  d('cpp-b-setw-table', 'An aligned table', 'Using setw, print a table of ada/91, bob/7 and cy/100 with names left in 8 columns and scores right in 5.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    std::cout << std::left << std::setw(8) << "ada" << std::right << std::setw(5) << 91 << \'\\n\';\n    std::cout << std::left << std::setw(8) << "bob" << std::right << std::setw(5) << 7 << \'\\n\';\n    std::cout << std::left << std::setw(8) << "cy" << std::right << std::setw(5) << 100 << \'\\n\';\n}\n', 'setw applies to the next item only.'),
  d('cpp-b-setfill', 'Padding with a character', 'Using setfill and setw, print the number 42 padded to eight characters with dots.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    std::cout << std::setfill(\'.\') << std::setw(8) << 42 << \'\\n\';\n}\n', 'setfill changes the padding character.'),
  d('cpp-b-boolalpha', 'Printing booleans', 'Print true and false as numbers, then again as words using std::boolalpha.',
    M('    std::cout << true << \' \' << false << \'\\n\';\n    std::cout << std::boolalpha << true << \' \' << false << \'\\n\';'), 'boolalpha switches to word form.'),
  d('cpp-b-hex', 'Number bases', 'Print 255 in decimal, hexadecimal and octal using stream manipulators.',
    M('    std::cout << std::dec << 255 << \'\\n\';\n    std::cout << std::hex << 255 << \'\\n\';\n    std::cout << std::oct << 255 << \'\\n\';'), 'std::hex and std::oct change the base.'),
  d('cpp-b-receipt', 'A formatted receipt', 'Print a three-line receipt with items left-aligned in 10 columns and prices to two decimals: coffee 3.50, bagel 2.25, TOTAL 5.75.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    std::cout << std::fixed << std::setprecision(2);\n    std::cout << std::left << std::setw(10) << "coffee" << 3.50 << \'\\n\';\n    std::cout << std::left << std::setw(10) << "bagel" << 2.25 << \'\\n\';\n    std::cout << std::left << std::setw(10) << "TOTAL" << 5.75 << \'\\n\';\n}\n', 'Set fixed and precision once, then reuse.'),
  d('cpp-b-triangle', 'Star triangle', 'Print a four-row left-aligned triangle of stars.',
    M('    std::cout << "*\\n";\n    std::cout << "**\\n";\n    std::cout << "***\\n";\n    std::cout << "****\\n";'), 'One insertion per row.'),
  d('cpp-b-basics-capstone', 'Basics summary', 'Print a header of dashes, then `lang: C++ (1985)` mixing a string and an int, then a footer, then the value 7.0/3.0 to three decimals.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    std::cout << "--------------------\\n";\n    std::cout << "lang: " << "C++" << " (" << 1985 << ")\\n";\n    std::cout << "--------------------\\n";\n    std::cout << std::fixed << std::setprecision(3) << 7.0 / 3.0 << \'\\n\';\n}\n', 'Use 7.0 rather than 7 to get floating-point division.'),
  {
    kind: 'quiz',
    slug: 'cpp-basics-quiz',
    title: 'Checkpoint: streams',
    questions: [
      { q: 'Why does std::cout << 42 need no format specifier?', options: ['cout only prints integers', '<< is overloaded per type, so the compiler picks the right one', 'The preprocessor converts it', 'Specifiers are optional in C++'], answer: 1, explain: 'operator<< is overloaded for each type and resolved at compile time, so mismatches are impossible.' },
      { q: "What is the difference between std::endl and '\\n'?", options: ['None', 'endl also flushes the stream, which is slower', "'\\n' flushes", 'endl prints two newlines'], answer: 1, explain: 'Both emit a newline, but endl forces a buffer flush, which can be very costly inside a loop.' },
      { q: 'Why is using namespace std; discouraged in large code?', options: ['It is slower', 'std contains many common words that will collide with your own names', 'It breaks includes', 'It is deprecated'], answer: 1, explain: 'Names like count, sort and data are all in std, so wholesale importing invites ambiguity.' },
      { q: 'How many items does std::setw affect?', options: ['All following items', 'Only the next one', 'Two', 'None'], answer: 1, explain: 'setw is consumed by the next insertion, unlike fixed and setprecision which persist.' },
      { q: 'What does std::fixed change about setprecision?', options: ['Nothing', 'It makes the precision mean decimal places rather than significant digits', 'It rounds to integers', 'It sets the field width'], answer: 1, explain: 'Without fixed, setprecision counts total significant digits; with it, the count applies after the decimal point.' },
    ],
  },
];

export const cppTypes = [
  {
    slug: 'cpp-variables',
    title: 'Declaring variables',
    subtitle: 'Fundamental types and initialisation',
    concept:
      'C++ has C\u2019s fundamental types \u2014 `int`, `double`, `char`, `bool` \u2014 with `bool` built into the language rather than bolted on by a header.\n\nThere are several initialisation syntaxes. `int x = 5;` is the familiar one. `int x{5};` is **brace initialisation**, which is stricter: it refuses narrowing conversions that would silently lose data, so `int x{3.7};` is a compile error where `int x = 3.7;` quietly truncates.\n\nPreferring braces catches real bugs at compile time.',
    example: '#include <iostream>\n\nint main() {\n    int count = 42;\n    double ratio{2.5};\n    char grade = \'A\';\n    bool ready{true};\n\n    std::cout << count << \' \' << ratio << \' \' << grade << \' \' << ready << \'\\n\';\n\n    int truncated = 3.7;\n    std::cout << truncated << \'\\n\';\n}',
    note: 'int truncated = 3.7; compiles and loses the fraction. int truncated{3.7}; would refuse to compile.',
    task: 'Declare `int items{7}` and `double price{2.5}`, compute the total, and print the three values, with the price and total to two decimals.',
    given: 'int items{7};\ndouble price{2.5};',
    solution: '#include <iostream>\n#include <iomanip>\n\nint main() {\n    int items{7};\n    double price{2.5};\n    double total = items * price;\n\n    std::cout << "items: " << items << \'\\n\';\n    std::cout << std::fixed << std::setprecision(2);\n    std::cout << "price: " << price << \'\\n\';\n    std::cout << "total: " << total << \'\\n\';\n}\n',
    hint: 'int times double promotes to double automatically.',
  },
  {
    slug: 'cpp-auto',
    title: 'Letting the compiler name the type',
    subtitle: 'auto and type deduction',
    concept:
      '`auto` asks the compiler to deduce a variable\u2019s type from its initialiser. The variable is still **statically typed** \u2014 you simply did not spell out something already obvious.\n\nIt earns its place with long types: `std::map<std::string, std::vector<int>>::iterator` becomes `auto`.\n\nThe rule of thumb: use `auto` when the type is obvious from the right-hand side or too long to be useful, and write the type explicitly when naming it documents intent. `auto` always requires an initialiser.',
    example: '#include <iostream>\n#include <string>\n#include <vector>\n\nint main() {\n    auto count = 42;\n    auto ratio = 2.5;\n    auto name = std::string{"ada"};\n    auto values = std::vector<int>{1, 2, 3};\n\n    std::cout << count << \' \' << ratio << \' \' << name << \'\\n\';\n\n    for (auto value : values) {\n        std::cout << value << \' \';\n    }\n    std::cout << \'\\n\';\n    std::cout << values.size() << \'\\n\';\n}',
    note: 'auto name = "ada"; would deduce const char *, not std::string \u2014 hence the explicit std::string on that line.',
    task: 'Using auto, declare an int, a double and a std::string, print all three, then loop over an auto-declared vector of three ints printing each.',
    solution: '#include <iostream>\n#include <string>\n#include <vector>\n\nint main() {\n    auto n = 10;\n    auto d = 1.5;\n    auto s = std::string{"auto"};\n\n    std::cout << n << \'\\n\';\n    std::cout << d << \'\\n\';\n    std::cout << s << \'\\n\';\n\n    auto values = std::vector<int>{4, 5, 6};\n    for (auto v : values) {\n        std::cout << v << \'\\n\';\n    }\n}\n',
    hint: 'auto always needs an initialiser to deduce from.',
  },
  {
    slug: 'cpp-references',
    title: 'Another name for a variable',
    subtitle: 'References',
    concept:
      'A **reference** (`int&`) is an alias for an existing variable. Unlike a pointer there is no address arithmetic, no null, and no dereferencing syntax \u2014 you use it exactly as you would the original.\n\nA reference must be initialised when declared and can never be reseated afterwards.\n\nThe main use is function parameters. `void f(int& n)` lets the function modify the caller\u2019s variable; `void f(const std::string& s)` passes a large object without copying it while promising not to change it. That `const&` form is the default choice for any non-trivial parameter.',
    example: '#include <iostream>\n#include <string>\n\nvoid grow(int& value) {\n    value += 10;\n}\n\nvoid show(const std::string& text) {\n    std::cout << text << " (" << text.size() << ")\\n";\n}\n\nint main() {\n    int n = 5;\n    int& alias = n;\n\n    alias = 20;\n    std::cout << n << \'\\n\';\n\n    grow(n);\n    std::cout << n << \'\\n\';\n\n    std::string message = "no copy made";\n    show(message);\n}',
    note: 'Assigning through alias changed n, because they are two names for one object.',
    task: 'Write `void triple(int& n)` multiplying its argument by three in place, and `void describe(const std::string& s)` printing the text and its length. Use both.',
    solution: '#include <iostream>\n#include <string>\n\nvoid triple(int& n) {\n    n *= 3;\n}\n\nvoid describe(const std::string& s) {\n    std::cout << s << \' \' << s.size() << \'\\n\';\n}\n\nint main() {\n    int value = 7;\n    std::cout << value << \'\\n\';\n    triple(value);\n    std::cout << value << \'\\n\';\n\n    describe("reference");\n}\n',
    hint: 'Inside triple just write n *= 3; the ampersand in the parameter does the rest.',
  },
  {
    slug: 'cpp-const',
    title: 'Promising not to change',
    subtitle: 'const and constexpr',
    concept:
      '`const` marks a value as unmodifiable, and the compiler enforces it. Applied to parameters and member functions it documents intent in a way comments cannot.\n\n`constexpr` goes further: the value must be computable **at compile time**, so it costs nothing at run time and can size arrays or appear in template arguments.\n\nThe habit worth forming is "const by default" \u2014 make everything const and remove it only where mutation is genuinely needed. It prevents accidental modification and helps the optimiser.',
    example: '#include <iostream>\n\nconstexpr int square(int n) {\n    return n * n;\n}\n\nint main() {\n    const int limit = 100;\n    constexpr int side = 4;\n    constexpr int area = square(side);\n\n    int values[area];\n    for (int i = 0; i < area; i++) values[i] = i;\n\n    std::cout << limit << \'\\n\';\n    std::cout << area << \'\\n\';\n    std::cout << values[area - 1] << \'\\n\';\n}',
    note: 'area was computed by the compiler, which is why it can size an array.',
    task: 'Declare `const double RATE = 0.2`, a `constexpr int DOZEN = 12`, and a constexpr function `cube(int)`. Print the rate, the dozen, cube(3) and cube(DOZEN).',
    solution: '#include <iostream>\n\nconstexpr int cube(int n) {\n    return n * n * n;\n}\n\nint main() {\n    const double RATE = 0.2;\n    constexpr int DOZEN = 12;\n\n    std::cout << RATE << \'\\n\';\n    std::cout << DOZEN << \'\\n\';\n    std::cout << cube(3) << \'\\n\';\n    std::cout << cube(DOZEN) << \'\\n\';\n}\n',
    hint: 'A constexpr function can also be called at run time with ordinary values.',
  },
  {
    slug: 'cpp-casting',
    title: 'Converting between types',
    subtitle: 'static_cast and integer division',
    concept:
      'C++ inherits C\u2019s integer division trap: `7 / 2` is `3` when both operands are int. The fix is to convert one operand.\n\nC++ prefers `static_cast<double>(a) / b` over the C-style `(double)a`. The named form is deliberately verbose so that casts are easy to find, and it performs only conversions the compiler considers sensible \u2014 a C-style cast will silently attempt almost anything.\n\nThere are four named casts; `static_cast` covers the ordinary numeric and class-hierarchy conversions you will actually need.',
    example: '#include <iostream>\n#include <iomanip>\n\nint main() {\n    int a = 7, b = 2;\n\n    std::cout << a / b << \'\\n\';\n    std::cout << static_cast<double>(a) / b << \'\\n\';\n\n    double value = 9.87;\n    std::cout << static_cast<int>(value) << \'\\n\';\n\n    char letter = \'A\';\n    std::cout << static_cast<int>(letter) << \'\\n\';\n}',
    note: 'static_cast<int> truncates toward zero rather than rounding.',
    task: 'With `int total = 47` and `int people = 6`, print the truncating division, the exact average to three decimals using static_cast, and the average truncated back to an int.',
    given: 'int total = 47;\nint people = 6;',
    solution: '#include <iostream>\n#include <iomanip>\n\nint main() {\n    int total = 47;\n    int people = 6;\n\n    std::cout << total / people << \'\\n\';\n    std::cout << std::fixed << std::setprecision(3)\n              << static_cast<double>(total) / people << \'\\n\';\n    std::cout << static_cast<int>(static_cast<double>(total) / people) << \'\\n\';\n}\n',
    hint: 'Casting one operand is enough; the other is promoted automatically.',
  },
  d('cpp-t-declare', 'Four declarations', 'Declare an int 42, a double 2.718, a char Z and a bool true using braces, then print all four.',
    M('    int i{42};\n    double d{2.718};\n    char c{\'Z\'};\n    bool b{true};\n    std::cout << i << \'\\n\' << d << \'\\n\' << c << \'\\n\' << b << \'\\n\';'), 'Brace initialisation refuses narrowing conversions.'),
  d('cpp-t-auto-basic', 'Deduced types', 'Using auto, declare and print an int, a double and a bool.',
    M('    auto n = 5;\n    auto d = 0.5;\n    auto flag = false;\n    std::cout << n << \'\\n\' << d << \'\\n\' << flag << \'\\n\';'), 'auto deduces from the initialiser.'),
  d('cpp-t-rect', 'Rectangle measurements', 'With int w{7} and h{3}, print the area and perimeter.',
    M('    int w{7}, h{3};\n    std::cout << "area: " << w * h << \'\\n\';\n    std::cout << "perimeter: " << 2 * (w + h) << \'\\n\';'), 'Parenthesise the sum before doubling.'),
  d('cpp-t-swap-ref', 'Swapping with references', 'Write void swap_ints(int&, int&) and use it on 3 and 8, printing before and after.',
    '#include <iostream>\n\nvoid swap_ints(int& a, int& b) {\n    int t = a;\n    a = b;\n    b = t;\n}\n\nint main() {\n    int x = 3, y = 8;\n    std::cout << x << \' \' << y << \'\\n\';\n    swap_ints(x, y);\n    std::cout << x << \' \' << y << \'\\n\';\n}\n', 'References need no ampersand at the call site.'),
  d('cpp-t-std-swap', 'The library swap', 'Use std::swap from <utility> to exchange two ints, printing before and after.',
    '#include <iostream>\n#include <utility>\n\nint main() {\n    int a = 10, b = 20;\n    std::cout << a << \' \' << b << \'\\n\';\n    std::swap(a, b);\n    std::cout << a << \' \' << b << \'\\n\';\n}\n', 'std::swap works for any assignable type.'),
  d('cpp-t-ref-modify', 'Modifying via reference', 'Write void add_ten(int&) and apply it twice to a variable holding 5, printing after each call.',
    '#include <iostream>\n\nvoid add_ten(int& n) {\n    n += 10;\n}\n\nint main() {\n    int v = 5;\n    add_ten(v);\n    std::cout << v << \'\\n\';\n    add_ten(v);\n    std::cout << v << \'\\n\';\n}\n', 'The reference binds to the caller\'s variable.'),
  d('cpp-t-const-ref', 'A const reference parameter', 'Write void show(const std::string&) printing the text and its size, and call it twice.',
    '#include <iostream>\n#include <string>\n\nvoid show(const std::string& s) {\n    std::cout << s << \' \' << s.size() << \'\\n\';\n}\n\nint main() {\n    show("first");\n    show("second string");\n}\n', 'const& avoids a copy without allowing modification.'),
  d('cpp-t-int-division', 'Integer versus real division', 'With int a{22} and b{7}, print a / b, a % b and the exact quotient to four decimals.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    int a{22}, b{7};\n    std::cout << a / b << \'\\n\';\n    std::cout << a % b << \'\\n\';\n    std::cout << std::fixed << std::setprecision(4) << static_cast<double>(a) / b << \'\\n\';\n}\n', 'static_cast one operand for the exact result.'),
  d('cpp-t-truncate', 'Truncating a double', 'With double value{9.87}, print it, its static_cast<int> value, and that value plus one.',
    M('    double value{9.87};\n    std::cout << value << \'\\n\';\n    std::cout << static_cast<int>(value) << \'\\n\';\n    std::cout << static_cast<int>(value) + 1 << \'\\n\';'), 'The cast truncates rather than rounding.'),
  d('cpp-t-char-int', 'Characters as numbers', 'Print the char A, its integer code, and the character three places later.',
    M('    char c{\'A\'};\n    std::cout << c << \'\\n\';\n    std::cout << static_cast<int>(c) << \'\\n\';\n    std::cout << static_cast<char>(c + 3) << \'\\n\';'), 'Cast back to char to display the letter.'),
  d('cpp-t-constexpr', 'Compile-time constants', 'Define constexpr int SIDE{5} and a constexpr area function, then print the area.',
    '#include <iostream>\n\nconstexpr int area(int side) {\n    return side * side;\n}\n\nint main() {\n    constexpr int SIDE{5};\n    constexpr int A = area(SIDE);\n    std::cout << A << \'\\n\';\n}\n', 'constexpr values are computed by the compiler.'),
  d('cpp-t-const-guard', 'A const value', 'Declare const double PI{3.14159} and int r{5}, then print the circumference and area to two decimals.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    const double PI{3.14159};\n    int r{5};\n    std::cout << std::fixed << std::setprecision(2);\n    std::cout << 2 * PI * r << \'\\n\';\n    std::cout << PI * r * r << \'\\n\';\n}\n', 'const prevents accidental reassignment.'),
  d('cpp-t-sizeof', 'Type sizes', 'Print the sizeof int, double, char and bool.',
    M('    std::cout << sizeof(int) << \'\\n\';\n    std::cout << sizeof(double) << \'\\n\';\n    std::cout << sizeof(char) << \'\\n\';\n    std::cout << sizeof(bool) << \'\\n\';'), 'sizeof works exactly as in C.'),
  d('cpp-t-bool-arith', 'Booleans in arithmetic', 'Print true + true, false + 5 and static_cast<int>(true).',
    M('    std::cout << true + true << \'\\n\';\n    std::cout << false + 5 << \'\\n\';\n    std::cout << static_cast<int>(true) << \'\\n\';'), 'true converts to 1 and false to 0.'),
  d('cpp-t-celsius', 'Temperature conversion', 'With int c{37}, print the Fahrenheit value to one decimal place.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    int c{37};\n    std::cout << std::fixed << std::setprecision(1)\n              << c * 9.0 / 5 + 32 << \'\\n\';\n}\n', 'Using 9.0 forces floating-point arithmetic.'),
  d('cpp-t-seconds', 'Total seconds', 'With ints days{2}, hours{5} and minutes{30}, print the total number of seconds.',
    M('    int days{2}, hours{5}, minutes{30};\n    std::cout << days * 86400 + hours * 3600 + minutes * 60 << \'\\n\';'), 'A day is 86400 seconds.'),
  d('cpp-t-average', 'Average of three', 'With three ints 88, 94 and 79, print the total and the average to two decimals.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    int a{88}, b{94}, c{79};\n    int total = a + b + c;\n    std::cout << total << \'\\n\';\n    std::cout << std::fixed << std::setprecision(2) << total / 3.0 << \'\\n\';\n}\n', 'Divide by 3.0 to avoid integer division.'),
  d('cpp-t-capstone', 'Types summary', 'With int items{13} and double unit{4.75}, print the count, the unit price to two decimals, the total to two decimals, the whole part as an int, and the size of a double.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    int items{13};\n    double unit{4.75};\n    double total = items * unit;\n\n    std::cout << items << \'\\n\';\n    std::cout << std::fixed << std::setprecision(2);\n    std::cout << unit << \'\\n\';\n    std::cout << total << \'\\n\';\n    std::cout << static_cast<int>(total) << \'\\n\';\n    std::cout << sizeof(double) << \'\\n\';\n}\n', 'Set the precision once before the decimal output.'),
  {
    kind: 'quiz',
    slug: 'cpp-types-quiz',
    title: 'Checkpoint: types',
    questions: [
      { q: 'What is the advantage of int x{3.7} over int x = 3.7?', options: ['It is faster', 'It refuses the narrowing conversion at compile time', 'It rounds instead of truncating', 'No difference'], answer: 1, explain: 'Brace initialisation rejects conversions that would lose data, turning a silent bug into a compile error.' },
      { q: 'Is auto dynamically typed?', options: ['Yes', 'No \u2014 the type is deduced at compile time and then fixed', 'Only for numbers', 'Only in templates'], answer: 1, explain: 'auto is pure convenience; the resulting variable has one static type just as if you had written it out.' },
      { q: 'What is the main difference between a reference and a pointer?', options: ['None', 'A reference cannot be null or reseated, and needs no dereferencing syntax', 'A reference is slower', 'Pointers cannot be const'], answer: 1, explain: 'A reference is an alias that must be bound at declaration, which makes it safer for parameters.' },
      { q: 'Why pass a large object as const T& ?', options: ['To allow modification', 'To avoid copying it while promising not to change it', 'To make it constexpr', 'It is required'], answer: 1, explain: 'Passing by value copies the whole object; a const reference avoids that cost without granting write access.' },
      { q: 'Why prefer static_cast<double>(a) over (double)a?', options: ['It is shorter', 'It is easy to search for and performs only sensible conversions', 'It is faster', 'C-style casts do not compile'], answer: 1, explain: 'The named casts are deliberately visible and more restricted, so dangerous conversions cannot happen by accident.' },
    ],
  },
];
