const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });

export const cppClasses = [
  {
    slug: 'cpp-struct-class',
    title: 'Defining your own types',
    subtitle: 'struct, class and access',
    concept:
      'A `struct` groups data; in C++ it can also hold **member functions** that operate on that data, which is what makes it an object rather than a bag of fields.\n\n`class` is the same construct with one difference: members are **private** by default, whereas a struct\u2019s are public.\n\nThe convention that follows: use `struct` for plain aggregates with no invariants, and `class` when the type maintains rules about its own state. `public:` and `private:` sections switch the access level for everything following.\n\nMarking a member function `const` promises it will not modify the object, which lets it be called on const instances.',
    example: '#include <iostream>\n\nstruct Point {\n    double x;\n    double y;\n\n    double distance_from_origin() const {\n        return x * x + y * y;\n    }\n};\n\nclass Counter {\nprivate:\n    int value = 0;\n\npublic:\n    void increment() { ++value; }\n    int get() const { return value; }\n};\n\nint main() {\n    Point p{3.0, 4.0};\n    std::cout << p.x << \' \' << p.distance_from_origin() << \'\\n\';\n\n    Counter c;\n    c.increment();\n    c.increment();\n    std::cout << c.get() << \'\\n\';\n}',
    note: 'Counter\u2019s value is private, so the only way to change it is through increment \u2014 that is encapsulation.',
    task: 'Define a `Rect` struct with width, height, and const member functions `area()` and `is_square()`. Create a 4x6 and a 5x5 and print both results for each.',
    solution: '#include <iostream>\n\nstruct Rect {\n    int width;\n    int height;\n\n    int area() const { return width * height; }\n    bool is_square() const { return width == height; }\n};\n\nint main() {\n    std::cout << std::boolalpha;\n    for (const Rect& r : {Rect{4, 6}, Rect{5, 5}}) {\n        std::cout << r.area() << \' \' << r.is_square() << \'\\n\';\n    }\n}\n',
    hint: 'Mark both member functions const since neither modifies the object.',
  },
  {
    slug: 'cpp-constructors',
    title: 'Setting up an object',
    subtitle: 'Constructors and initialiser lists',
    concept:
      'A **constructor** runs when an object is created. It has the class\u2019s name and no return type.\n\nThe preferred way to set members is the **member initialiser list** \u2014 the `: name{value}` clause between the signature and the body. It initialises members directly, rather than default-constructing them and then assigning, which is both faster and required for const members and references.\n\nConstructors can be overloaded, and a **default constructor** takes no arguments. If you declare any constructor, the compiler stops generating the default one for you.\n\nMembers are initialised in **declaration order**, regardless of the order you list them.',
    example: '#include <iostream>\n#include <string>\n\nclass Book {\nprivate:\n    std::string title;\n    int pages;\n\npublic:\n    Book() : title{"untitled"}, pages{0} {}\n\n    Book(std::string t, int p) : title{std::move(t)}, pages{p} {}\n\n    void show() const {\n        std::cout << title << " (" << pages << ")\\n";\n    }\n};\n\nint main() {\n    Book a;\n    Book b{"Dune", 412};\n\n    a.show();\n    b.show();\n}',
    note: 'std::move avoids copying the string parameter into the member \u2014 a small but idiomatic touch.',
    task: 'Define a `Student` class with private name and score, a two-argument constructor using an initialiser list, and a const `describe()` printing `Ada scored 91`. Create two students and describe both.',
    solution: '#include <iostream>\n#include <string>\n\nclass Student {\nprivate:\n    std::string name;\n    int score;\n\npublic:\n    Student(std::string n, int s) : name{std::move(n)}, score{s} {}\n\n    void describe() const {\n        std::cout << name << " scored " << score << \'\\n\';\n    }\n};\n\nint main() {\n    Student a{"Ada", 91};\n    Student b{"Bob", 78};\n\n    a.describe();\n    b.describe();\n}\n',
    hint: 'The initialiser list goes after the constructor signature, before the body.',
  },
  {
    slug: 'cpp-getters-setters',
    title: 'Controlling access to state',
    subtitle: 'Encapsulation and validation',
    concept:
      'Making members private and exposing functions is not bureaucracy \u2014 it is where **invariants** live. A setter can reject impossible values, so the object can never hold nonsense.\n\nThe pattern: a `const` getter returning the value, and a setter that validates before assigning. Once the class guarantees its own consistency, no caller can break it.\n\nDerived values need no storage at all: `area()` computes from width and height, so it can never disagree with them. Prefer computing over caching when the calculation is cheap.',
    example: '#include <iostream>\n\nclass Temperature {\nprivate:\n    double celsius = 0.0;\n\npublic:\n    double get_celsius() const { return celsius; }\n\n    bool set_celsius(double value) {\n        if (value < -273.15) return false;\n        celsius = value;\n        return true;\n    }\n\n    double fahrenheit() const { return celsius * 9.0 / 5.0 + 32.0; }\n};\n\nint main() {\n    Temperature t;\n    std::cout << std::boolalpha;\n\n    std::cout << t.set_celsius(22.0) << \'\\n\';\n    std::cout << t.get_celsius() << \' \' << t.fahrenheit() << \'\\n\';\n\n    std::cout << t.set_celsius(-500.0) << \'\\n\';\n    std::cout << t.get_celsius() << \'\\n\';\n}',
    note: 'The impossible temperature was refused, so the object still holds its last valid value.',
    task: 'Define an `Account` class with a private balance, a const `get_balance()`, a `deposit(amount)` rejecting non-positive amounts, and a `withdraw(amount)` refusing overdrafts. Both return bool. Demonstrate a valid deposit, a valid withdrawal and a refused withdrawal.',
    solution: '#include <iostream>\n\nclass Account {\nprivate:\n    int balance = 0;\n\npublic:\n    int get_balance() const { return balance; }\n\n    bool deposit(int amount) {\n        if (amount <= 0) return false;\n        balance += amount;\n        return true;\n    }\n\n    bool withdraw(int amount) {\n        if (amount <= 0 || amount > balance) return false;\n        balance -= amount;\n        return true;\n    }\n};\n\nint main() {\n    Account a;\n    std::cout << std::boolalpha;\n\n    std::cout << a.deposit(100) << \'\\n\';\n    std::cout << a.get_balance() << \'\\n\';\n    std::cout << a.withdraw(30) << \'\\n\';\n    std::cout << a.get_balance() << \'\\n\';\n    std::cout << a.withdraw(500) << \'\\n\';\n    std::cout << a.get_balance() << \'\\n\';\n}\n',
    hint: 'Validate before assigning, and return false without changing anything on failure.',
  },
  {
    slug: 'cpp-operator-overloading',
    title: 'Making your type feel built in',
    subtitle: 'Operator overloading',
    concept:
      'C++ lets you define what the operators mean for your own types. `operator+` enables `a + b`, `operator==` enables comparison, and `operator<<` enables stream output.\n\nThe stream operator must be a **free function**, not a member, because the left operand is the stream rather than your object:\n\n`std::ostream& operator<<(std::ostream& os, const Point& p)`\n\nIt returns the stream so insertions can chain.\n\nThe discipline: overload only when the meaning is genuinely obvious. `+` on two points is clear; `+` on two employees is not. Surprising operators are worse than named functions.',
    example: '#include <iostream>\n\nclass Vec2 {\npublic:\n    double x = 0.0;\n    double y = 0.0;\n\n    Vec2() = default;\n    Vec2(double x_, double y_) : x{x_}, y{y_} {}\n\n    Vec2 operator+(const Vec2& other) const {\n        return Vec2{x + other.x, y + other.y};\n    }\n\n    bool operator==(const Vec2& other) const {\n        return x == other.x && y == other.y;\n    }\n};\n\nstd::ostream& operator<<(std::ostream& os, const Vec2& v) {\n    return os << \'(\' << v.x << ", " << v.y << \')\';\n}\n\nint main() {\n    Vec2 a{1.0, 2.0};\n    Vec2 b{3.0, 4.0};\n\n    std::cout << a << " + " << b << " = " << a + b << \'\\n\';\n    std::cout << std::boolalpha << (a == Vec2{1.0, 2.0}) << \'\\n\';\n}',
    note: 'Because operator<< returns the stream, the whole expression chains in one statement.',
    task: 'Define a `Money` class holding cents, with `operator+`, `operator==` and a free `operator<<` printing it as `12.50`. Add two amounts and print the result and an equality test.',
    solution: '#include <iostream>\n#include <iomanip>\n\nclass Money {\npublic:\n    int cents = 0;\n\n    Money() = default;\n    explicit Money(int c) : cents{c} {}\n\n    Money operator+(const Money& other) const {\n        return Money{cents + other.cents};\n    }\n\n    bool operator==(const Money& other) const {\n        return cents == other.cents;\n    }\n};\n\nstd::ostream& operator<<(std::ostream& os, const Money& m) {\n    return os << m.cents / 100 << \'.\' << std::setw(2) << std::setfill(\'0\') << m.cents % 100;\n}\n\nint main() {\n    Money a{1250};\n    Money b{375};\n\n    std::cout << a + b << \'\\n\';\n    std::cout << std::boolalpha << (a == Money{1250}) << \'\\n\';\n}\n',
    hint: 'operator<< must be a free function returning std::ostream&.',
  },
  {
    slug: 'cpp-raii',
    title: 'Cleanup that cannot be forgotten',
    subtitle: 'Destructors and RAII',
    concept:
      'A **destructor** (`~ClassName()`) runs automatically when an object goes out of scope. This is the foundation of **RAII** \u2014 Resource Acquisition Is Initialisation \u2014 the single most important idea in C++.\n\nThe principle: acquire a resource in the constructor, release it in the destructor. Then it is released **no matter how** the scope exits: normal return, early return, or an exception. There is no `free` to forget and no cleanup path to miss.\n\nThis is why `std::vector` and `std::string` cannot leak, and why C++ needs no garbage collector. Destruction happens in reverse order of construction.',
    example: '#include <iostream>\n#include <string>\n\nclass Tracker {\nprivate:\n    std::string name;\n\npublic:\n    explicit Tracker(std::string n) : name{std::move(n)} {\n        std::cout << "acquire " << name << \'\\n\';\n    }\n\n    ~Tracker() {\n        std::cout << "release " << name << \'\\n\';\n    }\n};\n\nvoid demo() {\n    Tracker outer{"outer"};\n    {\n        Tracker inner{"inner"};\n        std::cout << "inner scope\\n";\n    }\n    std::cout << "after inner\\n";\n}\n\nint main() {\n    demo();\n    std::cout << "done\\n";\n}',
    note: 'inner was released at its closing brace, and outer when demo returned \u2014 reverse construction order, automatically.',
    task: 'Define a `Guard` class printing `open: name` in its constructor and `close: name` in its destructor. Create one in main and another inside a nested scope, printing a line between, to demonstrate the ordering.',
    solution: '#include <iostream>\n#include <string>\n\nclass Guard {\nprivate:\n    std::string name;\n\npublic:\n    explicit Guard(std::string n) : name{std::move(n)} {\n        std::cout << "open: " << name << \'\\n\';\n    }\n\n    ~Guard() {\n        std::cout << "close: " << name << \'\\n\';\n    }\n};\n\nint main() {\n    Guard outer{"outer"};\n    {\n        Guard inner{"inner"};\n        std::cout << "working\\n";\n    }\n    std::cout << "after\\n";\n}\n',
    hint: 'The destructor name is the class name with a leading tilde.',
  },
  {
    slug: 'cpp-static-members',
    title: 'Data belonging to the class',
    subtitle: 'Static members',
    concept:
      'A **static** data member belongs to the class, not to any instance: one copy shared by all objects. It is the right tool for counters and shared constants.\n\nA **static member function** likewise belongs to the class. It has no `this`, so it cannot touch instance members, and is called as `ClassName::function()`. Factory functions and utilities are the usual uses.\n\nSince C++17, a static member can be declared `inline` and initialised inside the class; before that it required a separate definition outside.',
    example: '#include <iostream>\n#include <string>\n\nclass Widget {\nprivate:\n    static inline int count = 0;\n    std::string name;\n\npublic:\n    explicit Widget(std::string n) : name{std::move(n)} { ++count; }\n\n    static int total() { return count; }\n\n    const std::string& get_name() const { return name; }\n};\n\nint main() {\n    std::cout << Widget::total() << \'\\n\';\n\n    Widget a{"first"};\n    Widget b{"second"};\n    std::cout << Widget::total() << \'\\n\';\n\n    {\n        Widget c{"third"};\n        std::cout << Widget::total() << \'\\n\';\n    }\n\n    std::cout << a.get_name() << \' \' << b.get_name() << \'\\n\';\n}',
    note: 'total() is called on the class itself, and works even before any Widget exists.',
    task: 'Define a `Ticket` class with a static counter, whose constructor assigns each instance a sequential id starting at 1. Provide a const `get_id()` and a static `issued()`. Create three tickets and print each id plus the total.',
    solution: '#include <iostream>\n\nclass Ticket {\nprivate:\n    static inline int counter = 0;\n    int id;\n\npublic:\n    Ticket() : id{++counter} {}\n\n    int get_id() const { return id; }\n    static int issued() { return counter; }\n};\n\nint main() {\n    Ticket a;\n    Ticket b;\n    Ticket c;\n\n    std::cout << a.get_id() << \'\\n\';\n    std::cout << b.get_id() << \'\\n\';\n    std::cout << c.get_id() << \'\\n\';\n    std::cout << Ticket::issued() << \'\\n\';\n}\n',
    hint: 'Use ++counter in the initialiser list so each id is assigned before the body runs.',
  },
  d('cpp-cl-point', 'A point struct', 'Define a Point struct with x and y and a const sum() member, then create one and print both members and the sum.',
    '#include <iostream>\n\nstruct Point {\n    int x;\n    int y;\n    int sum() const { return x + y; }\n};\n\nint main() {\n    Point p{3, 4};\n    std::cout << p.x << \'\\n\';\n    std::cout << p.y << \'\\n\';\n    std::cout << p.sum() << \'\\n\';\n}\n', 'A struct member function can read the members directly.'),
  d('cpp-cl-rect-methods', 'Rectangle members', 'Define Rect with area() and perimeter() const members, and print both for a 6x7 rectangle.',
    '#include <iostream>\n\nstruct Rect {\n    int w;\n    int h;\n    int area() const { return w * h; }\n    int perimeter() const { return 2 * (w + h); }\n};\n\nint main() {\n    Rect r{6, 7};\n    std::cout << r.area() << \'\\n\';\n    std::cout << r.perimeter() << \'\\n\';\n}\n', 'Mark members const when they only read.'),
  d('cpp-cl-private', 'Encapsulated counter', 'Define a Counter class with a private value, public increment() and const get(), then increment three times and print the value.',
    '#include <iostream>\n\nclass Counter {\nprivate:\n    int value = 0;\npublic:\n    void increment() { ++value; }\n    int get() const { return value; }\n};\n\nint main() {\n    Counter c;\n    for (int i = 0; i < 3; ++i) c.increment();\n    std::cout << c.get() << \'\\n\';\n}\n', 'Private members are only reachable from member functions.'),
  d('cpp-cl-constructor', 'A constructor', 'Define a Person class with a constructor taking name and age, and a const show(), then create two and show both.',
    '#include <iostream>\n#include <string>\n\nclass Person {\nprivate:\n    std::string name;\n    int age;\npublic:\n    Person(std::string n, int a) : name{std::move(n)}, age{a} {}\n    void show() const { std::cout << name << \' \' << age << \'\\n\'; }\n};\n\nint main() {\n    Person a{"Ada", 36};\n    Person b{"Bob", 24};\n    a.show();\n    b.show();\n}\n', 'Use a member initialiser list.'),
  d('cpp-cl-default-ctor', 'Two constructors', 'Define a Box class with a default constructor setting size to 1 and another taking a size, then print both sizes.',
    '#include <iostream>\n\nclass Box {\nprivate:\n    int size;\npublic:\n    Box() : size{1} {}\n    explicit Box(int s) : size{s} {}\n    int get_size() const { return size; }\n};\n\nint main() {\n    Box a;\n    Box b{10};\n    std::cout << a.get_size() << \'\\n\';\n    std::cout << b.get_size() << \'\\n\';\n}\n', 'Constructors can be overloaded like any function.'),
  d('cpp-cl-validate', 'Validating a setter', 'Define an Age class whose set(value) rejects negatives and returns bool, then set 30, print, attempt -5, and print again.',
    '#include <iostream>\n\nclass Age {\nprivate:\n    int value = 0;\npublic:\n    int get() const { return value; }\n    bool set(int v) {\n        if (v < 0) return false;\n        value = v;\n        return true;\n    }\n};\n\nint main() {\n    Age a;\n    std::cout << std::boolalpha;\n    std::cout << a.set(30) << \' \' << a.get() << \'\\n\';\n    std::cout << a.set(-5) << \' \' << a.get() << \'\\n\';\n}\n', 'Return false without changing anything on failure.'),
  d('cpp-cl-bank', 'A bank account', 'Define an Account with deposit and withdraw that refuses overdrafts, then run a sequence printing the balance after each step.',
    '#include <iostream>\n\nclass Account {\nprivate:\n    int balance = 0;\npublic:\n    int get() const { return balance; }\n    void deposit(int amount) { balance += amount; }\n    bool withdraw(int amount) {\n        if (amount > balance) return false;\n        balance -= amount;\n        return true;\n    }\n};\n\nint main() {\n    Account a;\n    a.deposit(100);\n    std::cout << a.get() << \'\\n\';\n    std::cout << std::boolalpha << a.withdraw(30) << \' \' << a.get() << \'\\n\';\n    std::cout << a.withdraw(500) << \' \' << a.get() << \'\\n\';\n}\n', 'Guard the withdrawal before modifying.'),
  d('cpp-cl-derived-value', 'A computed member', 'Define Circle with a radius and const area() using 3.14159, then print the radius and area to two decimals.',
    '#include <iostream>\n#include <iomanip>\n\nclass Circle {\nprivate:\n    double radius;\npublic:\n    explicit Circle(double r) : radius{r} {}\n    double get_radius() const { return radius; }\n    double area() const { return 3.14159 * radius * radius; }\n};\n\nint main() {\n    Circle c{5.0};\n    std::cout << std::fixed << std::setprecision(2);\n    std::cout << c.get_radius() << \'\\n\';\n    std::cout << c.area() << \'\\n\';\n}\n', 'Compute rather than store the derived value.'),
  d('cpp-cl-operator-plus', 'Overloading +', 'Define a Vec2 with operator+ and print the components of the sum of (1,2) and (3,4).',
    '#include <iostream>\n\nstruct Vec2 {\n    int x;\n    int y;\n    Vec2 operator+(const Vec2& o) const { return Vec2{x + o.x, y + o.y}; }\n};\n\nint main() {\n    Vec2 a{1, 2}, b{3, 4};\n    Vec2 c = a + b;\n    std::cout << c.x << \' \' << c.y << \'\\n\';\n}\n', 'Return a new object rather than modifying either operand.'),
  d('cpp-cl-operator-equals', 'Overloading ==', 'Define a Point with operator== and print whether two equal points and two different points compare equal.',
    '#include <iostream>\n\nstruct Point {\n    int x;\n    int y;\n    bool operator==(const Point& o) const { return x == o.x && y == o.y; }\n};\n\nint main() {\n    std::cout << std::boolalpha;\n    std::cout << (Point{1, 2} == Point{1, 2}) << \'\\n\';\n    std::cout << (Point{1, 2} == Point{3, 4}) << \'\\n\';\n}\n', 'Compare every member.'),
  d('cpp-cl-operator-stream', 'Overloading <<', 'Define a Point with a free operator<< printing (x, y), then print two points directly.',
    '#include <iostream>\n\nstruct Point {\n    int x;\n    int y;\n};\n\nstd::ostream& operator<<(std::ostream& os, const Point& p) {\n    return os << \'(\' << p.x << ", " << p.y << \')\';\n}\n\nint main() {\n    std::cout << Point{3, 4} << \'\\n\';\n    std::cout << Point{-1, 0} << \'\\n\';\n}\n', 'The free function returns the stream so insertions chain.'),
  d('cpp-cl-operator-less', 'Overloading <', 'Define a Score class with operator< and use std::sort on a vector of three, printing them in order.',
    '#include <iostream>\n#include <vector>\n#include <algorithm>\n\nstruct Score {\n    int value;\n    bool operator<(const Score& o) const { return value < o.value; }\n};\n\nint main() {\n    std::vector<Score> v{{78}, {95}, {61}};\n    std::sort(v.begin(), v.end());\n    for (const auto& s : v) std::cout << s.value << \'\\n\';\n}\n', 'std::sort uses operator< by default.'),
  d('cpp-cl-destructor', 'A destructor', 'Define a Guard class printing on construction and destruction, and create one inside a nested scope with a line after it.',
    '#include <iostream>\n\nclass Guard {\npublic:\n    Guard() { std::cout << "open\\n"; }\n    ~Guard() { std::cout << "close\\n"; }\n};\n\nint main() {\n    {\n        Guard g;\n        std::cout << "inside\\n";\n    }\n    std::cout << "outside\\n";\n}\n', 'The destructor runs at the closing brace.'),
  d('cpp-cl-raii-order', 'Destruction order', 'Create three named Guard objects in one scope and observe that they are destroyed in reverse order.',
    '#include <iostream>\n#include <string>\n\nclass Guard {\n    std::string name;\npublic:\n    explicit Guard(std::string n) : name{std::move(n)} { std::cout << "open " << name << \'\\n\'; }\n    ~Guard() { std::cout << "close " << name << \'\\n\'; }\n};\n\nint main() {\n    Guard a{"a"};\n    Guard b{"b"};\n    Guard c{"c"};\n}\n', 'Destruction is the reverse of construction.'),
  d('cpp-cl-static-count', 'Counting instances', 'Define a Widget class with a static counter incremented in the constructor, create four, and print the total.',
    '#include <iostream>\n\nclass Widget {\n    static inline int count = 0;\npublic:\n    Widget() { ++count; }\n    static int total() { return count; }\n};\n\nint main() {\n    Widget a, b, c, dd;\n    std::cout << Widget::total() << \'\\n\';\n}\n', 'Static members are shared by all instances.'),
  d('cpp-cl-static-id', 'Sequential ids', 'Define a Ticket class assigning sequential ids from 1, create three, and print each id.',
    '#include <iostream>\n\nclass Ticket {\n    static inline int counter = 0;\n    int id;\npublic:\n    Ticket() : id{++counter} {}\n    int get_id() const { return id; }\n};\n\nint main() {\n    Ticket a, b, c;\n    std::cout << a.get_id() << \'\\n\';\n    std::cout << b.get_id() << \'\\n\';\n    std::cout << c.get_id() << \'\\n\';\n}\n', 'Pre-increment in the initialiser list.'),
  d('cpp-cl-static-function', 'A static utility', 'Define a MathUtil class with a static double(int) function and call it without creating an instance.',
    '#include <iostream>\n\nclass MathUtil {\npublic:\n    static int twice(int n) { return n * 2; }\n};\n\nint main() {\n    std::cout << MathUtil::twice(21) << \'\\n\';\n}\n', 'A static function is called on the class.'),
  d('cpp-cl-vector-objects', 'A vector of objects', 'Define a Student class and build a vector of three, then print the name of the highest scorer.',
    '#include <iostream>\n#include <string>\n#include <vector>\n#include <algorithm>\n\nstruct Student {\n    std::string name;\n    int score;\n};\n\nint main() {\n    std::vector<Student> v{{"ada", 91}, {"bob", 78}, {"cy", 95}};\n    auto best = std::max_element(v.begin(), v.end(),\n        [](const auto& a, const auto& b) { return a.score < b.score; });\n    std::cout << best->name << \'\\n\';\n}\n', 'max_element returns an iterator, so use the arrow.'),
  d('cpp-cl-sort-objects', 'Sorting objects', 'Sort a vector of three Students by score descending and print each as `name score`.',
    '#include <iostream>\n#include <string>\n#include <vector>\n#include <algorithm>\n\nstruct Student {\n    std::string name;\n    int score;\n};\n\nint main() {\n    std::vector<Student> v{{"bob", 78}, {"ada", 91}, {"cy", 95}};\n    std::sort(v.begin(), v.end(), [](const auto& a, const auto& b) { return a.score > b.score; });\n    for (const auto& s : v) std::cout << s.name << \' \' << s.score << \'\\n\';\n}\n', 'The lambda compares the score members.'),
  d('cpp-cl-composition', 'Objects holding objects', 'Define Engine with horsepower and Car holding an Engine, then print the car brand and its engine power.',
    '#include <iostream>\n#include <string>\n\nstruct Engine {\n    int hp;\n};\n\nstruct Car {\n    std::string brand;\n    Engine engine;\n};\n\nint main() {\n    Car c{"Volvo", Engine{240}};\n    std::cout << c.brand << \'\\n\';\n    std::cout << c.engine.hp << \'\\n\';\n}\n', 'Composition stores another object as a member.'),
  d('cpp-cl-capstone', 'Class summary', 'Define a Book class with private title and pages, a constructor, const getters, an is_long() member, and a free operator<<. Build a vector of three, print each, then the total pages and the longest title.',
    '#include <iostream>\n#include <string>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n\nclass Book {\n    std::string title;\n    int pages;\npublic:\n    Book(std::string t, int p) : title{std::move(t)}, pages{p} {}\n    const std::string& get_title() const { return title; }\n    int get_pages() const { return pages; }\n    bool is_long() const { return pages > 300; }\n};\n\nstd::ostream& operator<<(std::ostream& os, const Book& b) {\n    return os << b.get_title() << " (" << b.get_pages() << ")";\n}\n\nint main() {\n    std::vector<Book> shelf{{"Dune", 412}, {"Anthem", 105}, {"Ulysses", 730}};\n\n    for (const auto& b : shelf) std::cout << b << \'\\n\';\n\n    int total = std::accumulate(shelf.begin(), shelf.end(), 0,\n        [](int sum, const Book& b) { return sum + b.get_pages(); });\n    std::cout << total << \'\\n\';\n\n    auto longest = std::max_element(shelf.begin(), shelf.end(),\n        [](const Book& a, const Book& b) { return a.get_pages() < b.get_pages(); });\n    std::cout << longest->get_title() << \'\\n\';\n}\n', 'accumulate can take a custom binary operation to sum a member.'),
  {
    kind: 'quiz',
    slug: 'cpp-classes-quiz',
    title: 'Checkpoint: classes',
    questions: [
      { q: 'What is the only difference between struct and class in C++?', options: ['Structs cannot have functions', 'Default access: struct members are public, class members are private', 'Classes cannot be copied', 'Structs are faster'], answer: 1, explain: 'Both support member functions, constructors and inheritance; only the default access level differs.' },
      { q: 'Why prefer a member initialiser list over assigning in the constructor body?', options: ['It is shorter', 'It initialises members directly instead of default-constructing then assigning, and is required for const members', 'It is required by the standard', 'No difference'], answer: 1, explain: 'Assignment in the body means the member is constructed twice over, and const members and references cannot be assigned at all.' },
      { q: 'What does marking a member function const promise?', options: ['The return value is const', 'The function will not modify the object', 'It cannot be overridden', 'It is static'], answer: 1, explain: 'A const member function can be called on const objects, and the compiler enforces that it makes no modifications.' },
      { q: 'Why must operator<< be a free function?', options: ['Style', 'The left operand is the stream, not your class', 'Members cannot be overloaded', 'It must be static'], answer: 1, explain: 'A member operator takes the object as its left operand, but here the stream is on the left, so it cannot be a member of your type.' },
      { q: 'What is RAII?', options: ['A naming convention', 'Acquiring a resource in the constructor and releasing it in the destructor, so cleanup cannot be skipped', 'A sorting algorithm', 'A memory allocator'], answer: 1, explain: 'Because destructors run on every exit path including exceptions, RAII is what makes leaks structurally impossible.' },
    ],
  },
];
