const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body, headers = ['iostream']) =>
  headers.map((h) => `#include <${h}>`).join('\n') + `\n\nint main() {\n${body}\n}\n`;

export const cppOperators = [
  {
    slug: 'cpp-arithmetic',
    title: 'Arithmetic and its traps',
    subtitle: 'The five operators',
    concept:
      'C++ has `+ - * / %`, behaving exactly as in C. There is no exponentiation operator \u2014 `std::pow` from `<cmath>` covers that.\n\nThe division trap persists: two ints divide as integers. `%` requires integers and gives the remainder.\n\nThe augmented forms `+= -= *= /= %=` update in place, and `++`/`--` come in prefix and postfix flavours. As a standalone statement they are identical; inside a larger expression, postfix yields the old value and prefix the new one.',
    example: '#include <iostream>\n#include <cmath>\n\nint main() {\n    int a = 17, b = 5;\n\n    std::cout << a + b << \' \' << a - b << \' \' << a * b << \'\\n\';\n    std::cout << a / b << \' \' << a % b << \'\\n\';\n    std::cout << static_cast<double>(a) / b << \'\\n\';\n    std::cout << std::pow(2, 10) << \'\\n\';\n\n    int i = 5;\n    std::cout << i++ << \' \' << i << \'\\n\';\n    std::cout << ++i << \' \' << i << \'\\n\';\n}',
    note: 'std::pow returns a double, so pow(2, 10) prints 1024 rather than an int.',
    task: 'With `int a{23}` and `int b{4}`, print the sum, product, integer quotient, remainder, exact quotient to two decimals, and 2 to the power 8.',
    given: 'int a{23}, b{4};',
    solution: '#include <iostream>\n#include <iomanip>\n#include <cmath>\n\nint main() {\n    int a{23}, b{4};\n\n    std::cout << a + b << \'\\n\';\n    std::cout << a * b << \'\\n\';\n    std::cout << a / b << \'\\n\';\n    std::cout << a % b << \'\\n\';\n    std::cout << std::fixed << std::setprecision(2)\n              << static_cast<double>(a) / b << \'\\n\';\n    std::cout << static_cast<int>(std::pow(2, 8)) << \'\\n\';\n}\n',
    hint: 'Cast pow back to int to print a whole number.',
  },
  {
    slug: 'cpp-comparison-logic',
    title: 'Comparing and combining',
    subtitle: 'Relational and logical operators',
    concept:
      'The six comparisons produce a genuine `bool`, not an int as in C. Printing one shows `1` or `0` unless you add `std::boolalpha`.\n\n`&&`, `||` and `!` combine conditions and **short-circuit**: `&&` stops at the first false, `||` at the first true. That guarantee makes `if (p != nullptr && p->value > 0)` safe.\n\nC++ has no chained comparisons \u2014 `0 < x < 10` compiles but is nonsense, because it means `(0 < x) < 10`, comparing a bool with 10. Write `0 < x && x < 10`.',
    example: '#include <iostream>\n\nint main() {\n    int x = 7;\n\n    std::cout << std::boolalpha;\n    std::cout << (x == 7) << \'\\n\';\n    std::cout << (x < 10 && x > 5) << \'\\n\';\n    std::cout << (x > 100 || x == 7) << \'\\n\';\n    std::cout << !(x == 7) << \'\\n\';\n\n    std::cout << (0 < x && x < 10) << \'\\n\';\n}',
    note: 'std::boolalpha makes the output read true/false, which is far clearer when debugging.',
    task: 'With `int n{15}`, using boolalpha print whether it equals 15, is under 10, lies strictly between 10 and 20, and is not 20.',
    given: 'int n{15};',
    solution: M('    int n{15};\n\n    std::cout << std::boolalpha;\n    std::cout << (n == 15) << \'\\n\';\n    std::cout << (n < 10) << \'\\n\';\n    std::cout << (10 < n && n < 20) << \'\\n\';\n    std::cout << (n != 20) << \'\\n\';'),
    hint: 'The range test needs two comparisons joined by &&.',
  },
  {
    slug: 'cpp-precedence',
    title: 'Which operation binds tighter',
    subtitle: 'Precedence and associativity',
    concept:
      'Precedence, tightest first: unary operators and casts, then `* / %`, then `+ -`, then shifts, then comparisons, then `&`, `^`, `|`, then `&&`, then `||`, then assignment.\n\nTwo practical consequences. The bitwise operators bind **looser** than comparisons, so `a & 1 == 0` means `a & (1 == 0)` \u2014 almost never intended.\n\nAnd because `<<` is a shift operator with middling precedence, `std::cout << a == b` parses as `(std::cout << a) == b`. Always parenthesise comparisons inside a stream insertion.',
    example: '#include <iostream>\n\nint main() {\n    std::cout << 2 + 3 * 4 << \'\\n\';\n    std::cout << (2 + 3) * 4 << \'\\n\';\n    std::cout << 20 / 4 / 5 << \'\\n\';\n\n    int a = 8;\n    std::cout << (a & 1) << \'\\n\';\n    std::cout << ((a & 1) == 0) << \'\\n\';\n\n    int b = 8;\n    std::cout << (a == b) << \'\\n\';\n}',
    note: 'Without the parentheses, the last line would try to compare the stream itself with b.',
    task: 'Print `5 + 2 * 3`, `(5 + 2) * 3`, `20 / 4 / 5`, and whether `(12 & 10) == 8`, each on its own line.',
    solution: M('    std::cout << 5 + 2 * 3 << \'\\n\';\n    std::cout << (5 + 2) * 3 << \'\\n\';\n    std::cout << 20 / 4 / 5 << \'\\n\';\n    std::cout << ((12 & 10) == 8) << \'\\n\';'),
    hint: 'Division is left-associative, so 20 / 4 / 5 is (20 / 4) / 5.',
  },
  {
    slug: 'cpp-bitwise',
    title: 'Working with bits',
    subtitle: 'Bitwise operators',
    concept:
      'C++ inherits C\u2019s bit operators: `&` and, `|` or, `^` xor, `~` not, `<<` and `>>` shift.\n\nThe four idioms:\n- set bit n: `x |= (1u << n)`\n- clear bit n: `x &= ~(1u << n)`\n- toggle bit n: `x ^= (1u << n)`\n- test bit n: `(x >> n) & 1u`\n\n`<<` is overloaded for streams as well as shifting, which is why bit expressions inside `std::cout <<` need parentheses.\n\nFor readable flag work, `std::bitset` from `<bitset>` prints the binary form directly.',
    example: '#include <iostream>\n#include <bitset>\n\nint main() {\n    unsigned int a = 12, b = 10;\n\n    std::cout << (a & b) << \'\\n\';\n    std::cout << (a | b) << \'\\n\';\n    std::cout << (a ^ b) << \'\\n\';\n    std::cout << (a << 1) << \'\\n\';\n\n    std::cout << std::bitset<8>(a) << \'\\n\';\n    std::cout << std::bitset<8>(a | b) << \'\\n\';\n}',
    note: '12 is 00001100 and 10 is 00001010, so & gives 8 and | gives 14.',
    task: 'With `unsigned int flags{10}`, print the result of anding with 12, oring with 5, xoring with 15, and the 8-bit binary form using std::bitset.',
    given: 'unsigned int flags{10};',
    solution: '#include <iostream>\n#include <bitset>\n\nint main() {\n    unsigned int flags{10};\n\n    std::cout << (flags & 12u) << \'\\n\';\n    std::cout << (flags | 5u) << \'\\n\';\n    std::cout << (flags ^ 15u) << \'\\n\';\n    std::cout << std::bitset<8>(flags) << \'\\n\';\n}\n',
    hint: 'Parenthesise each bitwise expression inside the stream insertion.',
  },
  {
    slug: 'cpp-ternary',
    title: 'Choosing inside an expression',
    subtitle: 'The conditional operator',
    concept:
      'The conditional operator is C++\u2019s only ternary operator:\n\n`condition ? value_if_true : value_if_false`\n\nBecause it is an expression it produces a value, so it can be assigned or inserted straight into a stream. Both branches must have compatible types, since the whole expression has one type.\n\nIt shines for small choices. Nesting several is legal but quickly unreadable \u2014 use `if` when there is real branching to express.',
    example: '#include <iostream>\n\nint main() {\n    int n = 7;\n\n    int larger = n > 5 ? n : 5;\n    std::cout << larger << \'\\n\';\n    std::cout << (n % 2 == 0 ? "even" : "odd") << \'\\n\';\n\n    int a = 17, b = 42;\n    std::cout << (a > b ? a : b) << \'\\n\';\n\n    int score = 84;\n    std::cout << (score >= 90 ? "A" : score >= 80 ? "B" : "C") << \'\\n\';\n}',
    note: 'Conditional operators chain to the right, which is what makes the grade ladder work.',
    task: 'With `int n{-8}`, use the conditional operator to print its magnitude and the word `neg` or `pos`.',
    given: 'int n{-8};',
    solution: M('    int n{-8};\n\n    std::cout << (n < 0 ? -n : n) << \'\\n\';\n    std::cout << (n < 0 ? "neg" : "pos") << \'\\n\';'),
    hint: 'Negating a negative value gives its magnitude.',
  },
  d('cpp-o-mixed', 'Four expressions', 'Print 7 + 3 * 2, (7 + 3) * 2, 19 % 4 and 19 / 4.',
    M('    std::cout << 7 + 3 * 2 << \'\\n\';\n    std::cout << (7 + 3) * 2 << \'\\n\';\n    std::cout << 19 % 4 << \'\\n\';\n    std::cout << 19 / 4 << \'\\n\';'), 'Multiplication binds tighter than addition.'),
  d('cpp-o-even', 'Divisibility booleans', 'With int n{34}, print with boolalpha whether it is even and whether it divides by 3.',
    M('    int n{34};\n    std::cout << std::boolalpha;\n    std::cout << (n % 2 == 0) << \'\\n\';\n    std::cout << (n % 3 == 0) << \'\\n\';'), 'Parenthesise the comparison inside the insertion.'),
  d('cpp-o-range', 'Range test', 'With int score{72}, print whether it lies between 70 and 80 inclusive.',
    M('    int score{72};\n    std::cout << std::boolalpha << (score >= 70 && score <= 80) << \'\\n\';'), 'C++ has no chained comparisons.'),
  d('cpp-o-increment', 'Prefix and postfix', 'Start at 8. Print n, then n++, then n, then ++n, then n.',
    M('    int n{8};\n    std::cout << n << \'\\n\';\n    std::cout << n++ << \'\\n\';\n    std::cout << n << \'\\n\';\n    std::cout << ++n << \'\\n\';\n    std::cout << n << \'\\n\';'), 'Postfix reports the old value.'),
  d('cpp-o-compound', 'Compound assignment', 'Start at 100. Apply -=20, /=2, %=7 and *=3, printing after each.',
    M('    int v{100};\n    v -= 20;\n    std::cout << v << \'\\n\';\n    v /= 2;\n    std::cout << v << \'\\n\';\n    v %= 7;\n    std::cout << v << \'\\n\';\n    v *= 3;\n    std::cout << v << \'\\n\';'), 'Each operator updates in place.'),
  d('cpp-o-logic-table', 'Truth table', 'With boolalpha, print the four && combinations of true and false.',
    M('    std::cout << std::boolalpha;\n    std::cout << (true && true) << \'\\n\';\n    std::cout << (true && false) << \'\\n\';\n    std::cout << (false && true) << \'\\n\';\n    std::cout << (false && false) << \'\\n\';'), 'Only the first is true.'),
  d('cpp-o-leap', 'Leap year', 'With int year{2024}, print whether it is a leap year as a single boolean expression.',
    M('    int year{2024};\n    std::cout << std::boolalpha\n              << ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) << \'\\n\';'), 'Group the && clause before the ||.'),
  d('cpp-o-short-circuit', 'Short-circuit safety', 'With int n{0}, print the value of n != 0 && 100 / n > 2 without crashing.',
    M('    int n{0};\n    std::cout << std::boolalpha << (n != 0 && 100 / n > 2) << \'\\n\';'), '&& skips the right operand when the left is false.'),
  d('cpp-o-pow', 'Powers with cmath', 'Print 2 to the power 10, 3 to the power 4 and the square root of 144, using cmath.',
    '#include <iostream>\n#include <cmath>\n\nint main() {\n    std::cout << static_cast<int>(std::pow(2, 10)) << \'\\n\';\n    std::cout << static_cast<int>(std::pow(3, 4)) << \'\\n\';\n    std::cout << std::sqrt(144.0) << \'\\n\';\n}\n', 'std::pow returns a double.'),
  d('cpp-o-abs-min-max', 'Library helpers', 'Using <algorithm> and <cmath>, print std::abs(-17), std::max(3, 9) and std::min(3, 9).',
    '#include <iostream>\n#include <algorithm>\n#include <cmath>\n\nint main() {\n    std::cout << std::abs(-17) << \'\\n\';\n    std::cout << std::max(3, 9) << \'\\n\';\n    std::cout << std::min(3, 9) << \'\\n\';\n}\n', 'std::max and std::min live in <algorithm>.'),
  d('cpp-o-bits-set', 'Setting bits', 'Starting from 0, set bits 0, 2 and 5, printing the value after each.',
    M('    unsigned int f{0};\n    f |= (1u << 0);\n    std::cout << f << \'\\n\';\n    f |= (1u << 2);\n    std::cout << f << \'\\n\';\n    f |= (1u << 5);\n    std::cout << f << \'\\n\';'), 'Bit n has the value 1 << n.'),
  d('cpp-o-bits-test', 'Testing bits', 'For the value 22, print whether bits 0 to 4 are set, one per line.',
    M('    unsigned int v{22};\n    for (int i = 0; i < 5; ++i) {\n        std::cout << ((v >> i) & 1u) << \'\\n\';\n    }'), 'Shift right then mask with 1.'),
  d('cpp-o-bitset', 'Binary with bitset', 'Print the 8-bit binary forms of 5, 42 and 255 using std::bitset.',
    '#include <iostream>\n#include <bitset>\n\nint main() {\n    std::cout << std::bitset<8>(5) << \'\\n\';\n    std::cout << std::bitset<8>(42) << \'\\n\';\n    std::cout << std::bitset<8>(255) << \'\\n\';\n}\n', 'The template argument sets the number of bits shown.'),
  d('cpp-o-bitset-count', 'Counting set bits', 'Using std::bitset, print how many bits are set in 255, 256 and 1023.',
    '#include <iostream>\n#include <bitset>\n\nint main() {\n    std::cout << std::bitset<16>(255).count() << \'\\n\';\n    std::cout << std::bitset<16>(256).count() << \'\\n\';\n    std::cout << std::bitset<16>(1023).count() << \'\\n\';\n}\n', 'bitset has a count() member.'),
  d('cpp-o-ternary-max3', 'Largest of three', 'With ints 14, 27 and 9, print the largest using conditional operators.',
    M('    int a{14}, b{27}, c{9};\n    int m = a > b ? a : b;\n    std::cout << (m > c ? m : c) << \'\\n\';'), 'Compare two, then the winner against the third.'),
  d('cpp-o-ternary-grade', 'Chained ternaries', 'With int score{84}, print A, B, C or F using chained conditional operators.',
    M('    int score{84};\n    std::cout << (score >= 90 ? "A" : score >= 80 ? "B" : score >= 70 ? "C" : "F") << \'\\n\';'), 'Conditional operators chain to the right.'),
  d('cpp-o-negatives', 'Negative division', 'Print -7 / 2, 7 / 2, -7 % 2 and 7 % 2.',
    M('    std::cout << -7 / 2 << \'\\n\';\n    std::cout << 7 / 2 << \'\\n\';\n    std::cout << -7 % 2 << \'\\n\';\n    std::cout << 7 % 2 << \'\\n\';'), 'C++ truncates toward zero.'),
  d('cpp-o-precedence-trap', 'The stream precedence trap', 'Print whether 8 equals 8 with proper parentheses, then print (8 & 1) and ((8 & 1) == 0).',
    M('    int a{8}, b{8};\n    std::cout << (a == b) << \'\\n\';\n    std::cout << (a & 1) << \'\\n\';\n    std::cout << ((a & 1) == 0) << \'\\n\';'), 'Comparisons inside a stream insertion always need parentheses.'),
  d('cpp-o-capstone', 'Operator summary', 'With int n{145}, print the full dozens, the leftover, whether it is odd, n squared, and its 16-bit binary form.',
    '#include <iostream>\n#include <bitset>\n\nint main() {\n    int n{145};\n    std::cout << n / 12 << \'\\n\';\n    std::cout << n % 12 << \'\\n\';\n    std::cout << std::boolalpha << (n % 2 == 1) << \'\\n\';\n    std::cout << n * n << \'\\n\';\n    std::cout << std::bitset<16>(n) << \'\\n\';\n}\n', 'Five separate expressions.'),
  {
    kind: 'quiz',
    slug: 'cpp-operators-quiz',
    title: 'Checkpoint: operators',
    questions: [
      { q: 'Why does std::cout << a == b fail to do what you expect?', options: ['It is a syntax error', '<< binds tighter than ==, so it means (std::cout << a) == b', '== is not overloaded', 'cout cannot compare'], answer: 1, explain: 'The shift operator has higher precedence than comparison, so comparisons inside an insertion need parentheses.' },
      { q: 'What does 0 < x < 10 mean in C++?', options: ['x is between 0 and 10', '(0 < x) < 10, comparing a bool with 10', 'A syntax error', 'The same as in Python'], answer: 1, explain: 'C++ has no chained comparisons; the first comparison yields a bool which is then compared with 10.' },
      { q: 'What does std::boolalpha do?', options: ['Sorts booleans', 'Makes bools print as true/false instead of 1/0', 'Converts strings to bools', 'Nothing'], answer: 1, explain: 'It is a stream manipulator that changes how boolean values are formatted from that point on.' },
      { q: 'How do you raise 2 to the power 10 in C++?', options: ['2 ** 10', '2 ^ 10', 'std::pow(2, 10)', '2 << 10'], answer: 2, explain: 'There is no exponentiation operator; ^ is bitwise xor. std::pow from <cmath> does the job.' },
      { q: 'What does x &= ~(1u << n) accomplish?', options: ['Sets bit n', 'Clears bit n', 'Toggles bit n', 'Tests bit n'], answer: 1, explain: 'Anding with the inverted mask forces bit n to zero while leaving every other bit unchanged.' },
    ],
  },
];

export const cppFlow = [
  {
    slug: 'cpp-if-else',
    title: 'Branching',
    subtitle: 'if, else and init-statements',
    concept:
      '`if` takes a condition in parentheses and a block in braces, exactly as in C. `else if` chains test in order, first match winning.\n\nC++17 added a useful refinement: an **init-statement** inside the if, declaring a variable scoped to the if and its else.\n\n`if (int remainder = n % 3; remainder == 0)`\n\nThe variable exists only where it is relevant, which keeps the surrounding scope clean \u2014 a small but genuine improvement over declaring it above.',
    example: '#include <iostream>\n\nint main() {\n    int score = 74;\n\n    if (score >= 90) {\n        std::cout << "A\\n";\n    } else if (score >= 80) {\n        std::cout << "B\\n";\n    } else if (score >= 70) {\n        std::cout << "C\\n";\n    } else {\n        std::cout << "F\\n";\n    }\n\n    if (int remainder = score % 10; remainder == 0) {\n        std::cout << "round number\\n";\n    } else {\n        std::cout << "remainder " << remainder << \'\\n\';\n    }\n}',
    note: 'remainder is visible in both branches of the if but nowhere afterwards.',
    task: 'With `int temp{5}`, print `freezing` below 0, `cold` below 10, `mild` below 25, otherwise `hot`. Then use an if with an init-statement to report whether temp is even.',
    given: 'int temp{5};',
    solution: M('    int temp{5};\n\n    if (temp < 0) {\n        std::cout << "freezing\\n";\n    } else if (temp < 10) {\n        std::cout << "cold\\n";\n    } else if (temp < 25) {\n        std::cout << "mild\\n";\n    } else {\n        std::cout << "hot\\n";\n    }\n\n    if (int r = temp % 2; r == 0) {\n        std::cout << "even\\n";\n    } else {\n        std::cout << "odd\\n";\n    }'),
    hint: 'The init-statement is separated from the condition by a semicolon.',
  },
  {
    slug: 'cpp-switch',
    title: 'Matching against many values',
    subtitle: 'switch and fall-through',
    concept:
      '`switch` compares one integral value against `case` labels. Without a `break`, execution **falls through** into the next case, which is occasionally useful for grouping and otherwise a bug.\n\nC++17 added `[[fallthrough]];` to mark deliberate fall-through, so compilers can warn about the accidental kind.\n\nAs in C, `switch` works only on integral types. For strings you need an `if` chain or a map of handlers.',
    example: '#include <iostream>\n\nint main() {\n    int day = 6;\n\n    switch (day) {\n        case 6:\n        case 7:\n            std::cout << "weekend\\n";\n            break;\n        case 1:\n            std::cout << "monday\\n";\n            break;\n        default:\n            std::cout << "weekday\\n";\n            break;\n    }\n\n    char grade = \'B\';\n    switch (grade) {\n        case \'A\': std::cout << "excellent\\n"; break;\n        case \'B\': std::cout << "good\\n"; break;\n        default:  std::cout << "other\\n"; break;\n    }\n}',
    note: 'Case 6 falls through into case 7 deliberately, grouping both weekend days.',
    task: 'With `char op{\'*\'}` and ints 6 and 7, use switch to print the result of the operation, handling +, -, * and / with a default of `unknown`.',
    given: "char op{'*'};\nint a{6}, b{7};",
    solution: M('    char op{\'*\'};\n    int a{6}, b{7};\n\n    switch (op) {\n        case \'+\': std::cout << a + b << \'\\n\'; break;\n        case \'-\': std::cout << a - b << \'\\n\'; break;\n        case \'*\': std::cout << a * b << \'\\n\'; break;\n        case \'/\': std::cout << a / b << \'\\n\'; break;\n        default:  std::cout << "unknown\\n"; break;\n    }'),
    hint: 'Each case needs its own break.',
  },
  {
    slug: 'cpp-loops',
    title: 'The three loop forms',
    subtitle: 'for, while and do-while',
    concept:
      'The counting `for` loop, the conditional `while`, and `do...while` which always runs its body once. All behave as in C.\n\nThe C++ convention is `++i` rather than `i++` in the step clause. For an int they are identical, but for a class type with an overloaded operator, postfix must construct and return a copy of the old value while prefix need not \u2014 so `++i` became the habit.\n\nDeclaring the counter inside the for scopes it to the loop.',
    example: '#include <iostream>\n\nint main() {\n    for (int i = 1; i <= 3; ++i) {\n        std::cout << i << \' \';\n    }\n    std::cout << \'\\n\';\n\n    int n = 100, steps = 0;\n    while (n > 1) {\n        n /= 2;\n        ++steps;\n    }\n    std::cout << steps << \'\\n\';\n\n    int k = 0;\n    do {\n        std::cout << "ran once\\n";\n        ++k;\n    } while (k < 1);\n}',
    note: 'The do-while body executes before the condition is ever tested.',
    task: 'Print the first five multiples of 3 one per line, then their sum. Use `++i` in the step clause.',
    solution: M('    int total = 0;\n\n    for (int i = 1; i <= 5; ++i) {\n        int value = i * 3;\n        std::cout << value << \'\\n\';\n        total += value;\n    }\n\n    std::cout << "sum: " << total << \'\\n\';'),
    hint: 'Declare the accumulator before the loop.',
  },
  {
    slug: 'cpp-range-for',
    title: 'Looping over a collection',
    subtitle: 'Range-based for',
    concept:
      'The **range-based for** walks any container directly, with no index to get wrong:\n\n`for (int value : values) { ... }`\n\nThe loop variable declaration matters. `for (auto v : items)` **copies** each element. `for (const auto& v : items)` binds a read-only reference, avoiding the copy \u2014 the right default for anything larger than a number. `for (auto& v : items)` allows modification of the elements in place.\n\nThat three-way choice is worth internalising: `const auto&` to read, `auto&` to modify, plain `auto` only for cheap types.',
    example: '#include <iostream>\n#include <vector>\n#include <string>\n\nint main() {\n    std::vector<int> nums{4, 8, 15};\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::vector<std::string> words{"alpha", "beta"};\n    for (const auto& w : words) std::cout << w << \' \';\n    std::cout << \'\\n\';\n\n    for (auto& n : nums) n *= 10;\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    for (char c : std::string{"abc"}) std::cout << c << \'.\';\n    std::cout << \'\\n\';\n}',
    note: 'The auto& loop modified nums in place; the plain-value loops only read copies.',
    task: 'Given a vector of the five values 2, 4, 6, 8, 10, print each with a range-for, then double every element in place with an `auto&` loop, then print the total using a `const auto&` loop.',
    given: 'std::vector<int> nums{2, 4, 6, 8, 10};',
    solution: '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums{2, 4, 6, 8, 10};\n\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    for (auto& n : nums) n *= 2;\n\n    int total = 0;\n    for (const auto& n : nums) total += n;\n    std::cout << total << \'\\n\';\n}\n',
    hint: 'Only the auto& form can modify the container.',
  },
  {
    slug: 'cpp-break-continue',
    title: 'Leaving early and skipping',
    subtitle: 'break, continue and nesting',
    concept:
      '`break` exits the innermost loop; `continue` skips to the next iteration. Both affect only the loop they sit in, so escaping two levels needs a flag or a restructure \u2014 often extracting the inner loop into a function and returning from it is cleanest.\n\nNested loops multiply work: an inner loop of ten inside an outer loop of ten runs its body a hundred times.\n\nIn a range-based for, `continue` simply moves to the next element.',
    example: '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums{3, 8, 12, 7, 20};\n\n    for (int n : nums) {\n        if (n % 2 != 0) continue;\n        std::cout << n << \' \';\n    }\n    std::cout << \'\\n\';\n\n    for (int n : nums) {\n        if (n > 10) {\n            std::cout << "first over ten: " << n << \'\\n\';\n            break;\n        }\n    }\n\n    for (int r = 1; r <= 3; ++r) {\n        for (int c = 1; c <= 3; ++c) std::cout << r * c << \' \';\n        std::cout << \'\\n\';\n    }\n}',
    note: 'The break stopped at 12 without examining the rest, which is the point of a search.',
    task: 'Loop from 1 to 30. Skip multiples of 3 with continue and break once the value exceeds 12, printing each surviving value. Then print a 3x4 grid of hashes.',
    solution: M('    for (int n = 1; n <= 30; ++n) {\n        if (n > 12) break;\n        if (n % 3 == 0) continue;\n        std::cout << n << \'\\n\';\n    }\n\n    for (int r = 0; r < 3; ++r) {\n        for (int c = 0; c < 4; ++c) std::cout << \'#\';\n        std::cout << \'\\n\';\n    }'),
    hint: 'Test the break condition before the continue condition.',
  },
  d('cpp-f-sign', 'Sign of a number', 'With int n{0}, print positive, negative or zero.',
    M('    int n{0};\n    if (n > 0) {\n        std::cout << "positive\\n";\n    } else if (n < 0) {\n        std::cout << "negative\\n";\n    } else {\n        std::cout << "zero\\n";\n    }'), 'Zero goes in the final else.'),
  d('cpp-f-grade', 'Letter grade', 'With int score{88}, print A/B/C/D/F using ten-point bands.',
    M('    int score{88};\n    if (score >= 90) std::cout << "A\\n";\n    else if (score >= 80) std::cout << "B\\n";\n    else if (score >= 70) std::cout << "C\\n";\n    else if (score >= 60) std::cout << "D\\n";\n    else std::cout << "F\\n";'), 'Highest threshold first.'),
  d('cpp-f-fizzbuzz', 'FizzBuzz to 20', 'Print 1 to 20 with Fizz, Buzz and FizzBuzz substitutions.',
    M('    for (int n = 1; n <= 20; ++n) {\n        if (n % 15 == 0) std::cout << "FizzBuzz\\n";\n        else if (n % 3 == 0) std::cout << "Fizz\\n";\n        else if (n % 5 == 0) std::cout << "Buzz\\n";\n        else std::cout << n << \'\\n\';\n    }'), 'Test divisibility by 15 first.'),
  d('cpp-f-init-if', 'An if with init-statement', 'With int n{17}, use an if init-statement to compute n % 5 and report whether it is zero, printing the remainder otherwise.',
    M('    int n{17};\n    if (int r = n % 5; r == 0) {\n        std::cout << "divisible\\n";\n    } else {\n        std::cout << "remainder " << r << \'\\n\';\n    }'), 'The variable is scoped to the if and its else.'),
  d('cpp-f-switch-month', 'Days in a month', 'With int month{4}, use switch to print 30, 31 or 28.',
    M('    int month{4};\n    switch (month) {\n        case 2:\n            std::cout << "28\\n";\n            break;\n        case 4:\n        case 6:\n        case 9:\n        case 11:\n            std::cout << "30\\n";\n            break;\n        default:\n            std::cout << "31\\n";\n            break;\n    }'), 'Stack the case labels for the 30-day months.'),
  d('cpp-f-switch-vowel', 'Vowel check', 'With char ch{\'e\'}, use switch to print vowel or consonant.',
    M('    char ch{\'e\'};\n    switch (ch) {\n        case \'a\': case \'e\': case \'i\': case \'o\': case \'u\':\n            std::cout << "vowel\\n";\n            break;\n        default:\n            std::cout << "consonant\\n";\n            break;\n    }'), 'Group the five vowel labels.'),
  d('cpp-f-sum100', 'Sum to one hundred', 'Print the sum of every integer from 1 to 100.',
    M('    int total = 0;\n    for (int n = 1; n <= 100; ++n) total += n;\n    std::cout << total << \'\\n\';'), 'Accumulate in a variable declared before the loop.'),
  d('cpp-f-countdown', 'Counting down', 'Print 10 down to 1 on one line separated by spaces, then `liftoff`.',
    M('    for (int n = 10; n >= 1; --n) std::cout << n << \' \';\n    std::cout << "\\nliftoff\\n";'), 'Use a decrementing step.'),
  d('cpp-f-times-table', 'Times table', 'Print the seven times table from 1 to 10 as `7 x 3 = 21`.',
    M('    for (int n = 1; n <= 10; ++n) {\n        std::cout << "7 x " << n << " = " << 7 * n << \'\\n\';\n    }'), 'Chain the insertions inside the loop.'),
  d('cpp-f-factorial', 'Factorial', 'Compute 10 factorial with a loop and print it.',
    M('    long long result = 1;\n    for (int n = 1; n <= 10; ++n) result *= n;\n    std::cout << result << \'\\n\';'), 'Use long long since the value grows fast.'),
  d('cpp-f-while-double', 'Doubling loop', 'Starting at 1, print each value while it stays below 100, doubling each time, then print the count.',
    M('    int value = 1, count = 0;\n    while (value < 100) {\n        std::cout << value << \'\\n\';\n        value *= 2;\n        ++count;\n    }\n    std::cout << "doublings: " << count << \'\\n\';'), 'Print before doubling so the initial value appears.'),
  d('cpp-f-do-while', 'A do-while loop', 'Using do-while, print the squares of 1 to 4 on separate lines.',
    M('    int n = 1;\n    do {\n        std::cout << n * n << \'\\n\';\n        ++n;\n    } while (n <= 4);'), 'The body runs before the first test.'),
  d('cpp-f-range-vector', 'Range-based for', 'Given a vector of 5, 10, 15, 20, print each element and then the total using range-based loops.',
    '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums{5, 10, 15, 20};\n    for (int n : nums) std::cout << n << \'\\n\';\n    int total = 0;\n    for (const auto& n : nums) total += n;\n    std::cout << total << \'\\n\';\n}\n', 'const auto& avoids copying each element.'),
  d('cpp-f-range-modify', 'Modifying in a range-for', 'Given a vector of 1, 2, 3, triple every element with auto& and print the result.',
    '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums{1, 2, 3};\n    for (auto& n : nums) n *= 3;\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}\n', 'Only auto& can modify the elements.'),
  d('cpp-f-range-string', 'Looping a string', 'Print each character of "range" on its own line using a range-based for.',
    '#include <iostream>\n#include <string>\n\nint main() {\n    std::string s{"range"};\n    for (char c : s) std::cout << c << \'\\n\';\n}\n', 'A std::string is iterable.'),
  d('cpp-f-nested-grid', 'A multiplication grid', 'Print a 4x4 multiplication grid with tab separators.',
    M('    for (int r = 1; r <= 4; ++r) {\n        for (int c = 1; c <= 4; ++c) std::cout << r * c << \'\\t\';\n        std::cout << \'\\n\';\n    }'), 'The row newline belongs to the outer loop.'),
  d('cpp-f-pyramid', 'A star pyramid', 'Print a five-row centred pyramid of stars where row n has 2n-1 stars.',
    '#include <iostream>\n#include <string>\n\nint main() {\n    int rows = 5;\n    for (int n = 1; n <= rows; ++n) {\n        std::cout << std::string(rows - n, \' \') << std::string(2 * n - 1, \'*\') << \'\\n\';\n    }\n}\n', 'std::string(count, char) builds a repeated string.'),
  d('cpp-f-primes', 'Primes below thirty', 'Print every prime below 30, one per line.',
    M('    for (int n = 2; n < 30; ++n) {\n        bool prime = true;\n        for (int dv = 2; dv * dv <= n; ++dv) {\n            if (n % dv == 0) { prime = false; break; }\n        }\n        if (prime) std::cout << n << \'\\n\';\n    }'), 'Testing divisors up to the square root suffices.'),
  d('cpp-f-fibonacci', 'Fibonacci numbers', 'Print the first twelve Fibonacci numbers on one line, starting 0 1.',
    M('    int a = 0, b = 1;\n    for (int i = 0; i < 12; ++i) {\n        std::cout << a << \' \';\n        int next = a + b;\n        a = b;\n        b = next;\n    }\n    std::cout << \'\\n\';'), 'A temporary holds the next value.'),
  d('cpp-f-collatz', 'Collatz steps', 'Starting at 27, count the Collatz steps to reach 1 and print the count.',
    M('    int n = 27, steps = 0;\n    while (n != 1) {\n        n = (n % 2 == 0) ? n / 2 : 3 * n + 1;\n        ++steps;\n    }\n    std::cout << steps << \'\\n\';'), 'The conditional operator fits the rule neatly.'),
  d('cpp-f-capstone', 'Control flow summary', 'In one pass over 1 to 40, count multiples of 6, sum them, then print the count, the sum and the average to two decimals.',
    '#include <iostream>\n#include <iomanip>\n\nint main() {\n    int count = 0, sum = 0;\n    for (int n = 1; n <= 40; ++n) {\n        if (n % 6 != 0) continue;\n        ++count;\n        sum += n;\n    }\n    std::cout << "count: " << count << \'\\n\';\n    std::cout << "sum: " << sum << \'\\n\';\n    std::cout << std::fixed << std::setprecision(2)\n              << "average: " << static_cast<double>(sum) / count << \'\\n\';\n}\n', 'continue skips the non-multiples cleanly.'),
  {
    kind: 'quiz',
    slug: 'cpp-flow-quiz',
    title: 'Checkpoint: control flow',
    questions: [
      { q: 'What is the difference between for (auto v : items) and for (const auto& v : items)?', options: ['None', 'The first copies each element; the second binds a read-only reference', 'The second can modify items', 'The first is safer'], answer: 1, explain: 'Copying is wasteful for anything larger than a scalar, so const auto& is the right default for reading.' },
      { q: 'Which range-for form lets you modify the elements?', options: ['auto v', 'const auto& v', 'auto& v', 'None of them'], answer: 2, explain: 'A non-const reference binds to the element itself, so assignments through it change the container.' },
      { q: 'Why does C++ convention prefer ++i over i++ in a for loop?', options: ['It is required', 'For class types, postfix must copy the old value while prefix need not', '++i is faster for ints', 'i++ is deprecated'], answer: 1, explain: 'For an int they are identical, but the habit avoids an unnecessary copy when the counter is a class type such as an iterator.' },
      { q: 'What does an if init-statement achieve?', options: ['Runs the if twice', 'Declares a variable scoped to the if and its else', 'Makes the condition constexpr', 'Nothing'], answer: 1, explain: 'It limits the variable\u2019s lifetime to where it is relevant, keeping the enclosing scope uncluttered.' },
      { q: 'Can switch test a std::string?', options: ['Yes', 'No \u2014 only integral types', 'Only with C++17', 'Only with a hash'], answer: 1, explain: 'switch requires an integral expression, so string dispatch needs an if chain or a map of handlers.' },
    ],
  },
];
