const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body, headers = ['iostream']) =>
  headers.map((h) => `#include <${h}>`).join('\n') + `\n\nint main() {\n${body}\n}\n`;
const V = (body) => M(body, ['iostream', 'vector']);
const VA = (body) => M(body, ['iostream', 'vector', 'algorithm']);

export const cppVector = [
  {
    slug: 'cpp-vector-basics',
    title: 'The array that grows',
    subtitle: 'std::vector',
    concept:
      '`std::vector` is the container to reach for by default. It is a dynamic array that knows its size, grows on demand, and **frees its own memory** when it goes out of scope \u2014 no malloc, no free, no leak possible.\n\nCore operations: `.push_back(x)` appends, `.size()` counts, `[i]` indexes, `.empty()` tests, `.clear()` empties, `.front()` and `.back()` reach the ends.\n\nCompare this with the C module on malloc: the same capability, none of the bookkeeping. That is what a container buys you.',
    example: '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums;\n    nums.push_back(10);\n    nums.push_back(20);\n    nums.push_back(30);\n\n    std::cout << nums.size() << \'\\n\';\n    std::cout << nums[0] << \' \' << nums.back() << \'\\n\';\n\n    std::vector<int> preset{4, 8, 15};\n    for (int n : preset) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::vector<int> filled(3, 7);\n    for (int n : filled) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}',
    note: 'Note the difference: {3, 7} makes a two-element vector, while (3, 7) makes three copies of 7.',
    task: 'Build a vector of the squares of 1 to 5 with push_back, then print the size, each element on its own line, and the total.',
    solution: V('    std::vector<int> squares;\n    for (int i = 1; i <= 5; ++i) squares.push_back(i * i);\n\n    std::cout << "size: " << squares.size() << \'\\n\';\n\n    int total = 0;\n    for (int s : squares) {\n        std::cout << s << \'\\n\';\n        total += s;\n    }\n\n    std::cout << "total: " << total << \'\\n\';'),
    hint: 'Fill the vector first, then print the size before the element loop.',
  },
  {
    slug: 'cpp-vector-modify',
    title: 'Changing a vector',
    subtitle: 'insert, erase, resize',
    concept:
      'Beyond `push_back`, a vector supports:\n\n- `.pop_back()` removes the last element\n- `.insert(it, value)` inserts at an iterator position\n- `.erase(it)` removes at a position; `.erase(first, last)` a range\n- `.resize(n)` changes the size, default-filling any new elements\n- `.assign(n, value)` replaces the contents\n\nInsertion and removal in the **middle** must shift every following element, so they cost time proportional to the distance from the end. Appending and removing at the back are cheap. That asymmetry is the main thing to know when choosing a container.',
    example: '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums{1, 2, 3, 4, 5};\n\n    nums.pop_back();\n    nums.insert(nums.begin(), 0);\n    nums.erase(nums.begin() + 2);\n\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    nums.resize(7);\n    std::cout << nums.size() << \' \' << nums.back() << \'\\n\';\n\n    nums.clear();\n    std::cout << std::boolalpha << nums.empty() << \'\\n\';\n}',
    note: 'resize filled the new slots with zero, since int default-initialises to 0 in a vector.',
    task: 'Start with {10, 20, 30}. Append 40, insert 5 at the front, erase the element at index 2, then print the contents on one line and the size.',
    given: 'std::vector<int> nums{10, 20, 30};',
    solution: V('    std::vector<int> nums{10, 20, 30};\n\n    nums.push_back(40);\n    nums.insert(nums.begin(), 5);\n    nums.erase(nums.begin() + 2);\n\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n    std::cout << nums.size() << \'\\n\';'),
    hint: 'insert and erase take iterators, so use nums.begin() + offset.',
  },
  {
    slug: 'cpp-iterators',
    title: 'Positions within a container',
    subtitle: 'begin, end and iterators',
    concept:
      'An **iterator** is a generalised position in a container. `.begin()` is the first element and `.end()` is **one past the last** \u2014 a deliberately invalid position that marks the boundary.\n\nThat half-open convention `[begin, end)` is used everywhere in C++. It means `end() - begin()` is exactly the size, and an empty range is simply `begin() == end()`.\n\nDereference with `*it`, advance with `++it`. Every standard algorithm takes a pair of iterators rather than a container, which is why the same `std::sort` works on vectors, arrays and deques alike.',
    example: '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<int> nums{10, 20, 30, 40};\n\n    for (auto it = nums.begin(); it != nums.end(); ++it) {\n        std::cout << *it << \' \';\n    }\n    std::cout << \'\\n\';\n\n    std::cout << *nums.begin() << \'\\n\';\n    std::cout << *(nums.end() - 1) << \'\\n\';\n    std::cout << nums.end() - nums.begin() << \'\\n\';\n\n    for (auto it = nums.rbegin(); it != nums.rend(); ++it) {\n        std::cout << *it << \' \';\n    }\n    std::cout << \'\\n\';\n}',
    note: 'rbegin and rend are reverse iterators, so incrementing them walks backwards.',
    task: 'Given a vector of 5, 10, 15, 20, print each element using an explicit iterator loop, then the first element, the last element, and the size computed as end() - begin().',
    given: 'std::vector<int> nums{5, 10, 15, 20};',
    solution: V('    std::vector<int> nums{5, 10, 15, 20};\n\n    for (auto it = nums.begin(); it != nums.end(); ++it) {\n        std::cout << *it << \' \';\n    }\n    std::cout << \'\\n\';\n\n    std::cout << *nums.begin() << \'\\n\';\n    std::cout << *(nums.end() - 1) << \'\\n\';\n    std::cout << nums.end() - nums.begin() << \'\\n\';'),
    hint: 'end() is one past the last element, so the last value is *(end() - 1).',
  },
  {
    slug: 'cpp-vector-2d',
    title: 'Vectors of vectors',
    subtitle: 'Two-dimensional data',
    concept:
      'A grid is a `std::vector<std::vector<int>>` \u2014 a vector whose elements are themselves vectors. Access with two subscripts: `grid[row][col]`.\n\nDeclare a fixed size with the fill constructor:\n\n`std::vector<std::vector<int>> grid(3, std::vector<int>(4, 0));`\n\nThat reads as "three rows, each a vector of four zeros". Unlike a C two-dimensional array, the rows need not be the same length \u2014 each is independent, which makes ragged structures possible.\n\nIterate with nested range-for loops, using `const auto&` for the rows to avoid copying them.',
    example: '#include <iostream>\n#include <vector>\n\nint main() {\n    std::vector<std::vector<int>> grid(3, std::vector<int>(3, 0));\n\n    for (std::size_t r = 0; r < grid.size(); ++r) {\n        for (std::size_t c = 0; c < grid[r].size(); ++c) {\n            grid[r][c] = static_cast<int>(r * c);\n        }\n    }\n\n    for (const auto& row : grid) {\n        for (int cell : row) std::cout << cell << \' \';\n        std::cout << \'\\n\';\n    }\n\n    std::vector<std::vector<int>> ragged{{1}, {1, 2}, {1, 2, 3}};\n    for (const auto& row : ragged) std::cout << row.size() << \' \';\n    std::cout << \'\\n\';\n}',
    note: 'The ragged example shows the rows are independent objects with their own sizes.',
    task: 'Build a 3x3 grid where each cell holds row times column, print each row on its own line, then print the total of all cells.',
    solution: V('    std::vector<std::vector<int>> grid(3, std::vector<int>(3, 0));\n    int total = 0;\n\n    for (std::size_t r = 0; r < grid.size(); ++r) {\n        for (std::size_t c = 0; c < grid[r].size(); ++c) {\n            grid[r][c] = static_cast<int>(r * c);\n            total += grid[r][c];\n        }\n    }\n\n    for (const auto& row : grid) {\n        for (int cell : row) std::cout << cell << \' \';\n        std::cout << \'\\n\';\n    }\n\n    std::cout << "total: " << total << \'\\n\';'),
    hint: 'The fill constructor takes a count and a value, and the value can itself be a vector.',
  },
  {
    slug: 'cpp-array',
    title: 'Fixed size, modern interface',
    subtitle: 'std::array',
    concept:
      '`std::array<int, 5>` is a fixed-size array with a container interface. The size is part of the type, fixed at compile time.\n\nCompared with a raw C array it gives you `.size()`, `.at()` with bounds checking, iterators, and normal copy semantics \u2014 while remaining exactly as fast, with no heap allocation.\n\nCompared with a vector it cannot resize, but avoids the allocation entirely.\n\nThe choice: `std::array` when the size is known at compile time and never changes, `std::vector` otherwise. A raw `int[]` is rarely the right answer in modern C++.',
    example: '#include <iostream>\n#include <array>\n\nint main() {\n    std::array<int, 5> nums{4, 8, 15, 16, 23};\n\n    std::cout << nums.size() << \'\\n\';\n    std::cout << nums[0] << \' \' << nums.back() << \'\\n\';\n\n    int total = 0;\n    for (int n : nums) total += n;\n    std::cout << total << \'\\n\';\n\n    std::array<int, 5> copy = nums;\n    copy[0] = 99;\n    std::cout << nums[0] << \' \' << copy[0] << \'\\n\';\n}',
    note: 'Assignment made a genuine copy, unlike a C array which cannot be assigned at all.',
    task: 'Create a `std::array<int, 4>` holding 10, 20, 30, 40. Print its size, each element on one line, the sum, and demonstrate that copying it produces an independent array.',
    solution: '#include <iostream>\n#include <array>\n\nint main() {\n    std::array<int, 4> nums{10, 20, 30, 40};\n\n    std::cout << nums.size() << \'\\n\';\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    int total = 0;\n    for (int n : nums) total += n;\n    std::cout << total << \'\\n\';\n\n    std::array<int, 4> copy = nums;\n    copy[0] = 99;\n    std::cout << nums[0] << \' \' << copy[0] << \'\\n\';\n}\n',
    hint: 'The size is a template argument, so it must be a compile-time constant.',
  },
  d('cpp-v-build', 'Building a vector', 'Push 5, 10, 15 into an empty vector and print the size and each element.',
    V('    std::vector<int> v;\n    v.push_back(5);\n    v.push_back(10);\n    v.push_back(15);\n    std::cout << v.size() << \'\\n\';\n    for (int n : v) std::cout << n << \'\\n\';'), 'push_back appends to the end.'),
  d('cpp-v-init-forms', 'Two constructors', 'Create a vector from a brace list {3, 7} and another with the fill constructor (3, 7), printing the size and contents of each.',
    V('    std::vector<int> braces{3, 7};\n    std::vector<int> fill(3, 7);\n\n    std::cout << braces.size() << \": \";\n    for (int n : braces) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::cout << fill.size() << \": \";\n    for (int n : fill) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'Braces list elements; parentheses give a count and value.'),
  d('cpp-v-front-back', 'Ends of a vector', 'For {2, 4, 6, 8}, print front(), back(), size() and whether it is empty.',
    V('    std::vector<int> v{2, 4, 6, 8};\n    std::cout << v.front() << \'\\n\';\n    std::cout << v.back() << \'\\n\';\n    std::cout << v.size() << \'\\n\';\n    std::cout << std::boolalpha << v.empty() << \'\\n\';'), 'front() and back() avoid manual indexing.'),
  d('cpp-v-sum-max', 'Aggregating a vector', 'For {45, 12, 78, 23}, print the sum, the largest and the average to two decimals.',
    '#include <iostream>\n#include <iomanip>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n\nint main() {\n    std::vector<int> v{45, 12, 78, 23};\n    int sum = std::accumulate(v.begin(), v.end(), 0);\n    std::cout << sum << \'\\n\';\n    std::cout << *std::max_element(v.begin(), v.end()) << \'\\n\';\n    std::cout << std::fixed << std::setprecision(2)\n              << static_cast<double>(sum) / v.size() << \'\\n\';\n}\n', 'accumulate comes from <numeric>.'),
  d('cpp-v-pop', 'Removing from the back', 'Start with {1, 2, 3, 4}, pop twice, and print the size and contents.',
    V('    std::vector<int> v{1, 2, 3, 4};\n    v.pop_back();\n    v.pop_back();\n    std::cout << v.size() << \'\\n\';\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'pop_back removes the last element.'),
  d('cpp-v-insert-front', 'Inserting at the front', 'Start with {2, 3}, insert 1 at the beginning, and print the contents.',
    V('    std::vector<int> v{2, 3};\n    v.insert(v.begin(), 1);\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'insert takes an iterator position.'),
  d('cpp-v-erase-middle', 'Erasing an element', 'From {10, 20, 30, 40}, erase the element at index 1 and print the result.',
    V('    std::vector<int> v{10, 20, 30, 40};\n    v.erase(v.begin() + 1);\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'begin() + 1 addresses the second element.'),
  d('cpp-v-erase-range', 'Erasing a range', 'From {1, 2, 3, 4, 5, 6}, erase the middle two elements and print the result.',
    V('    std::vector<int> v{1, 2, 3, 4, 5, 6};\n    v.erase(v.begin() + 2, v.begin() + 4);\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'The range form takes first and last iterators.'),
  d('cpp-v-resize', 'Resizing', 'Start with {1, 2}, resize to 5, print the size and contents, then resize to 1 and print again.',
    V('    std::vector<int> v{1, 2};\n    v.resize(5);\n    std::cout << v.size() << ": ";\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';\n    v.resize(1);\n    std::cout << v.size() << ": ";\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'New elements are value-initialised to zero.'),
  d('cpp-v-clear', 'Emptying a vector', 'Fill a vector with three values, print the size, clear it, and print the size and emptiness.',
    V('    std::vector<int> v{1, 2, 3};\n    std::cout << v.size() << \'\\n\';\n    v.clear();\n    std::cout << v.size() << \'\\n\';\n    std::cout << std::boolalpha << v.empty() << \'\\n\';'), 'clear removes every element.'),
  d('cpp-v-iterator-loop', 'Explicit iterator loop', 'Print the elements of {7, 14, 21} using an iterator loop rather than a range-for.',
    V('    std::vector<int> v{7, 14, 21};\n    for (auto it = v.begin(); it != v.end(); ++it) {\n        std::cout << *it << \'\\n\';\n    }'), 'Dereference the iterator with *it.'),
  d('cpp-v-reverse-iterate', 'Reverse iteration', 'Print {1, 2, 3, 4} backwards using rbegin and rend.',
    V('    std::vector<int> v{1, 2, 3, 4};\n    for (auto it = v.rbegin(); it != v.rend(); ++it) {\n        std::cout << *it << \' \';\n    }\n    std::cout << \'\\n\';'), 'Reverse iterators advance toward the front.'),
  d('cpp-v-modify-loop', 'Modifying in place', 'Triple every element of {1, 2, 3} using an auto& range-for, and print the result.',
    V('    std::vector<int> v{1, 2, 3};\n    for (auto& n : v) n *= 3;\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'Only auto& can modify the elements.'),
  d('cpp-v-strings', 'A vector of strings', 'Build a vector of three names, print each with its length, and print the count.',
    '#include <iostream>\n#include <vector>\n#include <string>\n\nint main() {\n    std::vector<std::string> names{"ada", "grace", "alan"};\n    for (const auto& n : names) {\n        std::cout << n << \' \' << n.size() << \'\\n\';\n    }\n    std::cout << names.size() << \'\\n\';\n}\n', 'Use const auto& to avoid copying each string.'),
  d('cpp-v-nested-grid', 'A 2D grid', 'Build a 2x4 grid of zeros, set each cell to row + col, and print each row.',
    V('    std::vector<std::vector<int>> grid(2, std::vector<int>(4, 0));\n    for (std::size_t r = 0; r < grid.size(); ++r) {\n        for (std::size_t c = 0; c < grid[r].size(); ++c) {\n            grid[r][c] = static_cast<int>(r + c);\n        }\n    }\n    for (const auto& row : grid) {\n        for (int cell : row) std::cout << cell << \' \';\n        std::cout << \'\\n\';\n    }'), 'The fill constructor nests naturally.'),
  d('cpp-v-ragged', 'Ragged rows', 'Build a vector of three vectors with 1, 2 and 3 elements respectively, and print each row and its size.',
    V('    std::vector<std::vector<int>> rows{{1}, {1, 2}, {1, 2, 3}};\n    for (const auto& row : rows) {\n        for (int n : row) std::cout << n << \' \';\n        std::cout << "(" << row.size() << ")\\n";\n    }'), 'Each inner vector is independent.'),
  d('cpp-v-row-sums', 'Summing rows', 'For the grid {{1,2,3},{4,5,6}}, print each row sum and then the grand total.',
    V('    std::vector<std::vector<int>> grid{{1, 2, 3}, {4, 5, 6}};\n    int grand = 0;\n    for (const auto& row : grid) {\n        int sum = 0;\n        for (int n : row) sum += n;\n        std::cout << sum << \'\\n\';\n        grand += sum;\n    }\n    std::cout << "total: " << grand << \'\\n\';'), 'Accumulate per row inside the outer loop.'),
  d('cpp-v-transpose', 'Transposing a grid', 'Transpose {{1,2,3},{4,5,6}} into a 3x2 grid and print each row.',
    V('    std::vector<std::vector<int>> src{{1, 2, 3}, {4, 5, 6}};\n    std::vector<std::vector<int>> dst(3, std::vector<int>(2, 0));\n    for (std::size_t r = 0; r < src.size(); ++r) {\n        for (std::size_t c = 0; c < src[r].size(); ++c) {\n            dst[c][r] = src[r][c];\n        }\n    }\n    for (const auto& row : dst) {\n        for (int n : row) std::cout << n << \' \';\n        std::cout << \'\\n\';\n    }'), 'Swap the indexes when assigning.'),
  d('cpp-v-array-fixed', 'A std::array', 'Create a std::array<int, 5> of 1 to 5, print its size, its contents and its sum.',
    '#include <iostream>\n#include <array>\n\nint main() {\n    std::array<int, 5> a{1, 2, 3, 4, 5};\n    std::cout << a.size() << \'\\n\';\n    for (int n : a) std::cout << n << \' \';\n    std::cout << \'\\n\';\n    int sum = 0;\n    for (int n : a) sum += n;\n    std::cout << sum << \'\\n\';\n}\n', 'The size is part of the type.'),
  d('cpp-v-array-copy', 'Copying a std::array', 'Copy a std::array, modify the copy, and print both first elements to show independence.',
    '#include <iostream>\n#include <array>\n\nint main() {\n    std::array<int, 3> a{1, 2, 3};\n    std::array<int, 3> b = a;\n    b[0] = 99;\n    std::cout << a[0] << \'\\n\';\n    std::cout << b[0] << \'\\n\';\n}\n', 'std::array has normal copy semantics, unlike a raw array.'),
  d('cpp-v-capstone', 'Vector summary', 'From {45, 12, 78, 23, 91, 5}, print the size, the sum, the average to two decimals, the largest, the smallest, and the elements above the average.',
    '#include <iostream>\n#include <iomanip>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n\nint main() {\n    std::vector<int> v{45, 12, 78, 23, 91, 5};\n\n    int sum = std::accumulate(v.begin(), v.end(), 0);\n    double avg = static_cast<double>(sum) / v.size();\n\n    std::cout << "size: " << v.size() << \'\\n\';\n    std::cout << "sum: " << sum << \'\\n\';\n    std::cout << std::fixed << std::setprecision(2) << "average: " << avg << \'\\n\';\n    std::cout << "max: " << *std::max_element(v.begin(), v.end()) << \'\\n\';\n    std::cout << "min: " << *std::min_element(v.begin(), v.end()) << \'\\n\';\n\n    for (int n : v) {\n        if (n > avg) std::cout << n << \' \';\n    }\n    std::cout << \'\\n\';\n}\n', 'Compute the average once and reuse it.'),
  {
    kind: 'quiz',
    slug: 'cpp-vector-quiz',
    title: 'Checkpoint: vectors',
    questions: [
      { q: 'What is the main advantage of std::vector over a malloc\u2019d array?', options: ['It is always faster', 'It knows its size and frees its own memory', 'It can hold mixed types', 'It never uses the heap'], answer: 1, explain: 'The vector tracks its size and releases its buffer in its destructor, so nothing can leak.' },
      { q: 'What does v.end() point to?', options: ['The last element', 'One position past the last element', 'The first element', 'Null'], answer: 1, explain: 'The half-open convention means end() is a boundary marker, which is why loops test it != end().' },
      { q: 'What is the difference between vector<int> v{3, 7} and vector<int> v(3, 7)?', options: ['None', 'Braces give two elements; parentheses give three copies of 7', 'Parentheses give two elements', 'The second is invalid'], answer: 1, explain: 'A brace list enumerates the elements, while the parenthesised form is the count-and-value fill constructor.' },
      { q: 'Why is inserting in the middle of a vector expensive?', options: ['It reallocates every time', 'Every following element must shift along', 'It sorts the vector', 'It is not expensive'], answer: 1, explain: 'Elements are contiguous, so making room in the middle requires moving everything after the insertion point.' },
      { q: 'When should you prefer std::array over std::vector?', options: ['Always', 'When the size is known at compile time and never changes', 'When you need to resize', 'For strings only'], answer: 1, explain: 'std::array avoids the heap allocation entirely, but its size is part of its type and cannot change.' },
    ],
  },
];

export const cppContainers = [
  {
    slug: 'cpp-map-basics',
    title: 'Key to value',
    subtitle: 'std::map',
    concept:
      '`std::map` stores sorted key/value pairs. Where a vector is indexed by position, a map is indexed by whatever key type you choose.\n\nThe behaviour to know: indexing with a key that does not exist **creates** it with a default value \u2014 zero for numbers, empty for strings. That makes counting a one-liner: `counts[word]++`.\n\nIt also means `if (m["key"] == 0)` silently inserts. Use `.count(key)` or `.find(key)` to test for presence without modifying, and `.at(key)` to read with an exception on failure.\n\nIterating a map always yields keys in **sorted** order.',
    example: '#include <iostream>\n#include <map>\n#include <string>\n\nint main() {\n    std::map<std::string, int> stock;\n    stock["apple"] = 4;\n    stock["cherry"] = 30;\n    stock["banana"] = 12;\n\n    std::cout << stock["apple"] << \'\\n\';\n    std::cout << stock.size() << \'\\n\';\n\n    for (const auto& [name, count] : stock) {\n        std::cout << name << ": " << count << \'\\n\';\n    }\n\n    std::cout << stock.count("pear") << \'\\n\';\n    std::cout << (stock.find("apple") != stock.end()) << \'\\n\';\n}',
    note: 'The output is apple, banana, cherry \u2014 sorted by key, not by insertion order.',
    task: 'Build a map of three capital cities (France/Paris, Japan/Tokyo, Peru/Lima). Print the size, the capital of Japan, then every entry as `France: Paris` in sorted order.',
    solution: '#include <iostream>\n#include <map>\n#include <string>\n\nint main() {\n    std::map<std::string, std::string> capitals;\n    capitals["France"] = "Paris";\n    capitals["Japan"] = "Tokyo";\n    capitals["Peru"] = "Lima";\n\n    std::cout << capitals.size() << \'\\n\';\n    std::cout << capitals["Japan"] << \'\\n\';\n\n    for (const auto& [country, city] : capitals) {\n        std::cout << country << ": " << city << \'\\n\';\n    }\n}\n',
    hint: 'Structured bindings unpack each pair into two names.',
  },
  {
    slug: 'cpp-map-counting',
    title: 'Counting with a map',
    subtitle: 'The counting idiom',
    concept:
      'Counting is the most common use of a map, and C++ makes it a single expression:\n\n`counts[key]++`\n\nBecause `operator[]` default-constructs the value when the key is absent, an int starts at 0 and the increment leaves it at 1. No special case for the first occurrence.\n\nThis generalises: `totals[key] += amount` accumulates, and `groups[key].push_back(item)` builds a map of vectors, since the vector is default-constructed empty.',
    example: '#include <iostream>\n#include <map>\n#include <string>\n#include <sstream>\n#include <vector>\n\nint main() {\n    std::istringstream in{"the fox the dog the cat"};\n    std::map<std::string, int> counts;\n    std::string word;\n\n    while (in >> word) counts[word]++;\n\n    for (const auto& [w, n] : counts) {\n        std::cout << w << ": " << n << \'\\n\';\n    }\n\n    std::map<std::size_t, std::vector<std::string>> by_length;\n    for (const auto& [w, n] : counts) by_length[w.size()].push_back(w);\n\n    for (const auto& [len, words] : by_length) {\n        std::cout << len << ": " << words.size() << \'\\n\';\n    }\n}',
    note: 'by_length[w.size()] created an empty vector on first use, so push_back always works.',
    task: 'Count the characters in `"mississippi"` with a map and print each as `i: 4` in sorted order, then print the number of distinct characters.',
    solution: '#include <iostream>\n#include <map>\n#include <string>\n\nint main() {\n    std::string text{"mississippi"};\n    std::map<char, int> counts;\n\n    for (char c : text) counts[c]++;\n\n    for (const auto& [ch, n] : counts) {\n        std::cout << ch << ": " << n << \'\\n\';\n    }\n\n    std::cout << "distinct: " << counts.size() << \'\\n\';\n}\n',
    hint: 'counts[c]++ handles the first occurrence automatically.',
  },
  {
    slug: 'cpp-set',
    title: 'Unique sorted values',
    subtitle: 'std::set',
    concept:
      '`std::set` stores unique values in sorted order. Inserting a duplicate is silently ignored, which makes it the natural deduplicator.\n\n`.insert(x)` adds, `.count(x)` tests membership, `.erase(x)` removes, and iteration yields sorted values.\n\n`.insert` returns a pair whose `.second` is a bool saying whether the insertion actually happened \u2014 useful when you need to know if a value was new.\n\nUse a set when you need uniqueness or ordered membership testing; use `std::unordered_set` when you need neither ordering and want faster lookups.',
    example: '#include <iostream>\n#include <set>\n#include <vector>\n\nint main() {\n    std::vector<int> raw{3, 1, 4, 1, 5, 9, 2, 6, 5, 3};\n    std::set<int> unique(raw.begin(), raw.end());\n\n    std::cout << raw.size() << " -> " << unique.size() << \'\\n\';\n    for (int n : unique) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    auto result = unique.insert(100);\n    std::cout << std::boolalpha << result.second << \'\\n\';\n    result = unique.insert(100);\n    std::cout << result.second << \'\\n\';\n\n    std::cout << unique.count(4) << \' \' << unique.count(42) << \'\\n\';\n}',
    note: 'The second insert returned false because 100 was already present.',
    task: 'From the vector {5, 3, 8, 3, 1, 5, 8}, build a set, print the original and unique sizes, the sorted unique values on one line, and whether 8 and 99 are present.',
    given: 'std::vector<int> raw{5, 3, 8, 3, 1, 5, 8};',
    solution: '#include <iostream>\n#include <set>\n#include <vector>\n\nint main() {\n    std::vector<int> raw{5, 3, 8, 3, 1, 5, 8};\n    std::set<int> unique(raw.begin(), raw.end());\n\n    std::cout << raw.size() << \'\\n\';\n    std::cout << unique.size() << \'\\n\';\n    for (int n : unique) std::cout << n << \' \';\n    std::cout << \'\\n\';\n    std::cout << std::boolalpha << (unique.count(8) > 0) << \'\\n\';\n    std::cout << (unique.count(99) > 0) << \'\\n\';\n}\n',
    hint: 'A set can be constructed from a pair of iterators.',
  },
  {
    slug: 'cpp-pair-tuple',
    title: 'Bundling two or more values',
    subtitle: 'std::pair and std::tuple',
    concept:
      '`std::pair<A, B>` holds exactly two values, accessed as `.first` and `.second`. It is what a map element is, and it is the simplest way for a function to return two things.\n\n`std::tuple` generalises to any number, accessed with `std::get<0>(t)`.\n\nSince C++17, **structured bindings** unpack either into named variables:\n\n`auto [name, score] = get_result();`\n\nThat is far more readable than `.first` and `.second`, and it is why the map iteration in these lessons reads so cleanly. For anything with meaning beyond "two values", a small struct with named members is still better.',
    example: '#include <iostream>\n#include <utility>\n#include <tuple>\n#include <string>\n#include <vector>\n\nstd::pair<int, int> min_max(const std::vector<int>& v) {\n    int lo = v[0], hi = v[0];\n    for (int n : v) {\n        if (n < lo) lo = n;\n        if (n > hi) hi = n;\n    }\n    return {lo, hi};\n}\n\nint main() {\n    auto p = std::make_pair(std::string{"ada"}, 91);\n    std::cout << p.first << \' \' << p.second << \'\\n\';\n\n    auto [lo, hi] = min_max({4, 9, 1, 7});\n    std::cout << lo << \' \' << hi << \'\\n\';\n\n    std::tuple<std::string, int, double> row{"widget", 7, 12.5};\n    std::cout << std::get<0>(row) << \' \' << std::get<1>(row) << \'\\n\';\n\n    auto [name, qty, price] = row;\n    std::cout << name << \' \' << qty << \' \' << price << \'\\n\';\n}',
    note: 'return {lo, hi}; constructs the pair implicitly \u2014 no need to name the type again.',
    task: 'Write a function returning a `std::pair<int, int>` of the sum and count of a vector. Call it on {4, 8, 15, 16} and unpack the result with structured bindings, printing both plus the average to two decimals.',
    solution: '#include <iostream>\n#include <iomanip>\n#include <utility>\n#include <vector>\n\nstd::pair<int, int> sum_and_count(const std::vector<int>& v) {\n    int sum = 0;\n    for (int n : v) sum += n;\n    return {sum, static_cast<int>(v.size())};\n}\n\nint main() {\n    auto [sum, count] = sum_and_count({4, 8, 15, 16});\n\n    std::cout << sum << \'\\n\';\n    std::cout << count << \'\\n\';\n    std::cout << std::fixed << std::setprecision(2)\n              << static_cast<double>(sum) / count << \'\\n\';\n}\n',
    hint: 'Structured bindings need auto and square brackets.',
  },
  {
    slug: 'cpp-unordered',
    title: 'Hashing instead of sorting',
    subtitle: 'unordered_map and unordered_set',
    concept:
      '`std::unordered_map` and `std::unordered_set` are hash-based. They provide the same interface as their ordered siblings, with two differences: lookups are faster on average, and iteration order is **unspecified**.\n\nThe trade is exactly that. Choose `map` when you need sorted iteration or ordered queries; choose `unordered_map` when you only need fast key lookups.\n\nBecause the order is unpredictable, any program that must produce deterministic output has to collect the entries and sort them before printing \u2014 which is worth remembering when comparing the two.',
    example: '#include <iostream>\n#include <unordered_map>\n#include <map>\n#include <string>\n#include <vector>\n#include <algorithm>\n\nint main() {\n    std::unordered_map<std::string, int> fast;\n    fast["ada"] = 91;\n    fast["bob"] = 78;\n    fast["cy"] = 95;\n\n    std::cout << fast["ada"] << \'\\n\';\n    std::cout << fast.size() << \'\\n\';\n    std::cout << fast.count("bob") << \'\\n\';\n\n    std::vector<std::pair<std::string, int>> entries(fast.begin(), fast.end());\n    std::sort(entries.begin(), entries.end());\n    for (const auto& [name, score] : entries) {\n        std::cout << name << ": " << score << \'\\n\';\n    }\n}',
    note: 'The entries had to be copied into a vector and sorted to guarantee a predictable printing order.',
    task: 'Count the words in `"a b a c b a"` using an unordered_map, then copy the entries into a vector, sort them, and print each as `a: 3` along with the distinct count.',
    solution: '#include <iostream>\n#include <unordered_map>\n#include <string>\n#include <sstream>\n#include <vector>\n#include <algorithm>\n\nint main() {\n    std::istringstream in{"a b a c b a"};\n    std::unordered_map<std::string, int> counts;\n    std::string word;\n    while (in >> word) counts[word]++;\n\n    std::vector<std::pair<std::string, int>> entries(counts.begin(), counts.end());\n    std::sort(entries.begin(), entries.end());\n\n    for (const auto& [w, n] : entries) {\n        std::cout << w << ": " << n << \'\\n\';\n    }\n    std::cout << "distinct: " << counts.size() << \'\\n\';\n}\n',
    hint: 'Sorting a vector of pairs orders by the first element, then the second.',
  },
  d('cpp-c-map-build', 'A simple map', 'Build a map of three name/score pairs, print the size and each entry in sorted order.',
    '#include <iostream>\n#include <map>\n#include <string>\n\nint main() {\n    std::map<std::string, int> scores;\n    scores["bob"] = 78;\n    scores["ada"] = 91;\n    scores["cy"] = 95;\n    std::cout << scores.size() << \'\\n\';\n    for (const auto& [k, v] : scores) std::cout << k << ": " << v << \'\\n\';\n}\n', 'Maps iterate in sorted key order.'),
  d('cpp-c-map-lookup', 'Looking up keys', 'For a map of two entries, print one value with [], test a missing key with count, and print the size.',
    '#include <iostream>\n#include <map>\n#include <string>\n\nint main() {\n    std::map<std::string, int> m{{"a", 1}, {"b", 2}};\n    std::cout << m["a"] << \'\\n\';\n    std::cout << m.count("z") << \'\\n\';\n    std::cout << m.size() << \'\\n\';\n}\n', 'count returns 0 or 1 for a map.'),
  d('cpp-c-map-at', 'Safe access with at()', 'Use .at() to read an existing key, then catch the exception from reading a missing one.',
    '#include <iostream>\n#include <map>\n#include <string>\n\nint main() {\n    std::map<std::string, int> m{{"a", 1}};\n    std::cout << m.at("a") << \'\\n\';\n    try {\n        std::cout << m.at("z") << \'\\n\';\n    } catch (const std::out_of_range&) {\n        std::cout << "missing\\n";\n    }\n}\n', 'at() throws rather than inserting.'),
  d('cpp-c-map-count-chars', 'Character frequencies', 'Count the characters in "abracadabra" and print each in sorted order.',
    '#include <iostream>\n#include <map>\n#include <string>\n\nint main() {\n    std::map<char, int> counts;\n    for (char c : std::string{"abracadabra"}) counts[c]++;\n    for (const auto& [ch, n] : counts) std::cout << ch << ": " << n << \'\\n\';\n}\n', 'counts[c]++ works even for a new key.'),
  d('cpp-c-map-accumulate', 'Accumulating into a map', 'Given pairs of category and amount, total each category into a map and print the results.',
    '#include <iostream>\n#include <map>\n#include <string>\n#include <vector>\n#include <utility>\n\nint main() {\n    std::vector<std::pair<std::string, int>> rows{\n        {"food", 20}, {"tools", 15}, {"food", 30}, {"tools", 5}\n    };\n    std::map<std::string, int> totals;\n    for (const auto& [cat, amount] : rows) totals[cat] += amount;\n    for (const auto& [cat, total] : totals) std::cout << cat << ": " << total << \'\\n\';\n}\n', 'The += works because the value starts at zero.'),
  d('cpp-c-map-group', 'Grouping into vectors', 'Group the words {"ant","bee","ape","bat"} by first letter into a map of vectors and print each group.',
    '#include <iostream>\n#include <map>\n#include <string>\n#include <vector>\n\nint main() {\n    std::map<char, std::vector<std::string>> groups;\n    for (const std::string& w : {"ant", "bee", "ape", "bat"}) {\n        groups[w[0]].push_back(w);\n    }\n    for (const auto& [letter, words] : groups) {\n        std::cout << letter << ": ";\n        for (const auto& w : words) std::cout << w << \' \';\n        std::cout << \'\\n\';\n    }\n}\n', 'The vector is default-constructed empty on first access.'),
  d('cpp-c-map-erase', 'Removing entries', 'From a three-entry map, erase one key and print the remaining entries and size.',
    '#include <iostream>\n#include <map>\n#include <string>\n\nint main() {\n    std::map<std::string, int> m{{"a", 1}, {"b", 2}, {"c", 3}};\n    m.erase("b");\n    for (const auto& [k, v] : m) std::cout << k << ": " << v << \'\\n\';\n    std::cout << m.size() << \'\\n\';\n}\n', 'erase accepts the key directly.'),
  d('cpp-c-map-sort-values', 'Ranking by value', 'From a map of three scores, copy to a vector and print the entries sorted by score descending.',
    '#include <iostream>\n#include <map>\n#include <string>\n#include <vector>\n#include <utility>\n#include <algorithm>\n\nint main() {\n    std::map<std::string, int> scores{{"ada", 91}, {"bob", 78}, {"cy", 95}};\n    std::vector<std::pair<std::string, int>> v(scores.begin(), scores.end());\n    std::sort(v.begin(), v.end(), [](const auto& a, const auto& b) { return a.second > b.second; });\n    for (const auto& [name, score] : v) std::cout << name << ": " << score << \'\\n\';\n}\n', 'A map cannot be sorted by value, so copy it out first.'),
  d('cpp-c-set-dedupe', 'Deduplicating', 'From {4, 2, 7, 2, 4, 9}, build a set and print the unique count and the sorted values.',
    '#include <iostream>\n#include <set>\n#include <vector>\n\nint main() {\n    std::vector<int> raw{4, 2, 7, 2, 4, 9};\n    std::set<int> s(raw.begin(), raw.end());\n    std::cout << s.size() << \'\\n\';\n    for (int n : s) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}\n', 'A set discards duplicates automatically.'),
  d('cpp-c-set-insert-result', 'Was it new?', 'Insert 5 into a set twice, printing whether each insertion actually happened.',
    '#include <iostream>\n#include <set>\n\nint main() {\n    std::set<int> s;\n    std::cout << std::boolalpha;\n    std::cout << s.insert(5).second << \'\\n\';\n    std::cout << s.insert(5).second << \'\\n\';\n}\n', 'insert returns a pair whose second member reports success.'),
  d('cpp-c-set-membership', 'Set membership', 'For the set {1, 3, 5, 7}, print whether 5 and 4 are members.',
    '#include <iostream>\n#include <set>\n\nint main() {\n    std::set<int> s{1, 3, 5, 7};\n    std::cout << std::boolalpha;\n    std::cout << (s.count(5) > 0) << \'\\n\';\n    std::cout << (s.count(4) > 0) << \'\\n\';\n}\n', 'count returns 0 or 1 for a set.'),
  d('cpp-c-set-intersection', 'Common elements', 'Find the sorted common elements of {1,2,3,4} and {3,4,5} using std::set_intersection.',
    '#include <iostream>\n#include <set>\n#include <vector>\n#include <algorithm>\n#include <iterator>\n\nint main() {\n    std::set<int> a{1, 2, 3, 4}, b{3, 4, 5};\n    std::vector<int> common;\n    std::set_intersection(a.begin(), a.end(), b.begin(), b.end(), std::back_inserter(common));\n    for (int n : common) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}\n', 'back_inserter appends to the destination vector.'),
  d('cpp-c-set-difference', 'Elements in one only', 'Print the sorted elements of {1,2,3,4} not present in {3,4,5}.',
    '#include <iostream>\n#include <set>\n#include <vector>\n#include <algorithm>\n#include <iterator>\n\nint main() {\n    std::set<int> a{1, 2, 3, 4}, b{3, 4, 5};\n    std::vector<int> only;\n    std::set_difference(a.begin(), a.end(), b.begin(), b.end(), std::back_inserter(only));\n    for (int n : only) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}\n', 'set_difference needs both ranges sorted, which sets guarantee.'),
  d('cpp-c-set-strings', 'A set of strings', 'Build a set from {"pear","apple","pear","fig"} and print the size and sorted contents.',
    '#include <iostream>\n#include <set>\n#include <string>\n\nint main() {\n    std::set<std::string> s{"pear", "apple", "pear", "fig"};\n    std::cout << s.size() << \'\\n\';\n    for (const auto& w : s) std::cout << w << \'\\n\';\n}\n', 'Strings sort alphabetically.'),
  d('cpp-c-pair-basic', 'Using a pair', 'Create a pair of a string and an int, print both members, then unpack it with structured bindings.',
    '#include <iostream>\n#include <utility>\n#include <string>\n\nint main() {\n    std::pair<std::string, int> p{"ada", 91};\n    std::cout << p.first << \' \' << p.second << \'\\n\';\n    auto [name, score] = p;\n    std::cout << name << \' \' << score << \'\\n\';\n}\n', 'Structured bindings avoid .first and .second.'),
  d('cpp-c-pair-return', 'Returning a pair', 'Write a function returning a pair of the quotient and remainder of 47 and 6, and unpack the result.',
    '#include <iostream>\n#include <utility>\n\nstd::pair<int, int> divide(int a, int b) {\n    return {a / b, a % b};\n}\n\nint main() {\n    auto [q, r] = divide(47, 6);\n    std::cout << q << \'\\n\';\n    std::cout << r << \'\\n\';\n}\n', 'Return a brace list to build the pair implicitly.'),
  d('cpp-c-pair-vector', 'A vector of pairs', 'Build a vector of three name/score pairs, sort it by score descending, and print each.',
    '#include <iostream>\n#include <vector>\n#include <utility>\n#include <string>\n#include <algorithm>\n\nint main() {\n    std::vector<std::pair<std::string, int>> v{{"bob", 78}, {"ada", 91}, {"cy", 95}};\n    std::sort(v.begin(), v.end(), [](const auto& a, const auto& b) { return a.second > b.second; });\n    for (const auto& [name, score] : v) std::cout << name << \' \' << score << \'\\n\';\n}\n', 'The lambda compares the second members.'),
  d('cpp-c-tuple', 'Using a tuple', 'Create a tuple of a string, an int and a double, print via std::get, then unpack with structured bindings.',
    '#include <iostream>\n#include <tuple>\n#include <string>\n\nint main() {\n    std::tuple<std::string, int, double> row{"widget", 7, 12.5};\n    std::cout << std::get<0>(row) << \'\\n\';\n    std::cout << std::get<1>(row) << \'\\n\';\n    auto [name, qty, price] = row;\n    std::cout << name << \' \' << qty << \' \' << price << \'\\n\';\n}\n', 'std::get takes the index as a template argument.'),
  d('cpp-c-unordered-map', 'A hash map', 'Build an unordered_map of three entries, print the size, one lookup, and a membership test.',
    '#include <iostream>\n#include <unordered_map>\n#include <string>\n\nint main() {\n    std::unordered_map<std::string, int> m{{"a", 1}, {"b", 2}, {"c", 3}};\n    std::cout << m.size() << \'\\n\';\n    std::cout << m["b"] << \'\\n\';\n    std::cout << m.count("z") << \'\\n\';\n}\n', 'The interface matches std::map apart from ordering.'),
  d('cpp-c-unordered-sorted-output', 'Deterministic output from a hash map', 'Count characters in "hello" with an unordered_map, then sort the entries into a vector before printing.',
    '#include <iostream>\n#include <unordered_map>\n#include <vector>\n#include <utility>\n#include <string>\n#include <algorithm>\n\nint main() {\n    std::unordered_map<char, int> counts;\n    for (char c : std::string{"hello"}) counts[c]++;\n    std::vector<std::pair<char, int>> v(counts.begin(), counts.end());\n    std::sort(v.begin(), v.end());\n    for (const auto& [ch, n] : v) std::cout << ch << ": " << n << \'\\n\';\n}\n', 'Hash order is unspecified, so sort before printing.'),
  d('cpp-c-capstone', 'Container summary', 'From the sentence "the cat and the hat and the bat", print the word count, the distinct count, each word with its tally in sorted order, and the most frequent word.',
    '#include <iostream>\n#include <map>\n#include <string>\n#include <sstream>\n#include <vector>\n#include <utility>\n#include <algorithm>\n\nint main() {\n    std::istringstream in{"the cat and the hat and the bat"};\n    std::map<std::string, int> counts;\n    std::string w;\n    int total = 0;\n\n    while (in >> w) {\n        counts[w]++;\n        ++total;\n    }\n\n    std::cout << "words: " << total << \'\\n\';\n    std::cout << "distinct: " << counts.size() << \'\\n\';\n    for (const auto& [word, n] : counts) std::cout << word << ": " << n << \'\\n\';\n\n    auto best = std::max_element(counts.begin(), counts.end(),\n        [](const auto& a, const auto& b) { return a.second < b.second; });\n    std::cout << "most: " << best->first << \'\\n\';\n}\n', 'max_element with a comparator finds the entry with the largest value.'),
  {
    kind: 'quiz',
    slug: 'cpp-containers-quiz',
    title: 'Checkpoint: maps and sets',
    questions: [
      { q: 'What does counts["new"]++ do on a map with no such key?', options: ['Throws', 'Does nothing', 'Inserts the key with value 0, then increments to 1', 'Inserts with value 1, then increments to 2'], answer: 2, explain: 'operator[] default-constructs the value, which is 0 for an int, so the increment leaves it at 1.' },
      { q: 'Why can reading m["key"] be surprising?', options: ['It is slow', 'It inserts the key if it is absent', 'It throws', 'It returns null'], answer: 1, explain: 'operator[] is not a read-only operation. Use .count(), .find() or .at() to inspect without inserting.' },
      { q: 'In what order does a std::map iterate?', options: ['Insertion order', 'Sorted by key', 'Random', 'Reverse insertion order'], answer: 1, explain: 'A map keeps its keys ordered, which is exactly the property unordered_map gives up for speed.' },
      { q: 'What happens when you insert a duplicate into a std::set?', options: ['It throws', 'It is silently ignored and insert returns false', 'It creates a second copy', 'The set clears'], answer: 1, explain: 'Sets hold unique values; the returned pair\u2019s second member tells you whether the insertion happened.' },
      { q: 'What do structured bindings let you write instead of .first and .second?', options: ['auto p = pair', 'auto [a, b] = pair', 'get<0>(pair)', 'pair.a'], answer: 1, explain: 'Structured bindings unpack a pair or tuple into named variables, which is why map iteration reads so cleanly.' },
    ],
  },
];
