const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body, headers = ['iostream']) =>
  headers.map((h) => `#include <${h}>`).join('\n') + `\n\nint main() {\n${body}\n}\n`;
const V = (body) => M(body, ['iostream', 'vector']);
const St = (body) => M(body, ['iostream', 'string']);

export const cppFunctions = [
  {
    slug: 'cpp-function-basics',
    title: 'Defining functions',
    subtitle: 'Parameters, returns and declarations',
    concept:
      'A C++ function declares its return type, name and parameter types, exactly as in C. `void` means it returns nothing; empty parentheses mean it takes nothing.\n\nAs in C, a function must be declared before use. A **declaration** (prototype) ends with a semicolon and lets you define the function later, which is how you put `main` at the top.\n\nParameters are passed by value by default, so the function receives copies \u2014 unless you use a reference.',
    example: '#include <iostream>\n\nint square(int n);\nvoid banner();\n\nint main() {\n    banner();\n    std::cout << square(7) << \'\\n\';\n    std::cout << square(3) + square(4) << \'\\n\';\n    banner();\n}\n\nint square(int n) {\n    return n * n;\n}\n\nvoid banner() {\n    std::cout << "=====\\n";\n}',
    note: 'Because square returns a value, results can be combined arithmetically.',
    task: 'Declare `int max_of(int, int)` above main, define it below, and print max_of(17, 42) and max_of(7, 3).',
    solution: '#include <iostream>\n\nint max_of(int a, int b);\n\nint main() {\n    std::cout << max_of(17, 42) << \'\\n\';\n    std::cout << max_of(7, 3) << \'\\n\';\n}\n\nint max_of(int a, int b) {\n    return a > b ? a : b;\n}\n',
    hint: 'The declaration ends with a semicolon; the definition follows main.',
  },
  {
    slug: 'cpp-overloading',
    title: 'One name, several versions',
    subtitle: 'Function overloading',
    concept:
      'C++ allows several functions to share a name as long as their **parameter lists differ**. The compiler picks the match from the argument types \u2014 this is **overload resolution**, decided entirely at compile time.\n\nThe return type alone is not enough to distinguish overloads, because the compiler cannot tell which you wanted from the call site.\n\nOverloading is why `std::cout <<` works for every type, and why `std::max` accepts ints or doubles. Use it when the operations are conceptually the same; different names are clearer when the behaviour differs.',
    example: '#include <iostream>\n#include <string>\n\nint area(int side) {\n    return side * side;\n}\n\nint area(int width, int height) {\n    return width * height;\n}\n\ndouble area(double radius) {\n    return 3.14159 * radius * radius;\n}\n\nvoid show(int n) { std::cout << "int " << n << \'\\n\'; }\nvoid show(const std::string& s) { std::cout << "string " << s << \'\\n\'; }\n\nint main() {\n    std::cout << area(4) << \'\\n\';\n    std::cout << area(3, 5) << \'\\n\';\n    std::cout << area(2.0) << \'\\n\';\n    show(42);\n    show("text");\n}',
    note: 'area(4) and area(2.0) call different functions, chosen by the argument type alone.',
    task: 'Overload `describe` for an int, a double and a std::string, each printing a distinguishing line, then call all three.',
    solution: '#include <iostream>\n#include <string>\n\nvoid describe(int n) {\n    std::cout << "integer: " << n << \'\\n\';\n}\n\nvoid describe(double d) {\n    std::cout << "double: " << d << \'\\n\';\n}\n\nvoid describe(const std::string& s) {\n    std::cout << "string: " << s << \'\\n\';\n}\n\nint main() {\n    describe(42);\n    describe(2.5);\n    describe(std::string{"hello"});\n}\n',
    hint: 'Wrap the literal in std::string so the string overload is selected.',
  },
  {
    slug: 'cpp-default-args',
    title: 'Optional parameters',
    subtitle: 'Default arguments',
    concept:
      'A parameter may have a **default value**, making it optional at the call site. Defaults must be the trailing parameters \u2014 you cannot skip one in the middle.\n\nThe default goes in the **declaration**, not the definition, when both exist. Repeating it in both is an error.\n\nDefaults often replace a family of overloads: one function with two defaults covers what three overloads would. Where they differ, an overload can change behaviour entirely while a default only supplies a value.',
    example: '#include <iostream>\n#include <string>\n\nstd::string greet(const std::string& name,\n                  const std::string& greeting = "Hello",\n                  char punct = \'!\') {\n    return greeting + ", " + name + punct;\n}\n\nint main() {\n    std::cout << greet("Ada") << \'\\n\';\n    std::cout << greet("Bob", "Hi") << \'\\n\';\n    std::cout << greet("Cy", "Welcome", \'.\') << \'\\n\';\n}',
    note: 'You may omit any trailing arguments, but not one from the middle.',
    task: 'Write `int scaled(int value, int factor = 2, int offset = 0)` returning value * factor + offset, and call it with one, two and three arguments.',
    solution: '#include <iostream>\n\nint scaled(int value, int factor = 2, int offset = 0) {\n    return value * factor + offset;\n}\n\nint main() {\n    std::cout << scaled(10) << \'\\n\';\n    std::cout << scaled(10, 3) << \'\\n\';\n    std::cout << scaled(10, 3, 5) << \'\\n\';\n}\n',
    hint: 'Defaults must come after all required parameters.',
  },
  {
    slug: 'cpp-pass-modes',
    title: 'How to pass an argument',
    subtitle: 'Value, reference and const reference',
    concept:
      'Three ways to pass a parameter, and choosing correctly matters for both correctness and speed.\n\n**By value** (`int n`) copies. Right for small cheap types: int, double, char, pointers.\n\n**By reference** (`int& n`) aliases the caller\u2019s object, so the function can modify it. Use when modification is the point.\n\n**By const reference** (`const std::string& s`) avoids the copy and forbids modification. This is the default for any type larger than a machine word \u2014 strings, vectors, structs.\n\nThe guideline: cheap types by value, everything else by `const&` unless you must modify, in which case plain `&`.',
    example: '#include <iostream>\n#include <string>\n#include <vector>\n\nvoid by_value(int n) { n = 999; }\nvoid by_ref(int& n) { n = 999; }\nstd::size_t by_const_ref(const std::vector<int>& v) { return v.size(); }\n\nint main() {\n    int a = 1;\n    by_value(a);\n    std::cout << a << \'\\n\';\n\n    int b = 1;\n    by_ref(b);\n    std::cout << b << \'\\n\';\n\n    std::vector<int> big(1000, 7);\n    std::cout << by_const_ref(big) << \'\\n\';\n}',
    note: 'by_const_ref inspected a thousand-element vector without copying a single one of them.',
    task: 'Write `void bump(int& n)` adding one, `int doubled(int n)` returning twice its argument, and `std::size_t length(const std::string& s)`. Demonstrate all three.',
    solution: '#include <iostream>\n#include <string>\n\nvoid bump(int& n) {\n    ++n;\n}\n\nint doubled(int n) {\n    return n * 2;\n}\n\nstd::size_t length(const std::string& s) {\n    return s.size();\n}\n\nint main() {\n    int value = 5;\n    bump(value);\n    std::cout << value << \'\\n\';\n    std::cout << doubled(value) << \'\\n\';\n    std::cout << length("const reference") << \'\\n\';\n}\n',
    hint: 'Only the reference parameter changes the caller\u2019s variable.',
  },
  {
    slug: 'cpp-lambdas',
    title: 'Functions defined where they are used',
    subtitle: 'Lambda expressions',
    concept:
      'A **lambda** is an anonymous function written inline:\n\n`[capture](parameters) { body }`\n\nThe capture list is the distinctive part \u2014 it says which surrounding variables the lambda may use. `[]` captures nothing, `[x]` captures x by copy, `[&x]` by reference, `[=]` and `[&]` capture everything used by copy or reference respectively.\n\nLambdas exist so that a small piece of behaviour can be passed to an algorithm without the ceremony of a named function. Store one in `auto`, or accept one as a template parameter.',
    example: '#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint main() {\n    auto twice = [](int n) { return n * 2; };\n    std::cout << twice(21) << \'\\n\';\n\n    int offset = 100;\n    auto shifted = [offset](int n) { return n + offset; };\n    std::cout << shifted(5) << \'\\n\';\n\n    int total = 0;\n    std::vector<int> nums{1, 2, 3, 4};\n    std::for_each(nums.begin(), nums.end(), [&total](int n) { total += n; });\n    std::cout << total << \'\\n\';\n\n    std::sort(nums.begin(), nums.end(), [](int a, int b) { return a > b; });\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}',
    note: '[&total] captured by reference so the lambda could accumulate into the outer variable.',
    task: 'Create a lambda squaring its argument, one capturing a `base` variable by copy and adding it, and one capturing a counter by reference to count calls. Demonstrate each.',
    solution: '#include <iostream>\n\nint main() {\n    auto square = [](int n) { return n * n; };\n    std::cout << square(7) << \'\\n\';\n\n    int base = 50;\n    auto add_base = [base](int n) { return n + base; };\n    std::cout << add_base(5) << \'\\n\';\n\n    int calls = 0;\n    auto counted = [&calls](int n) { ++calls; return n; };\n    counted(1);\n    counted(2);\n    counted(3);\n    std::cout << calls << \'\\n\';\n}\n',
    hint: 'Use [base] for a copy and [&calls] for a reference.',
  },
  {
    slug: 'cpp-recursion',
    title: 'Functions that call themselves',
    subtitle: 'Recursion',
    concept:
      'A recursive function calls itself on a smaller problem, with a **base case** that returns without recursing. Without one it exhausts the stack.\n\nEach call gets its own parameters and locals, stacked until the base case unwinds them.\n\nRecursion suits naturally recursive structures \u2014 trees, nested data, divide-and-conquer. For plain counting a loop is faster and uses no stack. Naive recursive Fibonacci is the classic illustration: it recomputes the same values exponentially many times.',
    example: '#include <iostream>\n\nlong long factorial(int n) {\n    if (n <= 1) return 1;\n    return n * factorial(n - 1);\n}\n\nint sum_digits(int n) {\n    if (n < 10) return n;\n    return n % 10 + sum_digits(n / 10);\n}\n\nint gcd(int a, int b) {\n    return b == 0 ? a : gcd(b, a % b);\n}\n\nint main() {\n    std::cout << factorial(10) << \'\\n\';\n    std::cout << sum_digits(9875) << \'\\n\';\n    std::cout << gcd(48, 36) << \'\\n\';\n}',
    note: 'gcd fits on one line because the conditional operator supplies both the base case and the recursive step.',
    task: 'Write recursive `power(int base, int exp)` and `int fib(int n)`, then print power(2, 10), power(5, 3) and the first ten Fibonacci numbers on one line.',
    solution: '#include <iostream>\n\nint power(int base, int exp) {\n    if (exp == 0) return 1;\n    return base * power(base, exp - 1);\n}\n\nint fib(int n) {\n    if (n < 2) return n;\n    return fib(n - 1) + fib(n - 2);\n}\n\nint main() {\n    std::cout << power(2, 10) << \'\\n\';\n    std::cout << power(5, 3) << \'\\n\';\n    for (int i = 0; i < 10; ++i) std::cout << fib(i) << \' \';\n    std::cout << \'\\n\';\n}\n',
    hint: 'Anything to the power zero is 1, and n < 2 covers both Fibonacci base cases.',
  },
  d('cpp-fn-square', 'A squaring function', 'Write square(int) and print the squares of 3, 7 and 12.',
    '#include <iostream>\n\nint square(int n) { return n * n; }\n\nint main() {\n    std::cout << square(3) << \'\\n\';\n    std::cout << square(7) << \'\\n\';\n    std::cout << square(12) << \'\\n\';\n}\n', 'Return the product.'),
  d('cpp-fn-min3', 'Smallest of three', 'Write min3(int, int, int) and print min3(5, 2, 9) and min3(7, 7, 3).',
    '#include <iostream>\n#include <algorithm>\n\nint min3(int a, int b, int c) {\n    return std::min(a, std::min(b, c));\n}\n\nint main() {\n    std::cout << min3(5, 2, 9) << \'\\n\';\n    std::cout << min3(7, 7, 3) << \'\\n\';\n}\n', 'std::min can be nested.'),
  d('cpp-fn-bool', 'A predicate function', 'Write bool is_even(int) and print it with boolalpha for 4, 7 and 0.',
    '#include <iostream>\n\nbool is_even(int n) { return n % 2 == 0; }\n\nint main() {\n    std::cout << std::boolalpha;\n    std::cout << is_even(4) << \'\\n\';\n    std::cout << is_even(7) << \'\\n\';\n    std::cout << is_even(0) << \'\\n\';\n}\n', 'The comparison already yields a bool.'),
  d('cpp-fn-overload-area', 'Overloaded area', 'Overload area for a square (one int) and a rectangle (two ints), and call both.',
    '#include <iostream>\n\nint area(int side) { return side * side; }\nint area(int w, int h) { return w * h; }\n\nint main() {\n    std::cout << area(5) << \'\\n\';\n    std::cout << area(3, 7) << \'\\n\';\n}\n', 'The parameter count distinguishes the overloads.'),
  d('cpp-fn-overload-types', 'Overload by type', 'Overload print_value for int and double, and call each.',
    '#include <iostream>\n\nvoid print_value(int n) { std::cout << "int " << n << \'\\n\'; }\nvoid print_value(double d) { std::cout << "double " << d << \'\\n\'; }\n\nint main() {\n    print_value(42);\n    print_value(2.5);\n}\n', 'The argument type selects the overload.'),
  d('cpp-fn-default', 'Default arguments', 'Write greet(name, greeting = "Hello") and call it with one and two arguments.',
    '#include <iostream>\n#include <string>\n\nstd::string greet(const std::string& name, const std::string& greeting = "Hello") {\n    return greeting + ", " + name;\n}\n\nint main() {\n    std::cout << greet("Ada") << \'\\n\';\n    std::cout << greet("Bob", "Hi") << \'\\n\';\n}\n', 'Defaults come after required parameters.'),
  d('cpp-fn-ref-modify', 'Modifying by reference', 'Write void add_ten(int&) and apply it twice to a variable holding 5.',
    '#include <iostream>\n\nvoid add_ten(int& n) { n += 10; }\n\nint main() {\n    int v = 5;\n    add_ten(v);\n    std::cout << v << \'\\n\';\n    add_ten(v);\n    std::cout << v << \'\\n\';\n}\n', 'A reference binds to the caller\'s variable.'),
  d('cpp-fn-two-outputs', 'Two results by reference', 'Write void min_max(const std::vector<int>&, int& lo, int& hi) and print both for {4, 9, 1, 7}.',
    '#include <iostream>\n#include <vector>\n\nvoid min_max(const std::vector<int>& v, int& lo, int& hi) {\n    lo = v[0];\n    hi = v[0];\n    for (int n : v) {\n        if (n < lo) lo = n;\n        if (n > hi) hi = n;\n    }\n}\n\nint main() {\n    std::vector<int> nums{4, 9, 1, 7};\n    int lo, hi;\n    min_max(nums, lo, hi);\n    std::cout << lo << \'\\n\';\n    std::cout << hi << \'\\n\';\n}\n', 'Reference parameters can carry results back.'),
  d('cpp-fn-const-ref', 'A const reference parameter', 'Write std::size_t words(const std::string&) counting spaces plus one, and call it twice.',
    '#include <iostream>\n#include <string>\n\nstd::size_t words(const std::string& s) {\n    std::size_t count = 1;\n    for (char c : s) {\n        if (c == \' \') ++count;\n    }\n    return count;\n}\n\nint main() {\n    std::cout << words("one two three") << \'\\n\';\n    std::cout << words("single") << \'\\n\';\n}\n', 'const& avoids copying the string.'),
  d('cpp-fn-vector-param', 'Passing a vector', 'Write int total(const std::vector<int>&) and use it on {10, 20, 30}.',
    '#include <iostream>\n#include <vector>\n\nint total(const std::vector<int>& v) {\n    int sum = 0;\n    for (int n : v) sum += n;\n    return sum;\n}\n\nint main() {\n    std::vector<int> nums{10, 20, 30};\n    std::cout << total(nums) << \'\\n\';\n}\n', 'Never pass a vector by value unless you need a copy.'),
  d('cpp-fn-lambda-basic', 'A simple lambda', 'Store a lambda that triples its argument in auto, and call it twice.',
    M('    auto triple = [](int n) { return n * 3; };\n    std::cout << triple(7) << \'\\n\';\n    std::cout << triple(10) << \'\\n\';'), 'Lambdas can be stored in auto variables.'),
  d('cpp-fn-lambda-capture', 'Capturing by copy', 'Capture an int variable by copy in a lambda that adds it, then print two results.',
    M('    int base = 100;\n    auto add_base = [base](int n) { return n + base; };\n    std::cout << add_base(5) << \'\\n\';\n    std::cout << add_base(50) << \'\\n\';'), '[base] captures a copy of the variable.'),
  d('cpp-fn-lambda-ref', 'Capturing by reference', 'Use a lambda capturing a total by reference to accumulate 1 to 5, then print the total.',
    M('    int total = 0;\n    auto add = [&total](int n) { total += n; };\n    for (int i = 1; i <= 5; ++i) add(i);\n    std::cout << total << \'\\n\';'), '[&total] lets the lambda modify the outer variable.'),
  d('cpp-fn-lambda-sort', 'Sorting with a lambda', 'Sort {5, 2, 9, 1} descending using std::sort with a lambda, and print the result.',
    '#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint main() {\n    std::vector<int> nums{5, 2, 9, 1};\n    std::sort(nums.begin(), nums.end(), [](int a, int b) { return a > b; });\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}\n', 'Return true when a should come before b.'),
  d('cpp-fn-recursive-factorial', 'Recursive factorial', 'Write a recursive factorial and print it for 0, 1, 5 and 10.',
    '#include <iostream>\n\nlong long factorial(int n) {\n    if (n <= 1) return 1;\n    return n * factorial(n - 1);\n}\n\nint main() {\n    std::cout << factorial(0) << \'\\n\';\n    std::cout << factorial(1) << \'\\n\';\n    std::cout << factorial(5) << \'\\n\';\n    std::cout << factorial(10) << \'\\n\';\n}\n', 'n <= 1 covers both base cases.'),
  d('cpp-fn-recursive-gcd', 'Recursive GCD', 'Write a recursive gcd and print gcd(48, 36) and gcd(17, 5).',
    '#include <iostream>\n\nint gcd(int a, int b) {\n    return b == 0 ? a : gcd(b, a % b);\n}\n\nint main() {\n    std::cout << gcd(48, 36) << \'\\n\';\n    std::cout << gcd(17, 5) << \'\\n\';\n}\n', 'The base case is b reaching zero.'),
  d('cpp-fn-recursive-sum', 'Recursive digit sum', 'Write a recursive digit sum and print it for 9875 and 7.',
    '#include <iostream>\n\nint digit_sum(int n) {\n    if (n < 10) return n;\n    return n % 10 + digit_sum(n / 10);\n}\n\nint main() {\n    std::cout << digit_sum(9875) << \'\\n\';\n    std::cout << digit_sum(7) << \'\\n\';\n}\n', 'Peel the last digit and recurse.'),
  d('cpp-fn-compose', 'Composing functions', 'Write doubled(int) and increment(int), then print doubled(increment(5)) and increment(doubled(5)).',
    '#include <iostream>\n\nint doubled(int n) { return n * 2; }\nint increment(int n) { return n + 1; }\n\nint main() {\n    std::cout << doubled(increment(5)) << \'\\n\';\n    std::cout << increment(doubled(5)) << \'\\n\';\n}\n', 'The inner call runs first.'),
  d('cpp-fn-capstone', 'Function summary', 'Write char grade(int), void report(const std::vector<int>&) printing each score with its grade and the average to one decimal, then run it on {95, 82, 71, 50}.',
    '#include <iostream>\n#include <iomanip>\n#include <vector>\n\nchar grade(int score) {\n    if (score >= 90) return \'A\';\n    if (score >= 80) return \'B\';\n    if (score >= 70) return \'C\';\n    if (score >= 60) return \'D\';\n    return \'F\';\n}\n\nvoid report(const std::vector<int>& scores) {\n    int total = 0;\n    for (int s : scores) {\n        std::cout << s << ": " << grade(s) << \'\\n\';\n        total += s;\n    }\n    std::cout << std::fixed << std::setprecision(1)\n              << "average: " << static_cast<double>(total) / scores.size() << \'\\n\';\n}\n\nint main() {\n    report({95, 82, 71, 50});\n}\n', 'A brace list converts implicitly to a vector parameter.'),
  {
    kind: 'quiz',
    slug: 'cpp-functions-quiz',
    title: 'Checkpoint: functions',
    questions: [
      { q: 'What distinguishes two overloads of the same function name?', options: ['The return type', 'The parameter list', 'The order of definition', 'The namespace'], answer: 1, explain: 'Overload resolution uses the argument types, so the return type alone cannot distinguish two overloads.' },
      { q: 'Where do default arguments belong when a function is declared and defined separately?', options: ['Both places', 'The declaration only', 'The definition only', 'Neither'], answer: 1, explain: 'The default goes in the declaration that callers see; repeating it in the definition is an error.' },
      { q: 'What should you use to pass a large std::vector you only read?', options: ['By value', 'const std::vector<int>&', 'std::vector<int>&', 'A pointer'], answer: 1, explain: 'A const reference avoids copying every element while guaranteeing the function will not modify it.' },
      { q: 'What does [&total] in a lambda capture list mean?', options: ['A copy of total', 'A reference to total, so the lambda can modify it', 'The address as an int', 'Nothing'], answer: 1, explain: 'The ampersand captures by reference, which is how a lambda accumulates into an enclosing variable.' },
      { q: 'What must every recursive function have?', options: ['Two parameters', 'A base case that returns without recursing', 'A loop', 'A void return type'], answer: 1, explain: 'Without a base case the recursion never terminates and eventually overflows the call stack.' },
    ],
  },
];

export const cppStrings = [
  {
    slug: 'cpp-string-basics',
    title: 'Strings that manage themselves',
    subtitle: 'std::string',
    concept:
      '`std::string` from `<string>` is the antidote to C\u2019s char arrays. It knows its own length, grows as needed, frees its own memory, and compares with `==` as you would expect.\n\nNo null terminator to manage, no buffer size to get wrong, no `strcpy` overflow. The class owns its storage and releases it when it goes out of scope.\n\nCore members: `.size()` (or `.length()`, identical), `.empty()`, `[i]` for characters, and `+` to concatenate.',
    example: '#include <iostream>\n#include <string>\n\nint main() {\n    std::string name = "Bjarne";\n    std::string greeting = "Hello, " + name + "!";\n\n    std::cout << greeting << \'\\n\';\n    std::cout << name.size() << \'\\n\';\n    std::cout << name[0] << \' \' << name[name.size() - 1] << \'\\n\';\n    std::cout << (name == "Bjarne") << \'\\n\';\n    std::cout << std::string{}.empty() << \'\\n\';\n}',
    note: 'Comparing with == just works, unlike C where it would compare addresses.',
    task: 'Given `std::string lang{"C++"}`, print the string, its size, its first character, and the sentence `I am learning C++ today`.',
    given: 'std::string lang{"C++"};',
    solution: St('    std::string lang{"C++"};\n\n    std::cout << lang << \'\\n\';\n    std::cout << lang.size() << \'\\n\';\n    std::cout << lang[0] << \'\\n\';\n    std::cout << "I am learning " + lang + " today" << \'\\n\';'),
    hint: 'Concatenation with + produces a new string.',
  },
  {
    slug: 'cpp-string-methods',
    title: 'What a string can do',
    subtitle: 'substr, find, replace and more',
    concept:
      '`std::string` carries a large interface. The essentials:\n\n- `.substr(pos, count)` a section, or from pos to the end if count is omitted\n- `.find(text)` the index of the first match, or `std::string::npos` when absent\n- `.replace(pos, count, text)` substitute in place\n- `.append(text)` / `+=` add to the end\n- `.insert(pos, text)`, `.erase(pos, count)`\n\nThe `npos` return is the one to remember: it is a huge unsigned value, so always compare against `std::string::npos` rather than testing for a negative result.',
    example: '#include <iostream>\n#include <string>\n\nint main() {\n    std::string text = "programming";\n\n    std::cout << text.substr(0, 7) << \'\\n\';\n    std::cout << text.substr(7) << \'\\n\';\n    std::cout << text.find("gram") << \'\\n\';\n    std::cout << (text.find("xyz") == std::string::npos) << \'\\n\';\n\n    std::string copy = text;\n    copy.replace(0, 7, "swim");\n    std::cout << copy << \'\\n\';\n\n    copy += "!";\n    std::cout << copy << \'\\n\';\n}',
    note: 'find returned 3 for "gram", and npos for the missing substring.',
    task: 'Given `std::string text{"the quick brown fox"}`, print the first three characters, everything from index 4, the index of `"brown"`, and whether it contains `"cat"`.',
    given: 'std::string text{"the quick brown fox"};',
    solution: St('    std::string text{"the quick brown fox"};\n\n    std::cout << text.substr(0, 3) << \'\\n\';\n    std::cout << text.substr(4) << \'\\n\';\n    std::cout << text.find("brown") << \'\\n\';\n    std::cout << (text.find("cat") != std::string::npos) << \'\\n\';'),
    hint: 'Compare find\u2019s result against std::string::npos.',
  },
  {
    slug: 'cpp-string-iteration',
    title: 'Walking a string',
    subtitle: 'Loops and character tests',
    concept:
      'A `std::string` is a container, so a range-based for works: `for (char c : text)`.\n\nUse `for (char& c : text)` to modify characters in place. `<cctype>` supplies `std::isalpha`, `std::isdigit`, `std::toupper` and `std::tolower`.\n\nOne subtlety carried over from C: those functions take an int and are undefined for negative values, so casting to `unsigned char` is the correct discipline. In practice you will see it in careful code and omitted elsewhere.',
    example: '#include <iostream>\n#include <string>\n#include <cctype>\n\nint main() {\n    std::string text = "Hello World 42";\n    int letters = 0, digits = 0, spaces = 0;\n\n    for (char c : text) {\n        if (std::isalpha(static_cast<unsigned char>(c))) ++letters;\n        else if (std::isdigit(static_cast<unsigned char>(c))) ++digits;\n        else if (c == \' \') ++spaces;\n    }\n\n    std::cout << letters << \' \' << digits << \' \' << spaces << \'\\n\';\n\n    std::string upper = text;\n    for (char& c : upper) c = static_cast<char>(std::toupper(static_cast<unsigned char>(c)));\n    std::cout << upper << \'\\n\';\n}',
    note: 'The char& loop modified upper in place; a plain char loop would only have changed copies.',
    task: 'Given `std::string text{"programming in C++"}`, count the vowels and the spaces, then print the text fully uppercased.',
    given: 'std::string text{"programming in C++"};',
    solution: '#include <iostream>\n#include <string>\n#include <cctype>\n\nint main() {\n    std::string text{"programming in C++"};\n    int vowels = 0, spaces = 0;\n\n    for (char c : text) {\n        char lower = static_cast<char>(std::tolower(static_cast<unsigned char>(c)));\n        if (lower == \'a\' || lower == \'e\' || lower == \'i\' || lower == \'o\' || lower == \'u\') ++vowels;\n        if (c == \' \') ++spaces;\n    }\n\n    std::cout << "vowels: " << vowels << \'\\n\';\n    std::cout << "spaces: " << spaces << \'\\n\';\n\n    for (char& c : text) c = static_cast<char>(std::toupper(static_cast<unsigned char>(c)));\n    std::cout << text << \'\\n\';\n}\n',
    hint: 'Lowercase each character first so the vowel test needs five comparisons.',
  },
  {
    slug: 'cpp-string-streams',
    title: 'Strings as streams',
    subtitle: 'stringstream for parsing and building',
    concept:
      '`<sstream>` lets you treat a string as a stream, which solves both parsing and formatting.\n\n`std::istringstream` reads from a string: `>>` extracts whitespace-separated tokens with automatic type conversion, so pulling an int out of text takes one line. `std::getline(stream, piece, delim)` splits on any delimiter.\n\n`std::ostringstream` builds a string with `<<`, giving you all the formatting manipulators and then `.str()` to retrieve the result.\n\nThis is the C++ answer to both `sscanf` and `sprintf`, with full type safety.',
    example: '#include <iostream>\n#include <sstream>\n#include <string>\n#include <iomanip>\n\nint main() {\n    std::istringstream in{"42 3.5 hello"};\n    int n;\n    double d;\n    std::string word;\n    in >> n >> d >> word;\n    std::cout << n << \' \' << d << \' \' << word << \'\\n\';\n\n    std::istringstream csv{"red,green,blue"};\n    std::string piece;\n    while (std::getline(csv, piece, \',\')) {\n        std::cout << piece << \'\\n\';\n    }\n\n    std::ostringstream out;\n    out << std::fixed << std::setprecision(2) << "total: " << 12.5;\n    std::cout << out.str() << \'\\n\';\n}',
    note: 'The extraction converted "42" to an int and "3.5" to a double automatically.',
    task: 'Parse `"ada 91 2.5"` into a string, an int and a double with istringstream and print each. Then split `"a,b,c"` on commas, and build `"score: 91"` with an ostringstream.',
    solution: '#include <iostream>\n#include <sstream>\n#include <string>\n\nint main() {\n    std::istringstream in{"ada 91 2.5"};\n    std::string name;\n    int score;\n    double ratio;\n    in >> name >> score >> ratio;\n\n    std::cout << name << \'\\n\';\n    std::cout << score << \'\\n\';\n    std::cout << ratio << \'\\n\';\n\n    std::istringstream parts{"a,b,c"};\n    std::string piece;\n    while (std::getline(parts, piece, \',\')) std::cout << piece << \'\\n\';\n\n    std::ostringstream out;\n    out << "score: " << score;\n    std::cout << out.str() << \'\\n\';\n}\n',
    hint: 'Chain the >> extractions in the order the tokens appear.',
  },
  {
    slug: 'cpp-string-conversion',
    title: 'Between strings and numbers',
    subtitle: 'to_string and sto* functions',
    concept:
      'Two directions, both one-liners.\n\n`std::to_string(value)` converts any arithmetic type to a string. Note it gives no control over precision \u2014 for formatted output use an ostringstream.\n\n`std::stoi`, `std::stod`, `std::stol` parse a string into a number. They **throw** on failure: `std::invalid_argument` when there are no digits, `std::out_of_range` when the value will not fit. That is a real improvement over `atoi`, which silently returned 0 and left you unable to distinguish failure from a genuine zero.',
    example: '#include <iostream>\n#include <string>\n\nint main() {\n    std::string n = std::to_string(42);\n    std::string d = std::to_string(3.5);\n    std::cout << n + " and " + d << \'\\n\';\n\n    std::cout << std::stoi("123") + 1 << \'\\n\';\n    std::cout << std::stod("2.75") * 2 << \'\\n\';\n\n    try {\n        std::cout << std::stoi("abc") << \'\\n\';\n    } catch (const std::invalid_argument&) {\n        std::cout << "not a number\\n";\n    }\n}',
    note: 'The throw is the point: failure cannot be mistaken for a successful parse of zero.',
    task: 'Convert 99 and 1.5 to strings and print them joined. Parse `"250"` and `"4.25"` back to numbers and print their product. Then attempt `std::stoi("xyz")` inside a try/catch printing `invalid`.',
    solution: '#include <iostream>\n#include <string>\n\nint main() {\n    std::string a = std::to_string(99);\n    std::string b = std::to_string(1.5);\n    std::cout << a + " " + b << \'\\n\';\n\n    std::cout << std::stoi("250") * std::stod("4.25") << \'\\n\';\n\n    try {\n        std::cout << std::stoi("xyz") << \'\\n\';\n    } catch (const std::invalid_argument&) {\n        std::cout << "invalid\\n";\n    }\n}\n',
    hint: 'Catch std::invalid_argument by const reference.',
  },
  d('cpp-s-length', 'String length', 'For "keyboard", print the string, its size and whether it is empty.',
    St('    std::string w{"keyboard"};\n    std::cout << w << \'\\n\';\n    std::cout << w.size() << \'\\n\';\n    std::cout << std::boolalpha << w.empty() << \'\\n\';'), 'size() and length() are identical.'),
  d('cpp-s-ends', 'First and last characters', 'For "alphabet", print the first and last characters.',
    St('    std::string w{"alphabet"};\n    std::cout << w.front() << \'\\n\';\n    std::cout << w.back() << \'\\n\';'), 'front() and back() are clearer than indexing.'),
  d('cpp-s-concat', 'Concatenation', 'Build "Hello, world!" from three pieces with + and print it with its size.',
    St('    std::string s = std::string{"Hello"} + ", " + "world!";\n    std::cout << s << \'\\n\';\n    std::cout << s.size() << \'\\n\';'), 'At least one operand must be a std::string.'),
  d('cpp-s-append', 'Appending', 'Start with "abc", append "def" with += and then "ghi" with .append(), printing the result.',
    St('    std::string s{"abc"};\n    s += "def";\n    s.append("ghi");\n    std::cout << s << \'\\n\';'), 'Both forms add to the end.'),
  d('cpp-s-substr', 'Substrings', 'For "abcdefgh", print the first three characters, the last three, and characters 2 to 4.',
    St('    std::string s{"abcdefgh"};\n    std::cout << s.substr(0, 3) << \'\\n\';\n    std::cout << s.substr(s.size() - 3) << \'\\n\';\n    std::cout << s.substr(2, 3) << \'\\n\';'), 'substr takes a position and a count.'),
  d('cpp-s-find', 'Finding substrings', 'In "the quick brown fox", print the index of "brown", and whether "cat" is present using npos.',
    St('    std::string s{"the quick brown fox"};\n    std::cout << s.find("brown") << \'\\n\';\n    std::cout << std::boolalpha << (s.find("cat") != std::string::npos) << \'\\n\';'), 'find returns npos when absent.'),
  d('cpp-s-replace', 'Replacing a section', 'In "hello world", replace the first five characters with "howdy" and print the result.',
    St('    std::string s{"hello world"};\n    s.replace(0, 5, "howdy");\n    std::cout << s << \'\\n\';'), 'replace takes a position, a count and the new text.'),
  d('cpp-s-insert-erase', 'Inserting and erasing', 'Start with "abcdef", insert "XY" at index 3, print it, then erase two characters at index 0 and print again.',
    St('    std::string s{"abcdef"};\n    s.insert(3, "XY");\n    std::cout << s << \'\\n\';\n    s.erase(0, 2);\n    std::cout << s << \'\\n\';'), 'Both take a position and operate in place.'),
  d('cpp-s-compare', 'Comparing strings', 'With boolalpha, print whether "abc" equals "abc", whether "apple" < "banana", and whether "cat" equals "Cat".',
    St('    std::cout << std::boolalpha;\n    std::cout << (std::string{"abc"} == "abc") << \'\\n\';\n    std::cout << (std::string{"apple"} < "banana") << \'\\n\';\n    std::cout << (std::string{"cat"} == "Cat") << \'\\n\';'), '== compares contents, unlike C.'),
  d('cpp-s-reverse', 'Reversing a string', 'Reverse "stressed" using std::reverse and print both the original copy and the reversed version.',
    '#include <iostream>\n#include <string>\n#include <algorithm>\n\nint main() {\n    std::string s{"stressed"};\n    std::string r = s;\n    std::reverse(r.begin(), r.end());\n    std::cout << s << \'\\n\';\n    std::cout << r << \'\\n\';\n}\n', 'std::reverse works in place on any range.'),
  d('cpp-s-palindrome', 'Palindrome check', 'Print whether "racecar" and "python" are palindromes.',
    '#include <iostream>\n#include <string>\n#include <algorithm>\n\nbool is_palindrome(const std::string& s) {\n    std::string r = s;\n    std::reverse(r.begin(), r.end());\n    return r == s;\n}\n\nint main() {\n    std::cout << std::boolalpha;\n    std::cout << is_palindrome("racecar") << \'\\n\';\n    std::cout << is_palindrome("python") << \'\\n\';\n}\n', 'Compare the string with its reverse.'),
  d('cpp-s-count-char', 'Counting a character', 'Count the letter s in "mississippi" using std::count and print it.',
    '#include <iostream>\n#include <string>\n#include <algorithm>\n\nint main() {\n    std::string s{"mississippi"};\n    std::cout << std::count(s.begin(), s.end(), \'s\') << \'\\n\';\n}\n', 'std::count works on any range.'),
  d('cpp-s-case', 'Changing case', 'Print "Hello World" fully uppercased and fully lowercased.',
    '#include <iostream>\n#include <string>\n#include <cctype>\n\nint main() {\n    std::string s{"Hello World"};\n    std::string up = s, low = s;\n    for (char& c : up) c = static_cast<char>(std::toupper(static_cast<unsigned char>(c)));\n    for (char& c : low) c = static_cast<char>(std::tolower(static_cast<unsigned char>(c)));\n    std::cout << up << \'\\n\';\n    std::cout << low << \'\\n\';\n}\n', 'Use char& to modify in place.'),
  d('cpp-s-repeat', 'Repeating a string', 'Print a line of twenty dashes and the string "ab" repeated four times, using the std::string fill constructor and a loop.',
    St('    std::cout << std::string(20, \'-\') << \'\\n\';\n    std::string out;\n    for (int i = 0; i < 4; ++i) out += "ab";\n    std::cout << out << \'\\n\';'), 'std::string(count, char) fills with one character.'),
  d('cpp-s-split', 'Splitting on a delimiter', 'Split "red,green,blue" on commas with getline and print each part plus the count.',
    '#include <iostream>\n#include <sstream>\n#include <string>\n#include <vector>\n\nint main() {\n    std::istringstream in{"red,green,blue"};\n    std::vector<std::string> parts;\n    std::string piece;\n    while (std::getline(in, piece, \',\')) parts.push_back(piece);\n    for (const auto& p : parts) std::cout << p << \'\\n\';\n    std::cout << parts.size() << \'\\n\';\n}\n', 'getline accepts a delimiter as its third argument.'),
  d('cpp-s-words', 'Splitting into words', 'Split "the quick brown fox" on whitespace with istringstream and print each word numbered from 1.',
    '#include <iostream>\n#include <sstream>\n#include <string>\n\nint main() {\n    std::istringstream in{"the quick brown fox"};\n    std::string word;\n    int i = 1;\n    while (in >> word) {\n        std::cout << i++ << ". " << word << \'\\n\';\n    }\n}\n', 'The >> operator skips whitespace automatically.'),
  d('cpp-s-join', 'Joining with a separator', 'Join the words {"usr", "local", "bin"} with slashes and print the result with a leading slash.',
    '#include <iostream>\n#include <string>\n#include <vector>\n\nint main() {\n    std::vector<std::string> parts{"usr", "local", "bin"};\n    std::string out;\n    for (const auto& p : parts) out += "/" + p;\n    std::cout << out << \'\\n\';\n}\n', 'Prefixing each part with the separator gives the leading slash for free.'),
  d('cpp-s-to-string', 'Numbers to strings', 'Convert 42 and 3.5 to strings, join them with a space, and print the result and its size.',
    St('    std::string s = std::to_string(42) + " " + std::to_string(3.5);\n    std::cout << s << \'\\n\';\n    std::cout << s.size() << \'\\n\';'), 'std::to_string handles any arithmetic type.'),
  d('cpp-s-stoi', 'Strings to numbers', 'Parse "150" and "2.5" and print their product, then catch a failure parsing "abc".',
    '#include <iostream>\n#include <string>\n\nint main() {\n    std::cout << std::stoi("150") * std::stod("2.5") << \'\\n\';\n    try {\n        std::stoi("abc");\n    } catch (const std::invalid_argument&) {\n        std::cout << "invalid\\n";\n    }\n}\n', 'stoi throws rather than returning zero.'),
  d('cpp-s-ostringstream', 'Building formatted text', 'Use an ostringstream with fixed and setprecision to build "price: 3.50" and print it.',
    '#include <iostream>\n#include <sstream>\n#include <iomanip>\n\nint main() {\n    std::ostringstream out;\n    out << std::fixed << std::setprecision(2) << "price: " << 3.5;\n    std::cout << out.str() << \'\\n\';\n}\n', 'Call .str() to retrieve the built string.'),
  d('cpp-s-capstone', 'String summary', 'For "  Data Science Rocks  ", print the size, the vowel count, the word count via istringstream, the reversed string, and the words joined by underscores.',
    '#include <iostream>\n#include <string>\n#include <sstream>\n#include <vector>\n#include <algorithm>\n#include <cctype>\n\nint main() {\n    std::string raw{"  Data Science Rocks  "};\n\n    int vowels = 0;\n    for (char c : raw) {\n        char l = static_cast<char>(std::tolower(static_cast<unsigned char>(c)));\n        if (l == \'a\' || l == \'e\' || l == \'i\' || l == \'o\' || l == \'u\') ++vowels;\n    }\n\n    std::istringstream in{raw};\n    std::vector<std::string> words;\n    std::string w;\n    while (in >> w) words.push_back(w);\n\n    std::string reversed = raw;\n    std::reverse(reversed.begin(), reversed.end());\n\n    std::string joined;\n    for (std::size_t i = 0; i < words.size(); ++i) {\n        if (i > 0) joined += "_";\n        joined += words[i];\n    }\n\n    std::cout << raw.size() << \'\\n\';\n    std::cout << vowels << \'\\n\';\n    std::cout << words.size() << \'\\n\';\n    std::cout << reversed << \'\\n\';\n    std::cout << joined << \'\\n\';\n}\n', 'istringstream >> skips the leading and trailing whitespace for you.'),
  {
    kind: 'quiz',
    slug: 'cpp-strings-quiz',
    title: 'Checkpoint: strings',
    questions: [
      { q: 'What is the main advantage of std::string over a char array?', options: ['It is faster', 'It knows its size and manages its own memory', 'It uses less memory', 'It can hold numbers'], answer: 1, explain: 'The class owns and resizes its buffer and frees it automatically, eliminating terminator and overflow bugs.' },
      { q: 'What does .find() return when the substring is absent?', options: ['-1', '0', 'std::string::npos', 'It throws'], answer: 2, explain: 'npos is a large unsigned sentinel, so the result must be compared against std::string::npos rather than tested for negativity.' },
      { q: 'Which loop form lets you modify a string in place?', options: ['for (char c : s)', 'for (char& c : s)', 'for (const char& c : s)', 'None'], answer: 1, explain: 'A non-const reference binds to each character itself, so assignments through it change the string.' },
      { q: 'What does std::stoi("abc") do?', options: ['Returns 0', 'Returns -1', 'Throws std::invalid_argument', 'Returns npos'], answer: 2, explain: 'Throwing makes failure impossible to confuse with a successful parse of zero, unlike the older atoi.' },
      { q: 'What is std::istringstream useful for?', options: ['Printing', 'Parsing tokens and numbers out of a string with type conversion', 'Sorting', 'Allocating memory'], answer: 1, explain: 'Extracting with >> converts each token to the target type, which is the type-safe replacement for sscanf.' },
    ],
  },
];
