const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const VA = (body) => `#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint main() {\n${body}\n}\n`;
const VAN = (body) => `#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n\nint main() {\n${body}\n}\n`;

export const cppAlgorithms = [
  {
    slug: 'cpp-sort',
    title: 'Ordering a range',
    subtitle: 'std::sort and comparators',
    concept:
      '`std::sort(first, last)` orders a range in place. It takes **iterators**, not a container, which is why the same function works on vectors, arrays and any other random-access range.\n\nA third argument supplies a custom comparison: a function or lambda returning `true` when the first argument must come **before** the second. `return a > b;` gives descending order.\n\nThe comparator must define a strict ordering \u2014 using `>=` instead of `>` is undefined behaviour, because the algorithm relies on the comparison being irreflexive.\n\n`std::stable_sort` preserves the relative order of equal elements, at slightly higher cost.',
    example: '#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <string>\n\nint main() {\n    std::vector<int> nums{5, 2, 9, 1, 7};\n\n    std::sort(nums.begin(), nums.end());\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::sort(nums.begin(), nums.end(), [](int a, int b) { return a > b; });\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::vector<std::string> words{"banana", "fig", "cherry"};\n    std::sort(words.begin(), words.end(),\n              [](const auto& a, const auto& b) { return a.size() < b.size(); });\n    for (const auto& w : words) std::cout << w << \' \';\n    std::cout << \'\\n\';\n}',
    note: 'The last sort ordered by length rather than alphabetically, purely by changing the comparator.',
    task: 'Sort {42, 7, 91, 15, 63} ascending and print it, then descending and print it, then sort the words {"delta","alpha","charlie"} by length and print them.',
    solution: '#include <iostream>\n#include <vector>\n#include <string>\n#include <algorithm>\n\nint main() {\n    std::vector<int> nums{42, 7, 91, 15, 63};\n\n    std::sort(nums.begin(), nums.end());\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::sort(nums.begin(), nums.end(), [](int a, int b) { return a > b; });\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::vector<std::string> words{"delta", "alpha", "charlie"};\n    std::sort(words.begin(), words.end(),\n              [](const auto& a, const auto& b) { return a.size() < b.size(); });\n    for (const auto& w : words) std::cout << w << \' \';\n    std::cout << \'\\n\';\n}\n',
    hint: 'Return true when the first argument should come first.',
  },
  {
    slug: 'cpp-find-count',
    title: 'Searching a range',
    subtitle: 'find, count and their _if variants',
    concept:
      'The searching algorithms all return **iterators**, and signal failure by returning the end iterator \u2014 the same half-open convention as everywhere else.\n\n- `std::find(first, last, value)` locates a value\n- `std::find_if(first, last, pred)` locates the first element satisfying a predicate\n- `std::count(first, last, value)` and `std::count_if(...)` tally matches\n- `std::any_of`, `std::all_of`, `std::none_of` answer questions about the whole range\n\nThe `_if` suffix is the pattern: the plain version compares against a value, the `_if` version calls your predicate. Always compare a result against `end()` before dereferencing it.',
    example: '#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint main() {\n    std::vector<int> nums{4, 8, 15, 16, 23, 42};\n\n    auto found = std::find(nums.begin(), nums.end(), 15);\n    std::cout << (found != nums.end()) << \'\\n\';\n    std::cout << *found << \' \' << (found - nums.begin()) << \'\\n\';\n\n    auto big = std::find_if(nums.begin(), nums.end(), [](int n) { return n > 20; });\n    std::cout << *big << \'\\n\';\n\n    std::cout << std::count_if(nums.begin(), nums.end(), [](int n) { return n % 2 == 0; }) << \'\\n\';\n    std::cout << std::boolalpha;\n    std::cout << std::any_of(nums.begin(), nums.end(), [](int n) { return n > 40; }) << \'\\n\';\n    std::cout << std::all_of(nums.begin(), nums.end(), [](int n) { return n > 0; }) << \'\\n\';\n}',
    note: 'Subtracting begin() from the found iterator converts it back to an index.',
    task: 'For {3, 12, 7, 20, 5}, print whether 7 is present, the index of the first value above 10, the count of values above 5, and whether all values are positive.',
    given: 'std::vector<int> nums{3, 12, 7, 20, 5};',
    solution: VA('    std::vector<int> nums{3, 12, 7, 20, 5};\n\n    std::cout << std::boolalpha;\n    std::cout << (std::find(nums.begin(), nums.end(), 7) != nums.end()) << \'\\n\';\n\n    auto big = std::find_if(nums.begin(), nums.end(), [](int n) { return n > 10; });\n    std::cout << (big - nums.begin()) << \'\\n\';\n\n    std::cout << std::count_if(nums.begin(), nums.end(), [](int n) { return n > 5; }) << \'\\n\';\n    std::cout << std::all_of(nums.begin(), nums.end(), [](int n) { return n > 0; }) << \'\\n\';'),
    hint: 'Convert an iterator to an index by subtracting begin().',
  },
  {
    slug: 'cpp-transform-accumulate',
    title: 'Mapping and reducing',
    subtitle: 'transform and accumulate',
    concept:
      '`std::transform` applies a function to every element of a range, writing the results to a destination. The destination may be the same range (transform in place) or a different one \u2014 use `std::back_inserter` to append to an empty vector.\n\n`std::accumulate` from `<numeric>` reduces a range to one value. The third argument is the initial value, and it also determines the result **type**: passing `0` with doubles truncates every intermediate result, so pass `0.0`.\n\nA fourth argument replaces addition with any binary operation, making it a general fold.',
    example: '#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n#include <iterator>\n\nint main() {\n    std::vector<int> nums{1, 2, 3, 4};\n\n    std::vector<int> squares;\n    std::transform(nums.begin(), nums.end(), std::back_inserter(squares),\n                   [](int n) { return n * n; });\n    for (int n : squares) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::cout << std::accumulate(nums.begin(), nums.end(), 0) << \'\\n\';\n    std::cout << std::accumulate(nums.begin(), nums.end(), 1, std::multiplies<int>()) << \'\\n\';\n\n    std::transform(nums.begin(), nums.end(), nums.begin(), [](int n) { return n * 10; });\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}',
    note: 'The last transform wrote back into nums itself, modifying it in place.',
    task: 'From {1, 2, 3, 4, 5}, build a vector of cubes with transform and print it, print the sum with accumulate, print the product using std::multiplies, then double every element in place.',
    given: 'std::vector<int> nums{1, 2, 3, 4, 5};',
    solution: '#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n#include <iterator>\n#include <functional>\n\nint main() {\n    std::vector<int> nums{1, 2, 3, 4, 5};\n\n    std::vector<int> cubes;\n    std::transform(nums.begin(), nums.end(), std::back_inserter(cubes),\n                   [](int n) { return n * n * n; });\n    for (int n : cubes) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::cout << std::accumulate(nums.begin(), nums.end(), 0) << \'\\n\';\n    std::cout << std::accumulate(nums.begin(), nums.end(), 1, std::multiplies<int>()) << \'\\n\';\n\n    std::transform(nums.begin(), nums.end(), nums.begin(), [](int n) { return n * 2; });\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}\n',
    hint: 'back_inserter appends to an empty destination vector.',
  },
  {
    slug: 'cpp-minmax-extremes',
    title: 'Extremes and rearrangement',
    subtitle: 'min_element, reverse, unique',
    concept:
      'A group of algorithms that operate on whole ranges:\n\n- `std::min_element` / `std::max_element` return **iterators**, so dereference to get the value\n- `std::minmax_element` returns both as a pair\n- `std::reverse` reverses in place\n- `std::rotate` shifts elements around\n- `std::unique` removes **consecutive** duplicates and returns the new logical end\n\nThat last one has a famous quirk: `unique` does not shrink the container, it only moves the unwanted elements to the back. The idiom is `v.erase(std::unique(v.begin(), v.end()), v.end())`, and the range must be sorted first for it to remove all duplicates.',
    example: '#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint main() {\n    std::vector<int> nums{5, 2, 9, 1, 7};\n\n    std::cout << *std::min_element(nums.begin(), nums.end()) << \'\\n\';\n    std::cout << *std::max_element(nums.begin(), nums.end()) << \'\\n\';\n\n    auto [lo, hi] = std::minmax_element(nums.begin(), nums.end());\n    std::cout << *lo << \' \' << *hi << \'\\n\';\n\n    std::reverse(nums.begin(), nums.end());\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::vector<int> dupes{1, 1, 2, 2, 2, 3, 1};\n    std::sort(dupes.begin(), dupes.end());\n    dupes.erase(std::unique(dupes.begin(), dupes.end()), dupes.end());\n    for (int n : dupes) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}',
    note: 'Sorting before unique is essential, since it only collapses runs of adjacent equal values.',
    task: 'For {8, 3, 91, 42, 17}, print the smallest, the largest, and the vector reversed. Then deduplicate {4, 1, 4, 2, 1} properly and print the result.',
    solution: VA('    std::vector<int> nums{8, 3, 91, 42, 17};\n\n    std::cout << *std::min_element(nums.begin(), nums.end()) << \'\\n\';\n    std::cout << *std::max_element(nums.begin(), nums.end()) << \'\\n\';\n\n    std::reverse(nums.begin(), nums.end());\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    std::vector<int> dupes{4, 1, 4, 2, 1};\n    std::sort(dupes.begin(), dupes.end());\n    dupes.erase(std::unique(dupes.begin(), dupes.end()), dupes.end());\n    for (int n : dupes) std::cout << n << \' \';\n    std::cout << \'\\n\';'),
    hint: 'min_element and max_element return iterators, so dereference them.',
  },
  {
    slug: 'cpp-binary-search-algos',
    title: 'Searching sorted ranges',
    subtitle: 'binary_search and bounds',
    concept:
      'On a **sorted** range, the library offers logarithmic searches.\n\n`std::binary_search` returns only a bool. When you need the position, use `std::lower_bound` \u2014 the first element **not less** than the value \u2014 or `std::upper_bound`, the first element **greater** than it.\n\nTogether they delimit the range of equal values, which `std::equal_range` returns as a pair. `lower_bound` is also exactly the correct insertion point to keep a vector sorted.\n\nAll of these are undefined on an unsorted range, and will silently give wrong answers rather than complaining.',
    example: '#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint main() {\n    std::vector<int> nums{1, 3, 3, 5, 7, 9};\n\n    std::cout << std::boolalpha;\n    std::cout << std::binary_search(nums.begin(), nums.end(), 5) << \'\\n\';\n    std::cout << std::binary_search(nums.begin(), nums.end(), 4) << \'\\n\';\n\n    auto lo = std::lower_bound(nums.begin(), nums.end(), 3);\n    auto hi = std::upper_bound(nums.begin(), nums.end(), 3);\n    std::cout << (lo - nums.begin()) << \' \' << (hi - nums.begin()) << \'\\n\';\n    std::cout << (hi - lo) << \'\\n\';\n\n    auto where = std::lower_bound(nums.begin(), nums.end(), 6);\n    nums.insert(where, 6);\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}',
    note: 'lower_bound gave the correct insertion point, keeping the vector sorted after the insert.',
    task: 'On the sorted vector {2, 4, 6, 6, 8, 10}, print whether 6 and 5 are present, the index range of the 6s, how many there are, and insert 7 at the correct sorted position before printing the whole vector.',
    given: 'std::vector<int> nums{2, 4, 6, 6, 8, 10};',
    solution: VA('    std::vector<int> nums{2, 4, 6, 6, 8, 10};\n\n    std::cout << std::boolalpha;\n    std::cout << std::binary_search(nums.begin(), nums.end(), 6) << \'\\n\';\n    std::cout << std::binary_search(nums.begin(), nums.end(), 5) << \'\\n\';\n\n    auto lo = std::lower_bound(nums.begin(), nums.end(), 6);\n    auto hi = std::upper_bound(nums.begin(), nums.end(), 6);\n    std::cout << (lo - nums.begin()) << \' \' << (hi - nums.begin()) << \'\\n\';\n    std::cout << (hi - lo) << \'\\n\';\n\n    nums.insert(std::lower_bound(nums.begin(), nums.end(), 7), 7);\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';'),
    hint: 'lower_bound gives the insertion point that preserves sorted order.',
  },
  {
    slug: 'cpp-algorithm-composition',
    title: 'Chaining algorithms together',
    subtitle: 'Building real pipelines',
    concept:
      'Real work combines several algorithms. The typical pipeline is filter, transform, then reduce.\n\n`std::copy_if` selects elements into a destination. `std::transform` maps them. `std::accumulate` reduces. Each step is a named operation with obvious intent, which is the argument for using them over hand-written loops.\n\n`std::remove_if` deserves particular note: like `unique`, it does not shrink the container. It moves the unwanted elements to the back and returns the new logical end, so the erase-remove idiom is:\n\n`v.erase(std::remove_if(v.begin(), v.end(), pred), v.end())`',
    example: '#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n#include <iterator>\n\nint main() {\n    std::vector<int> nums{1, 2, 3, 4, 5, 6, 7, 8};\n\n    std::vector<int> evens;\n    std::copy_if(nums.begin(), nums.end(), std::back_inserter(evens),\n                 [](int n) { return n % 2 == 0; });\n\n    std::vector<int> squared;\n    std::transform(evens.begin(), evens.end(), std::back_inserter(squared),\n                   [](int n) { return n * n; });\n\n    for (int n : squared) std::cout << n << \' \';\n    std::cout << \'\\n\';\n    std::cout << std::accumulate(squared.begin(), squared.end(), 0) << \'\\n\';\n\n    nums.erase(std::remove_if(nums.begin(), nums.end(), [](int n) { return n < 5; }),\n               nums.end());\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}',
    note: 'Without the surrounding erase, remove_if would leave the vector the same size with rubbish at the end.',
    task: 'From {3, 8, 12, 5, 20, 7}, copy the values above 6 into a new vector, transform them to their doubles, print them and their sum. Then erase every odd value from the original with the erase-remove idiom and print it.',
    given: 'std::vector<int> nums{3, 8, 12, 5, 20, 7};',
    solution: '#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n#include <iterator>\n\nint main() {\n    std::vector<int> nums{3, 8, 12, 5, 20, 7};\n\n    std::vector<int> big;\n    std::copy_if(nums.begin(), nums.end(), std::back_inserter(big),\n                 [](int n) { return n > 6; });\n\n    std::vector<int> doubled;\n    std::transform(big.begin(), big.end(), std::back_inserter(doubled),\n                   [](int n) { return n * 2; });\n\n    for (int n : doubled) std::cout << n << \' \';\n    std::cout << \'\\n\';\n    std::cout << std::accumulate(doubled.begin(), doubled.end(), 0) << \'\\n\';\n\n    nums.erase(std::remove_if(nums.begin(), nums.end(), [](int n) { return n % 2 != 0; }),\n               nums.end());\n    for (int n : nums) std::cout << n << \' \';\n    std::cout << \'\\n\';'.replace(/\n$/, '') + '\n}\n',
    hint: 'remove_if must be wrapped in erase to actually shrink the vector.',
  },
  d('cpp-a-sort-asc', 'Sorting ascending', 'Sort {5, 2, 9, 1} ascending and print it.',
    VA('    std::vector<int> v{5, 2, 9, 1};\n    std::sort(v.begin(), v.end());\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'std::sort takes two iterators.'),
  d('cpp-a-sort-desc', 'Sorting descending', 'Sort {5, 2, 9, 1} descending with a lambda and print it.',
    VA('    std::vector<int> v{5, 2, 9, 1};\n    std::sort(v.begin(), v.end(), [](int a, int b) { return a > b; });\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'Return true when a should come first.'),
  d('cpp-a-sort-strings', 'Sorting strings', 'Sort {"delta","alpha","charlie"} alphabetically and print each.',
    '#include <iostream>\n#include <vector>\n#include <string>\n#include <algorithm>\n\nint main() {\n    std::vector<std::string> v{"delta", "alpha", "charlie"};\n    std::sort(v.begin(), v.end());\n    for (const auto& s : v) std::cout << s << \'\\n\';\n}\n', 'Strings compare alphabetically by default.'),
  d('cpp-a-sort-by-length', 'Sorting by length', 'Sort {"banana","fig","cherry"} by length and print each.',
    '#include <iostream>\n#include <vector>\n#include <string>\n#include <algorithm>\n\nint main() {\n    std::vector<std::string> v{"banana", "fig", "cherry"};\n    std::sort(v.begin(), v.end(), [](const auto& a, const auto& b) { return a.size() < b.size(); });\n    for (const auto& s : v) std::cout << s << \'\\n\';\n}\n', 'Compare the sizes in the lambda.'),
  d('cpp-a-find', 'Finding a value', 'In {4, 8, 15, 16}, print whether 15 is present and its index.',
    VA('    std::vector<int> v{4, 8, 15, 16};\n    auto it = std::find(v.begin(), v.end(), 15);\n    std::cout << std::boolalpha << (it != v.end()) << \'\\n\';\n    std::cout << (it - v.begin()) << \'\\n\';'), 'Subtract begin() for the index.'),
  d('cpp-a-find-if', 'Finding by predicate', 'In {3, 7, 12, 5}, print the first value above 10 and its index.',
    VA('    std::vector<int> v{3, 7, 12, 5};\n    auto it = std::find_if(v.begin(), v.end(), [](int n) { return n > 10; });\n    std::cout << *it << \'\\n\';\n    std::cout << (it - v.begin()) << \'\\n\';'), 'find_if takes a predicate rather than a value.'),
  d('cpp-a-count', 'Counting occurrences', 'In {1, 3, 3, 7, 3}, count the 3s and count the values above 2.',
    VA('    std::vector<int> v{1, 3, 3, 7, 3};\n    std::cout << std::count(v.begin(), v.end(), 3) << \'\\n\';\n    std::cout << std::count_if(v.begin(), v.end(), [](int n) { return n > 2; }) << \'\\n\';'), 'The _if variant takes a predicate.'),
  d('cpp-a-any-all', 'Whole-range questions', 'For {2, 4, 6}, print whether all are even, any exceed 5, and none are negative.',
    VA('    std::vector<int> v{2, 4, 6};\n    std::cout << std::boolalpha;\n    std::cout << std::all_of(v.begin(), v.end(), [](int n) { return n % 2 == 0; }) << \'\\n\';\n    std::cout << std::any_of(v.begin(), v.end(), [](int n) { return n > 5; }) << \'\\n\';\n    std::cout << std::none_of(v.begin(), v.end(), [](int n) { return n < 0; }) << \'\\n\';'), 'Three related algorithms with the same shape.'),
  d('cpp-a-accumulate', 'Summing a range', 'Print the sum of {10, 20, 30, 40} and its product using accumulate.',
    '#include <iostream>\n#include <vector>\n#include <numeric>\n#include <functional>\n\nint main() {\n    std::vector<int> v{10, 20, 30, 40};\n    std::cout << std::accumulate(v.begin(), v.end(), 0) << \'\\n\';\n    std::cout << std::accumulate(v.begin(), v.end(), 1, std::multiplies<int>()) << \'\\n\';\n}\n', 'The initial value must be 1 for a product.'),
  d('cpp-a-accumulate-double', 'Summing doubles', 'Sum {1.5, 2.25, 3.0} with accumulate, being careful about the initial value type.',
    '#include <iostream>\n#include <vector>\n#include <numeric>\n\nint main() {\n    std::vector<double> v{1.5, 2.25, 3.0};\n    std::cout << std::accumulate(v.begin(), v.end(), 0.0) << \'\\n\';\n}\n', 'Passing 0 rather than 0.0 would truncate every step.'),
  d('cpp-a-transform', 'Transforming a range', 'Build a vector of the squares of {1, 2, 3, 4} with transform and print it.',
    '#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <iterator>\n\nint main() {\n    std::vector<int> v{1, 2, 3, 4};\n    std::vector<int> out;\n    std::transform(v.begin(), v.end(), std::back_inserter(out), [](int n) { return n * n; });\n    for (int n : out) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}\n', 'back_inserter appends to the destination.'),
  d('cpp-a-transform-inplace', 'Transforming in place', 'Triple every element of {1, 2, 3} using transform writing back to the same range.',
    VA('    std::vector<int> v{1, 2, 3};\n    std::transform(v.begin(), v.end(), v.begin(), [](int n) { return n * 3; });\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'The destination can be the source range.'),
  d('cpp-a-copy-if', 'Filtering a range', 'Copy the even values of {1,2,3,4,5,6} into a new vector and print it.',
    '#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <iterator>\n\nint main() {\n    std::vector<int> v{1, 2, 3, 4, 5, 6};\n    std::vector<int> evens;\n    std::copy_if(v.begin(), v.end(), std::back_inserter(evens), [](int n) { return n % 2 == 0; });\n    for (int n : evens) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}\n', 'copy_if selects into a destination.'),
  d('cpp-a-minmax', 'Extremes', 'For {8, 3, 91, 42}, print the smallest, the largest and their indexes.',
    VA('    std::vector<int> v{8, 3, 91, 42};\n    auto lo = std::min_element(v.begin(), v.end());\n    auto hi = std::max_element(v.begin(), v.end());\n    std::cout << *lo << \' \' << (lo - v.begin()) << \'\\n\';\n    std::cout << *hi << \' \' << (hi - v.begin()) << \'\\n\';'), 'Both return iterators.'),
  d('cpp-a-reverse-rotate', 'Reversing and rotating', 'Reverse {1,2,3,4,5} and print it, then rotate it left by two and print again.',
    VA('    std::vector<int> v{1, 2, 3, 4, 5};\n    std::reverse(v.begin(), v.end());\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';\n    std::rotate(v.begin(), v.begin() + 2, v.end());\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'rotate takes the new first element as its middle argument.'),
  d('cpp-a-unique', 'Deduplicating', 'Remove the duplicates from {3, 1, 3, 2, 1} and print the sorted unique values.',
    VA('    std::vector<int> v{3, 1, 3, 2, 1};\n    std::sort(v.begin(), v.end());\n    v.erase(std::unique(v.begin(), v.end()), v.end());\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'Sort first, then use the erase-unique idiom.'),
  d('cpp-a-remove-if', 'The erase-remove idiom', 'From {1,2,3,4,5,6}, remove every value below 4 and print the result and size.',
    VA('    std::vector<int> v{1, 2, 3, 4, 5, 6};\n    v.erase(std::remove_if(v.begin(), v.end(), [](int n) { return n < 4; }), v.end());\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';\n    std::cout << v.size() << \'\\n\';'), 'remove_if alone does not shrink the vector.'),
  d('cpp-a-binary-search', 'Binary search', 'On the sorted vector {1,3,5,7,9}, print whether 7 and 4 are present.',
    VA('    std::vector<int> v{1, 3, 5, 7, 9};\n    std::cout << std::boolalpha;\n    std::cout << std::binary_search(v.begin(), v.end(), 7) << \'\\n\';\n    std::cout << std::binary_search(v.begin(), v.end(), 4) << \'\\n\';'), 'The range must be sorted.'),
  d('cpp-a-lower-bound', 'Insertion point', 'On {2, 4, 6, 8}, find the sorted insertion point for 5, insert it, and print the vector.',
    VA('    std::vector<int> v{2, 4, 6, 8};\n    auto where = std::lower_bound(v.begin(), v.end(), 5);\n    std::cout << (where - v.begin()) << \'\\n\';\n    v.insert(where, 5);\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';'), 'lower_bound gives the position that preserves order.'),
  d('cpp-a-fill-generate', 'Filling a range', 'Create a vector of five elements, fill it with 7 using std::fill, and print it. Then use std::iota to fill it with 1 to 5.',
    '#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n\nint main() {\n    std::vector<int> v(5);\n    std::fill(v.begin(), v.end(), 7);\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';\n    std::iota(v.begin(), v.end(), 1);\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}\n', 'std::iota fills with consecutive increasing values.'),
  d('cpp-a-pipeline', 'A full pipeline', 'From 1 to 10, filter the multiples of 3, square them, and print the results and their sum.',
    '#include <iostream>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n#include <iterator>\n\nint main() {\n    std::vector<int> nums(10);\n    std::iota(nums.begin(), nums.end(), 1);\n\n    std::vector<int> picked;\n    std::copy_if(nums.begin(), nums.end(), std::back_inserter(picked),\n                 [](int n) { return n % 3 == 0; });\n\n    std::vector<int> squared;\n    std::transform(picked.begin(), picked.end(), std::back_inserter(squared),\n                   [](int n) { return n * n; });\n\n    for (int n : squared) std::cout << n << \' \';\n    std::cout << \'\\n\';\n    std::cout << std::accumulate(squared.begin(), squared.end(), 0) << \'\\n\';\n}\n', 'Filter, transform, then reduce.'),
  d('cpp-a-capstone', 'Algorithm summary', 'From {45, 12, 78, 23, 91, 5}, print the sorted vector, the sum, the average to two decimals, the largest, the count above the average, and the vector with values below 20 removed.',
    '#include <iostream>\n#include <iomanip>\n#include <vector>\n#include <algorithm>\n#include <numeric>\n\nint main() {\n    std::vector<int> v{45, 12, 78, 23, 91, 5};\n\n    std::sort(v.begin(), v.end());\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';\n\n    int sum = std::accumulate(v.begin(), v.end(), 0);\n    double avg = static_cast<double>(sum) / v.size();\n\n    std::cout << sum << \'\\n\';\n    std::cout << std::fixed << std::setprecision(2) << avg << \'\\n\';\n    std::cout << *std::max_element(v.begin(), v.end()) << \'\\n\';\n    std::cout << std::count_if(v.begin(), v.end(), [avg](int n) { return n > avg; }) << \'\\n\';\n\n    v.erase(std::remove_if(v.begin(), v.end(), [](int n) { return n < 20; }), v.end());\n    for (int n : v) std::cout << n << \' \';\n    std::cout << \'\\n\';\n}\n', 'The lambda can capture avg to compare against it.'),
  {
    kind: 'quiz',
    slug: 'cpp-algorithms-quiz',
    title: 'Checkpoint: algorithms',
    questions: [
      { q: 'Why do standard algorithms take iterators rather than containers?', options: ['For speed', 'So the same algorithm works on any range, including parts of a container', 'To avoid copying', 'Historical accident'], answer: 1, explain: 'Iterator pairs decouple algorithms from containers, so std::sort works on vectors, arrays and sub-ranges alike.' },
      { q: 'What does std::remove_if actually do?', options: ['Shrinks the container', 'Moves unwanted elements to the back and returns the new logical end', 'Throws if nothing matches', 'Sorts the range'], answer: 1, explain: 'It cannot resize the container, which is why it must be wrapped in the erase-remove idiom.' },
      { q: 'Why must a range be sorted before std::unique?', options: ['It is not required', 'unique only collapses runs of adjacent equal elements', 'unique needs random access', 'To avoid a throw'], answer: 1, explain: 'Non-adjacent duplicates are left untouched, so sorting first is what makes the deduplication complete.' },
      { q: 'What does accumulate(v.begin(), v.end(), 0) do with a vector of doubles?', options: ['Works correctly', 'Truncates each intermediate result because the accumulator is an int', 'Throws', 'Returns 0'], answer: 1, explain: 'The initial value fixes the accumulator type, so 0.0 must be passed for floating-point sums.' },
      { q: 'What does std::lower_bound return?', options: ['The smallest element', 'The first element not less than the value \u2014 the sorted insertion point', 'A bool', 'The last match'], answer: 1, explain: 'That property makes it both a search and the correct place to insert while keeping the range sorted.' },
    ],
  },
];
