const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });

export const pyLists = [
  {
    slug: 'list-basics',
    title: 'Many values under one name',
    subtitle: 'Creating and indexing lists',
    concept:
      'A **list** holds an ordered sequence of values in a single variable, written with square brackets. The values can be any type, and they can be mixed.\n\nIndexing works exactly as it does for strings: `items[0]` is the first, `items[-1]` is the last, and `len(items)` gives the count.\n\nUnlike a string, a list is **mutable** \u2014 `items[0] = "new"` replaces an element in place, changing the list itself rather than producing a copy.',
    example: 'langs = ["python", "c", "rust"]\n\nprint(langs)\nprint(langs[0])\nprint(langs[-1])\nprint(len(langs))\n\nlangs[1] = "cpp"\nprint(langs)',
    note: 'Printing a list shows its bracket form, with strings in quotes \u2014 useful for seeing exactly what it holds.',
    task: 'Create a list of the four seasons, print the whole list, the first season, the last season, and how many there are.',
    solution: 'seasons = ["spring", "summer", "autumn", "winter"]\n\nprint(seasons)\nprint(seasons[0])\nprint(seasons[-1])\nprint(len(seasons))',
    hint: 'Negative indexing avoids computing len(seasons) - 1.',
  },
  {
    slug: 'list-modify',
    title: 'Growing and shrinking a list',
    subtitle: 'append, insert, remove, pop',
    concept:
      'Lists change size on demand.\n\n- `.append(x)` adds one item to the end\n- `.insert(i, x)` inserts at a position, shifting the rest along\n- `.extend(other)` adds every item from another list\n- `.remove(x)` deletes the first occurrence **by value**\n- `.pop()` removes and returns the last item; `.pop(i)` removes at an index\n- `.clear()` empties it\n\nThe distinction between `remove` (by value) and `pop` (by index) is worth fixing in your memory now.',
    example: 'items = ["a", "b"]\nitems.append("c")\nitems.insert(0, "start")\nitems.extend(["d", "e"])\nprint(items)\n\nlast = items.pop()\nitems.remove("b")\nprint(items, "removed:", last)',
    note: 'append takes one item; extend takes a list. append([1,2]) would nest a list inside your list.',
    task: 'Start with `[10, 20, 30]`. Append 40, insert 5 at the front, remove the value 20, then print the list and its length.',
    solution: 'nums = [10, 20, 30]\nnums.append(40)\nnums.insert(0, 5)\nnums.remove(20)\n\nprint(nums)\nprint(len(nums))',
    hint: 'insert(0, 5) puts the value at the very start.',
  },
  {
    slug: 'list-slicing',
    title: 'Sections of a list',
    subtitle: 'Slices and copies',
    concept:
      'Lists slice exactly like strings: `items[a:b]` returns a **new list** from a up to but not including b, and the step form `items[::2]` works too.\n\nThis matters for copying. `b = a` gives two names for the *same* list, so changing one changes the other. `b = a[:]` (or `a.copy()`) makes a genuine copy that can be modified independently.\n\nThat single distinction is the source of a great many confusing bugs.',
    example: 'nums = [0, 1, 2, 3, 4, 5]\n\nprint(nums[2:5])\nprint(nums[:3])\nprint(nums[::2])\nprint(nums[::-1])\n\nalias = nums\ncopy = nums[:]\nalias.append(99)\nprint(len(nums), len(copy))',
    note: 'nums grew to 7 because alias is the same list; copy stayed at 6 because it is separate.',
    task: 'Given `nums = [1, 2, 3, 4, 5, 6, 7, 8]`, print the first four, the last three, every second value, and the list reversed.',
    solution: 'nums = [1, 2, 3, 4, 5, 6, 7, 8]\n\nprint(nums[:4])\nprint(nums[-3:])\nprint(nums[::2])\nprint(nums[::-1])',
    hint: 'The same slice syntax you learned for strings applies unchanged.',
  },
  {
    slug: 'list-sorting',
    title: 'Putting a list in order',
    subtitle: 'sort, sorted and reverse',
    concept:
      'There are two ways to order a list, and the difference matters.\n\n`items.sort()` rearranges the list **in place** and returns `None`. `sorted(items)` leaves the original alone and returns a **new** sorted list.\n\nBoth accept `reverse=True` for descending order, and `key=` to sort by a computed value \u2014 `key=len` sorts strings by length, for instance.\n\nA classic error is writing `items = items.sort()`, which sets items to None.',
    example: 'nums = [3, 1, 2]\nprint(sorted(nums))\nprint(nums)\n\nnums.sort()\nprint(nums)\n\nwords = ["pear", "fig", "banana"]\nprint(sorted(words, key=len))\nprint(sorted(words, reverse=True))',
    note: 'Use sorted() when you still need the original order; use .sort() when you do not.',
    task: 'Given `nums = [42, 7, 91, 15]`, print the sorted copy, the original unchanged, then sort it in place descending and print it.',
    solution: 'nums = [42, 7, 91, 15]\n\nprint(sorted(nums))\nprint(nums)\n\nnums.sort(reverse=True)\nprint(nums)',
    hint: 'sorted() returns a value; .sort() mutates and returns nothing.',
  },
  {
    slug: 'list-aggregates',
    title: 'Summarising a list',
    subtitle: 'len, sum, min, max and friends',
    concept:
      'Several built-in functions take a list and reduce it to one value: `len`, `sum`, `min`, `max`. There is no need to write a loop for these.\n\nOther useful operations:\n- `items.count(x)` how many times x appears\n- `items.index(x)` where x first appears\n- `x in items` membership\n- `any(items)` / `all(items)` whether any or every value is truthy\n\n`sum` only works on numbers; `min` and `max` also work on strings, comparing alphabetically.',
    example: 'nums = [4, 8, 15, 16, 23, 42]\n\nprint(len(nums), sum(nums))\nprint(min(nums), max(nums))\nprint(sum(nums) / len(nums))\nprint(nums.index(15), nums.count(8))\nprint(any(n > 40 for n in nums), all(n > 0 for n in nums))',
    note: 'sum(nums) / len(nums) is the standard way to compute a mean.',
    task: 'Given `scores = [67, 91, 42, 78, 57]`, print the count, the highest, the lowest, the total and the average.',
    solution: 'scores = [67, 91, 42, 78, 57]\n\nprint("count:", len(scores))\nprint("max:", max(scores))\nprint("min:", min(scores))\nprint("total:", sum(scores))\nprint("average:", sum(scores) / len(scores))',
    hint: 'All five come from built-in functions \u2014 no loops needed.',
  },
  {
    slug: 'tuples',
    title: 'Lists that cannot change',
    subtitle: 'Tuples and unpacking',
    concept:
      'A **tuple** is an ordered sequence like a list, but **immutable** \u2014 written with parentheses instead of brackets. Once created, its contents are fixed.\n\nUse a tuple when the collection represents one thing with fixed parts: a coordinate, an RGB colour, a database row. Use a list when the collection is a variable-length series of similar items.\n\nTuples support **unpacking**: `x, y = point` pulls the parts into separate names, which is how functions return several values.',
    example: 'point = (3, 7)\nprint(point, point[0], len(point))\n\nx, y = point\nprint("x =", x, "y =", y)\n\nfirst, *rest = (1, 2, 3, 4)\nprint(first, rest)',
    note: 'The starred name collects everything remaining into a list \u2014 handy when you only care about the first item.',
    task: 'Create a tuple holding `("Ada", 1815, "England")`. Unpack it into three variables and print `Ada was born in 1815 in England`.',
    solution: 'person = ("Ada", 1815, "England")\nname, year, country = person\n\nprint(f"{name} was born in {year} in {country}")',
    hint: 'Three names on the left of the = will match the three elements.',
  },
  d('list-build-loop', 'Building with a loop', 'Start with an empty list, append the squares of 1 to 6, then print the list and its total.',
    'squares = []\nfor n in range(1, 7):\n    squares.append(n * n)\nprint(squares)\nprint(sum(squares))', 'Create the empty list before the loop starts.'),
  d('list-second-largest', 'Second largest', 'From [12, 45, 7, 89, 23], print the second largest value.',
    'nums = [12, 45, 7, 89, 23]\nprint(sorted(nums)[-2])', 'Sort ascending and take the second from the end.'),
  d('list-remove-dupes', 'Unique values in order', 'From [3, 1, 3, 2, 1, 4], build a list of unique values keeping first-appearance order, then print it.',
    'nums = [3, 1, 3, 2, 1, 4]\nseen = []\nfor n in nums:\n    if n not in seen:\n        seen.append(n)\nprint(seen)', 'Check membership before appending.'),
  d('list-sum-evens', 'Sum of the evens', 'From the numbers 1 to 20, build a list of the even ones and print the list and its sum.',
    'evens = []\nfor n in range(1, 21):\n    if n % 2 == 0:\n        evens.append(n)\nprint(evens)\nprint(sum(evens))', 'Filter inside the loop with a remainder test.'),
  d('list-sort-words', 'Sorting words', 'Sort ["delta", "alpha", "charlie", "bravo"] alphabetically and print it, then sort by length and print that.',
    'words = ["delta", "alpha", "charlie", "bravo"]\nprint(sorted(words))\nprint(sorted(words, key=len))', 'key=len sorts by the length of each string.'),
  d('list-reverse-inplace', 'Reversing in place', 'Given [1, 2, 3, 4], print the reversed copy via slicing, then reverse the original in place and print it.',
    'nums = [1, 2, 3, 4]\nprint(nums[::-1])\nnums.reverse()\nprint(nums)', '.reverse() mutates; [::-1] returns a new list.'),
  d('list-pop-stack', 'A tiny stack', 'Push 1, 2 and 3 onto an empty list, then pop them all off, printing each popped value.',
    'stack = []\nfor n in [1, 2, 3]:\n    stack.append(n)\nwhile stack:\n    print(stack.pop())', 'pop() with no argument removes the last item, so the order reverses.'),
  d('list-count-value', 'Counting occurrences', 'In [1, 3, 3, 7, 3, 9], print how many 3s there are and the index of the first one.',
    'nums = [1, 3, 3, 7, 3, 9]\nprint(nums.count(3))\nprint(nums.index(3))', 'count and index are both list methods.'),
  d('list-merge', 'Merging two lists', 'Combine [1, 2, 3] and [4, 5] two ways: with + and with extend, printing both results.',
    'a = [1, 2, 3]\nb = [4, 5]\nprint(a + b)\na.extend(b)\nprint(a)', '+ creates a new list; extend modifies the first.'),
  d('list-flatten', 'Flattening nested lists', 'Flatten [[1, 2], [3, 4], [5]] into a single list and print it.',
    'nested = [[1, 2], [3, 4], [5]]\nflat = []\nfor inner in nested:\n    for n in inner:\n        flat.append(n)\nprint(flat)', 'A nested loop visits every inner element.'),
  d('list-running-total', 'Running totals', 'From [5, 3, 8, 1], build a list of running totals ([5, 8, 16, 17]) and print it.',
    'nums = [5, 3, 8, 1]\nrunning = []\ntotal = 0\nfor n in nums:\n    total += n\n    running.append(total)\nprint(running)', 'Keep the accumulator outside the loop.'),
  d('list-2d-grid', 'A grid of lists', 'Build a 3x3 grid where each cell holds row*col, then print each row on its own line.',
    'grid = []\nfor row in range(3):\n    line = []\n    for col in range(3):\n        line.append(row * col)\n    grid.append(line)\nfor line in grid:\n    print(line)', 'Create the inner list fresh on each outer pass.'),
  d('list-swap-ends', 'Swapping the ends', 'Given [1, 2, 3, 4, 5], swap the first and last elements and print the result.',
    'nums = [1, 2, 3, 4, 5]\nnums[0], nums[-1] = nums[-1], nums[0]\nprint(nums)', 'Tuple assignment swaps two elements in one line.'),
  d('list-filter-length', 'Long words only', 'From ["hi", "hello", "hey", "greetings"], print only the words longer than three characters, one per line.',
    'for w in ["hi", "hello", "hey", "greetings"]:\n    if len(w) > 3:\n        print(w)', 'Test the length inside the loop.'),
  d('list-min-max-index', 'Where the extremes are', 'From [8, 3, 91, 42], print the index of the smallest and the index of the largest value.',
    'nums = [8, 3, 91, 42]\nprint(nums.index(min(nums)))\nprint(nums.index(max(nums)))', 'Combine index() with min() and max().'),
  d('list-tuple-pairs', 'Pairs of values', 'Given [("ada", 91), ("bob", 78)], loop over it and print each as `ada: 91` using tuple unpacking.',
    'pairs = [("ada", 91), ("bob", 78)]\nfor name, score in pairs:\n    print(f"{name}: {score}")', 'The loop variable can unpack each tuple directly.'),
  d('list-zip-two', 'Combining two lists', 'Given names ["ada", "bob"] and scores [91, 78], print each pair as `ada scored 91`.',
    'names = ["ada", "bob"]\nscores = [91, 78]\nfor name, score in zip(names, scores):\n    print(f"{name} scored {score}")', 'zip pairs up corresponding elements.'),
  d('list-enumerate-idx', 'Numbered output', 'Print the list ["red", "green", "blue"] as a numbered list starting at 1.',
    'for i, colour in enumerate(["red", "green", "blue"], start=1):\n    print(i, colour)', 'enumerate takes a start argument.'),
  d('list-average-round', 'Rounded average', 'From [7, 8, 9, 10], print the total and the average rounded to two decimals.',
    'nums = [7, 8, 9, 10]\nprint(sum(nums))\nprint(round(sum(nums) / len(nums), 2))', 'Divide the sum by the count.'),
  d('list-sort-tuples', 'Sorting pairs', 'Sort [("bob", 78), ("ada", 91), ("cy", 65)] by score descending and print each as `name score`.',
    'pairs = [("bob", 78), ("ada", 91), ("cy", 65)]\nfor name, score in sorted(pairs, key=lambda p: p[1], reverse=True):\n    print(name, score)', 'key=lambda p: p[1] sorts by the second element.'),
  d('list-capstone', 'List summary', 'From [15, 8, 42, 4, 23], print the sorted list, the sum, the average rounded to one decimal, and the values above the average.',
    'nums = [15, 8, 42, 4, 23]\navg = sum(nums) / len(nums)\nprint(sorted(nums))\nprint(sum(nums))\nprint(round(avg, 1))\nfor n in nums:\n    if n > avg:\n        print(n)', 'Compute the average once and reuse it.'),
  {
    kind: 'quiz',
    slug: 'lists-quiz',
    title: 'Checkpoint: lists',
    questions: [
      { q: 'What does items.sort() return?', options: ['A new sorted list', 'None \u2014 it sorts in place', 'The first element', 'A copy of the list'], answer: 1, explain: 'sort() mutates the list and returns None. Use sorted(items) when you want a new list back.' },
      { q: 'After b = a where a is a list, what happens if you append to b?', options: ['Only b changes', 'Both a and b change \u2014 they are the same list', 'An error occurs', 'Only a changes'], answer: 1, explain: 'Assignment copies the reference, not the list. Use a[:] or a.copy() for an independent copy.' },
      { q: 'What is the difference between remove and pop?', options: ['None', 'remove takes a value, pop takes an index', 'remove takes an index, pop takes a value', 'pop only works on tuples'], answer: 1, explain: 'remove(x) deletes the first element equal to x; pop(i) deletes the element at index i and returns it.' },
      { q: 'Why can a tuple not be modified?', options: ['It has no methods', 'Tuples are immutable by design', 'It is stored as a string', 'It can be, using append'], answer: 1, explain: 'Immutability is the defining feature of a tuple, which is what makes it suitable for fixed records.' },
      { q: 'What does items[-2:] give?', options: ['Everything except the last two', 'The last two elements', 'The second element', 'An error'], answer: 1, explain: 'A negative start counts from the end, and an omitted stop means "to the end", so this is the final two elements.' },
    ],
  },
];

export const pyDicts = [
  {
    slug: 'dict-basics',
    title: 'Looking things up by name',
    subtitle: 'Creating and reading dictionaries',
    concept:
      'A **dictionary** stores pairs: each **key** maps to a **value**. Where a list is indexed by position, a dict is indexed by whatever key you chose \u2014 usually a string.\n\nWrite them with braces and colons, and read with `d[key]`. Assigning to a key that does not exist creates it; assigning to one that does replaces its value.\n\nSince Python 3.7, dictionaries preserve insertion order, so iteration is predictable.',
    example: 'ages = {"ada": 36, "alan": 41}\n\nprint(ages)\nprint(ages["ada"])\nprint(len(ages))\n\nages["grace"] = 45\nages["ada"] = 37\nprint(ages)',
    note: 'Keys must be immutable \u2014 strings, numbers and tuples work; lists cannot be keys.',
    task: 'Build a dictionary of three capital cities (France/Paris, Japan/Tokyo, Peru/Lima). Print the whole dict, the capital of Japan, and how many entries it has.',
    solution: 'capitals = {"France": "Paris", "Japan": "Tokyo", "Peru": "Lima"}\n\nprint(capitals)\nprint(capitals["Japan"])\nprint(len(capitals))',
    hint: 'Look up with square brackets and the key.',
  },
  {
    slug: 'dict-missing-keys',
    title: 'Keys that might not be there',
    subtitle: 'get, in and setdefault',
    concept:
      'Reading a missing key with `d[key]` raises a `KeyError` and stops your program. Three safer options exist.\n\n`d.get(key)` returns `None` instead of raising; `d.get(key, default)` returns your chosen fallback. `key in d` tests presence directly. `d.setdefault(key, default)` returns the existing value or inserts the default and returns that.\n\nThe `get` form with a default is the workhorse, and it is what makes counting so concise.',
    example: 'stock = {"apple": 4}\n\nprint(stock.get("apple"))\nprint(stock.get("pear"))\nprint(stock.get("pear", 0))\nprint("apple" in stock, "pear" in stock)\n\nstock.setdefault("pear", 0)\nprint(stock)',
    note: 'get never modifies the dict; setdefault does when the key is absent.',
    task: 'Given `stock = {"apple": 4, "banana": 12}`, print the count of apples, the count of cherries defaulting to 0, and whether `"banana"` is present.',
    solution: 'stock = {"apple": 4, "banana": 12}\n\nprint(stock.get("apple"))\nprint(stock.get("cherry", 0))\nprint("banana" in stock)',
    hint: 'The second argument to get is the fallback value.',
  },
  {
    slug: 'dict-iteration',
    title: 'Walking through a dictionary',
    subtitle: 'keys, values and items',
    concept:
      'Looping over a dict directly yields its **keys**. Three methods make the intent explicit:\n\n- `.keys()` the keys\n- `.values()` the values\n- `.items()` both, as `(key, value)` tuples\n\nThe `.items()` form combined with tuple unpacking is the standard way to process a dictionary: `for key, value in d.items():`.',
    example: 'scores = {"ada": 91, "bob": 78}\n\nfor name in scores:\n    print(name)\n\nfor score in scores.values():\n    print(score)\n\nfor name, score in scores.items():\n    print(f"{name} -> {score}")\n\nprint(sum(scores.values()))',
    note: 'Aggregate functions like sum, min and max work directly on .values().',
    task: 'Given `stock = {"apple": 4, "banana": 12, "cherry": 30}`, print each entry as `apple: 4`, then print the total quantity and the number of kinds.',
    solution: 'stock = {"apple": 4, "banana": 12, "cherry": 30}\n\nfor name, qty in stock.items():\n    print(f"{name}: {qty}")\n\nprint("total:", sum(stock.values()))\nprint("kinds:", len(stock))',
    hint: 'Unpack each item into two loop variables.',
  },
  {
    slug: 'dict-counting',
    title: 'Counting with a dictionary',
    subtitle: 'The standard counting idiom',
    concept:
      'Counting occurrences is the single most common use of a dictionary. The idiom is:\n\n`counts[key] = counts.get(key, 0) + 1`\n\nRead it as "take the current count, defaulting to zero if unseen, and add one". No special case for the first occurrence is needed.\n\nThis pattern generalises to grouping, totalling and any other accumulation keyed by name.',
    example: 'text = "mississippi"\ncounts = {}\n\nfor ch in text:\n    counts[ch] = counts.get(ch, 0) + 1\n\nprint(counts)\nprint(max(counts, key=counts.get))',
    note: 'max(counts, key=counts.get) finds the key with the highest value \u2014 the most frequent character.',
    task: 'Count the words in `"the fox the dog the cat"` and print each word with its count as `the: 3`, in first-appearance order.',
    solution: 'sentence = "the fox the dog the cat"\ncounts = {}\n\nfor word in sentence.split():\n    counts[word] = counts.get(word, 0) + 1\n\nfor word, n in counts.items():\n    print(f"{word}: {n}")',
    hint: 'Dictionaries preserve insertion order, so a plain items() loop already gives first-appearance order.',
  },
  {
    slug: 'dict-nested',
    title: 'Dictionaries inside dictionaries',
    subtitle: 'Nested structures',
    concept:
      'A dictionary value can itself be a dict or a list, which is how structured data is represented. Access chains left to right: `data["ada"]["score"]`.\n\nThis is exactly the shape of JSON, so once you can navigate nested dicts you can navigate almost any API response or configuration file.\n\nWhen the structure gets deep, use `.get()` at each level to avoid a KeyError cascade.',
    example: 'people = {\n    "ada": {"year": 1815, "field": "maths"},\n    "alan": {"year": 1912, "field": "logic"},\n}\n\nprint(people["ada"]["year"])\n\nfor name, info in people.items():\n    print(f"{name}: {info[\'field\']} ({info[\'year\']})")',
    note: 'Note the single quotes inside the f-string braces \u2014 the outer string already uses double quotes.',
    task: 'Build a dict of two books, each mapping to a dict with `pages` and `year`. Print each as `Dune: 412 pages, 1965`.',
    solution: 'books = {\n    "Dune": {"pages": 412, "year": 1965},\n    "Anthem": {"pages": 105, "year": 1938},\n}\n\nfor title, info in books.items():\n    print(f"{title}: {info[\'pages\']} pages, {info[\'year\']}")',
    hint: 'Loop with .items(), then index into the inner dict.',
  },
  {
    slug: 'sets',
    title: 'Collections without duplicates',
    subtitle: 'Sets and set algebra',
    concept:
      'A **set** holds unique, unordered values. Duplicates vanish automatically, which makes `set(items)` the quickest way to deduplicate a list.\n\nMembership testing is extremely fast, faster than scanning a list.\n\nSets support the algebra you would expect:\n- `a | b` union (everything in either)\n- `a & b` intersection (in both)\n- `a - b` difference (in a but not b)\n- `a ^ b` symmetric difference (in one but not both)\n\nAn empty set must be written `set()` \u2014 `{}` creates an empty dict.',
    example: 'nums = [1, 2, 2, 3, 3, 3]\nunique = set(nums)\nprint(unique, len(unique))\n\na = {1, 2, 3}\nb = {3, 4, 5}\nprint(a | b)\nprint(a & b)\nprint(a - b)\nprint(a ^ b)',
    note: 'Sets are unordered, so use sorted(a | b) whenever the printed order needs to be predictable.',
    task: 'Given `left = {1, 2, 3, 4}` and `right = {3, 4, 5, 6}`, print the sorted union, the sorted intersection and the sorted difference left-minus-right.',
    solution: 'left = {1, 2, 3, 4}\nright = {3, 4, 5, 6}\n\nprint(sorted(left | right))\nprint(sorted(left & right))\nprint(sorted(left - right))',
    hint: 'Wrap each result in sorted() so the output is deterministic.',
  },
  d('dict-phonebook', 'A small phonebook', 'Build a dict of three name/number pairs, add a fourth, then print every entry as `name: number`.',
    'book = {"ada": "555-0100", "bob": "555-0111", "cy": "555-0122"}\nbook["dee"] = "555-0133"\nfor name, num in book.items():\n    print(f"{name}: {num}")', 'Assigning a new key adds it at the end.'),
  d('dict-update-value', 'Incrementing a value', 'Given {"visits": 5}, increase visits by 3 and print the dict.',
    'stats = {"visits": 5}\nstats["visits"] += 3\nprint(stats)', 'Augmented assignment works on dict values.'),
  d('dict-char-count', 'Character frequencies', 'Count characters in "abracadabra" and print each as `a: 5` in first-appearance order.',
    'counts = {}\nfor ch in "abracadabra":\n    counts[ch] = counts.get(ch, 0) + 1\nfor ch, n in counts.items():\n    print(f"{ch}: {n}")', 'The get-with-default idiom handles the first occurrence.'),
  d('dict-most-common', 'Most frequent value', 'Find the most common character in "programming" and print it with its count.',
    'counts = {}\nfor ch in "programming":\n    counts[ch] = counts.get(ch, 0) + 1\nbest = max(counts, key=counts.get)\nprint(best, counts[best])', 'max with key=counts.get compares by value.'),
  d('dict-invert', 'Swapping keys and values', 'Invert {"a": 1, "b": 2, "c": 3} so the numbers become keys, then print it.',
    'original = {"a": 1, "b": 2, "c": 3}\nflipped = {}\nfor k, v in original.items():\n    flipped[v] = k\nprint(flipped)', 'Loop the items and assign in reverse.'),
  d('dict-sum-values', 'Totalling values', 'Given {"mon": 8, "tue": 6, "wed": 9}, print the total, the highest and the day with the highest.',
    'hours = {"mon": 8, "tue": 6, "wed": 9}\nprint(sum(hours.values()))\nprint(max(hours.values()))\nprint(max(hours, key=hours.get))', 'Aggregates work on .values() directly.'),
  d('dict-filter', 'Filtering entries', 'From {"a": 5, "b": 12, "c": 8, "d": 3}, print only the entries whose value exceeds 6.',
    'data = {"a": 5, "b": 12, "c": 8, "d": 3}\nfor k, v in data.items():\n    if v > 6:\n        print(k, v)', 'Test the value inside the loop.'),
  d('dict-keys-sorted', 'Alphabetical output', 'Given {"pear": 3, "apple": 7, "fig": 1}, print the entries in alphabetical key order.',
    'stock = {"pear": 3, "apple": 7, "fig": 1}\nfor k in sorted(stock):\n    print(k, stock[k])', 'sorted(dict) sorts the keys.'),
  d('dict-sort-by-value', 'Ranked output', 'Given {"ada": 91, "bob": 78, "cy": 95}, print the entries ordered by score descending.',
    'scores = {"ada": 91, "bob": 78, "cy": 95}\nfor name, score in sorted(scores.items(), key=lambda p: p[1], reverse=True):\n    print(name, score)', 'Sort the items list by the second element of each pair.'),
  d('dict-merge', 'Merging dictionaries', 'Merge {"a": 1, "b": 2} and {"b": 99, "c": 3} so the second wins on conflicts, then print the result.',
    'first = {"a": 1, "b": 2}\nsecond = {"b": 99, "c": 3}\nprint({**first, **second})', 'The ** unpacking operator merges, with later keys taking priority.'),
  d('dict-group', 'Grouping by length', 'Group ["hi", "cat", "dog", "a", "bird"] by word length into a dict of lists and print it.',
    'groups = {}\nfor w in ["hi", "cat", "dog", "a", "bird"]:\n    groups.setdefault(len(w), []).append(w)\nprint(groups)', 'setdefault returns the existing list or inserts a new one.'),
  d('dict-nested-access', 'Nested lookup', 'Given a dict of two users each with a "city" key, print each user and their city.',
    'users = {"ada": {"city": "London"}, "bob": {"city": "Lima"}}\nfor name, info in users.items():\n    print(name, info["city"])', 'Index the inner dict inside the loop.'),
  d('dict-delete', 'Removing entries', 'From {"a": 1, "b": 2, "c": 3}, delete "b" with del and pop "c", printing the popped value and the final dict.',
    'data = {"a": 1, "b": 2, "c": 3}\ndel data["b"]\nvalue = data.pop("c")\nprint(value)\nprint(data)', 'pop returns the removed value; del does not.'),
  d('set-dedupe', 'Removing duplicates', 'From [3, 1, 4, 1, 5, 9, 2, 6, 5, 3], print the sorted unique values and how many there are.',
    'nums = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3]\nunique = set(nums)\nprint(sorted(unique))\nprint(len(unique))', 'set() collapses duplicates automatically.'),
  d('set-common-letters', 'Shared letters', 'Print the sorted letters that appear in both "python" and "rhythm".',
    'print(sorted(set("python") & set("rhythm")))', 'Convert both strings to sets and intersect.'),
  d('set-only-in-first', 'Exclusive letters', 'Print the sorted letters in "keyboard" that do not appear in "board".',
    'print(sorted(set("keyboard") - set("board")))', 'The difference operator removes shared members.'),
  d('set-symmetric', 'In one but not both', 'For {1,2,3,4} and {3,4,5}, print the sorted symmetric difference.',
    'print(sorted({1, 2, 3, 4} ^ {3, 4, 5}))', 'The caret gives elements in exactly one of the sets.'),
  d('set-subset', 'Subset tests', 'Print whether {1,2} is a subset of {1,2,3}, and whether {1,5} is.',
    'print({1, 2} <= {1, 2, 3})\nprint({1, 5} <= {1, 2, 3})', 'The <= operator tests the subset relation.'),
  d('dict-word-lengths', 'Mapping words to lengths', 'Build a dict mapping each word in "the quick brown fox" to its length, then print it.',
    'lengths = {}\nfor w in "the quick brown fox".split():\n    lengths[w] = len(w)\nprint(lengths)', 'Assign len(w) under each word as key.'),
  d('dict-vowel-tally', 'Vowels only', 'Count only the vowels in "encyclopedia" and print each with its count in alphabetical order.',
    'counts = {}\nfor ch in "encyclopedia":\n    if ch in "aeiou":\n        counts[ch] = counts.get(ch, 0) + 1\nfor ch in sorted(counts):\n    print(ch, counts[ch])', 'Filter before counting, then sort the keys.'),
  d('dict-capstone', 'Dictionary summary', 'From "the cat and the hat and the bat", print the word count, the unique word count, and each word with its tally in descending order of frequency then alphabetical.',
    'words = "the cat and the hat and the bat".split()\ncounts = {}\nfor w in words:\n    counts[w] = counts.get(w, 0) + 1\nprint("words:", len(words))\nprint("unique:", len(counts))\nfor w, n in sorted(counts.items(), key=lambda p: (-p[1], p[0])):\n    print(f"{w}: {n}")', 'A tuple key sorts by frequency descending then name ascending.'),
  {
    kind: 'quiz',
    slug: 'dicts-quiz',
    title: 'Checkpoint: dicts and sets',
    questions: [
      { q: 'What happens when you read d["missing"]?', options: ['Returns None', 'Returns 0', 'Raises a KeyError', 'Creates the key'], answer: 2, explain: 'Square-bracket access on an absent key raises KeyError. Use d.get(key, default) to avoid it.' },
      { q: 'What does counts[k] = counts.get(k, 0) + 1 accomplish?', options: ['Deletes the key', 'Counts occurrences, handling the first one automatically', 'Sorts the dict', 'Inverts the dict'], answer: 1, explain: 'get supplies 0 for an unseen key, so no special case is needed for the first occurrence.' },
      { q: 'What does .items() yield?', options: ['Just keys', 'Just values', '(key, value) tuples', 'A sorted list'], answer: 2, explain: 'items() yields both parts as tuples, which is why for k, v in d.items() unpacks so neatly.' },
      { q: 'What is the fastest way to remove duplicates from a list?', options: ['A nested loop', 'set(items)', 'sorted(items)', 'items.clear()'], answer: 1, explain: 'A set stores only unique values, so converting and back is the concise and efficient approach.' },
      { q: 'How do you create an empty set?', options: ['{}', 'set()', '[]', '()'], answer: 1, explain: '{} creates an empty dictionary, so the set constructor must be called explicitly.' },
    ],
  },
];

export const pyFunctions = [
  {
    slug: 'def-basics',
    title: 'Packaging code under a name',
    subtitle: 'def and calling',
    concept:
      'A **function** is a named block of code you can run on demand. `def` defines one, and the indented block below is its body.\n\nDefining a function does not run it \u2014 nothing happens until you **call** it with parentheses. That separation is the point: the logic lives in one place and runs whenever needed.\n\nA function whose body only prints is fine, but usually you want it to hand a value back.',
    example: 'def greet():\n    print("hello")\n    print("world")\n\ngreet()\ngreet()',
    note: 'The two calls run the body twice. Without the calls, defining greet would produce no output at all.',
    task: 'Define a function `banner()` that prints a line of ten equals signs, then call it, print `MENU`, and call it again.',
    solution: 'def banner():\n    print("==========")\n\nbanner()\nprint("MENU")\nbanner()',
    hint: 'Define first, then call. The parentheses are what run it.',
  },
  {
    slug: 'parameters-return',
    title: 'Inputs and outputs',
    subtitle: 'Parameters and return',
    concept:
      '**Parameters** are named placeholders in the definition; **arguments** are the actual values passed at the call site.\n\n`return` sends a value back and ends the function immediately. A function with no `return` gives back `None`.\n\nThe difference between printing and returning is fundamental: printing shows something to a human, returning hands a value to the rest of your program so it can be used further.',
    example: 'def area(width, height):\n    return width * height\n\ndef shout(text):\n    return text.upper() + "!"\n\nresult = area(3, 4)\nprint(result)\nprint(area(10, 2) + area(1, 1))\nprint(shout("hello"))',
    note: 'Because area returns a number, its results can be added together. A printing version could not be used this way.',
    task: 'Write `celsius_to_f(c)` returning `c * 9 / 5 + 32`, then print the result for 0, 100 and 37 as `0C = 32.0F`.',
    solution: 'def celsius_to_f(c):\n    return c * 9 / 5 + 32\n\nfor c in [0, 100, 37]:\n    print(f"{c}C = {celsius_to_f(c)}F")',
    hint: 'Use return, not print, so the caller receives the value.',
  },
  {
    slug: 'default-keyword-args',
    title: 'Optional and named arguments',
    subtitle: 'Defaults and keywords',
    concept:
      'A parameter can have a **default value**, making it optional: `def greet(name, greeting="Hello")`. Parameters with defaults must come after those without.\n\nArguments can also be passed **by name** at the call site, which makes the meaning obvious and lets you skip earlier optional parameters: `power(base=2, exponent=8)`.\n\nOne trap: never use a mutable default like `[]`, because the same list is reused across every call. Use `None` and create the list inside instead.',
    example: 'def greet(name, greeting="Hello", punct="!"):\n    return f"{greeting}, {name}{punct}"\n\nprint(greet("Ada"))\nprint(greet("Alan", "Good morning"))\nprint(greet("Grace", punct="."))\nprint(greet(greeting="Hi", name="Cy"))',
    note: 'Keyword arguments can be given in any order, which is why the last call works.',
    task: 'Write `describe(name, age=0, city="unknown")` returning `Ada, 36, London`. Call it three ways: all three arguments, name and city by keyword, and name only.',
    solution: 'def describe(name, age=0, city="unknown"):\n    return f"{name}, {age}, {city}"\n\nprint(describe("Ada", 36, "London"))\nprint(describe("Bob", city="Lima"))\nprint(describe("Cy"))',
    hint: 'Skipping age requires passing city by name.',
  },
  {
    slug: 'scope',
    title: 'Where names live',
    subtitle: 'Local and global scope',
    concept:
      'Variables created inside a function are **local** to it: they exist only while the call runs and are invisible outside.\n\nA function can *read* names from the enclosing module, but assigning to one creates a new local variable instead of changing the outer one. That is deliberate \u2014 it stops functions from quietly corrupting shared state.\n\nThe cleaner design is almost always to take values in as parameters and hand results back with `return`, rather than reaching outside.',
    example: 'count = 10\n\ndef show():\n    print("can read:", count)\n\ndef attempt():\n    count = 99\n    print("local:", count)\n\nshow()\nattempt()\nprint("outer still:", count)',
    note: 'attempt() created its own count. The module-level count was untouched.',
    task: 'Set `total = 100` at module level. Write `add_ten(value)` that returns value + 10 without touching total, then print `add_ten(total)` and `total` to show the original is unchanged.',
    solution: 'total = 100\n\ndef add_ten(value):\n    return value + 10\n\nprint(add_ten(total))\nprint(total)',
    hint: 'Pass the value in and return the new one \u2014 no reassignment of total.',
  },
  {
    slug: 'args-kwargs',
    title: 'Any number of arguments',
    subtitle: '*args and **kwargs',
    concept:
      '`*args` collects any number of positional arguments into a **tuple**. `**kwargs` collects any number of named arguments into a **dict**.\n\nThe names are conventional, not required \u2014 the asterisks do the work.\n\nThe same syntax works in reverse at a call site: `f(*my_list)` spreads a list into separate arguments, and `f(**my_dict)` spreads a dict into keyword arguments.',
    example: 'def total(*numbers):\n    return sum(numbers)\n\ndef tag(name, **attrs):\n    parts = [f"{k}={v}" for k, v in attrs.items()]\n    return f"{name} " + " ".join(parts)\n\nprint(total(1, 2, 3))\nprint(total(1, 2, 3, 4, 5))\nprint(tag("img", src="a.png", width=100))\n\nvalues = [4, 5, 6]\nprint(total(*values))',
    note: 'total(*values) spreads the list, so it behaves like total(4, 5, 6) rather than passing one list.',
    task: 'Write `stats(*numbers)` returning a tuple of the count, sum and largest. Call it with 4, 8, 15, 16 and print each part on its own line.',
    solution: 'def stats(*numbers):\n    return len(numbers), sum(numbers), max(numbers)\n\ncount, total, largest = stats(4, 8, 15, 16)\nprint(count)\nprint(total)\nprint(largest)',
    hint: 'Returning several comma-separated values produces a tuple you can unpack.',
  },
  {
    slug: 'lambdas',
    title: 'Functions without names',
    subtitle: 'lambda expressions',
    concept:
      'A `lambda` is a small anonymous function written as a single expression: `lambda x: x * 2`. It has no name and no `return` \u2014 the expression *is* the result.\n\nLambdas exist for the moments when a function is needed as an argument and defining a full one would be noise. The classic use is the `key=` parameter of `sorted`.\n\nIf a lambda needs more than one expression, use `def`. Readability wins.',
    example: 'double = lambda x: x * 2\nprint(double(5))\n\nwords = ["banana", "fig", "cherry"]\nprint(sorted(words, key=lambda w: len(w)))\n\npairs = [("ada", 91), ("bob", 78)]\nprint(sorted(pairs, key=lambda p: p[1]))\nprint(sorted(pairs, key=lambda p: -p[1]))',
    note: 'Negating the key is the concise way to sort descending by a numeric field.',
    task: 'Given `people = [("ada", 36), ("bob", 24), ("cy", 45)]`, print them sorted by age ascending, then sorted by name length, using lambdas.',
    solution: 'people = [("ada", 36), ("bob", 24), ("cy", 45)]\n\nprint(sorted(people, key=lambda p: p[1]))\nprint(sorted(people, key=lambda p: len(p[0])))',
    hint: 'p[0] is the name and p[1] is the age.',
  },
  {
    slug: 'docstrings-recursion',
    title: 'Documenting and self-calling',
    subtitle: 'Docstrings and recursion',
    concept:
      'A **docstring** is a string literal on the first line of a function body. Tools and `help()` read it, so it is the conventional place to say what a function does.\n\nA **recursive** function calls itself on a smaller version of the problem. Every recursion needs a **base case** that returns without recursing, or it never stops.\n\nFactorial is the classic example: `n! = n \u00d7 (n-1)!`, with `0! = 1` as the base case.',
    example: 'def factorial(n):\n    """Return n! for a non-negative integer n."""\n    if n <= 1:\n        return 1\n    return n * factorial(n - 1)\n\nprint(factorial(5))\nprint(factorial.__doc__)\n\ndef countdown(n):\n    if n == 0:\n        print("done")\n        return\n    print(n)\n    countdown(n - 1)\n\ncountdown(3)',
    note: 'Each call waits for the one it made. factorial(5) unwinds to 5 x 4 x 3 x 2 x 1.',
    task: 'Write a recursive `total_to(n)` that sums every integer from 1 to n, with a docstring, and print the result for 5, 10 and 100.',
    solution: 'def total_to(n):\n    """Return the sum of every integer from 1 to n."""\n    if n <= 0:\n        return 0\n    return n + total_to(n - 1)\n\nprint(total_to(5))\nprint(total_to(10))\nprint(total_to(100))',
    hint: 'The base case returns 0 when n drops to zero.',
  },
  d('fn-square', 'A squaring function', 'Write square(n) returning n squared, then print the squares of 3, 7 and 12.',
    'def square(n):\n    return n * n\n\nprint(square(3))\nprint(square(7))\nprint(square(12))', 'Return the product rather than printing it.'),
  d('fn-max-of-two', 'Larger of two', 'Write bigger(a, b) returning the larger value, then print bigger(17, 42) and bigger(9, 3).',
    'def bigger(a, b):\n    if a > b:\n        return a\n    return b\n\nprint(bigger(17, 42))\nprint(bigger(9, 3))', 'An early return handles the first case; no else needed.'),
  d('fn-is-even', 'Even predicate', 'Write is_even(n) returning a boolean, then print it for 4, 7 and 0.',
    'def is_even(n):\n    return n % 2 == 0\n\nprint(is_even(4))\nprint(is_even(7))\nprint(is_even(0))', 'The comparison itself is already a boolean.'),
  d('fn-classify', 'Sign classifier', 'Write classify(n) returning "negative", "zero" or "positive", then print it for -5, 0 and 8.',
    'def classify(n):\n    if n < 0:\n        return "negative"\n    if n == 0:\n        return "zero"\n    return "positive"\n\nfor n in [-5, 0, 8]:\n    print(classify(n))', 'Three returns, no else required.'),
  d('fn-default-greet', 'Greeting with a default', 'Write greet(name, greeting="Hello") and call it with one and two arguments.',
    'def greet(name, greeting="Hello"):\n    return f"{greeting}, {name}!"\n\nprint(greet("Ada"))\nprint(greet("Bob", "Hi"))', 'Defaults come after required parameters.'),
  d('fn-average', 'Average of a list', 'Write average(nums) returning the mean, then print it for [1,2,3,4] and [10,20].',
    'def average(nums):\n    return sum(nums) / len(nums)\n\nprint(average([1, 2, 3, 4]))\nprint(average([10, 20]))', 'Divide the sum by the count.'),
  d('fn-count-vowels', 'Counting vowels', 'Write count_vowels(text) and print the result for "programming" and "xyz".',
    'def count_vowels(text):\n    total = 0\n    for ch in text:\n        if ch in "aeiou":\n            total += 1\n    return total\n\nprint(count_vowels("programming"))\nprint(count_vowels("xyz"))', 'Accumulate inside the function and return the total.'),
  d('fn-reverse', 'Reversing text', 'Write reverse(text) returning the reversed string, then print it for "python" and "abc".',
    'def reverse(text):\n    return text[::-1]\n\nprint(reverse("python"))\nprint(reverse("abc"))', 'A slice with step -1 does the work.'),
  d('fn-palindrome', 'Palindrome check', 'Write is_palindrome(text) ignoring case, then print it for "Racecar", "Level" and "python".',
    'def is_palindrome(text):\n    low = text.lower()\n    return low == low[::-1]\n\nfor w in ["Racecar", "Level", "python"]:\n    print(is_palindrome(w))', 'Lowercase first so case does not matter.'),
  d('fn-fizz', 'FizzBuzz as a function', 'Write fizz(n) returning "Fizz", "Buzz", "FizzBuzz" or the number as a string, and print it for 3, 5, 15 and 7.',
    'def fizz(n):\n    if n % 15 == 0:\n        return "FizzBuzz"\n    if n % 3 == 0:\n        return "Fizz"\n    if n % 5 == 0:\n        return "Buzz"\n    return str(n)\n\nfor n in [3, 5, 15, 7]:\n    print(fizz(n))', 'Test the combined case first.'),
  d('fn-multi-return', 'Returning two values', 'Write min_max(nums) returning both the smallest and largest, then unpack and print them for [4, 9, 1, 7].',
    'def min_max(nums):\n    return min(nums), max(nums)\n\nlow, high = min_max([4, 9, 1, 7])\nprint(low)\nprint(high)', 'Comma-separated returns produce a tuple.'),
  d('fn-args-sum', 'Variable arguments', 'Write add_all(*nums) returning the sum, then print add_all(1,2), add_all(1,2,3,4) and add_all().',
    'def add_all(*nums):\n    return sum(nums)\n\nprint(add_all(1, 2))\nprint(add_all(1, 2, 3, 4))\nprint(add_all())', 'sum of an empty tuple is 0.'),
  d('fn-kwargs', 'Keyword arguments', 'Write config(**opts) that prints each option as `key=value` in the order given, then call it with debug=True and level=3.',
    'def config(**opts):\n    for k, v in opts.items():\n        print(f"{k}={v}")\n\nconfig(debug=True, level=3)', 'kwargs arrives as a dictionary.'),
  d('fn-lambda-sort', 'Sorting with a lambda', 'Sort ["banana", "fig", "cherry"] by length and print the result, then by last letter.',
    'words = ["banana", "fig", "cherry"]\nprint(sorted(words, key=lambda w: len(w)))\nprint(sorted(words, key=lambda w: w[-1]))', 'w[-1] is the final character.'),
  d('fn-compose', 'Calling functions from functions', 'Write double(n) and increment(n), then print double(increment(5)) and increment(double(5)).',
    'def double(n):\n    return n * 2\n\ndef increment(n):\n    return n + 1\n\nprint(double(increment(5)))\nprint(increment(double(5)))', 'The inner call runs first.'),
  d('fn-recursive-fib', 'Recursive Fibonacci', 'Write fib(n) recursively with fib(0)=0 and fib(1)=1, then print the first ten values on one line.',
    'def fib(n):\n    if n < 2:\n        return n\n    return fib(n - 1) + fib(n - 2)\n\nfor n in range(10):\n    print(fib(n), end=" ")\nprint()', 'Two base cases collapse into n < 2.'),
  d('fn-recursive-power', 'Recursive power', 'Write power(base, exp) recursively without using **, then print power(2, 10) and power(5, 3).',
    'def power(base, exp):\n    if exp == 0:\n        return 1\n    return base * power(base, exp - 1)\n\nprint(power(2, 10))\nprint(power(5, 3))', 'Anything to the power zero is 1.'),
  d('fn-recursive-digits', 'Summing digits recursively', 'Write digit_sum(n) recursively and print it for 9875 and 1.',
    'def digit_sum(n):\n    if n < 10:\n        return n\n    return n % 10 + digit_sum(n // 10)\n\nprint(digit_sum(9875))\nprint(digit_sum(1))', 'Peel the last digit and recurse on the rest.'),
  d('fn-filter-list', 'Filtering with a function', 'Write only_even(nums) returning a new list of the even values, then print it for range 1 to 11.',
    'def only_even(nums):\n    result = []\n    for n in nums:\n        if n % 2 == 0:\n            result.append(n)\n    return result\n\nprint(only_even(range(1, 11)))', 'Build a fresh list inside the function.'),
  d('fn-word-stats', 'Text statistics', 'Write stats(text) returning the word count and the longest word, then print both for "the quick brown elephant".',
    'def stats(text):\n    words = text.split()\n    longest = max(words, key=len)\n    return len(words), longest\n\ncount, longest = stats("the quick brown elephant")\nprint(count)\nprint(longest)', 'max with key=len finds the longest string.'),
  d('fn-capstone', 'Function summary', 'Write grade(score) returning a letter, and report(scores) printing each score with its grade plus the average at the end. Run it on [95, 82, 71, 64, 50].',
    'def grade(score):\n    if score >= 90:\n        return "A"\n    if score >= 80:\n        return "B"\n    if score >= 70:\n        return "C"\n    if score >= 60:\n        return "D"\n    return "F"\n\ndef report(scores):\n    for s in scores:\n        print(f"{s}: {grade(s)}")\n    print(f"average: {sum(scores) / len(scores)}")\n\nreport([95, 82, 71, 64, 50])', 'One function decides the grade, the other drives the loop.'),
  {
    kind: 'quiz',
    slug: 'functions-quiz',
    title: 'Checkpoint: functions',
    questions: [
      { q: 'What does a function return when it has no return statement?', options: ['0', 'An empty string', 'None', 'It raises an error'], answer: 2, explain: 'Every Python function returns something; without an explicit return that value is None.' },
      { q: 'What is the difference between printing and returning?', options: ['None', 'print shows a value to the user; return hands it back for further use', 'return is faster', 'print works only in functions'], answer: 1, explain: 'A returned value can be stored, combined or passed on. A printed value has already left the program.' },
      { q: 'What does *args collect?', options: ['A list of keyword arguments', 'A tuple of extra positional arguments', 'A dict of all arguments', 'Nothing'], answer: 1, explain: 'The single asterisk gathers any number of positional arguments into a tuple; ** gathers named ones into a dict.' },
      { q: 'What happens when a function assigns to a module-level name?', options: ['The outer variable changes', 'A new local variable is created instead', 'It raises an error', 'The name becomes global'], answer: 1, explain: 'Assignment inside a function creates a local binding by default, which prevents accidental changes to shared state.' },
      { q: 'What must every recursive function have?', options: ['A loop', 'A base case that returns without recursing', 'At least two parameters', 'A docstring'], answer: 1, explain: 'Without a base case the recursion never terminates and eventually exhausts the call stack.' },
    ],
  },
];
