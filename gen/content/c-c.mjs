const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body) => `#include <stdio.h>\n\nint main(void) {\n${body}\n    return 0;\n}\n`;

export const cLoops = [
  {
    slug: 'c-for',
    title: 'The counting loop',
    subtitle: 'for in three parts',
    concept:
      'The C `for` loop packs three clauses into one line, separated by semicolons:\n\n`for (init; condition; step)`\n\nIt initialises once, tests the condition **before** each pass, and runs the step **after** each pass. So a loop whose condition is false initially never executes its body at all.\n\nDeclaring the counter inside the parentheses (`int i = 0`) scopes it to the loop, which is the modern style and prevents accidental reuse afterwards.',
    example: '#include <stdio.h>\n\nint main(void) {\n    for (int i = 1; i <= 3; i++) {\n        printf("i = %d\\n", i);\n    }\n\n    for (int i = 10; i > 0; i -= 3) {\n        printf("%d ", i);\n    }\n    printf("\\n");\n    return 0;\n}',
    note: 'The second loop prints 10 7 4 1 and stops when the condition fails.',
    task: 'Print the first five multiples of 3, one per line, then `sum: 45`.',
    solution: M('    int total = 0;\n\n    for (int i = 1; i <= 5; i++) {\n        int value = i * 3;\n        printf("%d\\n", value);\n        total += value;\n    }\n\n    printf("sum: %d\\n", total);'),
    hint: 'Declare the accumulator before the loop so it survives afterwards.',
  },
  {
    slug: 'c-while',
    title: 'Looping until a condition fails',
    subtitle: 'while',
    concept:
      '`while (condition)` repeats as long as the condition holds. Use it when the number of passes is not known in advance.\n\nSomething inside the body **must** move the condition toward false, or the loop never ends. Forgetting the update is the classic infinite loop.\n\n`while (1)` deliberately loops forever, relying on a `break` inside to escape \u2014 a common and perfectly idiomatic pattern for search and event loops.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int n = 100, steps = 0;\n\n    while (n > 1) {\n        n /= 2;\n        steps++;\n    }\n    printf("halvings: %d\\n", steps);\n\n    int i = 0;\n    while (1) {\n        i++;\n        if (i * i > 50) break;\n    }\n    printf("first: %d\\n", i);\n    return 0;\n}',
    note: 'n /= 2 shrinks n every pass, guaranteeing termination. Without it the loop would hang.',
    task: 'Starting at 1, double a value while it stays below 100, printing each value, then print how many doublings occurred.',
    solution: M('    int value = 1, count = 0;\n\n    while (value < 100) {\n        printf("%d\\n", value);\n        value *= 2;\n        count++;\n    }\n\n    printf("doublings: %d\\n", count);'),
    hint: 'Print before doubling so the initial 1 appears.',
  },
  {
    slug: 'c-do-while',
    title: 'A loop that always runs once',
    subtitle: 'do...while',
    concept:
      '`do { ... } while (condition);` tests the condition **after** the body, so the body always executes at least once.\n\nThat makes it right for menus and input validation \u2014 you must prompt before you can check the answer.\n\nNote the trailing semicolon after the closing parenthesis; it is required, and forgetting it is a compile error.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int n = 100;\n\n    do {\n        printf("body ran with n = %d\\n", n);\n        n++;\n    } while (n < 3);\n\n    int i = 0;\n    do {\n        printf("%d ", i);\n        i++;\n    } while (i < 4);\n    printf("\\n");\n    return 0;\n}',
    note: 'The first loop ran even though 100 < 3 was false from the start \u2014 that is the whole point.',
    task: 'Using do...while, print the numbers 1 to 5 on one line separated by spaces, then a newline.',
    solution: M('    int i = 1;\n\n    do {\n        printf("%d ", i);\n        i++;\n    } while (i <= 5);\n\n    printf("\\n");'),
    hint: 'Do not forget the semicolon after the while condition.',
  },
  {
    slug: 'c-break-continue',
    title: 'Leaving early and skipping',
    subtitle: 'break and continue',
    concept:
      '`break` exits the innermost loop immediately. `continue` abandons the current pass and jumps to the step clause, starting the next one.\n\n`break` is how a search stops as soon as it succeeds, rather than pointlessly scanning the remainder.\n\nBoth affect only the innermost enclosing loop. Escaping two levels at once requires a flag or a `goto`, which is one of the few defensible uses of `goto` in C.',
    example: '#include <stdio.h>\n\nint main(void) {\n    for (int n = 1; n < 20; n++) {\n        if (n % 7 == 0) {\n            printf("first multiple of 7: %d\\n", n);\n            break;\n        }\n    }\n\n    for (int n = 1; n <= 8; n++) {\n        if (n % 2 == 0) continue;\n        printf("%d ", n);\n    }\n    printf("\\n");\n    return 0;\n}',
    note: 'In a for loop, continue still runs the step clause, so the counter advances normally.',
    task: 'Loop from 1 to 30. Skip multiples of 3 with continue and stop with break once the value passes 12, printing each surviving value.',
    solution: M('    for (int n = 1; n <= 30; n++) {\n        if (n > 12) break;\n        if (n % 3 == 0) continue;\n        printf("%d\\n", n);\n    }'),
    hint: 'Test the break condition before the continue condition.',
  },
  {
    slug: 'c-nested-loops',
    title: 'Loops inside loops',
    subtitle: 'Nesting and grids',
    concept:
      'An inner loop runs **completely** for every single pass of the outer loop. Two loops of ten passes execute the inner body one hundred times, so the cost multiplies rather than adds.\n\nThis is the natural structure for grids, tables and comparing every pair. Use different counter names \u2014 reusing `i` for both is a guaranteed bug.\n\nA `printf("\\n")` after the inner loop ends each row.',
    example: '#include <stdio.h>\n\nint main(void) {\n    for (int row = 1; row <= 3; row++) {\n        for (int col = 1; col <= 3; col++) {\n            printf("%d ", row * col);\n        }\n        printf("\\n");\n    }\n    return 0;\n}',
    note: 'The row newline belongs to the outer loop, after the inner one has finished.',
    task: 'Print a 3-row by 4-column grid of hash characters, with no spaces.',
    solution: M('    for (int row = 0; row < 3; row++) {\n        for (int col = 0; col < 4; col++) {\n            printf("#");\n        }\n        printf("\\n");\n    }'),
    hint: 'The inner loop prints the characters; the outer one ends each line.',
  },
  {
    slug: 'c-loop-patterns',
    title: 'Accumulating across passes',
    subtitle: 'Sums, counts and running extremes',
    concept:
      'Most loops exist to build one answer from many values. Three patterns cover the majority:\n\n- **Accumulate**: `total += value;` with total initialised to 0 (or 1 for products)\n- **Count**: `if (test) count++;`\n- **Track an extreme**: initialise from the first element, then replace when a better one appears\n\nFor the extreme, initialising `max = 0` is a bug when the data may be negative. Start from the first actual value.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int sum = 0, count = 0, largest = -1;\n\n    for (int n = 1; n <= 20; n++) {\n        if (n % 4 == 0) {\n            sum += n;\n            count++;\n            if (n > largest) largest = n;\n        }\n    }\n\n    printf("count %d\\n", count);\n    printf("sum %d\\n", sum);\n    printf("largest %d\\n", largest);\n    return 0;\n}',
    note: 'All three accumulators are updated in one pass \u2014 there is rarely a reason to loop three times.',
    task: 'In one loop over 1 to 50, count the multiples of 7, sum them, and track the largest. Print all three labelled.',
    solution: M('    int count = 0, sum = 0, largest = 0;\n\n    for (int n = 1; n <= 50; n++) {\n        if (n % 7 == 0) {\n            count++;\n            sum += n;\n            largest = n;\n        }\n    }\n\n    printf("count: %d\\n", count);\n    printf("sum: %d\\n", sum);\n    printf("largest: %d\\n", largest);'),
    hint: 'Since the loop ascends, the last match is automatically the largest.',
  },
  d('c-loop-sum100', 'Sum to one hundred', 'Print the sum of every integer from 1 to 100.',
    M('    int total = 0;\n    for (int n = 1; n <= 100; n++) {\n        total += n;\n    }\n    printf("%d\\n", total);'), 'Accumulate into a variable declared outside the loop.'),
  d('c-loop-evens', 'Even numbers', 'Print every even number from 2 to 20, one per line.',
    M('    for (int n = 2; n <= 20; n += 2) {\n        printf("%d\\n", n);\n    }'), 'A step of 2 skips the odd values.'),
  d('c-loop-countdown', 'Counting backwards', 'Print 10 down to 1, one per line, then `liftoff`.',
    M('    for (int n = 10; n >= 1; n--) {\n        printf("%d\\n", n);\n    }\n    printf("liftoff\\n");'), 'Use a decrementing step and a >= condition.'),
  d('c-loop-times-table', 'Seven times table', 'Print the seven times table from 1 to 10 as `7 x 3 = 21`.',
    M('    for (int n = 1; n <= 10; n++) {\n        printf("7 x %d = %d\\n", n, 7 * n);\n    }'), 'Two specifiers in one format string.'),
  d('c-loop-factorial', 'Factorial', 'Compute 10 factorial with a loop and print it.',
    M('    long result = 1;\n    for (int n = 1; n <= 10; n++) {\n        result *= n;\n    }\n    printf("%ld\\n", result);'), 'Use long since the value grows quickly.'),
  d('c-loop-fizzbuzz', 'FizzBuzz to 20', 'Print 1 to 20 with Fizz, Buzz and FizzBuzz substitutions.',
    M('    for (int n = 1; n <= 20; n++) {\n        if (n % 15 == 0) {\n            printf("FizzBuzz\\n");\n        } else if (n % 3 == 0) {\n            printf("Fizz\\n");\n        } else if (n % 5 == 0) {\n            printf("Buzz\\n");\n        } else {\n            printf("%d\\n", n);\n        }\n    }'), 'Test divisibility by 15 first.'),
  d('c-loop-squares', 'Squares on one line', 'Print the squares of 1 to 10 on one line separated by spaces, then a newline.',
    M('    for (int n = 1; n <= 10; n++) {\n        printf("%d ", n * n);\n    }\n    printf("\\n");'), 'The trailing newline comes after the loop.'),
  d('c-loop-digits', 'Summing digits', 'With int n = 9875, sum its digits using arithmetic and print the total.',
    M('    int n = 9875, total = 0;\n    while (n > 0) {\n        total += n % 10;\n        n /= 10;\n    }\n    printf("%d\\n", total);'), '% 10 peels the last digit, / 10 removes it.'),
  d('c-loop-reverse-number', 'Reversing a number', 'With int n = 1234, build and print the reversed number 4321.',
    M('    int n = 1234, reversed = 0;\n    while (n > 0) {\n        reversed = reversed * 10 + n % 10;\n        n /= 10;\n    }\n    printf("%d\\n", reversed);'), 'Shift the accumulator left by multiplying by 10.'),
  d('c-loop-collatz', 'Collatz steps', 'Starting at 27, count the Collatz steps to reach 1 and print the count.',
    M('    int n = 27, steps = 0;\n    while (n != 1) {\n        if (n % 2 == 0) {\n            n /= 2;\n        } else {\n            n = 3 * n + 1;\n        }\n        steps++;\n    }\n    printf("steps: %d\\n", steps);'), 'Halve when even, triple and add one when odd.'),
  d('c-loop-primes', 'Primes below thirty', 'Print every prime below 30, one per line, using a nested loop.',
    M('    for (int n = 2; n < 30; n++) {\n        int is_prime = 1;\n        for (int div = 2; div < n; div++) {\n            if (n % div == 0) {\n                is_prime = 0;\n                break;\n            }\n        }\n        if (is_prime) printf("%d\\n", n);\n    }'), 'A flag plus break is the standard shape.'),
  d('c-loop-fibonacci', 'Fibonacci numbers', 'Print the first twelve Fibonacci numbers on one line, starting 0 1.',
    M('    int a = 0, b = 1;\n    for (int i = 0; i < 12; i++) {\n        printf("%d ", a);\n        int next = a + b;\n        a = b;\n        b = next;\n    }\n    printf("\\n");'), 'C needs a temporary to update both values.'),
  d('c-loop-triangle', 'Numbered triangle', 'Print five rows where row n contains the digit n repeated n times.',
    M('    for (int n = 1; n <= 5; n++) {\n        for (int i = 0; i < n; i++) {\n            printf("%d", n);\n        }\n        printf("\\n");\n    }'), 'The inner loop count depends on the outer counter.'),
  d('c-loop-pyramid', 'Centred pyramid', 'Print a five-row pyramid of stars, centred, where row n has 2n-1 stars.',
    M('    int rows = 5;\n    for (int n = 1; n <= rows; n++) {\n        for (int s = 0; s < rows - n; s++) printf(" ");\n        for (int s = 0; s < 2 * n - 1; s++) printf("*");\n        printf("\\n");\n    }'), 'Two inner loops: padding then stars.'),
  d('c-loop-multiplication-grid', 'Multiplication grid', 'Print a 4x4 multiplication grid with tab separators.',
    M('    for (int row = 1; row <= 4; row++) {\n        for (int col = 1; col <= 4; col++) {\n            printf("%d\\t", row * col);\n        }\n        printf("\\n");\n    }'), 'Tabs keep the columns aligned.'),
  d('c-loop-find-first', 'First common multiple', 'Find and print the first number above 100 divisible by both 7 and 11.',
    M('    int n = 101;\n    while (1) {\n        if (n % 7 == 0 && n % 11 == 0) {\n            printf("%d\\n", n);\n            break;\n        }\n        n++;\n    }'), 'while (1) with a break searches indefinitely.'),
  d('c-loop-power', 'Powers of two', 'Print the powers of two from 1 up to 1024, one per line, using a loop.',
    M('    for (int v = 1; v <= 1024; v *= 2) {\n        printf("%d\\n", v);\n    }'), 'The step clause can multiply rather than add.'),
  d('c-loop-gcd', 'Greatest common divisor', 'Compute the GCD of 48 and 36 with Euclid\'s algorithm and print it.',
    M('    int a = 48, b = 36;\n    while (b != 0) {\n        int r = a % b;\n        a = b;\n        b = r;\n    }\n    printf("%d\\n", a);'), 'Replace the pair with (b, a % b) until b hits zero.'),
  d('c-loop-count-digits', 'Counting digits', 'With int n = 98765, count how many digits it has and print the count.',
    M('    int n = 98765, digits = 0;\n    while (n > 0) {\n        digits++;\n        n /= 10;\n    }\n    printf("%d\\n", digits);'), 'Each division by 10 removes one digit.'),
  d('c-loop-do-while-menu', 'A do-while sequence', 'Using do...while, print the squares of 1 to 4 on separate lines.',
    M('    int n = 1;\n    do {\n        printf("%d\\n", n * n);\n        n++;\n    } while (n <= 4);'), 'The body runs before the first test.'),
  d('c-loop-capstone', 'Loop summary', 'In one pass over 1 to 40, count multiples of 6, sum them, and print the count, sum and average to two decimals.',
    M('    int count = 0, sum = 0;\n    for (int n = 1; n <= 40; n++) {\n        if (n % 6 == 0) {\n            count++;\n            sum += n;\n        }\n    }\n    printf("count: %d\\n", count);\n    printf("sum: %d\\n", sum);\n    printf("average: %.2f\\n", (double)sum / count);'), 'Cast for the average to avoid integer division.'),
  {
    kind: 'quiz',
    slug: 'c-loops-quiz',
    title: 'Checkpoint: loops',
    questions: [
      { q: 'In for (init; cond; step), when is the condition tested?', options: ['After the body', 'Before each pass, including the first', 'Only once', 'Never'], answer: 1, explain: 'The condition is checked before every pass, so a for loop whose condition starts false never runs its body.' },
      { q: 'What is unique about do...while?', options: ['It cannot use break', 'The body always runs at least once', 'It has no condition', 'It only counts down'], answer: 1, explain: 'The condition is tested after the body, which is why it suits prompt-then-validate patterns.' },
      { q: 'In a for loop, does continue skip the step clause?', options: ['Yes', 'No \u2014 the step still runs', 'Only for while loops', 'It depends on the compiler'], answer: 1, explain: 'continue jumps to the step clause, so the counter advances normally and the loop still terminates.' },
      { q: 'How many times does the inner body run for nested loops of 5 and 4?', options: ['9', '20', '5', '1'], answer: 1, explain: 'The inner loop runs fully for every outer pass, so the counts multiply.' },
      { q: 'Why is initialising max = 0 risky when finding a maximum?', options: ['It is slower', 'All values may be negative, so the answer would wrongly be 0', 'Zero is not an int', 'It causes overflow'], answer: 1, explain: 'Initialising from the first actual element avoids returning a value that was never in the data.' },
    ],
  },
];

export const cFunctions = [
  {
    slug: 'c-function-basics',
    title: 'Writing your own functions',
    subtitle: 'Return types and parameters',
    concept:
      'A C function declares its return type, its name, and the type of every parameter:\n\n`int add(int a, int b) { return a + b; }`\n\nA function returning nothing uses `void`. One taking no parameters uses `(void)`.\n\nC reads the file top to bottom, so a function must be **known** before it is used. The simplest approach is to define helpers above `main`.',
    example: '#include <stdio.h>\n\nint square(int n) {\n    return n * n;\n}\n\nvoid banner(void) {\n    printf("=====\\n");\n}\n\nint main(void) {\n    banner();\n    printf("%d\\n", square(7));\n    printf("%d\\n", square(3) + square(4));\n    banner();\n    return 0;\n}',
    note: 'Because square returns a value, its results can be combined arithmetically.',
    task: 'Write `int max_of(int a, int b)` returning the larger value, then print `max_of(17, 42)` and `max_of(7, 3)` as `max: 42`.',
    starter: '#include <stdio.h>\n\nint max_of(int a, int b) {\n    /* return the larger value */\n    return 0;\n}\n\nint main(void) {\n    printf("max: %d\\n", max_of(17, 42));\n    printf("max: %d\\n", max_of(7, 3));\n    return 0;\n}\n',
    solution: '#include <stdio.h>\n\nint max_of(int a, int b) {\n    if (a > b) {\n        return a;\n    }\n    return b;\n}\n\nint main(void) {\n    printf("max: %d\\n", max_of(17, 42));\n    printf("max: %d\\n", max_of(7, 3));\n    return 0;\n}\n',
    hint: 'An if with a return, then a bare return handles the other case.',
  },
  {
    slug: 'c-prototypes',
    title: 'Declaring before defining',
    subtitle: 'Function prototypes',
    concept:
      'A **prototype** declares a function\u2019s signature without its body, ending in a semicolon:\n\n`int add(int a, int b);`\n\nThat lets you call the function before its definition appears, which frees you to put `main` at the top where readers expect it. In multi-file projects, prototypes live in header files.\n\nWithout a prototype or prior definition, the compiler cannot check your call, and modern C treats that as an error.',
    example: '#include <stdio.h>\n\nint triple(int n);\nvoid line(void);\n\nint main(void) {\n    line();\n    printf("%d\\n", triple(14));\n    line();\n    return 0;\n}\n\nint triple(int n) {\n    return n * 3;\n}\n\nvoid line(void) {\n    printf("---\\n");\n}',
    note: 'Parameter names in a prototype are optional \u2014 int triple(int); is equally valid \u2014 but naming them documents intent.',
    task: 'Using prototypes, declare `int cube(int)` and `void header(void)` above main, define them below, and print the cubes of 2, 3 and 4.',
    solution: '#include <stdio.h>\n\nint cube(int n);\nvoid header(void);\n\nint main(void) {\n    header();\n    for (int n = 2; n <= 4; n++) {\n        printf("%d\\n", cube(n));\n    }\n    return 0;\n}\n\nint cube(int n) {\n    return n * n * n;\n}\n\nvoid header(void) {\n    printf("cubes\\n");\n}\n',
    hint: 'The prototypes end with semicolons; the definitions follow main.',
  },
  {
    slug: 'c-pass-by-value',
    title: 'Functions get copies',
    subtitle: 'Pass by value',
    concept:
      'C passes arguments **by value**: the function receives a copy. Modifying a parameter inside the function has no effect on the caller\u2019s variable.\n\nThis is why a naive `swap(int a, int b)` cannot work \u2014 it faithfully swaps its own two copies and then discards them.\n\nThe only way to let a function modify the caller\u2019s data is to pass an **address**, which is the subject of the pointers module. Recognising the limitation now makes pointers feel like a solution rather than an arbitrary complication.',
    example: '#include <stdio.h>\n\nvoid try_to_change(int n) {\n    n = 999;\n    printf("inside: %d\\n", n);\n}\n\nint main(void) {\n    int value = 5;\n    try_to_change(value);\n    printf("outside: %d\\n", value);\n    return 0;\n}',
    note: 'The function genuinely set its n to 999 \u2014 but that n was a copy that ceased to exist on return.',
    task: 'Write `void attempt(int n)` that sets n to 100 and prints it, then call it with a variable holding 7 and print that variable afterwards to show it is unchanged.',
    solution: '#include <stdio.h>\n\nvoid attempt(int n) {\n    n = 100;\n    printf("inside: %d\\n", n);\n}\n\nint main(void) {\n    int value = 7;\n    attempt(value);\n    printf("outside: %d\\n", value);\n    return 0;\n}\n',
    hint: 'The outer value stays 7 because the function only changed its copy.',
  },
  {
    slug: 'c-recursion',
    title: 'Functions that call themselves',
    subtitle: 'Recursion',
    concept:
      'A **recursive** function calls itself on a smaller version of the problem. Every recursion needs a **base case** that returns without recursing, or it exhausts the call stack and crashes.\n\nEach call gets its own copy of the parameters and locals, stacked up until the base case is reached, then unwound.\n\nRecursion is elegant for naturally recursive structures. For simple counting, a loop is usually faster and uses no stack.',
    example: '#include <stdio.h>\n\nint factorial(int n) {\n    if (n <= 1) return 1;\n    return n * factorial(n - 1);\n}\n\nint sum_to(int n) {\n    if (n <= 0) return 0;\n    return n + sum_to(n - 1);\n}\n\nint main(void) {\n    printf("%d\\n", factorial(6));\n    printf("%d\\n", sum_to(10));\n    return 0;\n}',
    note: 'factorial(6) expands to 6 x 5 x 4 x 3 x 2 x 1, with each call waiting for the one it made.',
    task: 'Write a recursive `int power(int base, int exp)` without using pow, and print power(2, 10) and power(5, 3).',
    solution: '#include <stdio.h>\n\nint power(int base, int exp) {\n    if (exp == 0) return 1;\n    return base * power(base, exp - 1);\n}\n\nint main(void) {\n    printf("%d\\n", power(2, 10));\n    printf("%d\\n", power(5, 3));\n    return 0;\n}\n',
    hint: 'Anything to the power zero is 1 \u2014 that is the base case.',
  },
  {
    slug: 'c-scope-static',
    title: 'Where variables live and how long',
    subtitle: 'Scope, lifetime and static',
    concept:
      'A variable declared inside a block exists only within that block \u2014 its **scope**. It is created on entry and destroyed on exit, which is its **lifetime**.\n\nA `static` local variable is different: it is initialised once and **retains its value between calls**, while still being invisible outside the function. That makes it a private counter.\n\nA variable declared outside all functions is **global**: visible everywhere and alive for the whole program. Globals make code hard to reason about, so use them sparingly.',
    example: '#include <stdio.h>\n\nint calls = 0;\n\nvoid counter(void) {\n    static int mine = 0;\n    int fresh = 0;\n\n    mine++;\n    fresh++;\n    calls++;\n    printf("static %d, local %d, global %d\\n", mine, fresh, calls);\n}\n\nint main(void) {\n    counter();\n    counter();\n    counter();\n    return 0;\n}',
    note: 'The static variable climbs 1, 2, 3 while the ordinary local resets to 1 every call.',
    task: 'Write `void tick(void)` with a static counter that prints `tick 1`, `tick 2`, `tick 3` across three calls.',
    solution: '#include <stdio.h>\n\nvoid tick(void) {\n    static int n = 0;\n    n++;\n    printf("tick %d\\n", n);\n}\n\nint main(void) {\n    tick();\n    tick();\n    tick();\n    return 0;\n}\n',
    hint: 'A static local keeps its value between calls.',
  },
  d('c-fn-square', 'Squaring function', 'Write square(int) and print the squares of 3, 7 and 12.',
    '#include <stdio.h>\n\nint square(int n) {\n    return n * n;\n}\n\nint main(void) {\n    printf("%d\\n", square(3));\n    printf("%d\\n", square(7));\n    printf("%d\\n", square(12));\n    return 0;\n}\n', 'Return the product.'),
  d('c-fn-is-even', 'Even predicate', 'Write is_even(int) returning 1 or 0, and print it for 4, 7 and 0.',
    '#include <stdio.h>\n\nint is_even(int n) {\n    return n % 2 == 0;\n}\n\nint main(void) {\n    printf("%d\\n", is_even(4));\n    printf("%d\\n", is_even(7));\n    printf("%d\\n", is_even(0));\n    return 0;\n}\n', 'The comparison already yields 1 or 0.'),
  d('c-fn-abs', 'Absolute value', 'Write my_abs(int) without using the library, and print it for -17, 0 and 42.',
    '#include <stdio.h>\n\nint my_abs(int n) {\n    return n < 0 ? -n : n;\n}\n\nint main(void) {\n    printf("%d\\n", my_abs(-17));\n    printf("%d\\n", my_abs(0));\n    printf("%d\\n", my_abs(42));\n    return 0;\n}\n', 'The conditional operator fits on one line.'),
  d('c-fn-void-banner', 'A void function', 'Write banner(void) printing a row of ten dashes, and call it twice around a message.',
    '#include <stdio.h>\n\nvoid banner(void) {\n    printf("----------\\n");\n}\n\nint main(void) {\n    banner();\n    printf("MENU\\n");\n    banner();\n    return 0;\n}\n', 'A void function returns nothing.'),
  d('c-fn-min3', 'Smallest of three', 'Write min3(int, int, int) and print min3(5, 2, 9) and min3(7, 7, 3).',
    '#include <stdio.h>\n\nint min3(int a, int b, int c) {\n    int m = a < b ? a : b;\n    return m < c ? m : c;\n}\n\nint main(void) {\n    printf("%d\\n", min3(5, 2, 9));\n    printf("%d\\n", min3(7, 7, 3));\n    return 0;\n}\n', 'Compare two, then compare the winner with the third.'),
  d('c-fn-celsius', 'Temperature function', 'Write double to_fahrenheit(int celsius) and print the values for 0, 37 and 100 to one decimal.',
    '#include <stdio.h>\n\ndouble to_fahrenheit(int celsius) {\n    return celsius * 9.0 / 5 + 32;\n}\n\nint main(void) {\n    printf("%.1f\\n", to_fahrenheit(0));\n    printf("%.1f\\n", to_fahrenheit(37));\n    printf("%.1f\\n", to_fahrenheit(100));\n    return 0;\n}\n', 'Use 9.0 to force floating-point division.'),
  d('c-fn-prototype', 'Using a prototype', 'Declare int add(int, int) above main, define it below, and print add(19, 23).',
    '#include <stdio.h>\n\nint add(int a, int b);\n\nint main(void) {\n    printf("%d\\n", add(19, 23));\n    return 0;\n}\n\nint add(int a, int b) {\n    return a + b;\n}\n', 'The prototype ends with a semicolon.'),
  d('c-fn-count-digits', 'Digit counter', 'Write int count_digits(int) and print it for 5, 12345 and 900000.',
    '#include <stdio.h>\n\nint count_digits(int n) {\n    int count = 0;\n    if (n == 0) return 1;\n    while (n > 0) {\n        count++;\n        n /= 10;\n    }\n    return count;\n}\n\nint main(void) {\n    printf("%d\\n", count_digits(5));\n    printf("%d\\n", count_digits(12345));\n    printf("%d\\n", count_digits(900000));\n    return 0;\n}\n', 'Handle zero as a special case.'),
  d('c-fn-is-prime', 'Prime predicate', 'Write int is_prime(int) and print it for 1, 2, 9 and 13.',
    '#include <stdio.h>\n\nint is_prime(int n) {\n    if (n < 2) return 0;\n    for (int d = 2; d * d <= n; d++) {\n        if (n % d == 0) return 0;\n    }\n    return 1;\n}\n\nint main(void) {\n    printf("%d\\n", is_prime(1));\n    printf("%d\\n", is_prime(2));\n    printf("%d\\n", is_prime(9));\n    printf("%d\\n", is_prime(13));\n    return 0;\n}\n', 'Testing divisors only up to the square root is enough.'),
  d('c-fn-recursive-factorial', 'Recursive factorial', 'Write a recursive factorial and print it for 0, 1, 5 and 10.',
    '#include <stdio.h>\n\nlong factorial(int n) {\n    if (n <= 1) return 1;\n    return n * factorial(n - 1);\n}\n\nint main(void) {\n    printf("%ld\\n", factorial(0));\n    printf("%ld\\n", factorial(1));\n    printf("%ld\\n", factorial(5));\n    printf("%ld\\n", factorial(10));\n    return 0;\n}\n', 'Both 0 and 1 give 1, so n <= 1 covers both.'),
  d('c-fn-recursive-fib', 'Recursive Fibonacci', 'Write a recursive fib(int) and print the first ten values on one line.',
    '#include <stdio.h>\n\nint fib(int n) {\n    if (n < 2) return n;\n    return fib(n - 1) + fib(n - 2);\n}\n\nint main(void) {\n    for (int n = 0; n < 10; n++) {\n        printf("%d ", fib(n));\n    }\n    printf("\\n");\n    return 0;\n}\n', 'n < 2 collapses both base cases.'),
  d('c-fn-recursive-digits', 'Recursive digit sum', 'Write a recursive digit_sum(int) and print it for 9875 and 7.',
    '#include <stdio.h>\n\nint digit_sum(int n) {\n    if (n < 10) return n;\n    return n % 10 + digit_sum(n / 10);\n}\n\nint main(void) {\n    printf("%d\\n", digit_sum(9875));\n    printf("%d\\n", digit_sum(7));\n    return 0;\n}\n', 'Peel the last digit and recurse on the rest.'),
  d('c-fn-recursive-gcd', 'Recursive GCD', 'Write a recursive gcd(int, int) and print gcd(48, 36) and gcd(17, 5).',
    '#include <stdio.h>\n\nint gcd(int a, int b) {\n    if (b == 0) return a;\n    return gcd(b, a % b);\n}\n\nint main(void) {\n    printf("%d\\n", gcd(48, 36));\n    printf("%d\\n", gcd(17, 5));\n    return 0;\n}\n', 'The base case is b reaching zero.'),
  d('c-fn-static-counter', 'Static counter', 'Write next_id(void) returning 1, 2, 3 on successive calls, and print four calls.',
    '#include <stdio.h>\n\nint next_id(void) {\n    static int id = 0;\n    return ++id;\n}\n\nint main(void) {\n    printf("%d\\n", next_id());\n    printf("%d\\n", next_id());\n    printf("%d\\n", next_id());\n    printf("%d\\n", next_id());\n    return 0;\n}\n', 'A static local persists between calls.'),
  d('c-fn-pass-by-value', 'Demonstrating copies', 'Write void bump(int n) that adds 10 and prints it, then show the caller\'s variable is unchanged.',
    '#include <stdio.h>\n\nvoid bump(int n) {\n    n += 10;\n    printf("inside: %d\\n", n);\n}\n\nint main(void) {\n    int value = 5;\n    bump(value);\n    printf("outside: %d\\n", value);\n    return 0;\n}\n', 'The function only modifies its own copy.'),
  d('c-fn-multiple-helpers', 'Several helpers', 'Write area(int, int) and perimeter(int, int), then print both for a 7x3 rectangle.',
    '#include <stdio.h>\n\nint area(int w, int h) {\n    return w * h;\n}\n\nint perimeter(int w, int h) {\n    return 2 * (w + h);\n}\n\nint main(void) {\n    printf("area: %d\\n", area(7, 3));\n    printf("perimeter: %d\\n", perimeter(7, 3));\n    return 0;\n}\n', 'Two independent functions above main.'),
  d('c-fn-fizz', 'FizzBuzz classifier', 'Write void classify(int n) printing Fizz, Buzz, FizzBuzz or the number, then call it for 3, 5, 15 and 7.',
    '#include <stdio.h>\n\nvoid classify(int n) {\n    if (n % 15 == 0) {\n        printf("FizzBuzz\\n");\n    } else if (n % 3 == 0) {\n        printf("Fizz\\n");\n    } else if (n % 5 == 0) {\n        printf("Buzz\\n");\n    } else {\n        printf("%d\\n", n);\n    }\n}\n\nint main(void) {\n    classify(3);\n    classify(5);\n    classify(15);\n    classify(7);\n    return 0;\n}\n', 'A void function can print directly.'),
  d('c-fn-nested-calls', 'Calling functions from functions', 'Write double_it(int) and add_one(int), then print double_it(add_one(5)) and add_one(double_it(5)).',
    '#include <stdio.h>\n\nint double_it(int n) {\n    return n * 2;\n}\n\nint add_one(int n) {\n    return n + 1;\n}\n\nint main(void) {\n    printf("%d\\n", double_it(add_one(5)));\n    printf("%d\\n", add_one(double_it(5)));\n    return 0;\n}\n', 'The inner call runs first.'),
  d('c-fn-capstone', 'Function summary', 'Write grade(int) returning a char and report(int) printing the score with its grade, then call report for 95, 82 and 50.',
    '#include <stdio.h>\n\nchar grade(int score) {\n    if (score >= 90) return \'A\';\n    if (score >= 80) return \'B\';\n    if (score >= 70) return \'C\';\n    if (score >= 60) return \'D\';\n    return \'F\';\n}\n\nvoid report(int score) {\n    printf("%d: %c\\n", score, grade(score));\n}\n\nint main(void) {\n    report(95);\n    report(82);\n    report(50);\n    return 0;\n}\n', 'One function decides, the other formats.'),
  {
    kind: 'quiz',
    slug: 'c-functions-quiz',
    title: 'Checkpoint: functions',
    questions: [
      { q: 'Why does void swap(int a, int b) fail to swap the caller\u2019s variables?', options: ['Ints cannot be swapped', 'The parameters are copies of the arguments', 'It needs a return value', 'The parameters must be const'], answer: 1, explain: 'C passes by value, so the function rearranges only its own copies. Passing addresses is the fix.' },
      { q: 'What is a function prototype for?', options: ['Making the function faster', 'Declaring a signature so the function can be called before it is defined', 'Creating a variable', 'Preventing recursion'], answer: 1, explain: 'It lets the compiler check calls that appear before the definition, which is essential across multiple files.' },
      { q: 'What does a static local variable do?', options: ['Becomes global', 'Keeps its value between calls while staying private to the function', 'Cannot be changed', 'Is stored on the heap'], answer: 1, explain: 'It is initialised once and persists for the program\u2019s lifetime, but its name is only visible inside the function.' },
      { q: 'What must every recursive function have?', options: ['Two parameters', 'A base case that returns without recursing', 'A loop', 'A void return type'], answer: 1, explain: 'Without a base case the recursion never terminates and eventually overflows the call stack.' },
      { q: 'What does void mean as a parameter list, as in int main(void)?', options: ['Returns nothing', 'Takes no parameters', 'Takes any parameters', 'Is recursive'], answer: 1, explain: 'In the parameter position void explicitly states that the function accepts no arguments.' },
    ],
  },
];
