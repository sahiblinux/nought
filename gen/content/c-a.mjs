const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body) => `#include <stdio.h>\n\nint main(void) {\n${body}\n    return 0;\n}\n`;

export const cBasics = [
  {
    slug: 'c-first',
    title: 'The shape of a C program',
    subtitle: 'main, printf and #include',
    concept:
      'C is **compiled**: a compiler translates your entire file into machine code before anything runs, and refuses to produce a program if the code is malformed. That feedback loop is slower than Python\u2019s but catches whole categories of mistake before execution.\n\nEvery C program begins at a function called `main`. `int main(void)` means "takes no arguments, returns an integer" \u2014 that integer is the exit status, where `0` means success.\n\n`printf` lives in the standard input/output library, so `#include <stdio.h>` must appear first. Without it the compiler does not know what printf is.\n\nTwo things Python did not need: every statement ends with a **semicolon**, and printf does **not** add a newline \u2014 you write `\\n` yourself.',
    example: '#include <stdio.h>\n\nint main(void) {\n    printf("Hello, world!\\n");\n    return 0;\n}',
    note: 'The braces mark the body of main. Omit the \\n and the next output continues on the same line.',
    task: 'Print exactly `Hello, world!` followed by a newline.',
    solution: M('    printf("Hello, world!\\n");'),
    hint: 'printf("Hello, world!\\n"); \u2014 note the semicolon.',
  },
  {
    slug: 'c-multiple-printf',
    title: 'Several lines of output',
    subtitle: 'Statements and newlines',
    concept:
      'Statements execute in order, top to bottom. Since printf adds nothing of its own, the layout of your output is entirely determined by the `\\n` characters you include.\n\nThis means three printf calls without newlines produce one line, and one printf with two newlines produces two lines. C does not care how your source is arranged \u2014 only what characters you actually emit.',
    example: '#include <stdio.h>\n\nint main(void) {\n    printf("one ");\n    printf("line\\n");\n    printf("two\\nthree\\n");\n    return 0;\n}',
    note: 'The first two calls join into `one line` because neither ended with a newline until the second.',
    task: 'Print these three lines using exactly two printf calls:\n\n`ready`\n`steady`\n`go`',
    solution: M('    printf("ready\\n");\n    printf("steady\\ngo\\n");'),
    hint: 'Put two \\n inside one of the calls.',
  },
  {
    slug: 'c-comments',
    title: 'Notes for humans',
    subtitle: 'Both comment styles',
    concept:
      'C has two comment forms. `//` comments run to the end of the line. `/* ... */` comments can span multiple lines, and are the older style.\n\nBlock comments cannot be nested \u2014 the first `*/` closes the comment, which occasionally bites people trying to comment out a region that already contains one.',
    example: '#include <stdio.h>\n\n/* A block comment,\n   spanning lines. */\nint main(void) {\n    // a line comment\n    printf("visible\\n");   // trailing comment\n    return 0;\n}',
    note: 'Comments are stripped before compilation, so they cannot affect behaviour.',
    task: 'Print `documented code` on one line and `and clear` on the next, including at least one comment of each style.',
    solution: '#include <stdio.h>\n\n/* summary comment */\nint main(void) {\n    // print the two lines\n    printf("documented code\\n");\n    printf("and clear\\n");\n    return 0;\n}\n',
    hint: 'Comments never appear in the output.',
  },
  {
    slug: 'c-escapes',
    title: 'Special characters',
    subtitle: 'Escape sequences',
    concept:
      'Because printf takes a plain string, awkward characters need escaping:\n\n- `\\n` newline\n- `\\t` tab\n- `\\\\` a literal backslash\n- `\\"` a literal double quote\n- `%%` a literal percent sign (printf-specific)\n\nThat last one is unique to printf: `%` starts a format specifier, so printing a real percent sign requires doubling it.',
    example: '#include <stdio.h>\n\nint main(void) {\n    printf("name\\tyear\\n");\n    printf("C\\t1972\\n");\n    printf("He said \\"hi\\"\\n");\n    printf("100%% done\\n");\n    return 0;\n}',
    note: 'The backslash and the doubled percent are instructions to the parser, so they never appear in the output.',
    task: 'Produce these three tab-separated lines:\n\n`lang\tyear`\n`C\t1972`\n`Rust\t2015`',
    solution: M('    printf("lang\\tyear\\n");\n    printf("C\\t1972\\n");\n    printf("Rust\\t2015\\n");'),
    hint: 'Use \\t between the columns and \\n at the end of each line.',
  },
  {
    slug: 'c-printf-format',
    title: 'Inserting values into text',
    subtitle: 'Format specifiers',
    concept:
      'printf\u2019s real purpose is substitution. A **format specifier** in the string marks a slot, and the values follow as extra arguments:\n\n- `%d` int\n- `%f` double (`%.2f` for two decimals)\n- `%c` a single char\n- `%s` a string\n- `%zu` a size_t\n\nThe specifiers must match the arguments **in order and in type**. A mismatch is undefined behaviour \u2014 you may get garbage rather than an error \u2014 which is why compiler warnings matter here.',
    example: '#include <stdio.h>\n\nint main(void) {\n    printf("%d items\\n", 42);\n    printf("%.2f each\\n", 3.5);\n    printf("%s scored %d\\n", "ada", 91);\n    printf("grade %c\\n", \'A\');\n    return 0;\n}',
    note: 'Single quotes mean a char; double quotes mean a string. They are different types in C.',
    task: 'Using one printf per line, print:\n\n`count: 7`\n`price: 2.50`\n`name: coffee`',
    solution: M('    printf("count: %d\\n", 7);\n    printf("price: %.2f\\n", 2.5);\n    printf("name: %s\\n", "coffee");'),
    hint: '%.2f forces exactly two decimal places.',
  },
  {
    slug: 'c-width-align',
    title: 'Lining columns up',
    subtitle: 'Field widths',
    concept:
      'A number between `%` and the type letter sets a **minimum field width**, padding with spaces. `%8d` right-aligns in eight columns; `%-8d` left-aligns. Combined with `%s` this produces neat tables without counting spaces by hand.\n\nFor floats, `%8.2f` means "eight columns wide, two decimal places".',
    example: '#include <stdio.h>\n\nint main(void) {\n    printf("%-10s%5s\\n", "item", "cost");\n    printf("%-10s%5.2f\\n", "coffee", 3.5);\n    printf("%-10s%5.2f\\n", "bagel", 2.25);\n    return 0;\n}',
    note: 'The minus sign left-aligns. Without it, text is pushed to the right of the field.',
    task: 'Print a two-column table with names left-aligned in 8 columns and scores right-aligned in 4, for ada/91, bob/7 and cy/100.',
    solution: M('    printf("%-8s%4d\\n", "ada", 91);\n    printf("%-8s%4d\\n", "bob", 7);\n    printf("%-8s%4d\\n", "cy", 100);'),
    hint: 'Use %-8s for the name and %4d for the number.',
  },
  {
    slug: 'c-return-status',
    title: 'What main gives back',
    subtitle: 'Exit status',
    concept:
      '`return 0;` at the end of main reports success to whatever launched the program. A non-zero value signals failure, and shell scripts use it to decide whether to continue.\n\nSince C99 you may omit the return and 0 is assumed, but writing it is clearer.\n\nThe `void` in `int main(void)` explicitly says "no parameters". The alternative form `int main(int argc, char *argv[])` receives command-line arguments.',
    example: '#include <stdio.h>\n\nint main(void) {\n    printf("working\\n");\n    printf("finished\\n");\n    return 0;\n}',
    note: 'Anything after the return statement never executes.',
    task: 'Print `start`, then `middle`, then `end`, and return 0 explicitly.',
    solution: M('    printf("start\\n");\n    printf("middle\\n");\n    printf("end\\n");'),
    hint: 'The starter already contains the return statement.',
  },
  d('c-banner', 'A star banner', 'Print ten asterisks, then `   WELCOME   `, then ten asterisks again.',
    M('    printf("**********\\n");\n    printf("   WELCOME   \\n");\n    printf("**********\\n");'), 'Count the characters carefully.'),
  d('c-receipt', 'A receipt', 'Print four lines: `ITEM      COST`, `coffee    3.50`, `bagel     2.25`, `TOTAL     5.75`.',
    M('    printf("ITEM      COST\\n");\n    printf("coffee    3.50\\n");\n    printf("bagel     2.25\\n");\n    printf("TOTAL     5.75\\n");'), 'Pad with plain spaces inside the strings.'),
  d('c-quote', 'A quoted saying', 'Print exactly `"Keep it simple."` including the double quotes.',
    M('    printf("\\"Keep it simple.\\"\\n");'), 'Escape each inner quote with a backslash.'),
  d('c-path', 'A backslash path', 'Print exactly `C:\\Users\\ada`.',
    M('    printf("C:\\\\Users\\\\ada\\n");'), 'Each visible backslash needs two in the source.'),
  d('c-percent', 'A percentage', 'Print `progress: 75% complete`.',
    M('    printf("progress: 75%% complete\\n");'), 'Double the percent sign to print it literally.'),
  d('c-triangle', 'Star triangle', 'Print a left-aligned triangle of stars, four rows tall.',
    M('    printf("*\\n");\n    printf("**\\n");\n    printf("***\\n");\n    printf("****\\n");'), 'One printf per row.'),
  d('c-countdown', 'Countdown', 'Print 5 down to 1 on separate lines using %d, then `liftoff`.',
    M('    printf("%d\\n", 5);\n    printf("%d\\n", 4);\n    printf("%d\\n", 3);\n    printf("%d\\n", 2);\n    printf("%d\\n", 1);\n    printf("liftoff\\n");'), 'Use %d for each number.'),
  d('c-format-mix', 'Mixed specifiers', 'In one printf, print `ada is 36 and scored 91.5` using %s, %d and %.1f.',
    M('    printf("%s is %d and scored %.1f\\n", "ada", 36, 91.5);'), 'The arguments follow the format string in order.'),
  d('c-table-wide', 'Wide table', 'Print a header `%-12s%6s` with `product` and `price`, then two rows for cheese/7.40 and honey/5.90.',
    M('    printf("%-12s%6s\\n", "product", "price");\n    printf("%-12s%6.2f\\n", "cheese", 7.4);\n    printf("%-12s%6.2f\\n", "honey", 5.9);'), 'The header uses %s for the price column since it is text.'),
  d('c-box', 'A boxed word', 'Print a box around `CODE` using plus signs, dashes and pipes.',
    M('    printf("+------+\\n");\n    printf("| CODE |\\n");\n    printf("+------+\\n");'), 'Six dashes between the plus signs.'),
  d('c-tabs-grid', 'Tabbed grid', 'Print three rows of three numbers separated by tabs: 1 2 3 / 4 5 6 / 7 8 9.',
    M('    printf("1\\t2\\t3\\n");\n    printf("4\\t5\\t6\\n");\n    printf("7\\t8\\t9\\n");'), 'Tabs align the columns.'),
  d('c-blank-line', 'A gap', 'Print `above`, an empty line, then `below`.',
    M('    printf("above\\n");\n    printf("\\n");\n    printf("below\\n");'), 'A lone \\n produces the empty line.'),
  d('c-arrow', 'An arrow', 'Print a three-line arrow:\n\n`  /\\`\n` /  \\`\n`/____\\`',
    M('    printf("  /\\\\\\n");\n    printf(" /  \\\\\\n");\n    printf("/____\\\\\\n");'), 'Two backslashes in the source produce one in the output.'),
  d('c-char-codes', 'Characters and codes', 'Print the character A using %c and its numeric value using %d, both from the literal \'A\'.',
    M('    printf("%c\\n", \'A\');\n    printf("%d\\n", \'A\');'), 'A char is a small integer, so %d shows its code.'),
  d('c-precision', 'Decimal places', 'Print 3.14159265 with zero, two, four and six decimal places on four lines.',
    M('    printf("%.0f\\n", 3.14159265);\n    printf("%.2f\\n", 3.14159265);\n    printf("%.4f\\n", 3.14159265);\n    printf("%.6f\\n", 3.14159265);'), 'The number after the dot sets the precision.'),
  d('c-checkerboard', 'Checkerboard', 'Print four alternating rows of `#-#-#-#-` and `-#-#-#-#`.',
    M('    printf("#-#-#-#-\\n");\n    printf("-#-#-#-#\\n");\n    printf("#-#-#-#-\\n");\n    printf("-#-#-#-#\\n");'), 'Each row is eight characters.'),
  d('c-labels', 'Labelled values', 'Print `width: 12`, `height: 8` and `area: 96`, computing the area in the printf.',
    M('    printf("width: %d\\n", 12);\n    printf("height: %d\\n", 8);\n    printf("area: %d\\n", 12 * 8);'), 'Expressions are allowed as printf arguments.'),
  d('c-hex-octal', 'Other number bases', 'Print 255 in decimal, hexadecimal and octal using %d, %x and %o.',
    M('    printf("%d\\n", 255);\n    printf("%x\\n", 255);\n    printf("%o\\n", 255);'), '%x gives hex and %o gives octal.'),
  d('c-basics-capstone', 'Basics summary', 'Print a formatted three-line report: a header row of dashes, `lang: C (1972)` using %s and %d, and a footer row of dashes.',
    M('    printf("--------------------\\n");\n    printf("lang: %s (%d)\\n", "C", 1972);\n    printf("--------------------\\n");'), 'Combine plain text and specifiers in one call.'),
  {
    kind: 'quiz',
    slug: 'c-basics-quiz',
    title: 'Checkpoint: first programs',
    questions: [
      { q: 'Why is #include <stdio.h> required?', options: ['It speeds up the program', 'It tells the compiler what printf is', 'It creates the main function', 'It is optional decoration'], answer: 1, explain: 'The header declares printf and friends. Without it the compiler has never heard of the function you are calling.' },
      { q: 'What does return 0 from main mean?', options: ['Print zero', 'The program succeeded', 'The program failed', 'Nothing at all'], answer: 1, explain: 'Zero is the conventional success exit status; non-zero signals failure to whatever launched the program.' },
      { q: 'Does printf add a newline?', options: ['Yes, always', 'No \u2014 you must include \\n yourself', 'Only for strings', 'Only on Windows'], answer: 1, explain: 'Unlike Python\u2019s print, printf emits exactly the characters you specify and nothing more.' },
      { q: 'How do you print a literal percent sign with printf?', options: ['%', '\\%', '%%', 'It cannot be done'], answer: 2, explain: 'A single % begins a format specifier, so it must be doubled to produce one in the output.' },
      { q: 'What does %-8s do?', options: ['Right-aligns in 8 columns', 'Left-aligns in 8 columns', 'Prints 8 copies', 'Subtracts 8'], answer: 1, explain: 'The width sets a minimum field size and the minus sign switches the padding to the right, left-aligning the text.' },
    ],
  },
];

export const cTypes = [
  {
    slug: 'c-vars',
    title: 'Declaring variables',
    subtitle: 'Types are fixed',
    concept:
      'In C every variable\u2019s type is decided at declaration and never changes. You write the type, then the name: `int count = 5;`\n\nThe types you need first:\n- `int` \u2014 whole number, typically 32 bits\n- `double` \u2014 floating point with good precision\n- `char` \u2014 a single character, in single quotes\n\nA declaration without an initialiser leaves **garbage** in the variable, not zero. Reading an uninitialised variable is undefined behaviour, so always initialise.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int year = 1972;\n    double pi = 3.14159;\n    char grade = \'A\';\n\n    printf("%d %.2f %c\\n", year, pi, grade);\n\n    year = 2026;\n    printf("%d\\n", year);\n    return 0;\n}',
    note: 'The type is permanent; the value is not. year can be reassigned but always holds an int.',
    task: 'Declare `int items = 7;` and `double price = 2.5;`, compute the total as a double, and print:\n\n`items: 7`\n`price: 2.50`\n`total: 17.50`',
    given: 'int items = 7;\ndouble price = 2.5;',
    solution: M('    int items = 7;\n    double price = 2.5;\n    double total = items * price;\n\n    printf("items: %d\\n", items);\n    printf("price: %.2f\\n", price);\n    printf("total: %.2f\\n", total);'),
    hint: 'int * double promotes to double, so declare total as double.',
  },
  {
    slug: 'c-int-division',
    title: 'The integer division trap',
    subtitle: 'Casting between types',
    concept:
      'This catches everyone once. Dividing two `int`s performs **integer division**: the fractional part is discarded, not rounded. `7 / 2` is `3`, full stop.\n\nTo get a real answer, at least one operand must be floating point. The usual tool is a **cast**: `(double)a / b` treats a as a double for that expression, so the division happens in floating point.\n\n`%` gives the remainder and works only on integers.',
    example: '#include <stdio.h>\n\nint main(void) {\n    int a = 7, b = 2;\n\n    printf("%d\\n", a / b);\n    printf("%d\\n", a % b);\n    printf("%.3f\\n", (double)a / b);\n    printf("%.3f\\n", a / (double)b);\n    return 0;\n}',
    note: 'Casting either operand is enough \u2014 the other is promoted automatically.',
    task: 'With `int total = 47;` and `int people = 6;` print the whole share, the remainder, and the exact average to three decimals.',
    given: 'int total = 47;\nint people = 6;',
    solution: M('    int total = 47;\n    int people = 6;\n\n    printf("each: %d\\n", total / people);\n    printf("left: %d\\n", total % people);\n    printf("average: %.3f\\n", (double)total / people);'),
    hint: 'The first line wants the truncating division; the third needs a cast.',
  },
  {
    slug: 'c-sizes',
    title: 'How much space a type takes',
    subtitle: 'sizeof and ranges',
    concept:
      'Every type occupies a fixed number of bytes, and `sizeof(type)` reports it. Typical values on a 64-bit system: `char` 1, `int` 4, `double` 8, pointers 8.\n\nThe C standard sets only **minimums**, so sizes can differ between platforms \u2014 which is exactly why `sizeof` exists rather than hard-coded numbers.\n\n`sizeof` returns a `size_t`, printed with `%zu`. It becomes essential later for arrays and memory allocation.',
    example: '#include <stdio.h>\n\nint main(void) {\n    printf("char   %zu\\n", sizeof(char));\n    printf("int    %zu\\n", sizeof(int));\n    printf("double %zu\\n", sizeof(double));\n    printf("long   %zu\\n", sizeof(long));\n    return 0;\n}',
    note: 'sizeof is evaluated at compile time, so it costs nothing at run time.',
    task: 'Print the sizes of char, short, int and double, each on its own line as `int 4`.',
    solution: M('    printf("char %zu\\n", sizeof(char));\n    printf("short %zu\\n", sizeof(short));\n    printf("int %zu\\n", sizeof(int));\n    printf("double %zu\\n", sizeof(double));'),
    hint: 'Use %zu for every sizeof result.',
  },
  {
    slug: 'c-const',
    title: 'Values that must not change',
    subtitle: 'const and #define',
    concept:
      'Marking a variable `const` promises it will never be reassigned, and the compiler enforces it. That turns an accidental modification into a compile error rather than a mysterious bug.\n\n`#define NAME value` is the older alternative: a preprocessor substitution performed before compilation. It has no type and no scope, so `const` is preferred in modern C.\n\nBy convention, both are named in capitals.',
    example: '#include <stdio.h>\n\n#define MAX_USERS 100\n\nint main(void) {\n    const double TAX_RATE = 0.2;\n    int subtotal = 80;\n\n    printf("max %d\\n", MAX_USERS);\n    printf("tax %.2f\\n", subtotal * TAX_RATE);\n    return 0;\n}',
    note: 'Attempting TAX_RATE = 0.3; would refuse to compile, which is the entire point.',
    task: 'Define `SECONDS_PER_DAY` as 86400 with #define, declare `const int days = 3;`, and print the total seconds.',
    solution: '#include <stdio.h>\n\n#define SECONDS_PER_DAY 86400\n\nint main(void) {\n    const int days = 3;\n    printf("total: %d\\n", days * SECONDS_PER_DAY);\n    return 0;\n}\n',
    hint: 'The #define goes above main, outside any function.',
  },
  {
    slug: 'c-char-ints',
    title: 'Characters are numbers',
    subtitle: 'char arithmetic and ASCII',
    concept:
      'A `char` is a one-byte integer holding a character **code**. `\'A\'` is 65 and `\'a\'` is 97 in ASCII, which means arithmetic on characters is meaningful.\n\n`\'a\' - 32` gives `\'A\'`, and `ch - \'0\'` converts a digit character to its numeric value. `\'A\' + 1` is `\'B\'`.\n\nPrinting the same char with `%c` shows the letter and with `%d` shows the code \u2014 same value, different interpretation.',
    example: '#include <stdio.h>\n\nint main(void) {\n    char upper = \'A\';\n    char lower = upper + 32;\n\n    printf("%c %d\\n", upper, upper);\n    printf("%c %d\\n", lower, lower);\n\n    char digit = \'7\';\n    printf("%d\\n", digit - \'0\');\n    return 0;\n}',
    note: 'digit - \'0\' works because the digit codes are consecutive: \'7\' is exactly seven past \'0\'.',
    task: 'Given `char ch = \'d\';` print the character, its code, its uppercase form, and the numeric value of the digit character `\'9\'`.',
    given: "char ch = 'd';",
    solution: M('    char ch = \'d\';\n\n    printf("%c\\n", ch);\n    printf("%d\\n", ch);\n    printf("%c\\n", ch - 32);\n    printf("%d\\n", \'9\' - \'0\');'),
    hint: 'Subtract 32 to convert lowercase to uppercase.',
  },
  {
    slug: 'c-overflow',
    title: 'When numbers run out of room',
    subtitle: 'Overflow and unsigned types',
    concept:
      'An `int` has a fixed range, roughly \u00b12.1 billion. Exceeding it is **overflow**, and for signed types it is undefined behaviour \u2014 the compiler is entitled to assume it never happens.\n\n`unsigned int` cannot be negative and wraps around predictably instead. `long long` gives a much larger range when you need it.\n\nThe practical lesson: choose a type wide enough for the values you expect, and be wary of subtracting from unsigned values, because the result can never go below zero.',
    example: '#include <stdio.h>\n#include <limits.h>\n\nint main(void) {\n    printf("int max  %d\\n", INT_MAX);\n    printf("int min  %d\\n", INT_MIN);\n\n    unsigned int u = 0;\n    u = u - 1;\n    printf("wrapped  %u\\n", u);\n\n    long long big = 9000000000LL;\n    printf("big      %lld\\n", big);\n    return 0;\n}',
    note: 'Subtracting one from an unsigned zero wraps to the maximum value \u2014 a classic loop bug.',
    task: 'Print INT_MAX, INT_MIN, and the result of adding 1 to an unsigned int holding 0, using the correct specifiers.',
    solution: '#include <stdio.h>\n#include <limits.h>\n\nint main(void) {\n    unsigned int u = 0;\n\n    printf("%d\\n", INT_MAX);\n    printf("%d\\n", INT_MIN);\n    printf("%u\\n", u + 1);\n    return 0;\n}\n',
    hint: 'Include <limits.h> for the INT_MAX macros and use %u for unsigned.',
  },
  d('c-type-decl', 'Four declarations', 'Declare an int 42, a double 2.718, a char \'Z\' and a long 100000, then print all four with the right specifiers.',
    M('    int i = 42;\n    double dd = 2.718;\n    char c = \'Z\';\n    long l = 100000;\n\n    printf("%d\\n", i);\n    printf("%.3f\\n", dd);\n    printf("%c\\n", c);\n    printf("%ld\\n", l);'), 'A long uses %ld.'),
  d('c-rect-area', 'Rectangle area', 'With int w = 7 and h = 3, print the area and perimeter.',
    M('    int w = 7, h = 3;\n    printf("area: %d\\n", w * h);\n    printf("perimeter: %d\\n", 2 * (w + h));'), 'Declare both on one line with a comma.'),
  d('c-swap-vars', 'Swapping two values', 'With int a = 3 and b = 8, swap them using a temporary variable and print before and after.',
    M('    int a = 3, b = 8;\n    printf("before: %d %d\\n", a, b);\n    int tmp = a;\n    a = b;\n    b = tmp;\n    printf("after: %d %d\\n", a, b);'), 'C has no tuple assignment, so a temporary is required.'),
  d('c-cast-average', 'Exact average', 'With int sum = 17 and count = 4, print the truncating division and the exact average to two decimals.',
    M('    int sum = 17, count = 4;\n    printf("%d\\n", sum / count);\n    printf("%.2f\\n", (double)sum / count);'), 'Cast one operand for the exact result.'),
  d('c-double-to-int', 'Truncating a double', 'With double value = 9.87, print it, its int cast, and its int cast plus one.',
    M('    double value = 9.87;\n    printf("%.2f\\n", value);\n    printf("%d\\n", (int)value);\n    printf("%d\\n", (int)value + 1);'), 'Casting to int discards the fraction rather than rounding.'),
  d('c-seconds', 'Seconds in a period', 'With int days = 2, hours = 5 and minutes = 30, print the total seconds.',
    M('    int days = 2, hours = 5, minutes = 30;\n    int total = days * 86400 + hours * 3600 + minutes * 60;\n    printf("seconds: %d\\n", total);'), 'A day is 86400 seconds.'),
  d('c-celsius', 'Temperature conversion', 'With int c = 37, print the Fahrenheit value to one decimal place.',
    M('    int c = 37;\n    printf("%.1f\\n", c * 9.0 / 5 + 32);'), 'Using 9.0 rather than 9 forces floating-point arithmetic.'),
  d('c-const-circle', 'Circle with a constant', 'Declare const double PI = 3.14159 and int r = 5, then print the circumference and area to two decimals.',
    M('    const double PI = 3.14159;\n    int r = 5;\n    printf("%.2f\\n", 2 * PI * r);\n    printf("%.2f\\n", PI * r * r);'), 'C has no power operator, so square by multiplying.'),
  d('c-sizeof-report', 'Size report', 'Print the sizes of int, long, double and char in that order, each as `int: 4`.',
    M('    printf("int: %zu\\n", sizeof(int));\n    printf("long: %zu\\n", sizeof(long));\n    printf("double: %zu\\n", sizeof(double));\n    printf("char: %zu\\n", sizeof(char));'), 'sizeof results need %zu.'),
  d('c-char-upper', 'Case conversion by arithmetic', 'With char ch = \'q\', print it, its uppercase form and its code, using arithmetic only.',
    M('    char ch = \'q\';\n    printf("%c\\n", ch);\n    printf("%c\\n", ch - 32);\n    printf("%d\\n", ch);'), 'The gap between cases is exactly 32.'),
  d('c-digit-value', 'Digit characters to numbers', 'Convert the characters \'3\' and \'8\' to their numeric values and print their sum.',
    M('    char a = \'3\', b = \'8\';\n    printf("%d\\n", (a - \'0\') + (b - \'0\'));'), 'Subtracting \'0\' maps a digit character to its value.'),
  d('c-divmod', 'Quotient and remainder', 'With int total = 100 and per_box = 12, print the number of full boxes and the leftover.',
    M('    int total = 100, per_box = 12;\n    printf("boxes: %d\\n", total / per_box);\n    printf("left: %d\\n", total % per_box);'), 'Integer division already truncates.'),
  d('c-unsigned-wrap', 'Unsigned wraparound', 'Declare unsigned int u = 3, subtract 5 from it, and print the result with %u.',
    M('    unsigned int u = 3;\n    u = u - 5;\n    printf("%u\\n", u);'), 'Unsigned values cannot go negative, so they wrap.'),
  d('c-mixed-arith', 'Mixed arithmetic', 'With int i = 7 and double dd = 2.0, print i / 2, i / dd and (int)(i / dd).',
    M('    int i = 7;\n    double dd = 2.0;\n    printf("%d\\n", i / 2);\n    printf("%.2f\\n", i / dd);\n    printf("%d\\n", (int)(i / dd));'), 'Mixing int and double promotes the int automatically.'),
  d('c-bmi', 'A small formula', 'With double weight = 70 and height = 1.75, print the BMI to one decimal place.',
    M('    double weight = 70, height = 1.75;\n    printf("%.1f\\n", weight / (height * height));'), 'Square the height by multiplying it by itself.'),
  d('c-price-tax', 'Adding tax', 'With double price = 80 and const double RATE = 0.25, print the tax and the total to two decimals.',
    M('    double price = 80;\n    const double RATE = 0.25;\n    double tax = price * RATE;\n    printf("tax: %.2f\\n", tax);\n    printf("total: %.2f\\n", price + tax);'), 'Store the tax so you can reuse it.'),
  d('c-average-three', 'Average of three ints', 'With three ints 88, 94 and 79, print the total and the average to two decimals.',
    M('    int a = 88, b = 94, c = 79;\n    int total = a + b + c;\n    printf("total: %d\\n", total);\n    printf("average: %.2f\\n", total / 3.0);'), 'Dividing by 3.0 rather than 3 avoids truncation.'),
  d('c-types-capstone', 'Types summary', 'With int items = 13 and double unit = 4.75, print the item count, the unit price, the total to two decimals, the whole pounds as an int, and the size of a double.',
    M('    int items = 13;\n    double unit = 4.75;\n    double total = items * unit;\n\n    printf("items: %d\\n", items);\n    printf("unit: %.2f\\n", unit);\n    printf("total: %.2f\\n", total);\n    printf("whole: %d\\n", (int)total);\n    printf("double size: %zu\\n", sizeof(double));'), 'Cast the total to int to truncate it.'),
  {
    kind: 'quiz',
    slug: 'c-types-quiz',
    title: 'Checkpoint: types',
    questions: [
      { q: 'What is 7 / 2 when both are int?', options: ['3.5', '3', '4', 'undefined'], answer: 1, explain: 'Integer division truncates toward zero, discarding the fraction entirely.' },
      { q: 'How do you get 3.5 from int a = 7, b = 2?', options: ['a / b', '(double)a / b', 'a % b', 'sizeof(a) / b'], answer: 1, explain: 'Casting one operand forces the division to happen in floating point; the other is promoted automatically.' },
      { q: 'What is in an uninitialised local int?', options: ['Zero', 'Whatever garbage was in that memory', 'NULL', 'A compile error'], answer: 1, explain: 'C does not zero local variables, so reading one before assignment is undefined behaviour.' },
      { q: 'What does \'7\' - \'0\' give?', options: ['7', '55', '\'7\'', 'An error'], answer: 0, explain: 'Digit character codes are consecutive, so subtracting \'0\' converts a digit character to its numeric value.' },
      { q: 'Which specifier prints a sizeof result?', options: ['%d', '%f', '%zu', '%s'], answer: 2, explain: 'sizeof yields a size_t, whose matching specifier is %zu. Using %d is a type mismatch.' },
    ],
  },
];
