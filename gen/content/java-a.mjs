const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body) => `public class Main {\n    public static void main(String[] args) {\n${body}\n    }\n}\n`;

export const javaBasics = [
  {
    slug: 'java-first',
    title: 'Everything lives in a class',
    subtitle: 'The class wrapper and main',
    concept:
      'Java has one firm rule: all code lives inside a **class**. Even a one-line program needs a class to hold it.\n\nExecution begins at a method with this exact signature:\n\n`public static void main(String[] args)`\n\nEvery word matters. `public` means callable from outside, `static` means it belongs to the class rather than an instance, `void` means it returns nothing, and `String[] args` receives command-line arguments.\n\n`System.out.println(...)` prints a line. Statements end with semicolons and blocks use braces. The ceremony looks heavy for one line, but it is the same structure a million-line codebase uses.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        System.out.println("Hello, world!");\n        System.out.println("Two lines, two calls.");\n    }\n}',
    note: 'The class must be named Main here, because that is the filename the runner uses.',
    task: 'Print exactly `Hello, world!` from inside main.',
    solution: M('        System.out.println("Hello, world!");'),
    hint: 'System.out.println("Hello, world!"); \u2014 note the semicolon.',
  },
  {
    slug: 'java-print-vs-println',
    title: 'With or without a newline',
    subtitle: 'print, println and printf',
    concept:
      'Three output methods, each with a distinct purpose.\n\n`println(x)` prints and adds a newline. `print(x)` prints without one, so the next output continues on the same line. `printf(format, args...)` uses format specifiers like C, and needs an explicit `%n` for the newline.\n\n`println` with no argument prints just a blank line. Prefer `printf` when you need alignment or a fixed number of decimals; prefer `println` for everything else.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        System.out.print("no newline...");\n        System.out.println("same line");\n        System.out.println();\n        System.out.printf("%s scored %d%n", "ada", 91);\n        System.out.printf("%.2f%n", 3.14159);\n    }\n}',
    note: 'printf without %n leaves the cursor mid-line, which is a common surprise.',
    task: 'Using print and println, build the single line `red-green-blue`, then use printf to print `count: 7` and `price: 2.50`.',
    solution: M('        System.out.print("red-");\n        System.out.print("green-");\n        System.out.println("blue");\n        System.out.printf("count: %d%n", 7);\n        System.out.printf("price: %.2f%n", 2.5);'),
    hint: 'Only the last piece of the joined line needs println.',
  },
  {
    slug: 'java-comments',
    title: 'Notes for humans',
    subtitle: 'Three comment styles',
    concept:
      'Java has three comment forms. `//` runs to the end of the line. `/* ... */` spans multiple lines. `/** ... */` is a **Javadoc** comment, which tooling extracts into API documentation.\n\nJavadoc is the distinctive one: placed above a class or method, it uses tags like `@param` and `@return` and becomes the official documentation. The entire Java standard library reference is generated this way.',
    example: '/**\n * Demonstrates the three comment styles.\n */\npublic class Main {\n    /* A block comment,\n       spanning lines. */\n    public static void main(String[] args) {\n        // a line comment\n        System.out.println("documented");   // trailing comment\n    }\n}',
    note: 'Comments are stripped before compilation, so they can never affect behaviour.',
    task: 'Print `well documented` and `and clear` on two lines, including a Javadoc comment above the class and a line comment inside main.',
    solution: '/**\n * A small documented program.\n */\npublic class Main {\n    public static void main(String[] args) {\n        // print the two lines\n        System.out.println("well documented");\n        System.out.println("and clear");\n    }\n}\n',
    hint: 'The Javadoc comment starts with /** and sits above the class declaration.',
  },
  {
    slug: 'java-escapes',
    title: 'Special characters',
    subtitle: 'Escape sequences',
    concept:
      'Awkward characters inside a string literal need escaping:\n\n- `\\n` newline\n- `\\t` tab\n- `\\\\` a literal backslash\n- `\\"` a literal double quote\n\nSince `println` already adds a newline, `\\n` is used to create additional line breaks inside one string. A single `println` containing two `\\n` characters therefore produces three lines.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        System.out.println("lang\\tyear");\n        System.out.println("Java\\t1995");\n        System.out.println("She said \\"hello\\"");\n        System.out.println("C:\\\\Users\\\\ada");\n        System.out.println("one\\ntwo");\n    }\n}',
    note: 'The backslash is an instruction to the compiler, so it never appears in the output.',
    task: 'Produce these three tab-separated lines using exactly two println calls:\n\n`lang\tyear`\n`Java\t1995`\n`Rust\t2015`',
    solution: M('        System.out.println("lang\\tyear");\n        System.out.println("Java\\t1995\\nRust\\t2015");'),
    hint: 'Put the \\n inside the second string to get two lines from one call.',
  },
  {
    slug: 'java-printf-format',
    title: 'Formatted output',
    subtitle: 'printf specifiers and widths',
    concept:
      '`printf` takes a format string with **specifiers** filled by the following arguments:\n\n- `%s` any object, via its toString\n- `%d` integer\n- `%f` floating point, `%.2f` for two decimals\n- `%b` boolean\n- `%n` platform newline\n\nA width before the type sets a minimum field: `%10s` right-aligns in ten columns, `%-10s` left-aligns. That is how tables are produced without counting spaces.\n\nUnlike C, a mismatch throws an exception at run time rather than producing garbage.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        System.out.printf("%-10s%5s%n", "item", "cost");\n        System.out.printf("%-10s%5.2f%n", "coffee", 3.5);\n        System.out.printf("%-10s%5.2f%n", "bagel", 2.25);\n        System.out.printf("%d items, %b available%n", 7, true);\n    }\n}',
    note: 'The minus sign left-aligns; without it the text is pushed right within the field.',
    task: 'Print a table with names left-aligned in 8 columns and scores right-aligned in 4, for ada/91, bob/7 and cy/100.',
    solution: M('        System.out.printf("%-8s%4d%n", "ada", 91);\n        System.out.printf("%-8s%4d%n", "bob", 7);\n        System.out.printf("%-8s%4d%n", "cy", 100);'),
    hint: 'Use %-8s for the name and %4d for the number.',
  },
  {
    slug: 'java-compilation',
    title: 'How Java runs',
    subtitle: 'Bytecode and the JVM',
    concept:
      'Java compiles in two stages. `javac` turns source into **bytecode** \u2014 a portable instruction set stored in `.class` files. The **JVM** then executes that bytecode, compiling the hot parts to native code as the program runs.\n\nThat is what "write once, run anywhere" means: the bytecode is identical on every platform, and only the JVM differs.\n\nOne practical consequence: a public class must live in a file matching its name, so `public class Main` belongs in `Main.java`. Another is that Java catches type errors at compile time, like C++, rather than at run time like Python.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        System.out.println("compiled to bytecode");\n        System.out.println("executed by the JVM");\n\n        String version = System.getProperty("java.version");\n        System.out.println(version != null);\n    }\n}',
    note: 'System.getProperty reads JVM properties \u2014 evidence that the program is running inside a managed runtime.',
    task: 'Print three lines: `source`, `bytecode` and `jvm`, then print whether the java.version property is non-null.',
    solution: M('        System.out.println("source");\n        System.out.println("bytecode");\n        System.out.println("jvm");\n        System.out.println(System.getProperty("java.version") != null);'),
    hint: 'Compare the property against null and print the resulting boolean.',
  },
  d('java-b-hello', 'A single line', 'Print `Java is verbose` on one line.',
    M('        System.out.println("Java is verbose");'), 'One println call.'),
  d('java-b-three-lines', 'Three lines', 'Print `ready`, `steady` and `go` on separate lines.',
    M('        System.out.println("ready");\n        System.out.println("steady");\n        System.out.println("go");'), 'Three println calls.'),
  d('java-b-concat', 'Joining values', 'In one println, print `count: 7 grade: A` using concatenation with an int and a char.',
    M('        System.out.println("count: " + 7 + " grade: " + \'A\');'), 'The + operator converts each value to text.'),
  d('java-b-expressions', 'Computed values', 'Print the results of 17 + 25, 8 * 9 and 100 - 37 on three lines.',
    M('        System.out.println(17 + 25);\n        System.out.println(8 * 9);\n        System.out.println(100 - 37);'), 'Expressions are evaluated before printing.'),
  d('java-b-banner', 'A star banner', 'Print ten asterisks, then `   WELCOME   `, then ten asterisks.',
    M('        System.out.println("**********");\n        System.out.println("   WELCOME   ");\n        System.out.println("**********");'), 'Three separate lines.'),
  d('java-b-tabs', 'A tabbed table', 'Print a header `id\tname` then rows `1\tada` and `2\tgrace`.',
    M('        System.out.println("id\\tname");\n        System.out.println("1\\tada");\n        System.out.println("2\\tgrace");'), 'Use \\t between the columns.'),
  d('java-b-quotes', 'Quoted text', 'Print exactly `"Keep it simple."` including the double quotes.',
    M('        System.out.println("\\"Keep it simple.\\"");'), 'Escape each inner quote.'),
  d('java-b-backslash', 'A Windows path', 'Print exactly `C:\\Users\\ada\\projects`.',
    M('        System.out.println("C:\\\\Users\\\\ada\\\\projects");'), 'Each visible backslash needs two in the source.'),
  d('java-b-blank', 'A blank line', 'Print `above`, an empty line, then `below`.',
    M('        System.out.println("above");\n        System.out.println();\n        System.out.println("below");'), 'println with no argument prints just a newline.'),
  d('java-b-print-chain', 'Building one line', 'Using print and println, produce the single line `a-b-c`.',
    M('        System.out.print("a-");\n        System.out.print("b-");\n        System.out.println("c");'), 'print omits the newline.'),
  d('java-b-printf-basic', 'Basic printf', 'Using printf, print `ada is 36 and scored 91.5` with %s, %d and %.1f.',
    M('        System.out.printf("%s is %d and scored %.1f%n", "ada", 36, 91.5);'), 'Remember the %n at the end.'),
  d('java-b-printf-width', 'Field widths', 'Print a table with products left-aligned in 12 columns and prices to two decimals right-aligned in 7, for cheese/7.40 and honey/5.90.',
    M('        System.out.printf("%-12s%7.2f%n", "cheese", 7.4);\n        System.out.printf("%-12s%7.2f%n", "honey", 5.9);'), 'The minus sign left-aligns.'),
  d('java-b-printf-precision', 'Decimal places', 'Print 3.14159265 with zero, two, four and six decimal places.',
    M('        System.out.printf("%.0f%n", 3.14159265);\n        System.out.printf("%.2f%n", 3.14159265);\n        System.out.printf("%.4f%n", 3.14159265);\n        System.out.printf("%.6f%n", 3.14159265);'), 'The number after the dot sets the precision.'),
  d('java-b-printf-bool', 'Printing booleans', 'Using printf with %b, print true and false on two lines.',
    M('        System.out.printf("%b%n", true);\n        System.out.printf("%b%n", false);'), '%b formats a boolean.'),
  d('java-b-box', 'A boxed word', 'Print a box around `CODE` using plus signs, dashes and pipes.',
    M('        System.out.println("+------+");\n        System.out.println("| CODE |");\n        System.out.println("+------+");'), 'Six dashes between the plus signs.'),
  d('java-b-triangle', 'A star triangle', 'Print a four-row left-aligned triangle of stars.',
    M('        System.out.println("*");\n        System.out.println("**");\n        System.out.println("***");\n        System.out.println("****");'), 'One println per row.'),
  d('java-b-receipt', 'A receipt', 'Print four lines: `ITEM      COST`, `coffee    3.50`, `bagel     2.25`, `TOTAL     5.75`.',
    M('        System.out.println("ITEM      COST");\n        System.out.println("coffee    3.50");\n        System.out.println("bagel     2.25");\n        System.out.println("TOTAL     5.75");'), 'Pad with spaces inside the strings.'),
  d('java-b-string-format', 'String.format', 'Use String.format to build the text `id=7 name=widget` into a variable, then print it.',
    M('        String line = String.format("id=%d name=%s", 7, "widget");\n        System.out.println(line);'), 'String.format returns the text instead of printing it.'),
  d('java-b-multiline-one-call', 'Several lines, one call', 'Using a single println, print three lines: `one`, `two`, `three`.',
    M('        System.out.println("one\\ntwo\\nthree");'), 'Two \\n characters create the extra breaks.'),
  d('java-b-capstone', 'Basics summary', 'Print a row of twenty dashes, then `lang: Java (1995)` using printf with %s and %d, then another row of dashes, then 7.0/3.0 to three decimals.',
    M('        System.out.println("--------------------");\n        System.out.printf("lang: %s (%d)%n", "Java", 1995);\n        System.out.println("--------------------");\n        System.out.printf("%.3f%n", 7.0 / 3.0);'), 'Use 7.0 to force floating-point division.'),
  {
    kind: 'quiz',
    slug: 'java-basics-quiz',
    title: 'Checkpoint: classes and output',
    questions: [
      { q: 'Why must main be declared static?', options: ['So it runs faster', 'So the JVM can call it without creating an instance of the class', 'Because it returns void', 'Because it takes an array'], answer: 1, explain: 'static members belong to the class itself, so the JVM can invoke main before any object exists.' },
      { q: 'What is the difference between print and println?', options: ['None', 'println adds a newline afterwards', 'print is faster', 'println takes no arguments'], answer: 1, explain: 'print leaves the cursor on the same line, which is how several calls can build one line of output.' },
      { q: 'What does printf need that println does not?', options: ['A semicolon', 'An explicit %n for the newline', 'A String argument', 'Nothing'], answer: 1, explain: 'printf emits exactly what the format string specifies, so the newline must be requested with %n.' },
      { q: 'What does javac produce?', options: ['Machine code', 'Bytecode in .class files, executed by the JVM', 'A shell script', 'Nothing'], answer: 1, explain: 'The portable bytecode is what makes the same compiled program run on any platform with a JVM.' },
      { q: 'What must be true of a file containing public class Main?', options: ['Nothing', 'It must be named Main.java', 'It must be in a package', 'It must have a main method'], answer: 1, explain: 'A public class must live in a file whose name matches the class exactly, including capitalisation.' },
    ],
  },
];

export const javaTypes = [
  {
    slug: 'java-primitives',
    title: 'The eight primitive types',
    subtitle: 'Declaring variables',
    concept:
      'Java has exactly eight **primitive** types, and every variable\u2019s type is fixed at declaration.\n\nThe ones you need first:\n- `int` \u2014 whole number, 32 bits\n- `double` \u2014 floating point, 64 bits\n- `boolean` \u2014 `true` or `false`, a real type unlike in C\n- `char` \u2014 a single character in single quotes\n\nThe others are `byte`, `short`, `long` and `float`. A `long` literal needs an `L` suffix and a `float` literal an `f`, because the defaults are `int` and `double`.\n\nUnlike a local variable in C, Java refuses to compile if you read a local before assigning it \u2014 a genuinely useful restriction.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        int count = 42;\n        double ratio = 2.5;\n        boolean ready = true;\n        char grade = \'A\';\n        long big = 9000000000L;\n        float small = 1.5f;\n\n        System.out.println(count + " " + ratio + " " + ready);\n        System.out.println(grade + " " + big + " " + small);\n    }\n}',
    note: 'Without the L suffix, 9000000000 would be an int literal and would not fit, causing a compile error.',
    task: 'Declare an int of 7, a double of 2.5, a boolean of true and a char of `Z`, then print each on its own line.',
    solution: M('        int count = 7;\n        double price = 2.5;\n        boolean ready = true;\n        char grade = \'Z\';\n\n        System.out.println(count);\n        System.out.println(price);\n        System.out.println(ready);\n        System.out.println(grade);'),
    hint: 'Char literals use single quotes; strings use double quotes.',
  },
  {
    slug: 'java-strings-objects',
    title: 'String is not a primitive',
    subtitle: 'Objects and references',
    concept:
      '`String` starts with a capital letter because it is a **class**, not a primitive. A String variable holds a **reference** to an object rather than the value itself.\n\nThat distinction has one immediate consequence: comparing Strings with `==` compares references, asking "are these the same object". Use `.equals()` to compare the characters. This is the single most common Java beginner bug.\n\nStrings are also **immutable** \u2014 every method returns a new String rather than modifying the original.\n\nString concatenation with `+` converts the other operand automatically, evaluating strictly left to right: `1 + 2 + "x"` is `3x`, but `"x" + 1 + 2` is `x12`.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        String name = "Java";\n        String other = "Ja" + "va";\n\n        System.out.println(name.equals(other));\n        System.out.println(name.length());\n        System.out.println(name.toUpperCase());\n        System.out.println(name);\n\n        System.out.println(1 + 2 + "x");\n        System.out.println("x" + 1 + 2);\n    }\n}',
    note: 'toUpperCase returned a new String; name itself was unchanged, as the last print of name shows.',
    task: 'Declare `String word = "java"`. Print it, its length, its uppercase form, the original again to show immutability, and whether it equals `"java"`.',
    given: 'String word = "java";',
    solution: M('        String word = "java";\n\n        System.out.println(word);\n        System.out.println(word.length());\n        System.out.println(word.toUpperCase());\n        System.out.println(word);\n        System.out.println(word.equals("java"));'),
    hint: 'Compare with .equals(), never with ==.',
  },
  {
    slug: 'java-var',
    title: 'Letting the compiler infer',
    subtitle: 'The var keyword',
    concept:
      'Since Java 10, `var` lets the compiler deduce a local variable\u2019s type from its initialiser. The variable is still **statically typed** \u2014 you simply avoided repeating something obvious.\n\n`var names = new ArrayList<String>();` is much better than writing the type twice.\n\nThe limits: `var` works only for local variables with an initialiser, never for fields, parameters or return types. And it cannot be `null` on its own, since there would be nothing to infer from.\n\nUse it when the type is obvious from the right-hand side; write the type out when naming it documents intent.',
    example: 'import java.util.ArrayList;\n\npublic class Main {\n    public static void main(String[] args) {\n        var count = 42;\n        var ratio = 2.5;\n        var name = "inferred";\n        var items = new ArrayList<String>();\n\n        items.add("first");\n        items.add("second");\n\n        System.out.println(count + " " + ratio + " " + name);\n        System.out.println(items.size());\n        System.out.println(items);\n    }\n}',
    note: 'var items avoided writing ArrayList<String> twice on one line, which is its clearest use.',
    task: 'Using var, declare an int, a double, a String and an ArrayList<String> with two elements added. Print all four.',
    solution: 'import java.util.ArrayList;\n\npublic class Main {\n    public static void main(String[] args) {\n        var n = 10;\n        var d = 1.5;\n        var s = "text";\n        var list = new ArrayList<String>();\n        list.add("a");\n        list.add("b");\n\n        System.out.println(n);\n        System.out.println(d);\n        System.out.println(s);\n        System.out.println(list);\n    }\n}\n',
    hint: 'var always needs an initialiser to infer from.',
  },
  {
    slug: 'java-casting',
    title: 'Converting between types',
    subtitle: 'Widening, narrowing and integer division',
    concept:
      'Java converts **widening** types automatically \u2014 an int becomes a double without asking, since nothing is lost. **Narrowing** requires an explicit cast, because data may be lost: `(int) 3.9` gives `3`, truncating rather than rounding.\n\nThe integer division trap is present as ever: `7 / 2` is `3` when both operands are int. Cast one to double to get `3.5`.\n\nFor strings, `Integer.parseInt("42")` and `Double.parseDouble("2.5")` parse text into numbers and **throw** `NumberFormatException` on failure. `String.valueOf(n)` or concatenating with `""` goes the other way.',
    example: 'public class Main {\n    public static void main(String[] args) {\n        int a = 7, b = 2;\n\n        System.out.println(a / b);\n        System.out.println((double) a / b);\n        System.out.println((int) 3.9);\n\n        double widened = a;\n        System.out.println(widened);\n\n        int parsed = Integer.parseInt("123");\n        System.out.println(parsed + 1);\n        System.out.println(String.valueOf(456) + "!");\n    }\n}',
    note: 'Widening from int to double needed no cast; narrowing from double to int did.',
    task: 'With `int total = 47` and `int people = 6`, print the truncating division, the exact average via a cast, that average truncated back to an int, and the result of parsing `"250"` plus one.',
    given: 'int total = 47;\nint people = 6;',
    solution: M('        int total = 47;\n        int people = 6;\n\n        System.out.println(total / people);\n        System.out.println((double) total / people);\n        System.out.println((int) ((double) total / people));\n        System.out.println(Integer.parseInt("250") + 1);'),
    hint: 'Casting one operand is enough; the other is promoted automatically.',
  },
  {
    slug: 'java-wrappers',
    title: 'Objects wrapping primitives',
    subtitle: 'Wrapper classes and autoboxing',
    concept:
      'Every primitive has a matching **wrapper class**: `Integer` for int, `Double` for double, `Boolean` for boolean, `Character` for char.\n\nWrappers are needed because collections hold objects, not primitives \u2014 `ArrayList<int>` is illegal, so you write `ArrayList<Integer>`. Java converts between the two automatically, which is called **autoboxing** and **unboxing**.\n\nWrappers also carry useful statics: `Integer.MAX_VALUE`, `Integer.parseInt`, `Double.parseDouble`.\n\nThe trap: `==` on two Integer objects compares references. Small values are cached so it sometimes appears to work, which makes the bug worse. Always use `.equals()` or compare the unboxed `intValue()`.',
    example: 'import java.util.ArrayList;\n\npublic class Main {\n    public static void main(String[] args) {\n        Integer boxed = 42;\n        int unboxed = boxed;\n\n        System.out.println(boxed + " " + unboxed);\n        System.out.println(Integer.MAX_VALUE);\n        System.out.println(Integer.MIN_VALUE);\n\n        var numbers = new ArrayList<Integer>();\n        numbers.add(1);\n        numbers.add(2);\n        System.out.println(numbers);\n\n        Integer a = 1000, b = 1000;\n        System.out.println(a == b);\n        System.out.println(a.equals(b));\n    }\n}',
    note: 'a == b was false despite equal values, because the two Integer objects are distinct. That is why .equals matters.',
    task: 'Declare an Integer holding 100 and unbox it into an int, printing both. Print Integer.MAX_VALUE. Add two Integers to an ArrayList<Integer> and print it. Then compare two Integers holding 500 with both == and .equals().',
    solution: 'import java.util.ArrayList;\n\npublic class Main {\n    public static void main(String[] args) {\n        Integer boxed = 100;\n        int plain = boxed;\n\n        System.out.println(boxed);\n        System.out.println(plain);\n        System.out.println(Integer.MAX_VALUE);\n\n        var list = new ArrayList<Integer>();\n        list.add(5);\n        list.add(10);\n        System.out.println(list);\n\n        Integer a = 500, b = 500;\n        System.out.println(a == b);\n        System.out.println(a.equals(b));\n    }\n}\n',
    hint: 'Autoboxing means you can add an int literal directly to an ArrayList<Integer>.',
  },
  {
    slug: 'java-final-constants',
    title: 'Values that must not change',
    subtitle: 'final',
    concept:
      '`final` on a variable means it can be assigned exactly once. Attempting to reassign it is a compile error.\n\nConstants are conventionally `static final` fields in UPPER_SNAKE_CASE, declared at class level. `static` means one shared copy, `final` means unchangeable.\n\nOne subtlety worth understanding: `final` on a reference prevents **repointing**, not modification. A `final List` cannot be reassigned to a different list, but items can still be added to it. Immutability of the object is a separate concern from finality of the reference.',
    example: 'public class Main {\n    static final int MAX_USERS = 100;\n    static final double TAX_RATE = 0.2;\n\n    public static void main(String[] args) {\n        final int limit = 50;\n        int subtotal = 80;\n\n        System.out.println(MAX_USERS);\n        System.out.println(limit);\n        System.out.printf("%.2f%n", subtotal * TAX_RATE);\n\n        final StringBuilder sb = new StringBuilder("start");\n        sb.append(" and more");\n        System.out.println(sb);\n    }\n}',
    note: 'The final StringBuilder could still be appended to \u2014 final locked the reference, not the object.',
    task: 'Declare `static final int SECONDS_PER_DAY = 86400` at class level and a `final int days = 3` inside main, then print the constant, the local, and the total seconds.',
    solution: 'public class Main {\n    static final int SECONDS_PER_DAY = 86400;\n\n    public static void main(String[] args) {\n        final int days = 3;\n\n        System.out.println(SECONDS_PER_DAY);\n        System.out.println(days);\n        System.out.println(days * SECONDS_PER_DAY);\n    }\n}\n',
    hint: 'Class-level constants go outside main but inside the class.',
  },
  d('java-t-declare', 'Four declarations', 'Declare an int 42, a double 2.718, a char Z and a boolean false, then print all four.',
    M('        int i = 42;\n        double dd = 2.718;\n        char c = \'Z\';\n        boolean b = false;\n\n        System.out.println(i);\n        System.out.println(dd);\n        System.out.println(c);\n        System.out.println(b);'), 'Char literals use single quotes.'),
  d('java-t-long-float', 'Long and float literals', 'Declare a long of 9000000000 and a float of 1.5, then print both.',
    M('        long big = 9000000000L;\n        float small = 1.5f;\n        System.out.println(big);\n        System.out.println(small);'), 'The L and f suffixes are required.'),
  d('java-t-rect', 'Rectangle measurements', 'With int w = 7 and h = 3, print the area and perimeter.',
    M('        int w = 7, h = 3;\n        System.out.println("area: " + w * h);\n        System.out.println("perimeter: " + 2 * (w + h));'), 'Parenthesise the sum before doubling.'),
  d('java-t-string-methods', 'String basics', 'For "keyboard", print the string, its length, its uppercase form and its first character.',
    M('        String w = "keyboard";\n        System.out.println(w);\n        System.out.println(w.length());\n        System.out.println(w.toUpperCase());\n        System.out.println(w.charAt(0));'), 'charAt returns a char at the given index.'),
  d('java-t-equals', 'Comparing strings', 'Print whether "abc" equals "abc" with .equals, whether "cat" equals "Cat", and whether "cat" equals "Cat" ignoring case.',
    M('        System.out.println("abc".equals("abc"));\n        System.out.println("cat".equals("Cat"));\n        System.out.println("cat".equalsIgnoreCase("Cat"));'), 'equalsIgnoreCase exists for case-insensitive comparison.'),
  d('java-t-concat-order', 'Concatenation order', 'Print 1 + 2 + "x" and "x" + 1 + 2 to see the difference.',
    M('        System.out.println(1 + 2 + "x");\n        System.out.println("x" + 1 + 2);'), 'Evaluation runs strictly left to right.'),
  d('java-t-var', 'Using var', 'With var, declare an int, a double and a String, then print all three.',
    M('        var n = 5;\n        var d = 0.5;\n        var s = "inferred";\n        System.out.println(n);\n        System.out.println(d);\n        System.out.println(s);'), 'var needs an initialiser.'),
  d('java-t-int-division', 'Integer versus real division', 'With int a = 22 and b = 7, print a / b, a % b and the exact quotient to four decimals.',
    M('        int a = 22, b = 7;\n        System.out.println(a / b);\n        System.out.println(a % b);\n        System.out.printf("%.4f%n", (double) a / b);'), 'Cast one operand for the exact result.'),
  d('java-t-truncate', 'Truncating a double', 'With double value = 9.87, print it, its int cast, and Math.round of it.',
    M('        double value = 9.87;\n        System.out.println(value);\n        System.out.println((int) value);\n        System.out.println(Math.round(value));'), 'A cast truncates while Math.round rounds.'),
  d('java-t-parse', 'Parsing strings', 'Parse "150" and "2.5" and print their product, then print 99 converted to a String joined with "!".',
    M('        System.out.println(Integer.parseInt("150") * Double.parseDouble("2.5"));\n        System.out.println(String.valueOf(99) + "!");'), 'parseInt and parseDouble come from the wrapper classes.'),
  d('java-t-wrapper-limits', 'Type limits', 'Print Integer.MAX_VALUE, Integer.MIN_VALUE and Long.MAX_VALUE.',
    M('        System.out.println(Integer.MAX_VALUE);\n        System.out.println(Integer.MIN_VALUE);\n        System.out.println(Long.MAX_VALUE);'), 'The wrapper classes expose these constants.'),
  d('java-t-autoboxing', 'Autoboxing', 'Assign an int to an Integer and back, printing both, then add two int literals to an ArrayList<Integer> and print it.',
    'import java.util.ArrayList;\n\npublic class Main {\n    public static void main(String[] args) {\n        Integer boxed = 7;\n        int plain = boxed;\n        System.out.println(boxed);\n        System.out.println(plain);\n\n        var list = new ArrayList<Integer>();\n        list.add(1);\n        list.add(2);\n        System.out.println(list);\n    }\n}\n', 'Conversion happens automatically in both directions.'),
  d('java-t-integer-equals', 'Comparing Integers', 'Compare two Integers holding 1000 with == and with .equals, printing both results.',
    M('        Integer a = 1000, b = 1000;\n        System.out.println(a == b);\n        System.out.println(a.equals(b));'), '== compares references for objects.'),
  d('java-t-char-arithmetic', 'Characters as numbers', 'Print the char A, its int value, and the character three places later.',
    M('        char c = \'A\';\n        System.out.println(c);\n        System.out.println((int) c);\n        System.out.println((char) (c + 3));'), 'Cast back to char to display the letter.'),
  d('java-t-final', 'Final variables', 'Declare a static final constant of 60 at class level and a final local of 7, then print their product.',
    'public class Main {\n    static final int MINUTES_PER_HOUR = 60;\n\n    public static void main(String[] args) {\n        final int hours = 7;\n        System.out.println(MINUTES_PER_HOUR * hours);\n    }\n}\n', 'Class-level constants sit outside main.'),
  d('java-t-celsius', 'Temperature conversion', 'With int c = 37, print the Fahrenheit value to one decimal place.',
    M('        int c = 37;\n        System.out.printf("%.1f%n", c * 9.0 / 5 + 32);'), 'Using 9.0 forces floating-point arithmetic.'),
  d('java-t-seconds', 'Total seconds', 'With ints days = 2, hours = 5 and minutes = 30, print the total number of seconds.',
    M('        int days = 2, hours = 5, minutes = 30;\n        System.out.println(days * 86400 + hours * 3600 + minutes * 60);'), 'A day is 86400 seconds.'),
  d('java-t-average', 'Average of three', 'With ints 88, 94 and 79, print the total and the average to two decimals.',
    M('        int a = 88, b = 94, c = 79;\n        int total = a + b + c;\n        System.out.println(total);\n        System.out.printf("%.2f%n", total / 3.0);'), 'Divide by 3.0 to avoid integer division.'),
  d('java-t-math', 'The Math class', 'Print Math.abs(-17), Math.max(3, 9), Math.pow(2, 10) and Math.sqrt(144).',
    M('        System.out.println(Math.abs(-17));\n        System.out.println(Math.max(3, 9));\n        System.out.println(Math.pow(2, 10));\n        System.out.println(Math.sqrt(144));'), 'Math.pow and Math.sqrt return doubles.'),
  d('java-t-capstone', 'Types summary', 'With int items = 13 and double unit = 4.75, print the count, the unit price to two decimals, the total to two decimals, the whole part as an int, and Integer.MAX_VALUE.',
    M('        int items = 13;\n        double unit = 4.75;\n        double total = items * unit;\n\n        System.out.println(items);\n        System.out.printf("%.2f%n", unit);\n        System.out.printf("%.2f%n", total);\n        System.out.println((int) total);\n        System.out.println(Integer.MAX_VALUE);'), 'Cast the total to int to truncate it.'),
  {
    kind: 'quiz',
    slug: 'java-types-quiz',
    title: 'Checkpoint: types',
    questions: [
      { q: 'How should two Strings be compared for equal contents?', options: ['a == b', 'a.equals(b)', 'a.compare(b)', 'a === b'], answer: 1, explain: '== compares references for objects, so it asks whether they are the same object rather than whether the text matches.' },
      { q: 'What does "x" + 1 + 2 produce?', options: ['x3', 'x12', '3x', 'A compile error'], answer: 1, explain: 'Evaluation is left to right: "x" + 1 gives "x1", then + 2 gives "x12".' },
      { q: 'Why does ArrayList<int> not compile?', options: ['A syntax error', 'Collections hold objects, so the wrapper Integer is required', 'int is too small', 'It does compile'], answer: 1, explain: 'Generics work only with reference types, which is exactly why the wrapper classes exist.' },
      { q: 'What does (int) 3.9 give?', options: ['4', '3', '3.9', 'A compile error'], answer: 1, explain: 'A narrowing cast truncates toward zero. Math.round would give 4.' },
      { q: 'What does final on a reference variable prevent?', options: ['Modifying the object', 'Reassigning the variable to a different object', 'Both', 'Neither'], answer: 1, explain: 'final locks the reference, not the object, so a final List can still have items added to it.' },
    ],
  },
];
