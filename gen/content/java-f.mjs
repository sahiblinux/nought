const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });
const M = (body) => `public class Main {\n    public static void main(String[] args) {\n${body}\n    }\n}\n`;

export const javaInheritance = [
  {
    slug: 'java-extends',
    title: 'Building on an existing class',
    subtitle: 'extends and super',
    concept:
      '`class Dog extends Animal` makes Dog a **subclass**, inheriting every accessible member of Animal.\n\n`super(...)` calls the parent constructor and must be the **first statement** in the subclass constructor. If you omit it, Java inserts an implicit `super()`, which fails to compile when the parent has no no-argument constructor.\n\n`super.method()` calls the parent\u2019s version of an overridden method, which is how a subclass extends behaviour rather than replacing it.\n\nJava allows only **single inheritance** of classes \u2014 one parent each \u2014 which avoids the ambiguities of multiple inheritance. Interfaces provide the multiple-type flexibility instead.',
    example: 'class Animal {\n    protected String name;\n\n    Animal(String name) {\n        this.name = name;\n    }\n\n    String describe() {\n        return "animal " + name;\n    }\n}\n\nclass Dog extends Animal {\n    private String breed;\n\n    Dog(String name, String breed) {\n        super(name);\n        this.breed = breed;\n    }\n\n    @Override\n    String describe() {\n        return super.describe() + ", a " + breed;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println(new Animal("thing").describe());\n        System.out.println(new Dog("Rex", "collie").describe());\n    }\n}',
    note: 'Dog.describe called super.describe() and added to it, extending rather than discarding the parent behaviour.',
    task: 'Define a `Vehicle` class with a wheels field, a constructor and a `describe()` method. Define `Car extends Vehicle` adding a brand, calling super, and overriding describe to append the brand. Print both descriptions.',
    solution: 'class Vehicle {\n    protected int wheels;\n\n    Vehicle(int wheels) {\n        this.wheels = wheels;\n    }\n\n    String describe() {\n        return wheels + " wheels";\n    }\n}\n\nclass Car extends Vehicle {\n    private String brand;\n\n    Car(int wheels, String brand) {\n        super(wheels);\n        this.brand = brand;\n    }\n\n    @Override\n    String describe() {\n        return super.describe() + ", " + brand;\n    }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println(new Vehicle(2).describe());\n        System.out.println(new Car(4, "Volvo").describe());\n    }\n}\n',
    hint: 'super(...) must be the first statement in the subclass constructor.',
  },
  {
    slug: 'java-polymorphism',
    title: 'One reference, many types',
    subtitle: 'Overriding and dynamic dispatch',
    concept:
      'A subclass instance can be held in a parent-typed variable: `Animal a = new Dog(...)`. When you call an overridden method through it, Java runs the **subclass** version.\n\nThat is **dynamic dispatch**, and unlike C++ it is the default \u2014 there is no `virtual` keyword because every method is virtual unless marked `final`, `static` or `private`.\n\nThe consequence is powerful: a loop over `List<Animal>` calls each object\u2019s own behaviour without knowing the concrete types.\n\nThe `@Override` annotation is optional but valuable: it makes the compiler verify that you really are overriding something, catching signature typos that would otherwise create an unrelated method.',
    example: 'import java.util.List;\n\nclass Shape {\n    double area() { return 0; }\n    String name() { return "shape"; }\n\n    void report() {\n        System.out.println(name() + " " + area());\n    }\n}\n\nclass Square extends Shape {\n    private double side;\n    Square(double side) { this.side = side; }\n\n    @Override double area() { return side * side; }\n    @Override String name() { return "square"; }\n}\n\nclass Circle extends Shape {\n    private double radius;\n    Circle(double radius) { this.radius = radius; }\n\n    @Override double area() { return 3.14159 * radius * radius; }\n    @Override String name() { return "circle"; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Shape> shapes = List.of(new Square(4), new Circle(1), new Shape());\n        for (Shape s : shapes) s.report();\n    }\n}',
    note: 'report is defined only on Shape, yet each object\u2019s own area and name were used inside it.',
    task: 'Define an `Animal` class with a `speak()` returning `"..."` and a `describe()` printing `name says sound`. Derive Dog and Cat overriding speak. Put all three in a List<Animal> and describe each in one loop.',
    solution: 'import java.util.List;\n\nclass Animal {\n    protected String name;\n\n    Animal(String name) { this.name = name; }\n\n    String speak() { return "..."; }\n\n    void describe() {\n        System.out.println(name + " says " + speak());\n    }\n}\n\nclass Dog extends Animal {\n    Dog(String name) { super(name); }\n    @Override String speak() { return "woof"; }\n}\n\nclass Cat extends Animal {\n    Cat(String name) { super(name); }\n    @Override String speak() { return "meow"; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Animal> pets = List.of(new Animal("thing"), new Dog("Rex"), new Cat("Tom"));\n        for (Animal a : pets) a.describe();\n    }\n}\n',
    hint: 'The loop never needs to know which subclass each object is.',
  },
  {
    slug: 'java-abstract',
    title: 'A class that cannot be instantiated',
    subtitle: 'abstract classes and methods',
    concept:
      'An `abstract` class cannot be instantiated \u2014 it exists to be extended. It may contain both **abstract methods** (declared without a body) and concrete ones.\n\nAn abstract method forces every concrete subclass to provide an implementation, which the compiler enforces. That is stronger than a default implementation returning nothing, because forgetting to override becomes an error rather than a silent bug.\n\nThe pattern this enables is the **template method**: a concrete method in the abstract class defines the overall algorithm and calls abstract methods for the parts that vary.\n\nUse an abstract class when subclasses share state and partial behaviour; use an interface when they share only a contract.',
    example: 'import java.util.List;\n\nabstract class Employee {\n    protected String name;\n\n    Employee(String name) { this.name = name; }\n\n    abstract double monthlyPay();\n\n    void payslip() {\n        System.out.printf("%s: %.2f%n", name, monthlyPay());\n    }\n}\n\nclass Salaried extends Employee {\n    private double annual;\n    Salaried(String name, double annual) {\n        super(name);\n        this.annual = annual;\n    }\n    @Override double monthlyPay() { return annual / 12; }\n}\n\nclass Hourly extends Employee {\n    private double rate;\n    private int hours;\n    Hourly(String name, double rate, int hours) {\n        super(name);\n        this.rate = rate;\n        this.hours = hours;\n    }\n    @Override double monthlyPay() { return rate * hours; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Employee> staff = List.of(\n            new Salaried("Ada", 60000),\n            new Hourly("Bob", 25, 160)\n        );\n        for (Employee e : staff) e.payslip();\n    }\n}',
    note: 'payslip is the template method: it defines the format once and delegates the varying calculation.',
    task: 'Define an abstract `Shape` class with a name field, an abstract `area()` and a concrete `report()` printing the name and area to two decimals. Derive Square and Rect, put both in a list and report each.',
    solution: 'import java.util.List;\n\nabstract class Shape {\n    protected String name;\n\n    Shape(String name) { this.name = name; }\n\n    abstract double area();\n\n    void report() {\n        System.out.printf("%s %.2f%n", name, area());\n    }\n}\n\nclass Square extends Shape {\n    private double side;\n    Square(double side) {\n        super("square");\n        this.side = side;\n    }\n    @Override double area() { return side * side; }\n}\n\nclass Rect extends Shape {\n    private double w;\n    private double h;\n    Rect(double w, double h) {\n        super("rect");\n        this.w = w;\n        this.h = h;\n    }\n    @Override double area() { return w * h; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Shape> shapes = List.of(new Square(4), new Rect(3, 5));\n        for (Shape s : shapes) s.report();\n    }\n}\n',
    hint: 'The abstract method has no body, just a semicolon.',
  },
  {
    slug: 'java-interfaces',
    title: 'A contract without implementation',
    subtitle: 'interface and implements',
    concept:
      'An `interface` declares methods a class promises to provide. Unlike a class, a type can `implements` **several** interfaces, which is how Java gets the flexibility of multiple inheritance without its ambiguities.\n\nInterface methods are implicitly public and abstract. Since Java 8 an interface may also have `default` methods with a body, allowing new methods to be added without breaking existing implementors, and `static` helper methods.\n\nThe distinction from an abstract class: an interface describes what a type **can do**, an abstract class describes what a type **is**. A class extends one thing and implements many.',
    example: 'import java.util.List;\n\ninterface Drawable {\n    String draw();\n\n    default String describe() {\n        return "drawable: " + draw();\n    }\n}\n\ninterface Sizeable {\n    double area();\n}\n\nclass Square implements Drawable, Sizeable {\n    private double side;\n    Square(double side) { this.side = side; }\n\n    @Override public String draw() { return "[]"; }\n    @Override public double area() { return side * side; }\n}\n\nclass Dot implements Drawable {\n    @Override public String draw() { return "."; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Drawable> items = List.of(new Square(3), new Dot());\n        for (Drawable dr : items) System.out.println(dr.describe());\n\n        Sizeable s = new Square(4);\n        System.out.println(s.area());\n    }\n}',
    note: 'Square implements two interfaces at once, and inherited describe() from the default method without writing it.',
    task: 'Define an interface `Payable` with `double amount()` and a default `summary()` returning `owed: N`. Define two classes implementing it differently, put them in a List<Payable> and print each summary.',
    solution: 'import java.util.List;\n\ninterface Payable {\n    double amount();\n\n    default String summary() {\n        return "owed: " + amount();\n    }\n}\n\nclass Invoice implements Payable {\n    private double total;\n    Invoice(double total) { this.total = total; }\n    @Override public double amount() { return total; }\n}\n\nclass Subscription implements Payable {\n    private double monthly;\n    private int months;\n    Subscription(double monthly, int months) {\n        this.monthly = monthly;\n        this.months = months;\n    }\n    @Override public double amount() { return monthly * months; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Payable> items = List.of(new Invoice(250.0), new Subscription(9.99, 12));\n        for (Payable p : items) System.out.println(p.summary());\n    }\n}\n',
    hint: 'Interface implementations must be declared public.',
  },
  {
    slug: 'java-comparable',
    title: 'Making your type sortable',
    subtitle: 'Comparable and Comparator',
    concept:
      'Implementing `Comparable<T>` gives your class a **natural ordering**, so `Collections.sort` and `TreeSet` work on it without extra arguments.\n\n`compareTo(other)` must return a negative number when this comes first, zero when equal, and positive otherwise. `Integer.compare(a, b)` produces exactly that, and avoids the overflow that `a - b` risks.\n\nA `Comparator` is the alternative: an **external** ordering, so you can sort the same type several different ways. Use Comparable for the one obvious ordering and Comparators for everything else.',
    example: 'import java.util.ArrayList;\nimport java.util.Collections;\nimport java.util.Comparator;\nimport java.util.List;\n\nclass Student implements Comparable<Student> {\n    String name;\n    int score;\n\n    Student(String name, int score) {\n        this.name = name;\n        this.score = score;\n    }\n\n    @Override\n    public int compareTo(Student other) {\n        return Integer.compare(score, other.score);\n    }\n\n    @Override\n    public String toString() { return name + ":" + score; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Student> list = new ArrayList<>(List.of(\n            new Student("bob", 78),\n            new Student("ada", 91),\n            new Student("cy", 65)\n        ));\n\n        Collections.sort(list);\n        System.out.println(list);\n\n        list.sort(Comparator.comparing((Student s) -> s.name));\n        System.out.println(list);\n\n        System.out.println(Collections.max(list));\n    }\n}',
    note: 'Collections.sort needed no comparator because Student has a natural ordering; the second sort supplied a different one.',
    task: 'Define a `Book` class implementing Comparable<Book> ordered by pages, with a toString. Sort a list naturally and print it, then sort by title with a Comparator and print again, then print the maximum.',
    solution: 'import java.util.ArrayList;\nimport java.util.Collections;\nimport java.util.Comparator;\nimport java.util.List;\n\nclass Book implements Comparable<Book> {\n    String title;\n    int pages;\n\n    Book(String title, int pages) {\n        this.title = title;\n        this.pages = pages;\n    }\n\n    @Override\n    public int compareTo(Book other) {\n        return Integer.compare(pages, other.pages);\n    }\n\n    @Override\n    public String toString() { return title + "(" + pages + ")"; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Book> shelf = new ArrayList<>(List.of(\n            new Book("Ulysses", 730),\n            new Book("Anthem", 105),\n            new Book("Dune", 412)\n        ));\n\n        Collections.sort(shelf);\n        System.out.println(shelf);\n\n        shelf.sort(Comparator.comparing((Book b) -> b.title));\n        System.out.println(shelf);\n\n        System.out.println(Collections.max(shelf));\n    }\n}\n',
    hint: 'Integer.compare avoids the overflow risk of subtraction.',
  },
  {
    slug: 'java-instanceof-casting',
    title: 'Checking and narrowing types',
    subtitle: 'instanceof and pattern matching',
    concept:
      'Assigning a subclass to a parent-typed variable is **upcasting** and always safe. Going the other way is **downcasting** and needs a check, or it throws `ClassCastException`.\n\n`instanceof` tests the actual type. Java 16 added **pattern matching**, which combines the test and the cast:\n\n`if (shape instanceof Square sq) { ... sq.getSide() ... }`\n\nThe variable is in scope only where the test succeeded, so the cast can never be wrong.\n\nA design note: frequent `instanceof` chains usually mean the behaviour belongs on the classes as an overridden method instead. Polymorphism is the better tool when you can use it.',
    example: 'import java.util.List;\n\nclass Shape { }\n\nclass Square extends Shape {\n    double side;\n    Square(double side) { this.side = side; }\n}\n\nclass Circle extends Shape {\n    double radius;\n    Circle(double radius) { this.radius = radius; }\n}\n\npublic class Main {\n    static String describe(Shape s) {\n        if (s instanceof Square sq) {\n            return "square of " + sq.side;\n        }\n        if (s instanceof Circle c) {\n            return "circle of " + c.radius;\n        }\n        return "unknown shape";\n    }\n\n    public static void main(String[] args) {\n        List<Shape> shapes = List.of(new Square(3), new Circle(2), new Shape());\n        for (Shape s : shapes) System.out.println(describe(s));\n\n        Object o = "text";\n        System.out.println(o instanceof String);\n    }\n}',
    note: 'The pattern variable sq exists only inside the successful branch, so no separate cast statement is needed.',
    task: 'Define a Shape base with Square and Circle subclasses holding a side and a radius. Write a static method using instanceof pattern matching to describe each, and call it on a list of all three types.',
    solution: 'import java.util.List;\n\nclass Shape { }\n\nclass Square extends Shape {\n    double side;\n    Square(double side) { this.side = side; }\n}\n\nclass Circle extends Shape {\n    double radius;\n    Circle(double radius) { this.radius = radius; }\n}\n\npublic class Main {\n    static String describe(Shape s) {\n        if (s instanceof Square sq) return "square " + sq.side;\n        if (s instanceof Circle c) return "circle " + c.radius;\n        return "plain shape";\n    }\n\n    public static void main(String[] args) {\n        List<Shape> shapes = List.of(new Square(4), new Circle(1.5), new Shape());\n        for (Shape s : shapes) System.out.println(describe(s));\n    }\n}\n',
    hint: 'The pattern variable is declared inside the instanceof test.',
  },
  d('java-i-extends', 'Basic inheritance', 'Define Animal with a speak() returning "..." and Dog overriding it, then print both.',
    'class Animal {\n    String speak() { return "..."; }\n}\n\nclass Dog extends Animal {\n    @Override String speak() { return "woof"; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println(new Animal().speak());\n        System.out.println(new Dog().speak());\n    }\n}\n', 'The subclass method with the same signature replaces the parent one.'),
  d('java-i-super-ctor', 'Calling the parent constructor', 'Define Vehicle(wheels) and Car(wheels, brand) calling super, then print both fields.',
    'class Vehicle {\n    protected int wheels;\n    Vehicle(int wheels) { this.wheels = wheels; }\n}\n\nclass Car extends Vehicle {\n    private String brand;\n    Car(int wheels, String brand) {\n        super(wheels);\n        this.brand = brand;\n    }\n    String getBrand() { return brand; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Car c = new Car(4, "Volvo");\n        System.out.println(c.wheels);\n        System.out.println(c.getBrand());\n    }\n}\n', 'super(...) must come first.'),
  d('java-i-super-method', 'Extending a method', 'Define a base describe() and a subclass overriding it to call super.describe() and append text.',
    'class Base {\n    String describe() { return "base"; }\n}\n\nclass Child extends Base {\n    @Override String describe() { return super.describe() + " and child"; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println(new Child().describe());\n    }\n}\n', 'super.method() reaches the parent version.'),
  d('java-i-polymorphic-list', 'One loop, many types', 'Define three Shape subclasses with an area() override and print each area in a single loop over a List<Shape>.',
    'import java.util.List;\n\nclass Shape {\n    double area() { return 0; }\n}\n\nclass Sq extends Shape {\n    double s;\n    Sq(double s) { this.s = s; }\n    @Override double area() { return s * s; }\n}\n\nclass Rect extends Shape {\n    double w, h;\n    Rect(double w, double h) { this.w = w; this.h = h; }\n    @Override double area() { return w * h; }\n}\n\nclass Tri extends Shape {\n    double b, h;\n    Tri(double b, double h) { this.b = b; this.h = h; }\n    @Override double area() { return b * h / 2; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Shape> shapes = List.of(new Sq(2), new Rect(3, 5), new Tri(6, 4));\n        for (Shape s : shapes) System.out.println(s.area());\n    }\n}\n', 'The loop needs no knowledge of the concrete types.'),
  d('java-i-upcast', 'Holding a subclass in a parent variable', 'Assign a Dog to an Animal variable and call the overridden method to show dynamic dispatch.',
    'class Animal {\n    String speak() { return "..."; }\n}\n\nclass Dog extends Animal {\n    @Override String speak() { return "woof"; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Animal a = new Dog();\n        System.out.println(a.speak());\n    }\n}\n', 'The object\'s actual type decides which method runs.'),
  d('java-i-override-annotation', 'The Override annotation', 'Define a base and a subclass using @Override, and print the result through a parent reference.',
    'class Base {\n    int value() { return 1; }\n}\n\nclass Derived extends Base {\n    @Override int value() { return 2; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Base b = new Derived();\n        System.out.println(b.value());\n    }\n}\n', '@Override makes the compiler verify the signature.'),
  d('java-i-protected', 'Protected members', 'Define a base with a protected field and a subclass reading it in its own method.',
    'class Base {\n    protected String label = "shared";\n}\n\nclass Child extends Base {\n    String describe() { return "label is " + label; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println(new Child().describe());\n    }\n}\n', 'protected is visible to subclasses.'),
  d('java-i-abstract-basic', 'An abstract class', 'Define an abstract Shape with an abstract area(), derive two concrete shapes, and print both areas.',
    'import java.util.List;\n\nabstract class Shape {\n    abstract double area();\n}\n\nclass Sq extends Shape {\n    double s;\n    Sq(double s) { this.s = s; }\n    @Override double area() { return s * s; }\n}\n\nclass Rect extends Shape {\n    double w, h;\n    Rect(double w, double h) { this.w = w; this.h = h; }\n    @Override double area() { return w * h; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        for (Shape s : List.of(new Sq(3), new Rect(2, 6))) {\n            System.out.println(s.area());\n        }\n    }\n}\n', 'An abstract method has no body.'),
  d('java-i-abstract-template', 'A template method', 'Define an abstract class with a concrete report() calling an abstract calculate(), then two subclasses, and report each.',
    'import java.util.List;\n\nabstract class Task {\n    protected String name;\n    Task(String name) { this.name = name; }\n\n    abstract int cost();\n\n    void report() {\n        System.out.println(name + ": " + cost());\n    }\n}\n\nclass Simple extends Task {\n    Simple() { super("simple"); }\n    @Override int cost() { return 1; }\n}\n\nclass Complex extends Task {\n    Complex() { super("complex"); }\n    @Override int cost() { return 10; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        for (Task t : List.of(new Simple(), new Complex())) t.report();\n    }\n}\n', 'The concrete method defines the shape, the abstract one the detail.'),
  d('java-i-abstract-cannot-instantiate', 'Abstract classes cannot be instantiated', 'Define an abstract class with a concrete subclass, and instantiate only the subclass.',
    'abstract class Base {\n    abstract String name();\n}\n\nclass Concrete extends Base {\n    @Override String name() { return "concrete"; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Base b = new Concrete();\n        System.out.println(b.name());\n    }\n}\n', 'Only concrete subclasses can be created.'),
  d('java-i-interface-basic', 'A simple interface', 'Define an interface with one method, implement it in two classes, and call it through the interface type in a loop.',
    'import java.util.List;\n\ninterface Greeter {\n    String greet();\n}\n\nclass English implements Greeter {\n    @Override public String greet() { return "hello"; }\n}\n\nclass French implements Greeter {\n    @Override public String greet() { return "bonjour"; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        for (Greeter g : List.of(new English(), new French())) {\n            System.out.println(g.greet());\n        }\n    }\n}\n', 'Interface implementations must be public.'),
  d('java-i-interface-multiple', 'Implementing two interfaces', 'Define two interfaces and a class implementing both, then call a method through each interface type.',
    'interface Named {\n    String name();\n}\n\ninterface Sized {\n    int size();\n}\n\nclass Box implements Named, Sized {\n    @Override public String name() { return "box"; }\n    @Override public int size() { return 10; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Box b = new Box();\n        Named n = b;\n        Sized s = b;\n        System.out.println(n.name());\n        System.out.println(s.size());\n    }\n}\n', 'A class can implement any number of interfaces.'),
  d('java-i-interface-default', 'A default method', 'Define an interface with an abstract method and a default method using it, then implement it and call both.',
    'interface Priced {\n    double price();\n\n    default String label() { return "costs " + price(); }\n}\n\nclass Item implements Priced {\n    @Override public double price() { return 4.5; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Item i = new Item();\n        System.out.println(i.price());\n        System.out.println(i.label());\n    }\n}\n', 'The default method is inherited without being written.'),
  d('java-i-interface-static', 'A static interface method', 'Define an interface with a static helper method and call it on the interface itself.',
    'interface MathOps {\n    static int twice(int n) { return n * 2; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println(MathOps.twice(21));\n    }\n}\n', 'Static interface methods are called on the interface name.'),
  d('java-i-comparable', 'Natural ordering', 'Define a class implementing Comparable ordered by a numeric field, then sort a list and print it.',
    'import java.util.ArrayList;\nimport java.util.Collections;\nimport java.util.List;\n\nclass Score implements Comparable<Score> {\n    int value;\n    Score(int value) { this.value = value; }\n\n    @Override public int compareTo(Score o) { return Integer.compare(value, o.value); }\n    @Override public String toString() { return String.valueOf(value); }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Score> list = new ArrayList<>(List.of(new Score(78), new Score(95), new Score(61)));\n        Collections.sort(list);\n        System.out.println(list);\n    }\n}\n', 'Integer.compare returns the right sign.'),
  d('java-i-comparable-max', 'Using the natural order', 'With a Comparable class, print the maximum of a list using Collections.max.',
    'import java.util.Collections;\nimport java.util.List;\n\nclass Score implements Comparable<Score> {\n    int value;\n    Score(int value) { this.value = value; }\n    @Override public int compareTo(Score o) { return Integer.compare(value, o.value); }\n    @Override public String toString() { return String.valueOf(value); }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        System.out.println(Collections.max(List.of(new Score(4), new Score(9), new Score(2))));\n    }\n}\n', 'Collections.max uses compareTo.'),
  d('java-i-comparator-external', 'An external ordering', 'Sort a list of objects two different ways using Comparators, printing each result.',
    'import java.util.ArrayList;\nimport java.util.Comparator;\nimport java.util.List;\n\nclass Item {\n    String name;\n    int qty;\n    Item(String name, int qty) { this.name = name; this.qty = qty; }\n    @Override public String toString() { return name + ":" + qty; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Item> items = new ArrayList<>(List.of(new Item("b", 3), new Item("a", 9)));\n\n        items.sort(Comparator.comparing((Item i) -> i.name));\n        System.out.println(items);\n\n        items.sort(Comparator.comparingInt((Item i) -> i.qty).reversed());\n        System.out.println(items);\n    }\n}\n', 'Comparators let one type be sorted several ways.'),
  d('java-i-instanceof', 'Testing a type', 'With an Object holding a String, print whether it is a String and whether it is an Integer.',
    M('        Object o = "text";\n        System.out.println(o instanceof String);\n        System.out.println(o instanceof Integer);'), 'instanceof reports the actual type.'),
  d('java-i-pattern-matching', 'Pattern matching', 'Write a method using instanceof pattern matching to describe an Object as a String with its length, an Integer with its value, or unknown.',
    'public class Main {\n    static String describe(Object o) {\n        if (o instanceof String s) return "string of " + s.length();\n        if (o instanceof Integer i) return "int " + i;\n        return "unknown";\n    }\n\n    public static void main(String[] args) {\n        System.out.println(describe("hello"));\n        System.out.println(describe(42));\n        System.out.println(describe(2.5));\n    }\n}\n', 'The pattern variable is scoped to the successful branch.'),
  d('java-i-downcast', 'Safe downcasting', 'Hold a subclass in a parent variable, test with instanceof, and access a subclass-only field.',
    'class Animal { }\n\nclass Dog extends Animal {\n    String breed = "collie";\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Animal a = new Dog();\n        if (a instanceof Dog dg) {\n            System.out.println(dg.breed);\n        }\n    }\n}\n', 'Pattern matching combines the test and the cast.'),
  d('java-i-final-method', 'A final method', 'Define a base with a final method and a subclass that inherits but cannot override it, then call it through the subclass.',
    'class Base {\n    final String fixed() { return "cannot change"; }\n    String flexible() { return "base"; }\n}\n\nclass Child extends Base {\n    @Override String flexible() { return "child"; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        Child c = new Child();\n        System.out.println(c.fixed());\n        System.out.println(c.flexible());\n    }\n}\n', 'final prevents overriding.'),
  d('java-i-capstone', 'Inheritance summary', 'Define an abstract Employee with a name, an abstract pay() and a concrete payslip(). Derive Salaried and Hourly, put them in a list, print each payslip, then the total payroll to two decimals.',
    'import java.util.List;\n\nabstract class Employee {\n    protected String name;\n    Employee(String name) { this.name = name; }\n\n    abstract double pay();\n\n    void payslip() {\n        System.out.printf("%s %.2f%n", name, pay());\n    }\n}\n\nclass Salaried extends Employee {\n    private double annual;\n    Salaried(String name, double annual) {\n        super(name);\n        this.annual = annual;\n    }\n    @Override double pay() { return annual / 12; }\n}\n\nclass Hourly extends Employee {\n    private double rate;\n    private int hours;\n    Hourly(String name, double rate, int hours) {\n        super(name);\n        this.rate = rate;\n        this.hours = hours;\n    }\n    @Override double pay() { return rate * hours; }\n}\n\npublic class Main {\n    public static void main(String[] args) {\n        List<Employee> staff = List.of(\n            new Salaried("Ada", 60000),\n            new Hourly("Bob", 25, 160),\n            new Hourly("Cy", 30, 120)\n        );\n\n        double total = 0;\n        for (Employee e : staff) {\n            e.payslip();\n            total += e.pay();\n        }\n        System.out.printf("total %.2f%n", total);\n    }\n}\n', 'The abstract method forces each subclass to define its own calculation.'),
  {
    kind: 'quiz',
    slug: 'java-inheritance-quiz',
    title: 'Checkpoint: inheritance',
    questions: [
      { q: 'Does Java need a virtual keyword for dynamic dispatch?', options: ['Yes', 'No \u2014 every method is virtual unless final, static or private', 'Only for abstract classes', 'Only with interfaces'], answer: 1, explain: 'Unlike C++, overriding works by default, which is why forgetting a keyword cannot silently break polymorphism.' },
      { q: 'Where must super(...) appear in a subclass constructor?', options: ['Anywhere', 'As the first statement', 'Last', 'It is optional and never needed'], answer: 1, explain: 'The parent must be initialised before the subclass adds to it, so the call has to come first.' },
      { q: 'What is the key difference between an abstract class and an interface?', options: ['None', 'A class extends one abstract class but can implement many interfaces', 'Interfaces cannot have methods', 'Abstract classes cannot have fields'], answer: 1, explain: 'An abstract class describes what a type is and can hold state; interfaces describe what a type can do and are multiply implementable.' },
      { q: 'What must compareTo return when this object should sort first?', options: ['true', 'A negative number', 'Zero', 'A positive number'], answer: 1, explain: 'The sign carries the meaning, which is why Integer.compare is preferred over subtraction that could overflow.' },
      { q: 'What does if (o instanceof String s) achieve?', options: ['Only a type test', 'Tests the type and declares s as the cast value, scoped to that branch', 'Casts unconditionally', 'Nothing new'], answer: 1, explain: 'Pattern matching binds the variable only where the test succeeded, so the cast can never fail.' },
    ],
  },
];
