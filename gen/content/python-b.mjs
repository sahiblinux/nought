const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });

export const pyConditionals = [
  {
    slug: 'if-basics',
    title: 'Running code only sometimes',
    subtitle: 'The if statement',
    concept:
      'An `if` statement runs a block of code only when its condition is True.\n\nThe critical Python detail is that the block is defined by **indentation**, not by braces or keywords. Four spaces is the universal convention. The colon at the end of the `if` line opens the block, and the block ends when the indentation returns to the previous level.\n\nThis is not cosmetic \u2014 indentation is real syntax in Python, and getting it wrong is a genuine error.',
    example: 'temp = 31\n\nif temp > 30:\n    print("hot")\n    print("stay hydrated")\n\nprint("always runs")',
    note: 'Both indented lines belong to the if. The last line is not indented, so it always runs.',
    task: 'With `n = 17`, print `positive` if n is above zero, then print `done` unconditionally.',
    solution: 'n = 17\n\nif n > 0:\n    print("positive")\n\nprint("done")',
    hint: 'Indent the body by four spaces and leave the final print at the outer level.',
  },
  {
    slug: 'if-else',
    title: 'Two paths',
    subtitle: 'else',
    concept:
      '`else` supplies the block to run when the condition was False. Exactly one of the two blocks runs \u2014 never both, never neither.\n\nThe `else` keyword lines up with its `if` at the same indentation, and takes its own colon.',
    example: 'age = 15\n\nif age >= 18:\n    print("adult")\nelse:\n    print("minor")',
    note: 'else has no condition of its own \u2014 it simply catches everything the if did not.',
    task: 'With `n = 12`, print `even` or `odd` on the first line, then `big` or `small` on the second, treating over 10 as big.',
    solution: 'n = 12\n\nif n % 2 == 0:\n    print("even")\nelse:\n    print("odd")\n\nif n > 10:\n    print("big")\nelse:\n    print("small")',
    hint: 'Two independent if/else blocks, one after the other.',
  },
  {
    slug: 'elif',
    title: 'Many paths',
    subtitle: 'elif chains',
    concept:
      '`elif` (short for "else if") tests another condition when the previous ones failed. A chain can have as many `elif` branches as you like, with an optional `else` at the end.\n\nConditions are checked **in order**, and the first match wins \u2014 the rest are skipped entirely. That means order matters: put the most specific or highest thresholds first, or a broad condition will swallow the cases below it.',
    example: 'score = 74\n\nif score >= 90:\n    print("A")\nelif score >= 80:\n    print("B")\nelif score >= 70:\n    print("C")\nelse:\n    print("F")',
    note: 'Because 74 fails the first two tests and passes the third, it prints C. Reversing the order would print C for everything above 70, including 95.',
    task: 'With `temp = 5`, print `freezing` under 0, `cold` under 10, `mild` under 25, otherwise `hot`.',
    solution: 'temp = 5\n\nif temp < 0:\n    print("freezing")\nelif temp < 10:\n    print("cold")\nelif temp < 25:\n    print("mild")\nelse:\n    print("hot")',
    hint: 'Order the thresholds from lowest to highest so each elif only sees what fell through.',
  },
  {
    slug: 'nested-if',
    title: 'Conditions inside conditions',
    subtitle: 'Nesting and flattening',
    concept:
      'An `if` can contain another `if`. Each level adds four more spaces of indentation.\n\nNesting is sometimes the clearest way to express a decision, but deep nesting quickly becomes hard to read. Often the same logic can be **flattened** by combining conditions with `and`, which is usually preferable.',
    example: 'age = 25\nhas_ticket = True\n\nif age >= 18:\n    if has_ticket:\n        print("enter")\n    else:\n        print("buy a ticket")\nelse:\n    print("too young")\n\n# flattened version of the happy path\nif age >= 18 and has_ticket:\n    print("enter (flat)")',
    note: 'Two levels of nesting is usually fine; four is a sign the logic wants restructuring.',
    task: 'With `logged_in = True` and `is_admin = False`, print `admin panel` when both hold, `user area` when logged in but not admin, and `please log in` otherwise.',
    solution: 'logged_in = True\nis_admin = False\n\nif logged_in:\n    if is_admin:\n        print("admin panel")\n    else:\n        print("user area")\nelse:\n    print("please log in")',
    hint: 'Test logged_in on the outside and is_admin on the inside.',
  },
  {
    slug: 'conditional-expression',
    title: 'A choice inside an expression',
    subtitle: 'The ternary form',
    concept:
      'When you only need to pick between two **values**, Python offers a one-line conditional expression:\n\n`value = a if condition else b`\n\nIt reads almost like English: "a if the condition holds, otherwise b". Unlike a statement it produces a value, so it can go directly inside a call or an assignment.\n\nUse it for simple choices; a full `if` statement is clearer for anything with multiple steps.',
    example: 'n = 7\nlabel = "even" if n % 2 == 0 else "odd"\nprint(label)\n\nprint("high" if n > 5 else "low")\n\nage = 20\nprice = 5 if age < 18 else 10\nprint(price)',
    note: 'The condition sits in the middle, which is unusual but becomes natural quickly.',
    task: 'With `n = -4`, use conditional expressions to print its sign word (`negative` or `non-negative`) and its absolute value without calling abs().',
    solution: 'n = -4\nprint("negative" if n < 0 else "non-negative")\nprint(-n if n < 0 else n)',
    hint: 'Negating a negative number gives its magnitude.',
  },
  {
    slug: 'match-statement',
    title: 'Branching on a value',
    subtitle: 'Multi-way branching with elif',
    concept:
      'When you need to test one value against many possible options, a chain of `if`/`elif`/`else` is the classic Python approach. Each `elif` tests another condition, and the final `else` is the catch-all.\n\nYou can combine several values in one branch with `or`. This makes it visually obvious that every branch is testing the same subject.',
    example: 'command = "start"\n\nif command == "start":\n    print("starting")\nelif command == "stop" or command == "halt":\n    print("stopping")\nelse:\n    print("unknown")',
    note: 'The else branch is the catch-all that always matches, so it belongs last.',
    task: 'With `day = "sat"`, use if/elif/else to print `weekend` for `sat` or `sun`, `weekday` for `mon`, `tue`, `wed`, `thu` or `fri`, and `unknown` otherwise.',
    solution: 'day = "sat"\n\nif day == "sat" or day == "sun":\n    print("weekend")\nelif day in ("mon", "tue", "wed", "thu", "fri"):\n    print("weekday")\nelse:\n    print("unknown")',
    hint: 'You can join alternatives with `or`, or test membership with `in`.',
  },
  d('cond-sign', 'Sign of a number', 'With `n = 0`, print `positive`, `zero` or `negative` using an if/elif/else chain.',
    'n = 0\n\nif n > 0:\n    print("positive")\nelif n < 0:\n    print("negative")\nelse:\n    print("zero")', 'Zero belongs in the else branch.'),
  d('cond-grade', 'Letter grade', 'With `score = 88`, print A for 90+, B for 80+, C for 70+, D for 60+, otherwise F.',
    'score = 88\n\nif score >= 90:\n    print("A")\nelif score >= 80:\n    print("B")\nelif score >= 70:\n    print("C")\nelif score >= 60:\n    print("D")\nelse:\n    print("F")', 'Test the highest threshold first.'),
  d('cond-fizz-one', 'FizzBuzz for one number', 'With `n = 15`, print `FizzBuzz` if divisible by both 3 and 5, `Fizz` if only 3, `Buzz` if only 5, otherwise the number.',
    'n = 15\n\nif n % 3 == 0 and n % 5 == 0:\n    print("FizzBuzz")\nelif n % 3 == 0:\n    print("Fizz")\nelif n % 5 == 0:\n    print("Buzz")\nelse:\n    print(n)', 'Check the combined case first or it will never be reached.'),
  d('cond-max3', 'Largest of three', 'With a=14, b=27, c=9, print the largest using only if/elif/else \u2014 no max().',
    'a, b, c = 14, 27, 9\n\nif a >= b and a >= c:\n    print(a)\nelif b >= c:\n    print(b)\nelse:\n    print(c)', 'Once a is ruled out, only b and c remain to compare.'),
  d('cond-leap', 'Leap year', 'With `year = 1900`, print `leap` or `not leap` using the full rule.',
    'year = 1900\n\nif year % 400 == 0:\n    print("leap")\nelif year % 100 == 0:\n    print("not leap")\nelif year % 4 == 0:\n    print("leap")\nelse:\n    print("not leap")', '1900 is divisible by 100 but not 400, so it is not a leap year.'),
  d('cond-ticket', 'Ticket pricing', 'With `age = 67`, print 0 for under 5, 8 for under 18, 15 for under 65, otherwise 10.',
    'age = 67\n\nif age < 5:\n    print(0)\nelif age < 18:\n    print(8)\nelif age < 65:\n    print(15)\nelse:\n    print(10)', 'The bands must ascend so each elif catches the remainder.'),
  d('cond-bmi', 'BMI category', 'With `bmi = 22.5`, print `under` below 18.5, `normal` below 25, `over` below 30, otherwise `obese`.',
    'bmi = 22.5\n\nif bmi < 18.5:\n    print("under")\nelif bmi < 25:\n    print("normal")\nelif bmi < 30:\n    print("over")\nelse:\n    print("obese")', 'Float comparisons work exactly like integer ones.'),
  d('cond-vowel', 'Vowel or consonant', 'With `ch = "e"`, print `vowel` if it is one of aeiou, otherwise `consonant`.',
    'ch = "e"\n\nif ch in "aeiou":\n    print("vowel")\nelse:\n    print("consonant")', 'The in operator checks membership in the string of vowels.'),
  d('cond-password', 'Password strength', 'With `pw = "abc12345"`, print `weak` under 8 characters, `medium` under 12, otherwise `strong`.',
    'pw = "abc12345"\n\nif len(pw) < 8:\n    print("weak")\nelif len(pw) < 12:\n    print("medium")\nelse:\n    print("strong")', 'len() gives the character count.'),
  d('cond-shipping', 'Shipping cost', 'With `total = 45` and `member = False`, print 0 if total is at least 50 or the customer is a member, otherwise 6.',
    'total = 45\nmember = False\n\nif total >= 50 or member:\n    print(0)\nelse:\n    print(6)', 'A single or condition covers both free-shipping routes.'),
  d('cond-ternary-abs', 'Ternary practice', 'With `n = -12`, use a conditional expression to print its magnitude, and another to print `neg` or `pos`.',
    'n = -12\nprint(-n if n < 0 else n)\nprint("neg" if n < 0 else "pos")', 'Both fit on one line each.'),
  d('cond-triangle-type', 'Triangle type', 'With sides 5, 5 and 8, print `equilateral`, `isosceles` or `scalene`.',
    'a, b, c = 5, 5, 8\n\nif a == b == c:\n    print("equilateral")\nelif a == b or b == c or a == c:\n    print("isosceles")\nelse:\n    print("scalene")', 'a == b == c chains two comparisons.'),
  d('cond-match-http', 'HTTP status words', 'With `code = 404`, use if/elif/else to print `ok` for 200, `not found` for 404, `server error` for 500, otherwise `other`.',
    'code = 404\n\nif code == 200:\n    print("ok")\nelif code == 404:\n    print("not found")\nelif code == 500:\n    print("server error")\nelse:\n    print("other")', 'Each elif tests another possible value; else is the catch-all.'),
  d('cond-nested-login', 'Nested access check', 'With `user = "ada"`, `password = "secret"` and `active = True`, print `welcome` only when the password is `secret` and the account is active; print `inactive` when the password is right but the account is not; otherwise `denied`.',
    'user = "ada"\npassword = "secret"\nactive = True\n\nif password == "secret":\n    if active:\n        print("welcome")\n    else:\n        print("inactive")\nelse:\n    print("denied")', 'Check the password on the outer level.'),
  d('cond-quadrant', 'Which quadrant', 'With `x = -3` and `y = 4`, print `Q1`, `Q2`, `Q3`, `Q4` or `axis`.',
    'x, y = -3, 4\n\nif x == 0 or y == 0:\n    print("axis")\nelif x > 0 and y > 0:\n    print("Q1")\nelif x < 0 and y > 0:\n    print("Q2")\nelif x < 0 and y < 0:\n    print("Q3")\nelse:\n    print("Q4")', 'Handle the axis case first so the sign tests are unambiguous.'),
  d('cond-discount', 'Tiered discount', 'With `qty = 250`, print the unit price: 10 under 100 units, 8 under 500, otherwise 6.',
    'qty = 250\n\nif qty < 100:\n    print(10)\nelif qty < 500:\n    print(8)\nelse:\n    print(6)', 'The bands ascend, so each elif sees only what fell through.'),
  d('cond-rock-paper', 'Rock paper scissors', 'With `p1 = "rock"` and `p2 = "scissors"`, print `p1`, `p2` or `draw`.',
    'p1 = "rock"\np2 = "scissors"\n\nif p1 == p2:\n    print("draw")\nelif (p1 == "rock" and p2 == "scissors") or (p1 == "paper" and p2 == "rock") or (p1 == "scissors" and p2 == "paper"):\n    print("p1")\nelse:\n    print("p2")', 'Handle the draw first, then list the three winning combinations for p1.'),
  d('cond-month-days', 'Days in a month', 'With `month = 2` and `year = 2024`, print the number of days in that month.',
    'month = 2\nyear = 2024\n\nif month == 2:\n    if (year % 4 == 0 and year % 100 != 0) or year % 400 == 0:\n        print(29)\n    else:\n        print(28)\nelif month in (4, 6, 9, 11):\n    print(30)\nelse:\n    print(31)', 'February needs the leap year rule; four months have 30 days.'),
  d('cond-speed-fine', 'Speeding fine', 'With `speed = 92` and a limit of 70, print `ok` at or under the limit, `fine 100` up to 10 over, `fine 250` up to 30 over, otherwise `court`.',
    'speed = 92\nlimit = 70\nover = speed - limit\n\nif over <= 0:\n    print("ok")\nelif over <= 10:\n    print("fine 100")\nelif over <= 30:\n    print("fine 250")\nelse:\n    print("court")', 'Compute the excess once, then band it.'),
  d('cond-char-kind', 'Classify a character', 'With `ch = "7"`, print `digit`, `letter` or `other`.',
    'ch = "7"\n\nif ch.isdigit():\n    print("digit")\nelif ch.isalpha():\n    print("letter")\nelse:\n    print("other")', 'Strings have .isdigit() and .isalpha() methods that return booleans.'),
  d('cond-truthy-check', 'Guarding on truthiness', 'With `items = []` and `name = "ada"`, print `no items` when the list is empty and print the name when it is non-empty.',
    'items = []\nname = "ada"\n\nif items:\n    print("has items")\nelse:\n    print("no items")\n\nif name:\n    print(name)', 'An empty list is falsy, so if items: reads naturally.'),
  d('cond-capstone', 'Combined decision', 'With `hour = 14`, print `morning` under 12, `afternoon` under 18, `evening` under 22, otherwise `night`. Then on a second line print `working` if the hour is between 9 and 17 inclusive, else `off`.',
    'hour = 14\n\nif hour < 12:\n    print("morning")\nelif hour < 18:\n    print("afternoon")\nelif hour < 22:\n    print("evening")\nelse:\n    print("night")\n\nprint("working" if 9 <= hour <= 17 else "off")', 'The second line suits a conditional expression.'),
  {
    kind: 'quiz',
    slug: 'conditionals-quiz',
    title: 'Checkpoint: conditionals',
    questions: [
      { q: 'How does Python know which lines belong to an if block?', options: ['Curly braces', 'The indentation of the lines', 'An end keyword', 'Semicolons'], answer: 1, explain: 'Indentation is genuine syntax in Python. Consistently indented lines under the colon form the block.' },
      { q: 'In an if/elif/elif/else chain, how many branches run?', options: ['All that match', 'Exactly one', 'At most two', 'None unless else is present'], answer: 1, explain: 'Conditions are tested in order and the first match wins; every other branch is skipped.' },
      { q: 'What does x = "a" if n > 0 else "b" do?', options: ['A syntax error', 'Assigns "a" when n > 0, otherwise "b"', 'Always assigns "a"', 'Prints a or b'], answer: 1, explain: 'This is a conditional expression: it produces a value rather than executing statements.' },
      { q: 'Why must the FizzBuzz check for divisibility by both 3 and 5 come first?', options: ['It is faster', 'Otherwise the single-divisor branches match first and the combined case never runs', 'Python requires and-conditions first', 'It does not matter'], answer: 1, explain: 'A number divisible by 15 also satisfies the divisible-by-3 test, so if that branch comes first the combined case is unreachable.' },
      { q: 'What does the else branch do in an if/elif chain?', options: ['It always runs', 'It runs only when no if or elif matched', 'It runs before the elif branches', 'It is required in every chain'], answer: 1, explain: 'The else branch is the catch-all that runs when none of the preceding conditions were true, so it belongs last.' },
    ],
  },
];

export const pyLoops = [
  {
    slug: 'for-range',
    title: 'Repeating a fixed number of times',
    subtitle: 'for and range',
    concept:
      'A `for` loop runs its body once for each item in a sequence. The simplest source of items is `range`.\n\n- `range(n)` counts `0` up to but **not including** n\n- `range(a, b)` starts at a and stops before b\n- `range(a, b, step)` moves by step, which may be negative\n\nThe exclusive end feels odd at first but means `range(len(x))` covers exactly the valid indices.',
    example: 'for i in range(3):\n    print(i)\n\nfor i in range(2, 5):\n    print(i)\n\nfor i in range(10, 0, -3):\n    print(i)',
    note: 'The last loop prints 10, 7, 4, 1 \u2014 it stops as soon as continuing would pass the end.',
    task: 'Print the first five multiples of 3 (3, 6, 9, 12, 15), one per line, then `sum: 45`.',
    solution: 'total = 0\n\nfor i in range(1, 6):\n    value = i * 3\n    print(value)\n    total += value\n\nprint("sum:", total)',
    hint: 'Accumulate into a variable declared before the loop, then print it after.',
  },
  {
    slug: 'for-iterables',
    title: 'Looping over things directly',
    subtitle: 'Strings and lists',
    concept:
      'A `for` loop works on anything **iterable**, not just ranges. Looping over a string yields each character; looping over a list yields each element.\n\nThis is the idiomatic Python style. Writing `for i in range(len(items))` and then indexing is almost always the clumsier option \u2014 loop over the items themselves when you need the values.',
    example: 'for ch in "cat":\n    print(ch)\n\nfor colour in ["red", "green"]:\n    print(colour)\n\ntotal = 0\nfor n in [4, 8, 15]:\n    total += n\nprint(total)',
    note: 'The loop variable is rebound on each pass and remains available after the loop ends.',
    task: 'Given `word = "loop"`, print each character on its own line, then print the number of characters.',
    solution: 'word = "loop"\n\nfor ch in word:\n    print(ch)\n\nprint(len(word))',
    hint: 'Iterate the string directly rather than over a range of indices.',
  },
  {
    slug: 'while-loops',
    title: 'Looping until something changes',
    subtitle: 'while',
    concept:
      'A `while` loop repeats as long as its condition stays True. It is the right tool when you do not know in advance how many passes are needed.\n\nThe danger is the infinite loop: **something inside the body must eventually make the condition False**. Forgetting to update the variable is the single most common cause.\n\nUse `for` when counting a known number of times; use `while` when looping until a state is reached.',
    example: 'n = 100\nsteps = 0\n\nwhile n > 1:\n    n //= 2\n    steps += 1\n\nprint("halvings:", steps)',
    note: 'n shrinks every pass, guaranteeing the condition eventually fails. Without that line it would run forever.',
    task: 'Starting from 1, repeatedly double a value while it stays below 100, printing each value including the first. Then print how many doublings happened.',
    solution: 'value = 1\ncount = 0\n\nwhile value < 100:\n    print(value)\n    value *= 2\n    count += 1\n\nprint("doublings:", count)',
    hint: 'Print before doubling so the initial 1 appears.',
  },
  {
    slug: 'break-continue',
    title: 'Leaving early and skipping ahead',
    subtitle: 'break and continue',
    concept:
      '`break` exits the loop immediately, skipping any remaining passes. `continue` abandons only the current pass and jumps to the next one.\n\n`break` is how you implement a search: stop as soon as you find what you wanted, rather than scanning the rest pointlessly.\n\nBoth affect only the innermost loop they appear in.',
    example: 'for n in range(1, 20):\n    if n % 7 == 0:\n        print("first multiple of 7:", n)\n        break\n\nfor n in range(1, 8):\n    if n % 2 == 0:\n        continue\n    print(n)',
    note: 'The second loop prints only the odd numbers, because continue skips the rest of the body for even ones.',
    task: 'Loop from 1 to 30. Skip every multiple of 3 with continue, and stop entirely with break once the value exceeds 12. Print each value that survives.',
    solution: 'for n in range(1, 31):\n    if n > 12:\n        break\n    if n % 3 == 0:\n        continue\n    print(n)',
    hint: 'Test the break condition before the continue condition.',
  },
  {
    slug: 'nested-loops',
    title: 'A loop inside a loop',
    subtitle: 'Nesting',
    concept:
      'Placing one loop inside another runs the inner loop completely for **every single pass** of the outer loop. Two loops of ten passes each therefore execute the inner body a hundred times.\n\nThis is the natural shape for grids, tables and pairs. It is also where performance disappears quickly, since the work multiplies rather than adds.',
    example: 'for row in range(1, 4):\n    for col in range(1, 4):\n        print(row * col, end=" ")\n    print()',
    note: 'The bare print() after the inner loop ends the row. Without it, everything would land on one line.',
    task: 'Print a 3 by 4 grid of hash characters \u2014 three rows, each containing four hashes with no spaces.',
    solution: 'for row in range(3):\n    for col in range(4):\n        print("#", end="")\n    print()',
    hint: 'Use end="" inside the inner loop and an empty print() to break the line.',
  },
  {
    slug: 'loop-else',
    title: 'The loop that knows it finished',
    subtitle: 'for...else',
    concept:
      'Python loops accept an `else` clause, which runs **only if the loop completed without hitting a break**.\n\nThis is unusual and unique to Python. It is exactly what a search needs: the `else` is the "not found" case, and it removes the need for a separate flag variable.\n\nRead it as "for ... else nothing broke out".',
    example: 'target = 9\n\nfor n in [2, 4, 6, 8]:\n    if n == target:\n        print("found")\n        break\nelse:\n    print("not found")',
    note: 'Because no break happened, the else runs and prints "not found".',
    task: 'Search the list [4, 7, 12, 19] for a value divisible by 5. Print `found: N` if one exists, otherwise `none divisible by 5`.',
    solution: 'for n in [4, 7, 12, 19]:\n    if n % 5 == 0:\n        print("found:", n)\n        break\nelse:\n    print("none divisible by 5")',
    hint: 'The else aligns with the for, not with the if.',
  },
  d('loop-sum-100', 'Sum to one hundred', 'Print the sum of every integer from 1 to 100 inclusive.',
    'total = 0\nfor n in range(1, 101):\n    total += n\nprint(total)', 'range(1, 101) covers 1 through 100.'),
  d('loop-evens', 'Even numbers', 'Print every even number from 2 to 20 inclusive, one per line.',
    'for n in range(2, 21, 2):\n    print(n)', 'A step of 2 skips the odd values entirely.'),
  d('loop-countdown', 'Counting backwards', 'Print 10 down to 1, one per line, then `liftoff`.',
    'for n in range(10, 0, -1):\n    print(n)\nprint("liftoff")', 'A negative step counts down; the end value 0 is excluded.'),
  d('loop-times-table', 'Seven times table', 'Print the seven times table from 1 to 10 in the form `7 x 3 = 21`.',
    'for n in range(1, 11):\n    print("7 x", n, "=", 7 * n)', 'The commas in print supply the spacing.'),
  d('loop-factorial', 'Factorial', 'Compute 8 factorial with a loop and print the result.',
    'result = 1\nfor n in range(1, 9):\n    result *= n\nprint(result)', 'Start the accumulator at 1, not 0, since you are multiplying.'),
  d('loop-fizzbuzz', 'FizzBuzz to 20', 'Print the numbers 1 to 20, replacing multiples of 3 with `Fizz`, of 5 with `Buzz` and of both with `FizzBuzz`.',
    'for n in range(1, 21):\n    if n % 15 == 0:\n        print("FizzBuzz")\n    elif n % 3 == 0:\n        print("Fizz")\n    elif n % 5 == 0:\n        print("Buzz")\n    else:\n        print(n)', 'Divisible by 15 is the same as divisible by both 3 and 5.'),
  d('loop-vowel-count', 'Counting vowels', 'Count the vowels in `"programming is fun"` and print `vowels: N`.',
    'text = "programming is fun"\ncount = 0\nfor ch in text:\n    if ch in "aeiou":\n        count += 1\nprint("vowels:", count)', 'Loop the characters and test membership in "aeiou".'),
  d('loop-reverse-string', 'Reversing by hand', 'Build the reverse of `"python"` character by character with a loop, then print it.',
    'text = "python"\nresult = ""\nfor ch in text:\n    result = ch + result\nprint(result)', 'Prepending each character reverses the order.'),
  d('loop-square-list', 'Squares up to ten', 'Print the squares of 1 to 10 on one line, separated by spaces.',
    'for n in range(1, 11):\n    print(n * n, end=" ")\nprint()', 'Use end=" " and a bare print() at the end.'),
  d('loop-while-sum', 'Accumulate until', 'Starting at 1, keep adding consecutive integers until the running total passes 50. Print the total and how many numbers were used.',
    'total = 0\nn = 0\nwhile total <= 50:\n    n += 1\n    total += n\nprint("total:", total)\nprint("count:", n)', 'Increment n before adding it so the count matches.'),
  d('loop-collatz', 'Collatz sequence', 'Starting at 27, apply the Collatz rule (halve if even, else triple and add one) until reaching 1, and print the number of steps.',
    'n = 27\nsteps = 0\nwhile n != 1:\n    if n % 2 == 0:\n        n //= 2\n    else:\n        n = 3 * n + 1\n    steps += 1\nprint("steps:", steps)', 'Use floor division so n stays an integer.'),
  d('loop-digits', 'Summing digits', 'With `n = 9875`, sum its digits using arithmetic only (no string conversion) and print the total.',
    'n = 9875\ntotal = 0\nwhile n > 0:\n    total += n % 10\n    n //= 10\nprint(total)', '% 10 peels off the last digit and // 10 removes it.'),
  d('loop-primes', 'Primes below thirty', 'Print every prime number below 30, one per line.',
    'for n in range(2, 30):\n    is_prime = True\n    for d in range(2, n):\n        if n % d == 0:\n            is_prime = False\n            break\n    if is_prime:\n        print(n)', 'Use an inner loop to test divisors and break as soon as one is found.'),
  d('loop-triangle-nested', 'Numbered triangle', 'Print five rows where row n contains the digit n repeated n times.',
    'for n in range(1, 6):\n    for _ in range(n):\n        print(n, end="")\n    print()', 'An underscore is the conventional name for an unused loop variable.'),
  d('loop-multiplication-grid', 'Multiplication grid', 'Print a 4 by 4 multiplication grid, values separated by tabs, one row per line.',
    'for row in range(1, 5):\n    for col in range(1, 5):\n        print(row * col, end="\\t")\n    print()', 'Tab separators keep the columns aligned.'),
  d('loop-find-first', 'First match', 'Find the first number above 100 that is divisible by both 7 and 11, printing it and stopping.',
    'n = 101\nwhile True:\n    if n % 7 == 0 and n % 11 == 0:\n        print(n)\n        break\n    n += 1', 'while True with a break is a clean way to search indefinitely.'),
  d('loop-fibonacci', 'Fibonacci numbers', 'Print the first twelve Fibonacci numbers on one line separated by spaces, starting 0 1.',
    'a, b = 0, 1\nfor _ in range(12):\n    print(a, end=" ")\n    a, b = b, a + b\nprint()', 'The tuple assignment updates both values simultaneously.'),
  d('loop-skip-multiples', 'Skipping with continue', 'Print the numbers 1 to 25 that are divisible by neither 2 nor 3, one per line.',
    'for n in range(1, 26):\n    if n % 2 == 0 or n % 3 == 0:\n        continue\n    print(n)', 'continue skips the print for unwanted values.'),
  d('loop-max-scan', 'Largest by hand', 'Given the list [12, 45, 7, 89, 23], find the largest with a loop (no max()) and print it.',
    'nums = [12, 45, 7, 89, 23]\nlargest = nums[0]\nfor n in nums:\n    if n > largest:\n        largest = n\nprint(largest)', 'Start from the first element rather than zero.'),
  d('loop-count-down-by', 'Custom step', 'Print from 100 down to 55 in steps of 5, all on one line separated by spaces.',
    'for n in range(100, 54, -5):\n    print(n, end=" ")\nprint()', 'The end value must be below 55 for 55 to be included.'),
  d('loop-search-else', 'Search with for-else', 'Search [3, 6, 9, 12] for a value above 20. Print it if found, otherwise `nothing above 20`.',
    'for n in [3, 6, 9, 12]:\n    if n > 20:\n        print(n)\n        break\nelse:\n    print("nothing above 20")', 'The else runs only when no break happened.'),
  d('loop-pyramid', 'Centred pyramid', 'Print a five row pyramid of stars, centred, where row n has 2n-1 stars padded with leading spaces.',
    'rows = 5\nfor n in range(1, rows + 1):\n    print(" " * (rows - n) + "*" * (2 * n - 1))', 'String multiplication builds both the padding and the stars.'),
  d('loop-capstone', 'Loop summary', 'For the numbers 1 to 30, print the count of multiples of 4, their sum, and the largest one \u2014 three labelled lines.',
    'count = 0\ntotal = 0\nlargest = 0\nfor n in range(1, 31):\n    if n % 4 == 0:\n        count += 1\n        total += n\n        largest = n\nprint("count:", count)\nprint("sum:", total)\nprint("largest:", largest)', 'One pass can gather all three facts.'),
  {
    kind: 'quiz',
    slug: 'loops-quiz',
    title: 'Checkpoint: loops',
    questions: [
      { q: 'What does range(2, 5) produce?', options: ['2, 3, 4, 5', '2, 3, 4', '0, 1, 2, 3, 4', '3, 4, 5'], answer: 1, explain: 'range includes the start and excludes the end, so it yields 2, 3 and 4.' },
      { q: 'What is the most common cause of an infinite while loop?', options: ['Using the wrong operator', 'Forgetting to change the variable the condition tests', 'Nesting loops', 'Using break'], answer: 1, explain: 'If nothing in the body moves the condition toward False, it never becomes False.' },
      { q: 'What does continue do?', options: ['Exits the loop', 'Skips the rest of the current pass and moves to the next', 'Restarts the loop from the beginning', 'Pauses execution'], answer: 1, explain: 'continue abandons only the current iteration; break is what exits the loop entirely.' },
      { q: 'When does a for...else clause run?', options: ['Always', 'Only when the loop body never executed', 'Only when the loop finished without hitting a break', 'Only when an error occurred'], answer: 2, explain: 'The else is the "no break happened" case, which makes it ideal for the not-found branch of a search.' },
      { q: 'How many times does the inner body run for two nested loops of 5 and 4 passes?', options: ['9', '20', '5', '4'], answer: 1, explain: 'The inner loop runs completely for every outer pass, so the work multiplies: 5 x 4 = 20.' },
    ],
  },
];

export const pyStrings = [
  {
    slug: 'string-indexing',
    title: 'Reaching individual characters',
    subtitle: 'Indexing and negative indexes',
    concept:
      'A string is an ordered sequence of characters, and `text[i]` reaches the one at position `i`. Counting starts at **zero**, so the first character is `text[0]`.\n\nNegative indexes count from the end: `text[-1]` is the last character, `text[-2]` the one before it. That saves computing `len(text) - 1` constantly.\n\nAsking for an index that does not exist raises an `IndexError` rather than returning something empty.',
    example: 'word = "python"\n\nprint(word[0])\nprint(word[3])\nprint(word[-1])\nprint(word[-2])\nprint(len(word))',
    note: 'Valid indexes for a six-character string run 0 to 5, or -1 to -6.',
    task: 'Given `word = "keyboard"`, print its first character, its last character, its fourth character and its length.',
    solution: 'word = "keyboard"\nprint(word[0])\nprint(word[-1])\nprint(word[3])\nprint(len(word))',
    hint: 'The fourth character is at index 3, because counting starts at zero.',
  },
  {
    slug: 'slicing',
    title: 'Taking a section',
    subtitle: 'Slices',
    concept:
      '`text[start:end]` returns a new string from `start` up to but **not including** `end`. Omitting either side means "from the beginning" or "to the end".\n\nA third value gives the step: `text[::2]` takes every second character, and `text[::-1]` reverses the string \u2014 the standard Python idiom for reversal.\n\nSlices never raise an error for out-of-range values; they simply return as much as exists.',
    example: 'text = "programming"\n\nprint(text[0:4])\nprint(text[4:])\nprint(text[:3])\nprint(text[-4:])\nprint(text[::2])\nprint(text[::-1])',
    note: 'text[:n] and text[n:] always split the string into two parts that rejoin perfectly.',
    task: 'Given `text = "abcdefgh"`, print the first three characters, the last three, everything except the first and last, and the whole string reversed.',
    solution: 'text = "abcdefgh"\nprint(text[:3])\nprint(text[-3:])\nprint(text[1:-1])\nprint(text[::-1])',
    hint: 'text[1:-1] drops one character from each end.',
  },
  {
    slug: 'string-methods',
    title: 'Asking a string to do things',
    subtitle: 'Case, trimming and testing',
    concept:
      'Strings carry **methods** \u2014 functions attached to the value, called with a dot.\n\n- `.upper()`, `.lower()`, `.title()`, `.capitalize()` change case\n- `.strip()`, `.lstrip()`, `.rstrip()` remove surrounding whitespace\n- `.startswith()`, `.endswith()`, `.count()`, `.find()` inspect content\n- `.isdigit()`, `.isalpha()`, `.isspace()` test the characters\n\nStrings are **immutable**, so every one of these returns a *new* string rather than changing the original.',
    example: 'raw = "  Hello World  "\n\nprint(raw.strip())\nprint(raw.strip().upper())\nprint(raw.strip().lower())\nprint("hello".capitalize())\nprint("hello world".title())\nprint("banana".count("a"))\nprint("hello".find("ll"))',
    note: '.find() returns the index of the first match, or -1 when there is none.',
    task: 'Given `raw = "  Python Programming  "`, print the trimmed text, its uppercase form, the number of times `"m"` appears, and whether the trimmed text starts with `"Py"`.',
    solution: 'raw = "  Python Programming  "\ntext = raw.strip()\n\nprint(text)\nprint(text.upper())\nprint(text.count("m"))\nprint(text.startswith("Py"))',
    hint: 'Store the stripped version once and reuse it.',
  },
  {
    slug: 'fstrings',
    title: 'Building text from values',
    subtitle: 'f-strings',
    concept:
      'An **f-string** substitutes values directly into text. Put `f` before the quote and place any expression inside `{}`.\n\nA colon inside the braces introduces a **format spec**:\n- `{v:.2f}` two decimal places\n- `{v:>8}` right-align in 8 columns, `{v:<8}` left-align, `{v:^8}` centre\n- `{v:,}` thousands separators\n- `{v=}` prints both the expression and its value, which is superb for debugging',
    example: 'name = "Ada"\nscore = 91.5678\n\nprint(f"{name} scored {score:.1f}")\nprint(f"|{name:<10}|{name:>10}|{name:^10}|")\nprint(f"{1234567:,}")\nprint(f"{score=}")',
    note: 'Anything valid as an expression works inside the braces, including method calls and arithmetic.',
    task: 'Given `item = "coffee"` and `price = 3.5`, print `coffee costs $3.50` using an f-string with two decimal places.',
    solution: 'item = "coffee"\nprice = 3.5\nprint(f"{item} costs ${price:.2f}")',
    hint: 'The dollar sign is literal text; the format spec is :.2f.',
  },
  {
    slug: 'split-join',
    title: 'Between text and lists',
    subtitle: 'split and join',
    concept:
      '`.split(sep)` breaks a string into a list. With no argument it splits on any run of whitespace, which is what you want for words.\n\n`sep.join(list)` is the inverse: it glues a list of strings together with `sep` between them. Note the direction \u2014 the separator is the thing you call `.join` on.\n\nEvery element must already be a string; join will not convert numbers for you.',
    example: 'sentence = "the quick brown fox"\nwords = sentence.split()\nprint(words)\nprint(len(words))\n\nprint("-".join(words))\nprint(" ".join(["a", "b", "c"]))\n\nprint("2026-08-01".split("-"))',
    note: '"".join(parts) concatenates with nothing between, which is the fastest way to build a string from pieces.',
    task: 'Given `csv = "red,green,blue"`, split it into a list, print the list, print how many parts there are, and print them rejoined with `" | "`.',
    solution: 'csv = "red,green,blue"\nparts = csv.split(",")\n\nprint(parts)\nprint(len(parts))\nprint(" | ".join(parts))',
    hint: 'Pass the comma explicitly to split, since the default splits on whitespace.',
  },
  {
    slug: 'string-replace',
    title: 'Substituting content',
    subtitle: 'replace and immutability',
    concept:
      '`.replace(old, new)` returns a new string with every occurrence swapped. An optional third argument limits how many are replaced.\n\nBecause strings are immutable, `text.replace(...)` on its own changes nothing \u2014 you must assign the result. This catches almost everyone once.\n\nThe same applies to every string method: they all return new values.',
    example: 'text = "one two one two"\n\nprint(text.replace("one", "1"))\nprint(text.replace("one", "1", 1))\nprint(text)\n\ntext = text.replace("two", "2")\nprint(text)',
    note: 'The third print shows the original unchanged \u2014 proof that replace did not modify it in place.',
    task: 'Given `text = "the cat sat on the mat"`, print it with every `"at"` replaced by `"og"`, then print the number of spaces it contains.',
    solution: 'text = "the cat sat on the mat"\nprint(text.replace("at", "og"))\nprint(text.count(" "))',
    hint: 'replace returns a new string, so print it directly or assign it first.',
  },
  d('str-first-last', 'Ends of a word', 'With `w = "alphabet"`, print the first two characters and the last two.',
    'w = "alphabet"\nprint(w[:2])\nprint(w[-2:])', 'Slices from the start and from the end.'),
  d('str-middle', 'The middle character', 'With `w = "python3"`, print the character at the exact middle index.',
    'w = "python3"\nprint(w[len(w) // 2])', 'Floor division of the length gives the middle index for odd lengths.'),
  d('str-reverse', 'Reverse a phrase', 'Reverse `"never odd or even"` and print it.',
    'print("never odd or even"[::-1])', 'A step of -1 walks the string backwards.'),
  d('str-palindrome', 'Palindrome test', 'Print whether `"racecar"` reads the same backwards, as a boolean.',
    'w = "racecar"\nprint(w == w[::-1])', 'Compare the string with its reverse.'),
  d('str-count-letter', 'Letter frequency', 'Count how many times `"s"` appears in `"mississippi"` and print it.',
    'print("mississippi".count("s"))', 'The count method takes the substring to look for.'),
  d('str-case-report', 'Case conversions', 'With `s = "Hello World"`, print its upper, lower, title and swapcase forms on four lines.',
    's = "Hello World"\nprint(s.upper())\nprint(s.lower())\nprint(s.title())\nprint(s.swapcase())', 'swapcase() inverts the case of every letter.'),
  d('str-strip-pad', 'Trimming whitespace', 'With `s = "   spaced   "`, print the stripped version wrapped in pipes, then the left-stripped and right-stripped versions the same way.',
    's = "   spaced   "\nprint("|" + s.strip() + "|")\nprint("|" + s.lstrip() + "|")\nprint("|" + s.rstrip() + "|")', 'The pipes make the remaining whitespace visible.'),
  d('str-fstring-table', 'Aligned columns', 'Using f-strings, print three rows aligning a name left in 10 columns and a score right in 5: ada/91, bob/7, chandra/100.',
    'for name, score in [("ada", 91), ("bob", 7), ("chandra", 100)]:\n    print(f"{name:<10}{score:>5}")', 'Use :<10 for the name and :>5 for the score.'),
  d('str-currency', 'Formatting money', 'With `amount = 1234567.891`, print it with thousands separators and two decimals.',
    'amount = 1234567.891\nprint(f"{amount:,.2f}")', 'Combine the comma and the precision in one spec.'),
  d('str-split-words', 'Word count', 'Split `"to be or not to be"` into words, then print the list and its length.',
    'words = "to be or not to be".split()\nprint(words)\nprint(len(words))', 'split() with no argument splits on whitespace.'),
  d('str-join-path', 'Building a path', 'Join the parts `["usr", "local", "bin"]` with slashes and print the result with a leading slash.',
    'parts = ["usr", "local", "bin"]\nprint("/" + "/".join(parts))', 'join puts the separator between elements only, so add the leading slash yourself.'),
  d('str-initials', 'Initials', 'From `"ada lovelace king"`, print the uppercase initials joined by dots: `A.L.K`.',
    'name = "ada lovelace king"\nprint(".".join(w[0].upper() for w in name.split()))', 'Take the first character of each word.'),
  d('str-replace-multi', 'Chained replacements', 'From `"a-b-c-d"`, print the version with dashes replaced by underscores, and another with dashes removed entirely.',
    's = "a-b-c-d"\nprint(s.replace("-", "_"))\nprint(s.replace("-", ""))', 'Replacing with an empty string deletes the character.'),
  d('str-find-index', 'Locating a substring', 'In `"the quick brown fox"`, print the index of `"brown"`, the index of `"cat"`, and whether `"quick"` appears.',
    's = "the quick brown fox"\nprint(s.find("brown"))\nprint(s.find("cat"))\nprint("quick" in s)', 'find returns -1 when the substring is absent.'),
  d('str-startswith', 'Prefix and suffix checks', 'For `"report_2026.csv"`, print whether it starts with `"report"`, ends with `".csv"`, and ends with `".txt"`.',
    'f = "report_2026.csv"\nprint(f.startswith("report"))\nprint(f.endswith(".csv"))\nprint(f.endswith(".txt"))', 'Both methods return booleans.'),
  d('str-char-tests', 'Character classification', 'For the strings `"12345"`, `"abc"` and `"ab 12"`, print whether each is all digits.',
    'for s in ["12345", "abc", "ab 12"]:\n    print(s.isdigit())', 'isdigit() requires every character to be a digit.'),
  d('str-title-case', 'Fixing a title', 'From `"the LORD of the RINGS"`, print a properly title-cased version.',
    'print("the LORD of the RINGS".title())', 'title() capitalises the first letter of every word and lowercases the rest.'),
  d('str-every-other', 'Alternate characters', 'From `"abcdefghij"`, print every second character starting at index 0, then starting at index 1.',
    's = "abcdefghij"\nprint(s[::2])\nprint(s[1::2])', 'The third slice value is the step.'),
  d('str-longest-word', 'Longest word', 'From `"the quick brown elephant"`, find and print the longest word.',
    'words = "the quick brown elephant".split()\nlongest = words[0]\nfor w in words:\n    if len(w) > len(longest):\n        longest = w\nprint(longest)', 'Track the best candidate as you loop.'),
  d('str-vowel-strip', 'Removing vowels', 'Print `"programming"` with every vowel removed.',
    'result = ""\nfor ch in "programming":\n    if ch not in "aeiou":\n        result += ch\nprint(result)', 'Build a new string from the characters you keep.'),
  d('str-capstone', 'String summary', 'From `"  Data Science Rocks  "`, print the stripped text, its length, its word count, its reverse, and it joined with underscores.',
    'raw = "  Data Science Rocks  "\ns = raw.strip()\nprint(s)\nprint(len(s))\nprint(len(s.split()))\nprint(s[::-1])\nprint("_".join(s.split()))', 'Strip once and reuse the result throughout.'),
  {
    kind: 'quiz',
    slug: 'strings-quiz',
    title: 'Checkpoint: strings',
    questions: [
      { q: 'What is "python"[1:4]?', options: ['"pyt"', '"yth"', '"ytho"', '"ythv"'], answer: 1, explain: 'Slices include the start index and exclude the end, so positions 1, 2 and 3 give "yth".' },
      { q: 'What does text[::-1] do?', options: ['Removes the last character', 'Reverses the string', 'Returns the last character', 'Raises an error'], answer: 1, explain: 'A step of -1 walks the string from the end to the start, producing the reverse.' },
      { q: 'After text.replace("a", "b"), what is text?', options: ['The modified string', 'Unchanged \u2014 strings are immutable', 'An empty string', 'A list'], answer: 1, explain: 'String methods return new values. You must assign the result to keep it.' },
      { q: 'What does " ".join(["a", "b"]) produce?', options: ['["a b"]', '"a b"', '"ab"', 'An error'], answer: 1, explain: 'join glues the list elements together with the separator between them, giving the string "a b".' },
      { q: 'What does "x".find("y") return?', options: ['0', 'None', '-1', 'An error'], answer: 2, explain: 'find returns -1 when the substring is not present, which is why it must be compared explicitly rather than treated as a boolean.' },
    ],
  },
];
