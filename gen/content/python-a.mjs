// Python modules 1-3: output, values, operators
const d = (slug, title, task, solution, hint, given = '') => ({ kind: 'drill', slug, title, task, solution, hint, given });

export const pyOutput = [
  {
    slug: 'first-print',
    title: 'Your first line of output',
    subtitle: 'Making the computer say something',
    concept:
      'A program is a list of instructions run top to bottom. The most useful first instruction is one that shows you something, because otherwise you cannot tell whether anything happened at all.\n\nIn Python that instruction is `print(...)`. Whatever sits between the parentheses is displayed. Text must be wrapped in quotes so Python knows it is literal text rather than the name of something \u2014 a piece of text like this is called a **string**.\n\nThat is a complete program. No setup, no boilerplate, no class to declare.',
    example: 'print("Hello, world!")\nprint("Two lines, two prints.")',
    note: 'Each print() puts its text on its own line. Execution order is strictly top to bottom.',
    task: 'Print exactly `Hello, world!` \u2014 capital H, comma, exclamation mark.',
    solution: 'print("Hello, world!")',
    hint: 'Use print() with the text inside double quotes. The quotes are not part of the output.',
  },
  {
    slug: 'many-lines',
    title: 'Several lines in a row',
    subtitle: 'Statements run in order',
    concept:
      'Python executes one statement per line, in the order they appear. Three `print` calls produce three lines, always in the same order.\n\nThis sounds obvious, but it is the foundation of everything: a program is a sequence, and the sequence is exactly what you wrote.',
    example: 'print("first")\nprint("second")\nprint("third")',
    note: 'Swap two of those lines and the output order changes to match. Nothing is reordered behind your back.',
    task: 'Print these three lines in order:\n\n`ready`\n`steady`\n`go`',
    solution: 'print("ready")\nprint("steady")\nprint("go")',
    hint: 'Three separate print() calls, one per line.',
  },
  {
    slug: 'comments',
    title: 'Notes the computer ignores',
    subtitle: 'Writing comments',
    concept:
      'A **comment** is a note for humans. Python sees a `#` and ignores the rest of that line entirely.\n\nGood comments explain *why* code does something \u2014 the code already says *what*. A comment can sit on its own line or trail after a statement.',
    example: '# This whole line is ignored\nprint("visible")   # so is everything after the hash\n# print("this never runs")',
    note: 'Commenting out a line is a quick way to disable it without deleting it.',
    task: 'Print `code speaks` on one line and `comments explain` on the next. Include at least one comment in your file.',
    solution: '# a short note for whoever reads this\nprint("code speaks")\nprint("comments explain")',
    hint: 'Anything after # on a line is ignored, so it cannot affect your output.',
  },
  {
    slug: 'print-commas',
    title: 'Several values in one call',
    subtitle: 'Comma-separated arguments',
    concept:
      'You can pass `print` several values at once by separating them with commas. Python inserts a single space between each and prints them on one line.\n\nThis works for any mix of types \u2014 numbers do not need quotes, and you do not have to convert them yourself.',
    example: 'print("one", "two", "three")\nprint("total:", 42)\nprint("x", 1, "y", 2)',
    note: 'Output: `one two three`, then `total: 42`, then `x 1 y 2`. The spaces come from the commas.',
    task: 'Using a single print call with commas, produce exactly:\n\n`score: 95 grade: A`',
    solution: 'print("score:", 95, "grade:", "A")',
    hint: 'Each comma adds exactly one space, so do not add spaces inside the strings as well.',
  },
  {
    slug: 'escapes',
    title: 'Characters you cannot type directly',
    subtitle: 'Escape sequences',
    concept:
      'Some characters are awkward to put in a string: a newline, a tab, a quote of the same kind that surrounds the string. Python solves this with **escape sequences**, all starting with a backslash.\n\n- `\\n` newline\n- `\\t` tab\n- `\\\\` a literal backslash\n- `\\"` a literal double quote\n\nOne string containing `\\n` can therefore produce several lines from a single print.',
    example: 'print("line one\\nline two")\nprint("name\\tyear")\nprint("She said \\"hi\\"")\nprint("C:\\\\Users")',
    note: 'The backslash disappears from the output \u2014 it is an instruction to the parser, not a character.',
    task: 'With a single print call, produce these two lines separated by a tab in each:\n\n`lang\tyear`\n`Python\t1991`',
    solution: 'print("lang\\tyear\\nPython\\t1991")',
    hint: 'Put \\t between the columns and \\n between the two lines, all inside one string.',
  },
  {
    slug: 'print-sep-end',
    title: 'Controlling the joins',
    subtitle: 'sep and end',
    concept:
      'Two optional settings change how `print` assembles its output.\n\n`sep=` replaces the space placed between arguments. `end=` replaces the newline placed at the end \u2014 setting `end=""` keeps the cursor on the same line so the next print continues it.\n\nThese are **keyword arguments**: they are named, so their position does not matter.',
    example: 'print("2026", "08", "01", sep="-")\nprint("no newline...", end="")\nprint("same line")\nprint("a", "b", sep="", end="!\\n")',
    note: 'Output: `2026-08-01`, then `no newline...same line`, then `ab!`.',
    task: 'Using sep, print the three parts joined by slashes:\n\n`usr/local/bin`',
    solution: 'print("usr", "local", "bin", sep="/")',
    hint: 'Pass sep="/" after the three strings.',
  },
  {
    slug: 'print-expressions',
    title: 'Printing computed values',
    subtitle: 'Expressions inside print',
    concept:
      'Anything you put inside `print` is **evaluated first**, then displayed. `print(2 + 3)` shows `5`, not `2 + 3`, because Python works out the value before printing it.\n\nThis is the difference between a **literal** (a value written out) and an **expression** (something that computes a value).',
    example: 'print(2 + 3)\nprint(10 * 4)\nprint("2 + 3")\nprint("answer:", 6 * 7)',
    note: 'The third line prints the characters 2 + 3 because quotes make it text, not arithmetic.',
    task: 'Print the results of these three calculations, one per line: 17 plus 25, 8 times 9, and 100 minus 37.',
    solution: 'print(17 + 25)\nprint(8 * 9)\nprint(100 - 37)',
    hint: 'No quotes \u2014 quotes would turn the sum into text and print it literally.',
  },
  d('banner-stars', 'A star banner', 'Print three lines: a row of ten asterisks, the word `WELCOME` centred with three spaces of padding on each side, then another row of ten asterisks.',
    'print("**********")\nprint("   WELCOME   ")\nprint("**********")', 'Count the characters carefully \u2014 ten stars and three spaces either side.'),
  d('receipt', 'A tiny receipt', 'Print a four line receipt exactly as shown:\n\n`ITEM      COST`\n`coffee    3.50`\n`bagel     2.25`\n`TOTAL     5.75`',
    'print("ITEM      COST")\nprint("coffee    3.50")\nprint("bagel     2.25")\nprint("TOTAL     5.75")', 'Line the columns up with plain spaces inside each string.'),
  d('tabbed-table', 'Tab-aligned table', 'Using tab escapes, print a header row `id\tname` followed by `1\tada` and `2\tgrace`.',
    'print("id\\tname")\nprint("1\\tada")\nprint("2\\tgrace")', 'Use \\t between the two columns on every line.'),
  d('quote-line', 'A quoted saying', 'Print exactly this, including the double quotes:\n\n`"Simple is better than complex."`',
    'print("\\"Simple is better than complex.\\"")', 'Escape each inner double quote with a backslash, or wrap the whole thing in single quotes.'),
  d('path-print', 'A Windows path', 'Print exactly `C:\\Users\\ada\\projects`.',
    'print("C:\\\\Users\\\\ada\\\\projects")', 'Every backslash you want to see needs to be written as two backslashes.'),
  d('triangle', 'A star triangle', 'Print a left-aligned triangle of stars four rows tall: one star, then two, then three, then four.',
    'print("*")\nprint("**")\nprint("***")\nprint("****")', 'Four separate prints, adding one star each time.'),
  d('countdown', 'Countdown', 'Print the numbers 5, 4, 3, 2, 1 each on its own line, then `liftoff`.',
    'print(5)\nprint(4)\nprint(3)\nprint(2)\nprint(1)\nprint("liftoff")', 'Numbers need no quotes; the final word does.'),
  d('sep-dots', 'Joined by dots', 'Using sep, print `192.168.0.1` from the four separate numbers 192, 168, 0 and 1.',
    'print(192, 168, 0, 1, sep=".")', 'sep="." replaces the default space between arguments.'),
  d('end-chain', 'Building one line from three prints', 'Using end, produce the single line `red-green-blue` from three separate print calls.',
    'print("red", end="-")\nprint("green", end="-")\nprint("blue")', 'Set end="-" on the first two prints and leave the last one default.'),
  d('math-lines', 'Four calculations', 'Print the results of 144 divided by 12, 7 to the power of 3, 99 plus 1, and 20 minus 45 \u2014 one per line in that order.',
    'print(144 / 12)\nprint(7 ** 3)\nprint(99 + 1)\nprint(20 - 45)', 'Division with / always shows a decimal point; ** is the power operator.'),
  d('label-values', 'Labelled values', 'On three lines print `width: 12`, `height: 8` and `area: 96`, computing the area rather than typing it.',
    'print("width:", 12)\nprint("height:", 8)\nprint("area:", 12 * 8)', 'The comma supplies the space after each colon.'),
  d('boxed-word', 'A boxed word', 'Print a box around the word `CODE` using plus signs, dashes and pipes:\n\n`+------+`\n`| CODE |`\n`+------+`',
    'print("+------+")\nprint("| CODE |")\nprint("+------+")', 'The top and bottom rows have six dashes between the plus signs.'),
  d('blank-line', 'A gap in the middle', 'Print `above`, then a completely empty line, then `below`.',
    'print("above")\nprint()\nprint("below")', 'print() with no arguments outputs just a newline.'),
  d('multi-escape', 'Everything at once', 'With one print call produce three lines: `a\tb`, `c\td`, `done`.',
    'print("a\\tb\\nc\\td\\ndone")', 'Chain \\t and \\n inside a single string.'),
  d('repeat-string', 'Repeating text', 'Python repeats a string with `*`. Print `abc` repeated four times on one line, then a line of twenty dashes.',
    'print("abc" * 4)\nprint("-" * 20)', 'Multiplying a string by an integer repeats it.'),
  d('sep-newline', 'Newline as separator', 'Using sep, print the words `alpha`, `beta` and `gamma` on three separate lines from a single print call.',
    'print("alpha", "beta", "gamma", sep="\\n")', 'sep can be any string, including "\\n".'),
  d('temperature-note', 'Two-column note', 'Print exactly:\n\n`city      temp`\n`Oslo      -3`\n`Cairo     31`',
    'print("city      temp")\nprint("Oslo      -3")\nprint("Cairo     31")', 'Pad with spaces so the second column starts at the same position.'),
  d('arrow', 'An arrow', 'Print a small arrow made of characters:\n\n`  /\\`\n` /  \\`\n`/____\\`',
    'print("  /\\\\")\nprint(" /  \\\\")\nprint("/____\\\\")', 'Each visible backslash must be written as \\\\ inside the string.'),
  d('checkerboard', 'A checkerboard row', 'Print four lines alternating `#-#-#-#-` and `-#-#-#-#`, starting with the hash version.',
    'print("#-#-#-#-")\nprint("-#-#-#-#")\nprint("#-#-#-#-")\nprint("-#-#-#-#")', 'Each row is eight characters long.'),
  d('mixed-quotes', 'Both kinds of quote', "Print exactly: `It's called \"Python\"`.",
    'print("It\'s called \\"Python\\"")', 'A single quote needs no escape inside a double-quoted string; the double quotes do.'),
  d('final-output-drill', 'Putting it together', 'Print a three line summary using sep and computed values:\n\n`lang/Python`\n`year/1991`\n`age/35`\n\nCompute the age as 2026 minus 1991.',
    'print("lang", "Python", sep="/")\nprint("year", 1991, sep="/")\nprint("age", 2026 - 1991, sep="/")', 'sep="/" on each line, and let Python do the subtraction.'),
  {
    kind: 'quiz',
    slug: 'output-quiz',
    title: 'Checkpoint: output',
    questions: [
      { q: 'What does print("2" + "3") display?', options: ['5', '23', '2 3', 'an error'], answer: 1, explain: 'Both are strings, so + joins them into "23". Without quotes, 2 + 3 would be arithmetic giving 5.' },
      { q: 'What does print("a", "b") display?', options: ['ab', 'a b', 'a,b', 'a\\nb'], answer: 1, explain: 'Comma-separated arguments are joined with a single space by default. Use sep="" to remove it.' },
      { q: 'Which escape produces a tab?', options: ['\\\\t', '\\\\n', '\\\\\\\\', '\\\\"'], answer: 0, explain: '\\t is a tab, \\n is a newline, \\\\ is a literal backslash and \\" is a literal double quote.' },
      { q: 'What does print("hi", end="") do?', options: ['Prints nothing', 'Prints hi with no trailing newline', 'Prints hi twice', 'Raises an error'], answer: 1, explain: 'end replaces the trailing newline, so the next print continues on the same line.' },
      { q: 'How many lines does print("a\\\\nb") produce?', options: ['One', 'Two', 'Three', 'Zero'], answer: 1, explain: 'The \\n inside the string breaks it into two lines, and the print itself ends the second one.' },
    ],
  },
];

export const pyValues = [
  {
    slug: 'variables',
    title: 'Naming values',
    subtitle: 'Assignment with =',
    concept:
      'A **variable** is a name attached to a value. You create one with `=`, which here does not mean "equals" \u2014 it means "store the value on the right under the name on the left".\n\nOnce a value has a name you can reuse it anywhere and change it in one place. Python works out the type on its own, so there is no type keyword to write.\n\nNames are lowercase with underscores: `user_name`, `total_cost`.',
    example: 'language = "Python"\nyear = 1991\n\nprint(language, year)\n\nyear = year + 1\nprint("next:", year)',
    note: '`year = year + 1` reads the old value, adds one, and stores the result back under the same name.',
    task: 'Create `name` holding `Ada` and `age` holding `36`, then print exactly:\n\n`Ada is 36 years old`',
    solution: 'name = "Ada"\nage = 36\nprint(name, "is", age, "years old")',
    hint: 'Strings need quotes; numbers do not. The commas in print supply the spaces.',
  },
  {
    slug: 'types',
    title: 'What kind of value is this?',
    subtitle: 'int, float, str, bool',
    concept:
      'Every value has a **type**. The four you meet first:\n\n- `int` \u2014 a whole number, like `42`\n- `float` \u2014 a number with a decimal point, like `3.5`\n- `str` \u2014 text in quotes\n- `bool` \u2014 exactly `True` or `False`\n\n`type(x)` reports the type of a value. Types matter because they decide what operations are allowed: `+` adds numbers but joins strings.',
    example: 'print(type(42))\nprint(type(3.5))\nprint(type("hi"))\nprint(type(True))',
    note: 'The output looks like <class \'int\'> \u2014 Python is telling you the value belongs to the int class.',
    task: 'Create four variables holding `7`, `2.5`, `"seven"` and `True`, then print the type of each on its own line.',
    solution: 'a = 7\nb = 2.5\nc = "seven"\nd = True\n\nprint(type(a))\nprint(type(b))\nprint(type(c))\nprint(type(d))',
    hint: 'Pass each variable to type() inside print().',
  },
  {
    slug: 'reassign',
    title: 'Changing what a name refers to',
    subtitle: 'Rebinding and swapping',
    concept:
      'A variable can be pointed at a new value at any time, and the new value need not be the same type as the old one.\n\nPython also allows **multiple assignment**: `a, b = 1, 2` assigns both at once. That makes swapping two variables a single line: `a, b = b, a`, with no temporary variable needed.',
    example: 'x = 10\nx = "now a string"\nprint(x)\n\na, b = 1, 2\na, b = b, a\nprint(a, b)',
    note: 'In `a, b = b, a` the right-hand side is fully evaluated first, which is why the swap works.',
    task: 'Set `left` to `"up"` and `right` to `"down"`, print them, swap them in one line, then print them again.\n\nExpected:\n`up down`\n`down up`',
    solution: 'left = "up"\nright = "down"\nprint(left, right)\n\nleft, right = right, left\nprint(left, right)',
    hint: 'The swap line is left, right = right, left.',
  },
  {
    slug: 'conversion',
    title: 'Converting between types',
    subtitle: 'int(), float(), str()',
    concept:
      'Types can be converted explicitly. `int("42")` turns text into a number, `str(42)` turns a number into text, and `float("3.5")` produces a float.\n\nThis matters because Python refuses to guess. `"5" + 5` is an error, not `10` or `"55"` \u2014 you must say which you meant.\n\n`int()` on a float truncates toward zero: `int(3.9)` is `3`, not `4`.',
    example: 'text = "42"\nnumber = int(text)\nprint(number + 8)\n\nprint(str(99) + " bottles")\nprint(int(3.9), float(7))',
    note: 'int(3.9) gives 3 because it truncates rather than rounds. Use round(3.9) for 4.',
    task: 'Given `a = "12"` and `b = "30"` as strings, print their numeric sum, then print the two joined as text.\n\nExpected:\n`42`\n`1230`',
    solution: 'a = "12"\nb = "30"\nprint(int(a) + int(b))\nprint(a + b)',
    hint: 'Convert with int() for the sum; leave them as strings for the join.',
  },
  {
    slug: 'constants-naming',
    title: 'Naming things well',
    subtitle: 'Conventions and readability',
    concept:
      'Python names are case sensitive and cannot start with a digit or contain spaces. Beyond the rules there are strong conventions:\n\n- variables and functions: `lower_snake_case`\n- constants that never change: `UPPER_SNAKE_CASE`\n- avoid single letters except for counters and coordinates\n\nGood names remove the need for comments. `d = 86400` needs explaining; `SECONDS_PER_DAY = 86400` does not.',
    example: 'SECONDS_PER_DAY = 86400\ndays_elapsed = 3\n\ntotal_seconds = SECONDS_PER_DAY * days_elapsed\nprint("total:", total_seconds)',
    note: 'Python does not enforce constants \u2014 the capitals are a message to other programmers, not to the interpreter.',
    task: 'Define `MINUTES_PER_HOUR` as 60 and `hours_worked` as 7, then print `worked: 420 minutes`.',
    solution: 'MINUTES_PER_HOUR = 60\nhours_worked = 7\nprint("worked:", MINUTES_PER_HOUR * hours_worked, "minutes")',
    hint: 'Multiply the two names rather than typing 420 directly.',
  },
  {
    slug: 'input-free-values',
    title: 'Several values, one calculation',
    subtitle: 'Chaining variables together',
    concept:
      'Variables become powerful when one is defined in terms of others. Change the input at the top and every derived value updates, because each line is recomputed when it runs.\n\nThe order matters: a name must already exist before it is used. Python reads top to bottom and will complain if you reference something that has not been assigned yet.',
    example: 'price = 4.5\nquantity = 3\nsubtotal = price * quantity\ntax = subtotal * 0.2\ntotal = subtotal + tax\n\nprint("subtotal:", subtotal)\nprint("total:", total)',
    note: 'Naming each intermediate step keeps the arithmetic readable and makes mistakes easy to spot.',
    task: 'Given `length = 12` and `width = 5`, compute and print the area and the perimeter.\n\nExpected:\n`area: 60`\n`perimeter: 34`',
    solution: 'length = 12\nwidth = 5\n\narea = length * width\nperimeter = 2 * (length + width)\n\nprint("area:", area)\nprint("perimeter:", perimeter)',
    hint: 'Perimeter is twice the sum of the two sides \u2014 mind the parentheses.',
  },
  d('rectangle-vals', 'Rectangle report', 'With `w = 7` and `h = 3`, print `area: 21` and `perimeter: 20` on two lines.',
    'w = 7\nh = 3\nprint("area:", w * h)\nprint("perimeter:", 2 * (w + h))', 'Parenthesise w + h before doubling.'),
  d('swap-three', 'Rotate three values', 'Set a, b, c to 1, 2, 3, then rotate them so a gets c, b gets a and c gets b, using a single assignment line. Print before and after.',
    'a, b, c = 1, 2, 3\nprint(a, b, c)\na, b, c = c, a, b\nprint(a, b, c)', 'The whole right-hand side is evaluated before anything is assigned.'),
  d('seconds-total', 'Seconds in a period', 'With `days = 2`, `hours = 5` and `minutes = 30`, print the total number of seconds as `seconds: N`.',
    'days = 2\nhours = 5\nminutes = 30\ntotal = days * 86400 + hours * 3600 + minutes * 60\nprint("seconds:", total)', 'A day is 86400 seconds and an hour is 3600.'),
  d('type-report', 'Report four types', 'Print the type of `0`, `0.0`, `"0"` and `False`, one per line.',
    'print(type(0))\nprint(type(0.0))\nprint(type("0"))\nprint(type(False))', 'Pass each literal directly to type().'),
  d('string-numbers', 'Text that looks numeric', 'Given `x = "8"` and `y = "3"`, print their product as a number, then print x repeated three times.',
    'x = "8"\ny = "3"\nprint(int(x) * int(y))\nprint(x * 3)', 'int() converts for arithmetic; multiplying a string repeats it.'),
  d('float-round', 'Rounding a float', 'Given `value = 7.6789`, print it rounded to zero, one and two decimal places on three lines.',
    'value = 7.6789\nprint(round(value))\nprint(round(value, 1))\nprint(round(value, 2))', 'round() takes an optional second argument for the number of decimals.'),
  d('celsius-value', 'One conversion', 'With `c = 37`, print the Fahrenheit value as `98.6`.',
    'c = 37\nprint(c * 9 / 5 + 32)', 'Using / keeps the result a float, which is what you want here.'),
  d('name-concat', 'Building a full name', 'Given `first = "Ada"` and `last = "Lovelace"`, print the full name and its total character count (including the space).',
    'first = "Ada"\nlast = "Lovelace"\nfull = first + " " + last\nprint(full)\nprint(len(full))', 'Join with + and a literal space, then measure with len().'),
  d('bool-values', 'Booleans are numbers too', 'Print `True + True`, `False + 5` and `int(True)` on three lines to see how booleans behave in arithmetic.',
    'print(True + True)\nprint(False + 5)\nprint(int(True))', 'True counts as 1 and False as 0.'),
  d('divmod-vals', 'Quotient and remainder', 'With `total = 100` and `per_box = 12`, print `boxes: 8` and `left: 4`.',
    'total = 100\nper_box = 12\nprint("boxes:", total // per_box)\nprint("left:", total % per_box)', '// is floor division and % is the remainder.'),
  d('price-tax', 'Adding tax', 'With `price = 80` and a tax rate of 0.25, print the tax and the total on two lines labelled `tax:` and `total:`.',
    'price = 80\nrate = 0.25\ntax = price * rate\nprint("tax:", tax)\nprint("total:", price + tax)', 'Store the tax in its own variable so you can reuse it.'),
  d('big-numbers', 'Underscores in numerals', 'Python allows underscores inside numbers for readability. Print `1_000_000` and the result of dividing it by 8.',
    'population = 1_000_000\nprint(population)\nprint(population / 8)', 'The underscores are ignored by Python and never appear in output.'),
  d('multi-assign', 'Assigning one value to many', 'Set x, y and z all to 0 in a single statement, then print them on one line.',
    'x = y = z = 0\nprint(x, y, z)', 'Chained assignment binds the same value to every name.'),
  d('str-of-number', 'Numbers as text', 'Given `n = 2026`, print the number, then the number as a string joined to `" AD"`.',
    'n = 2026\nprint(n)\nprint(str(n) + " AD")', 'Convert with str() before joining, or Python will refuse to add int and str.'),
  d('average-three', 'Average of three', 'With scores 88, 94 and 79, print the total and the average on two lines.',
    'a, b, c = 88, 94, 79\ntotal = a + b + c\nprint("total:", total)\nprint("average:", total / 3)', 'Multiple assignment sets all three at once.'),
  d('unit-convert', 'Kilometres to miles', 'With `km = 42`, print the distance in miles using a factor of 0.621371, rounded to two decimals.',
    'km = 42\nmiles = km * 0.621371\nprint(round(miles, 2))', 'round(value, 2) fixes the display to two decimal places.'),
  d('abs-vals', 'Absolute values', 'Print the absolute value of -17, of 17 and of -3.5 on three lines.',
    'print(abs(-17))\nprint(abs(17))\nprint(abs(-3.5))', 'abs() removes the sign and leaves the magnitude.'),
  d('min-max-vals', 'Smallest and largest', 'Given three variables holding 34, 91 and 12, print the smallest then the largest.',
    'a, b, c = 34, 91, 12\nprint(min(a, b, c))\nprint(max(a, b, c))', 'min() and max() accept any number of arguments.'),
  d('shadow-value', 'Overwriting a name', 'Set `value` to 10, print it, set it to the string `"ten"`, print it, then print its type.',
    'value = 10\nprint(value)\nvalue = "ten"\nprint(value)\nprint(type(value))', 'Python lets a name refer to a different type later.'),
  d('circle-area', 'Circle measurements', 'With `r = 5` and pi as 3.14159, print the circumference and the area, each rounded to two decimals.',
    'r = 5\npi = 3.14159\nprint(round(2 * pi * r, 2))\nprint(round(pi * r ** 2, 2))', '** is the power operator, so r ** 2 squares the radius.'),
  d('bmi-calc', 'A small formula', 'With `weight = 70` and `height = 1.75`, print the BMI (weight divided by height squared) rounded to one decimal.',
    'weight = 70\nheight = 1.75\nprint(round(weight / height ** 2, 1))', 'Exponentiation happens before division, so no parentheses are needed.'),
  d('values-capstone', 'Values summary', 'Given `title = "python"` and `year = 1991`, print three lines: the title in capitals, the year, and the age in 2026.',
    'title = "python"\nyear = 1991\nprint(title.upper())\nprint(year)\nprint(2026 - year)', '.upper() returns a new uppercase string.'),
  {
    kind: 'quiz',
    slug: 'values-quiz',
    title: 'Checkpoint: values',
    questions: [
      { q: 'What is the type of 3.0?', options: ['int', 'float', 'str', 'bool'], answer: 1, explain: 'Any literal with a decimal point is a float, even when the fractional part is zero.' },
      { q: 'What does int("7") + 3 give?', options: ['"73"', '10', 'an error', '7 3'], answer: 1, explain: 'int("7") converts the text to the number 7, so the addition is arithmetic and gives 10.' },
      { q: 'What does "5" + 5 do?', options: ['Gives 10', 'Gives "55"', 'Raises a TypeError', 'Gives 5'], answer: 2, explain: 'Python refuses to guess whether you meant addition or joining, so mixing str and int with + is an error.' },
      { q: 'After a, b = 1, 2 then a, b = b, a, what is a?', options: ['1', '2', 'None', 'an error'], answer: 1, explain: 'The right-hand side is evaluated first, producing (2, 1), which is then unpacked \u2014 so a becomes 2.' },
      { q: 'What is int(3.9)?', options: ['4', '3', '3.9', 'an error'], answer: 1, explain: 'int() truncates toward zero rather than rounding. Use round(3.9) if you want 4.' },
    ],
  },
];

export const pyOperators = [
  {
    slug: 'arithmetic',
    title: 'The arithmetic operators',
    subtitle: 'Seven ways to combine numbers',
    concept:
      'Python has seven arithmetic operators. Most behave as you expect; three are worth memorising.\n\n- `+ - *` as usual\n- `/` **always** produces a float, so `6 / 3` gives `2.0`\n- `//` floor division, discarding the remainder\n- `%` the remainder itself\n- `**` exponentiation, so `2 ** 10` is `1024`\n\n`//` and `%` are a pair: for `17` and `5`, `17 // 5` is `3` and `17 % 5` is `2`, because 17 = 5\u00d73 + 2.',
    example: 'a, b = 17, 5\nprint(a + b, a - b, a * b)\nprint(a / b)\nprint(a // b)\nprint(a % b)\nprint(a ** 2)',
    note: 'The remainder operator is the standard test for divisibility: n % 2 == 0 means n is even.',
    task: 'With `total = 47` and `people = 6`, print the whole share, the leftover and the exact average on three lines labelled `each:`, `left:` and `average:`.',
    solution: 'total = 47\npeople = 6\nprint("each:", total // people)\nprint("left:", total % people)\nprint("average:", total / people)',
    hint: 'Use // for the whole share, % for the leftover, and plain / for the exact value.',
  },
  {
    slug: 'precedence',
    title: 'Which operation happens first',
    subtitle: 'Precedence and parentheses',
    concept:
      'Operators have a fixed **precedence**. From strongest to weakest: `**`, then unary `-`, then `* / // %`, then `+ -`.\n\nSo `2 + 3 * 4` is `14`, not `20`. Parentheses override everything and cost nothing \u2014 when in doubt, add them.\n\nOne quirk worth knowing: `**` is right-associative, so `2 ** 3 ** 2` is `2 ** 9`, which is `512`.',
    example: 'print(2 + 3 * 4)\nprint((2 + 3) * 4)\nprint(2 ** 3 ** 2)\nprint(-3 ** 2)\nprint((-3) ** 2)',
    note: '-3 ** 2 is -9 because ** binds tighter than the minus sign. (-3) ** 2 is 9.',
    task: 'Print the value of 10 plus 6 divided by 2 without parentheses, then the value with parentheses forcing the addition first.\n\nExpected:\n`13.0`\n`8.0`',
    solution: 'print(10 + 6 / 2)\nprint((10 + 6) / 2)',
    hint: 'Division binds tighter than addition, so the first is 10 + 3.0.',
  },
  {
    slug: 'augmented',
    title: 'Updating a value in place',
    subtitle: 'The += family',
    concept:
      'Adding to a variable is so common that Python has a shorthand: `total += 5` means `total = total + 5`.\n\nEvery arithmetic operator has an augmented form: `-=`, `*=`, `/=`, `//=`, `%=`, `**=`. They work on strings too, where `+=` appends.\n\nThe shorthand is not just less typing \u2014 it makes the intent ("update this") clearer than repeating the name.',
    example: 'total = 10\ntotal += 5\ntotal *= 2\ntotal -= 4\nprint(total)\n\ntext = "ab"\ntext += "cd"\nprint(text)',
    note: 'The updates apply in order: 10 becomes 15, then 30, then 26.',
    task: 'Start `count` at 100. Add 20, then halve it with floor division, then subtract 5. Print the value after each step, one per line.',
    solution: 'count = 100\ncount += 20\nprint(count)\ncount //= 2\nprint(count)\ncount -= 5\nprint(count)',
    hint: 'Use //= to keep the value an integer.',
  },
  {
    slug: 'comparison',
    title: 'Comparing values',
    subtitle: 'The six comparison operators',
    concept:
      'Comparisons produce a **bool**: either `True` or `False`.\n\n`==` equal, `!=` not equal, `<`, `>`, `<=`, `>=`.\n\nA common beginner error is writing `=` when comparing. `=` assigns, `==` compares \u2014 they are unrelated.\n\nPython also allows **chained comparisons**: `0 < x < 10` reads exactly like the mathematics and means both conditions must hold.',
    example: 'x = 7\nprint(x == 7)\nprint(x != 7)\nprint(x < 10, x > 10)\nprint(0 < x < 10)\nprint("apple" < "banana")',
    note: 'Strings compare alphabetically, which is why "apple" < "banana" is True.',
    task: 'With `n = 15`, print on four lines whether it equals 15, whether it is under 10, whether it is between 10 and 20 exclusive, and whether it is not 20.',
    solution: 'n = 15\nprint(n == 15)\nprint(n < 10)\nprint(10 < n < 20)\nprint(n != 20)',
    hint: 'The third uses a chained comparison, exactly as written in mathematics.',
  },
  {
    slug: 'logic',
    title: 'Combining conditions',
    subtitle: 'and, or, not',
    concept:
      '`and` is True only when both sides are True. `or` is True when at least one side is. `not` flips a boolean.\n\nBoth `and` and `or` **short-circuit**: `and` stops at the first False and `or` stops at the first True, without evaluating the rest. That is useful when the second part would fail otherwise.\n\nPrecedence runs `not`, then `and`, then `or`, so `a or b and c` means `a or (b and c)`.',
    example: 'age = 25\nmember = False\n\nprint(age > 18 and age < 65)\nprint(member or age > 21)\nprint(not member)\nprint(not (age > 18 and member))',
    note: 'Parenthesise freely \u2014 the precedence rules are correct but rarely obvious to a reader.',
    task: 'With `temp = 22` and `raining = True`, print whether it is warm and dry, whether it is warm or dry, and whether it is not raining. Treat warm as above 20.',
    solution: 'temp = 22\nraining = True\n\nprint(temp > 20 and not raining)\nprint(temp > 20 or not raining)\nprint(not raining)',
    hint: '"Dry" is simply not raining.',
  },
  {
    slug: 'truthiness',
    title: 'Values that behave like booleans',
    subtitle: 'Truthy and falsy',
    concept:
      'Every Python value can be used where a boolean is expected. The **falsy** values are few and worth memorising:\n\n`False`, `0`, `0.0`, `""` (empty string), `[]` (empty list), `{}` (empty dict), and `None`.\n\nEverything else is **truthy**. `bool(x)` shows which a value is. This is why `if items:` is the idiomatic way to ask "is this list non-empty".',
    example: 'print(bool(0), bool(1), bool(-1))\nprint(bool(""), bool("a"), bool(" "))\nprint(bool([]), bool([0]))\nprint(bool(None))',
    note: 'bool(" ") is True \u2014 a space is a character, so the string is not empty.',
    task: 'Print the truthiness of `0`, `""`, `"0"`, `[]` and `[0]`, one per line.',
    solution: 'print(bool(0))\nprint(bool(""))\nprint(bool("0"))\nprint(bool([]))\nprint(bool([0]))',
    hint: 'The string "0" is not empty, and the list [0] has one element \u2014 both are truthy.',
  },
  {
    slug: 'identity-membership',
    title: 'Two more operator pairs',
    subtitle: 'in, not in, is, is not',
    concept:
      '`in` tests **membership**: whether a value appears inside a string, list or other collection. `not in` is its negation.\n\n`is` tests **identity**: whether two names refer to the very same object, which is different from being equal. Use `is` only with `None`, `True` and `False`; use `==` for values.\n\nThe rule of thumb: `x == 5` asks "does it equal five", `x is None` asks "is it the None object".',
    example: 'print("a" in "cat")\nprint("dog" in "cat")\nprint(3 in [1, 2, 3])\nprint("x" not in "cat")\n\nvalue = None\nprint(value is None)',
    note: 'Membership on a string checks for a substring, so "at" in "cat" is also True.',
    task: 'With `word = "keyboard"`, print whether it contains `"boar"`, whether it contains `"z"`, and whether the letter `"k"` is not in it.',
    solution: 'word = "keyboard"\nprint("boar" in word)\nprint("z" in word)\nprint("k" not in word)',
    hint: 'in works on substrings of any length.',
  },
  d('op-mixed-1', 'Four expressions', 'Print the results of `7 + 3 * 2`, `(7 + 3) * 2`, `2 ** 5` and `19 % 4`, one per line.',
    'print(7 + 3 * 2)\nprint((7 + 3) * 2)\nprint(2 ** 5)\nprint(19 % 4)', 'Multiplication binds tighter than addition.'),
  d('even-odd-bool', 'Even without if', 'With `n = 34`, print whether it is even and whether it is divisible by 3, as two booleans.',
    'n = 34\nprint(n % 2 == 0)\nprint(n % 3 == 0)', 'A remainder of zero means it divides exactly.'),
  d('range-check', 'Inside a range', 'With `score = 72`, print whether it falls between 70 and 80 inclusive using one chained comparison.',
    'score = 72\nprint(70 <= score <= 80)', 'Chained comparisons allow 70 <= score <= 80 directly.'),
  d('logic-table', 'A truth table', 'Print the four combinations of `and` for True/False pairs, one per line: True and True, True and False, False and True, False and False.',
    'print(True and True)\nprint(True and False)\nprint(False and True)\nprint(False and False)', 'Only the first is True.'),
  d('or-defaults', 'or as a fallback', 'Given `name = ""`, print `name or "anonymous"` and then the same with name set to `"ada"`.',
    'name = ""\nprint(name or "anonymous")\nname = "ada"\nprint(name or "anonymous")', 'or returns the first truthy operand, which makes it a neat default.'),
  d('augmented-chain', 'Chained updates', 'Start at 5. Multiply by 4, add 12, take the remainder mod 7, then raise to the power 2 \u2014 printing after each step.',
    'v = 5\nv *= 4\nprint(v)\nv += 12\nprint(v)\nv %= 7\nprint(v)\nv **= 2\nprint(v)', 'Each augmented operator has its own shorthand form.'),
  d('float-int-div', 'Two kinds of division', 'With `a = 22` and `b = 7`, print a / b, a // b, a % b and round(a / b, 4).',
    'a, b = 22, 7\nprint(a / b)\nprint(a // b)\nprint(a % b)\nprint(round(a / b, 4))', '/ gives a float; // truncates downward.'),
  d('negative-floor', 'Floor division goes down', 'Print `-7 // 2`, `7 // 2`, `-7 % 2` and `7 % 2` to see how negatives behave.',
    'print(-7 // 2)\nprint(7 // 2)\nprint(-7 % 2)\nprint(7 % 2)', 'Floor division rounds toward negative infinity, not toward zero.'),
  d('membership-str', 'Substring checks', 'With `sentence = "the quick brown fox"`, print whether it contains `"quick"`, `"slow"` and the letter `"z"`.',
    'sentence = "the quick brown fox"\nprint("quick" in sentence)\nprint("slow" in sentence)\nprint("z" in sentence)', 'in searches for a substring anywhere in the text.'),
  d('membership-list', 'List membership', 'With `nums = [2, 4, 6, 8]`, print whether 6 is in it, whether 5 is in it, and whether 5 is not in it.',
    'nums = [2, 4, 6, 8]\nprint(6 in nums)\nprint(5 in nums)\nprint(5 not in nums)', 'in checks elements of a list, not substrings.'),
  d('not-chain', 'Negating conditions', 'With `x = 10`, print `not x > 5`, `not x == 10` and `not (x < 5 or x > 20)`.',
    'x = 10\nprint(not x > 5)\nprint(not x == 10)\nprint(not (x < 5 or x > 20))', 'not applies to the whole comparison to its right.'),
  d('leap-year-bool', 'Leap year test', 'With `year = 2024`, print whether it is a leap year: divisible by 4 and not by 100, or divisible by 400.',
    'year = 2024\nprint((year % 4 == 0 and year % 100 != 0) or year % 400 == 0)', 'Group the and-clause in parentheses before the or.'),
  d('compare-strings', 'Alphabetical comparison', 'Print whether `"apple" < "banana"`, `"Zebra" < "apple"` and `"cat" == "Cat"`.',
    'print("apple" < "banana")\nprint("Zebra" < "apple")\nprint("cat" == "Cat")', 'Capital letters sort before lowercase in ASCII order.'),
  d('bitwise-intro', 'Bitwise operators', 'Print `12 & 10`, `12 | 10`, `12 ^ 10` and `12 << 1` to see the bit-level operators.',
    'print(12 & 10)\nprint(12 | 10)\nprint(12 ^ 10)\nprint(12 << 1)', '& is and, | is or, ^ is exclusive or, << shifts bits left.'),
  d('divisible-report', 'Divisibility report', 'With `n = 60`, print booleans for divisibility by 2, 3, 4 and 7 on four lines.',
    'n = 60\nprint(n % 2 == 0)\nprint(n % 3 == 0)\nprint(n % 4 == 0)\nprint(n % 7 == 0)', 'Test each divisor with the remainder operator.'),
  d('short-circuit', 'Short-circuit safety', 'With `n = 0`, print `n != 0 and 10 / n > 1`. It must not crash \u2014 explain to yourself why.',
    'n = 0\nprint(n != 0 and 10 / n > 1)', 'and stops at the first False, so the division is never evaluated.'),
  d('mixed-precedence', 'Precedence puzzles', 'Print `2 + 3 * 4 ** 2`, `(2 + 3) * 4 ** 2`, `((2 + 3) * 4) ** 2` and `2 ** 2 ** 3`.',
    'print(2 + 3 * 4 ** 2)\nprint((2 + 3) * 4 ** 2)\nprint(((2 + 3) * 4) ** 2)\nprint(2 ** 2 ** 3)', '** binds tightest and groups right to left.'),
  d('is-vs-equals', 'Identity versus equality', 'Print `1000 == 1000`, `None is None` and `"" == False`.',
    'print(1000 == 1000)\nprint(None is None)\nprint("" == False)', 'Equality compares values; is compares object identity.'),
  d('grade-bools', 'Grade boundaries', 'With `score = 84`, print booleans for: at least 90, at least 80, at least 70 \u2014 three lines.',
    'score = 84\nprint(score >= 90)\nprint(score >= 80)\nprint(score >= 70)', 'Only the first should be False.'),
  d('triangle-valid', 'Is it a triangle?', 'With sides 3, 4 and 5, print whether each pair sums to more than the third \u2014 the triangle inequality \u2014 as one boolean.',
    'a, b, c = 3, 4, 5\nprint(a + b > c and a + c > b and b + c > a)', 'Chain three comparisons with and.'),
  d('op-capstone', 'Operator summary', 'With `n = 145`, print: the number of full dozens, the leftover, whether it is odd, and n squared.',
    'n = 145\nprint(n // 12)\nprint(n % 12)\nprint(n % 2 == 1)\nprint(n ** 2)', 'Four separate expressions, one per line.'),
  {
    kind: 'quiz',
    slug: 'operators-quiz',
    title: 'Checkpoint: operators',
    questions: [
      { q: 'What is 2 + 3 * 4?', options: ['20', '14', '24', '9'], answer: 1, explain: 'Multiplication binds tighter than addition, so it is 2 + 12.' },
      { q: 'What is 7 // 2?', options: ['3.5', '3', '4', '1'], answer: 1, explain: '// is floor division, which discards the fractional part and rounds downward.' },
      { q: 'Which of these is falsy?', options: ['"0"', '[0]', '0.0', '-1'], answer: 2, explain: '0.0 is falsy. The string "0" and the list [0] are both non-empty, and -1 is a non-zero number.' },
      { q: 'What does 0 < x < 10 mean when x is 5?', options: ['A syntax error', 'True, because both comparisons hold', '(0 < x) < 10 evaluated left to right', 'Always True'], answer: 1, explain: 'Python supports genuine chained comparisons, so it means 0 < x and x < 10.' },
      { q: 'Why does n != 0 and 10 / n > 1 never divide by zero?', options: ['Python catches the error automatically', 'and short-circuits, so the division is skipped when the first part is False', 'Division by zero returns infinity', 'The expression is invalid'], answer: 1, explain: 'and evaluates left to right and stops at the first falsy operand, so the right side is never reached when n is 0.' },
    ],
  },
];
